# Exp073AY — Article-3 controlled twin Wm_S2 dormant preregistration v0.1

**Status:** PROSPECTIVELY FROZEN / DORMANT / NOT AUTHORIZED TO EXECUTE  
**Date:** 2026-08-30  
**Repository:** `pppuu7-cmd/Dark-Sector-Influence-Reconstruction`

## Purpose

Freeze the next angular production task, `Wm_S2`, while Exp073AQ (`Wm_S1`) is still running, without consuming any Exp073AQ result and without launching a duplicate or successor heavy computation.

This is infrastructure/methodological preparation only. It contributes **+0** to strict Article-3 scientific readiness, which remains **52%**.

## Activation firewall

Exp073AY is **not executable** unless all of the following are simultaneously true:

1. Exp073AQ run `33327372191` has reached a terminal hosted state.
2. A hosted immutable comparator authority artifact exists for Exp073AQ.
3. Its terminal class is exactly `PASS_EXP073AQ_WM_S1_CONTROLLED_TWIN_EXACT_V0_1`.
4. The comparator confirms exact canonical SHA equality and `numpy.array_equal == True` between independent A/B replicas.
5. Exp073AQ readiness increment is exactly zero and Article-3 readiness remains 52%.
6. No tolerance, rounding, ULP, preferred-replica, majority-vote, historical-P proximity, or manual rescue has been used.

If Exp073AQ ends as `SCIENTIFIC_REPEATABILITY_FAIL_EXP073AQ_WM_S1_CONTROLLED_TWIN_EXACT_V0_1`, Exp073AY remains permanently dormant pending a separately preregistered scientific resolution path.

If Exp073AQ fails before valid comparator authority exists, classify it as infrastructure-INCOMPLETE; do not execute Exp073AY.

## Frozen task

Only the following angular task is admitted by this preregistration:

`Wm_S2`

The production implementation must use the already frozen Article-3 DES angular task runner lineage corresponding to:

`ci/exp073aa_article3_des_angular_task_runner_v0_1.py`

No scientific support, covariance, nuisance, relation, null, G8, or discovery read is permitted during this angular production stage.

## Exact controlled-twin environment

Each production replica must run independently in hosted GitHub Actions with:

- `OMP_NUM_THREADS=1`
- `OPENBLAS_NUM_THREADS=1`
- `MKL_NUM_THREADS=1`
- `NUMEXPR_NUM_THREADS=1`
- `VECLIB_MAXIMUM_THREADS=1`
- `BLIS_NUM_THREADS=1`
- `OMP_DYNAMIC=FALSE`

The environment, runner image metadata, package versions, CPU information, task identifier, repository commit, and input-authority digests must be recorded in machine-readable receipts.

## Input authority

Use the same exact hosted R1 observational authority and exact DES Y1 lens-mask authority frozen for the controlled single-thread angular route unless a separately preregistered authority migration is approved before execution.

Controlled Wm_S0 remains the admitted anchor:

`8ac59fc060195addcc5cd8b6d75e32fbc6dbfeea8456f4c83e8bf0cf034b9220`

No historical-primary-P output may replace the controlled route.

## Comparator rule

Two independent hosted `Wm_S2` replicas are required.

Scientific/computational admission requires both:

1. exact canonical SHA equality of the full frozen numerical payload;
2. exact array equality using `numpy.array_equal == True` for all authority arrays.

Only two terminal comparator classes are permitted:

- `PASS_EXP073AY_WM_S2_CONTROLLED_TWIN_EXACT_V0_1`
- `SCIENTIFIC_REPEATABILITY_FAIL_EXP073AY_WM_S2_CONTROLLED_TWIN_EXACT_V0_1`

No approximate equality class is allowed.

## Anti-leakage firewall

Before and during Exp073AY production, all of the following must remain false / unread:

- radial-kernel scientific support evaluation beyond inputs required by the frozen angular operator;
- physical k support classification;
- retained-coordinate scientific selection;
- fiducial-P weighting;
- covariance restriction or whitening;
- nuisance geometry;
- quotient construction;
- relation fitting;
- null/control evaluation;
- G8 reads;
- any scientific gate scoring.

The output is an angular operator authority only.

## Accounting

A valid exact PASS admits only the `Wm_S2` angular object to the successor 14-window authority chain.

It does **not** close G7/G8/G9 and does **not** increase readiness.

Required accounting fields:

- `readiness_increment = 0`
- `article3_scientific_readiness_percent = 52`
- `science_gate_scored = false`
- `scientific_pass_claimed = false`
- `gate_state = {G7: OPEN, G8: OPEN, G9: OPEN}`

## Successor order

If and only if Exp073AY later obtains hosted exact PASS authority, the next eligible angular task is `Wm_S3`, to be prospectively frozen separately before execution.

The remaining controlled exact-twin sequence remains:

`Wm_S2 -> Wm_S3 -> WW_S0_S0 -> WW_S0_S1 -> WW_S0_S2 -> WW_S0_S3 -> WW_S1_S1 -> WW_S1_S2 -> WW_S1_S3 -> WW_S2_S2 -> WW_S2_S3 -> WW_S3_S3`.

## Non-claims

This preregistration is not a physical result, not a G7 result, not evidence for a dark-sector relation, and not a scientific PASS. It is a prospective anti-leakage and exact-repeatability freeze prepared while Exp073AQ remains active.
