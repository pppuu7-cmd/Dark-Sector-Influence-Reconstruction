# Exp073AS — Article 3 execution-qualified pre-support join succession v0.1

**Frozen:** 2026-08-30 after hosted Exp073AR synthetic PASS, while real Exp073AQ Wm_S1 controlled twins are still in progress and before any AQ comparator authority exists, before Wm_S2 production, before a real 14-window execution-qualified aggregate, and before any real Layer-A support value is evaluated.

## Purpose

Exp073AS prospectively supersedes Exp073AE **only for the future execution-qualified successor route** authorized by Exp073AO/AP and protected by Exp073AR. Historical Exp073AE remains an immutable valid synthetic QA record for the historical X2/Exp073AA route.

Exp073AS is a non-scientific schema/firewall gate. It freezes how a future real `controlled_single_thread_exact_v1` 14-window aggregate may be joined to the already-frozen observation/radial/mapping/BOSS authorities to create the immutable 1410-row finite-operator candidate manifest before Layer A.

It must not read real AQ output, compute support, select retained coordinates, read covariance, perform nuisance geometry, or evaluate relation/null/G8. Hosted synthetic PASS adds 0 scientific-readiness points; strict Article-3 readiness remains 52%.

## Immutable non-angular upstream authorities

These identities are inherited unchanged from Exp073AE:

- Exp073U observation skeleton: run `33274852199`, job `99159670108`, artifact `9721184683`, digest `sha256:d44e628e9312fb5a919a6681b69d9e06e18418cdd299de641e6465e60dadfd68`, row count `1410`, block order `Wm[780] -> WW[390] -> BOSS[240]`, ordered-ID SHA256 `bfaf582518cdbfd34b1e8392da83dac6b0885948bc31f2c29d4e48247c23af75`.
- Exp073Z2 exact DES radial authority: run `33279208949`, job `99171355322`, artifact `9722468056`, digest `sha256:3eb8b025711e8df6d5452a3a57002f36c9d7de2b9116734b71d15d6822dd20be`, token `PASS_EXP073Z2_DES_RADIAL_KERNEL_STABLE_DIRECT_V0_2`.
- Exp073AB row-to-operator mapping authority: run `33279639316`, job `99172491781`, artifact `9722589222`, digest `sha256:e7bc461eb2066067ac356a23eb073218401181070350bd3ab37555a0b9d66fd4`.
- Exp073W BOSS compatibility/broad-row authority: run `33277001376`, job `99165356858`, artifact `9721800577`, digest `sha256:b4d6207bda8f7fd9f446609faecfba9adb8fe1783f0e84ec3814be06f3fcac8b`.

The recorded BOSS retained-support count `54/240` remains upstream information and is forbidden from selecting rows in this pre-support join.

## Required angular aggregate authority

The join accepts exactly one upstream aggregate record validated under Exp073AR. It must declare:

- `authority_class = controlled_single_thread_exact_v1`;
- `aggregate_schema = exp073ar_execution_qualified_14window_v0_1`;
- exactly 14 tasks, once each and in frozen order:
  `Wm_S0, Wm_S1, Wm_S2, Wm_S3, WW_S0_S0, WW_S0_S1, WW_S0_S2, WW_S0_S3, WW_S1_S1, WW_S1_S2, WW_S1_S3, WW_S2_S2, WW_S2_S3, WW_S3_S3`;
- Wm_S0 selected-window SHA exactly `8ac59fc060195addcc5cd8b6d75e32fbc6dbfeea8456f4c83e8bf0cf034b9220` from Exp073AM;
- all 14 windows canonical `<f8 [39,12288]`;
- every non-Wm_S0 task has exact twin evidence: identical replica SHA, `array_equal=true`, controlled execution verified, hosted comparator provenance present;
- aggregate metadata SHA256 is 64 lowercase hexadecimal characters.

Historical `canonical_exp073x2`, `exp073aa`, historical primary-P Wm_S0 SHA, single-replica authorities, tolerance/rounding/ULP equivalence, majority vote, preferred replica, or closeness-to-P rescue are forbidden for this successor join.

## Candidate manifest contract

The synthetic gate validates the future real join receipt only. The receipt must preserve:

- row count exactly `1410`;
- block counts exactly `Wm=780`, `WW=390`, `BOSS=240`;
- Exp073U ordered-ID SHA exactly frozen above;
- explicit immutable bindings to U, Z2, AB, W and the Exp073AR aggregate;
- `candidate_manifest_complete=true` only when all 1410 pre-support finite-operator rows are bound;
- `support_selection_applied=false`;
- deterministic metadata manifest SHA from canonical sorted compact JSON payload without timestamps, mutable URLs, local paths or runner-specific fields.

This gate does not authorize declaring the real candidate manifest complete until the real 14-window aggregate exists and is hosted-authority validated.

## Anti-leakage firewall

All must remain false:

- `physical_support_evaluated`
- `operator_f_invalid_computed`
- `retained_coordinates_evaluated`
- `layer_b_evaluated`
- `fiducial_P_weighting_used`
- `covariance_read`
- `whitening_performed`
- `nuisance_geometry_read`
- `nuisance_svd_performed`
- `relation_null_read`
- `chi_square_read`
- `p_value_read`
- `G8_read`
- `scientific_pass_claimed`

Readiness must remain exactly `52`, readiness increment `0`, and G7/G8/G9 must remain `OPEN`.

## Hosted synthetic test matrix

At minimum test:

1. exact valid successor join -> accept;
2. old `canonical_exp073x2` class -> reject;
3. old `exp073aa` class -> reject;
4. historical primary-P Wm_S0 hash -> reject;
5. wrong controlled anchor hash -> reject;
6. reordered tasks -> reject;
7. duplicate task -> reject;
8. missing task -> reject;
9. wrong dtype -> reject;
10. wrong shape -> reject;
11. non-Wm_S0 twin SHA mismatch -> reject;
12. non-Wm_S0 `array_equal=false` -> reject;
13. controlled execution unverified -> reject;
14. missing/invalid hosted comparator provenance -> reject;
15. malformed aggregate SHA -> reject;
16. U row-count drift -> reject;
17. U ordered-ID SHA drift -> reject;
18. U/Z2/AB/W authority drift -> reject;
19. block-count drift -> reject;
20. support-selection activation -> reject;
21. covariance/G8 leakage -> reject;
22. unknown top-level or nested field -> reject;
23. readiness/gate-state drift -> reject;
24. deterministic manifest SHA invariant under dictionary insertion order -> pass.

## Required hosted synthetic QA token

`PASS_EXP073AS_EXECUTION_QUALIFIED_PRESUPPORT_JOIN_SUCCESSION_SYNTHETIC_V0_1`

This token is schema/provenance QA only. It is not a scientific PASS, not a real 1410-row candidate manifest, not Layer A, and gives +0 readiness.