# Exp071A — G7 physical-provider eligibility ledger v0.1

Date: 2026-08-27
Status: CONTROL / ORDERING LEDGER — NOT A SCIENTIFIC PASS GATE

## Purpose

Freeze the exact G7 ordering boundary after Exp070C and while the independent C5 mechanism audit Exp069D is still unresolved. This file exists to prevent accidental promotion of downstream support/covariance/nuisance steps before both physical input families are actually eligible.

Mandatory sequence remains:

`validated C3 + C5 physical providers -> preregistered common physical support-validity mask -> covariance restriction/whitening -> nuisance tangent rank/SVD -> quotient/relation/null control -> fresh G8 withheld family -> G9`.

No downstream step is authorized by this ledger.

## C3 / GDM status

Exp070A remains permanently

`FAIL_C3_GDM_READONLY_DM_PHYSICAL_POWER_BRIDGE_V0_1`.

Exp070B localized the ~4.75% target-grid discrepancy to DSIR-side signed-amplitude interpolation rather than the native physical source.

Exp070C then prospectively certified a native-grid provider with no amplitude interpolation:

`PASS_C3_GDM_NATIVE_GRID_PHYSICAL_POWER_PROVIDER_V0_1`.

Hard provider facts from the merged Exp070C result:

- solver-native `P_mm` closure maximum relative error <= `2.82e-14`;
- same-mode coherence residual <= `4.45e-16`;
- 33 retained native nodes per redshift/case;
- bitwise accessor repeatability and no solver-state mutation;
- signed `P_Wm` retained;
- no ACT/unWISE projection, common support mask, covariance whitening, nuisance projection, relation fit, or likelihood evaluation was authorized.

Therefore C3 is **physical-provider eligible** for the next G7 stage, but cannot start that stage alone.

## C5 / designer f(R) status

Exp069B remains permanently

`FAIL_C5_EXPLICIT_EFT_PYTHON_POWER_BRIDGE_V0_1`.

Its exact designer `B0=0` versus ordinary-GR mismatch exceeded the frozen `5e-6` criterion.

Exp069C classified the defect as

`RAW_POWER_ZERO_LIMIT_RESIDUAL + KGRID_NONCONVERGENCE`.

The same-node raw residual persists at approximately `7.3e-6..7.6e-6`, while target-grid residuals remain approximately `5.3e-6` as `k_per_logint` increases from 40 to 320. Thus neither target interpolation nor matter-power k-grid density is an authorized corrective explanation.

Exp069D is the next causal audit and is allowed only to separate branch/initialization/background-grid/Return-to-GR mechanisms. It cannot itself reclassify Exp069B or certify C5 unless followed by a separately preregistered corrective bridge satisfying an unchanged hard GR-limit criterion.

Therefore C5 is currently **NOT physical-provider eligible**.

## Downstream authorization matrix

| Stage | Authorization on 2026-08-27 | Reason |
|---|---|---|
| C3 native physical provider | YES | Exp070C prospective PASS |
| C5 physical provider | NO | Exp069B permanent FAIL; Exp069D mechanism audit unresolved |
| common physical support-validity mask | NO | requires validated C3 + C5 providers |
| covariance restriction/whitening | NO | support mask not yet authorized |
| nuisance tangent rank/SVD | NO | covariance-restricted observable plane not yet frozen |
| quotient/relation/null control | NO | nuisance quotient not yet available |
| fresh G8 withheld family | NO | G7 sequence incomplete |
| G9 | NO | G7/G8 incomplete |

## Scientific integrity rules

1. Never weaken or reinterpret the frozen Exp069B threshold after seeing C5 outputs.
2. Preserve all Exp069A/B/C/D negative or diagnostic classifications separately.
3. A numerical mechanism diagnosis is not a physical-provider certification.
4. No support mask may be selected to remove cells that are inconvenient for a failed zero-limit test.
5. The future common support mask must be preregistered from physical validity/domain criteria, not from relation residuals or downstream covariance behavior.
6. Infrastructure failure and scientific FAIL must remain separate labels.
7. A fresh G8 family must remain genuinely unseen by whatever final G7 relation/operator is frozen.

## Exact continuation rule

If Exp069D identifies a concrete solver-branch mechanism that admits a scientifically justified correction, the next step is a separately numbered **prospective C5 corrective physical-power bridge** with the pre-existing hard GR-limit acceptance scale retained or made stricter for an independently justified reason.

If Exp069D does not identify such a mechanism, C5 remains blocked and the correct action is to redesign the C5 physical provider from an independent solver-native route, not to advance to the support mask.

G7=OPEN. G8=OPEN. G9=OPEN.
