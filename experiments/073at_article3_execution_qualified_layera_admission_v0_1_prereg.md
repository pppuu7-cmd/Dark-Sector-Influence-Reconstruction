# Exp073AT — Article 3 execution-qualified Layer-A admission firewall v0.1

**Frozen:** 2026-08-30 after hosted Exp073AS synthetic PASS, while real Exp073AQ Wm_S1 controlled twins are still in progress, before any AQ comparator authority exists, before Wm_S2 production, before any real Exp073AR aggregate, before any real Exp073AS 1410-row candidate manifest, and before any real Layer-A support value exists.

## Purpose

Exp073AT is a **non-scientific release/admission firewall** between the future execution-qualified immutable pre-support candidate manifest and the already-frozen Layer-A numerical semantics.

It does not recompute or modify the Layer-A operator. It freezes the exact conditions under which a future real candidate manifest may be admitted to real Layer-A evaluation.

Historical Exp073AC remains the numerical/architecture QA authority for the factorized support evaluator. Exp073AT only adds successor-route provenance and completeness requirements that did not exist when Exp073AC was frozen.

Hosted synthetic PASS adds **0 scientific-readiness points**. Strict Article-3 readiness remains 52%, Layer A/B remain OPEN, covariance/whitening BLOCKED, G7/G8/G9 OPEN.

## Required upstream candidate authority

A future real Layer-A caller must bind one immutable hosted candidate-manifest receipt validated under Exp073AS and produced only after a real Exp073AR aggregate exists.

The candidate must declare exactly:

- `authority_route = controlled_single_thread_exact_v1`;
- `join_schema = exp073as_execution_qualified_presupport_join_v0_1`;
- `candidate_manifest_complete = true`;
- `support_selection_applied = false`;
- `row_count = 1410`;
- `block_counts = {Wm:780, WW:390, BOSS:240}`;
- Exp073U ordered-ID SHA256 `bfaf582518cdbfd34b1e8392da83dac6b0885948bc31f2c29d4e48247c23af75`;
- Wm_S0 anchor SHA256 `8ac59fc060195addcc5cd8b6d75e32fbc6dbfeea8456f4c83e8bf0cf034b9220`;
- immutable hosted source run/job/artifact/digest for the real Exp073AS candidate receipt;
- deterministic candidate metadata SHA256 of 64 lowercase hexadecimal characters.

Historical `canonical_exp073x2`, old `exp073aa`, historical primary-P Wm_S0 SHA, historical Exp073AE join schema, incomplete manifests, single-replica successor admission, tolerance/ULP/rounding equivalence, majority vote and preferred-replica rescue are forbidden.

## Frozen Layer-A numerical semantics — inherited unchanged

Exp073AT does not alter the already-frozen Layer-A evaluator semantics:

- valid redshift domain `0.295 <= z <= 2.33` inclusive;
- valid wavenumber domain `0 < k <= 0.06664762008318016 Mpc^-1`;
- factorized angular support uses discrete integer-ell `abs(W_ell)` with no extra `(2ell+1)` measure;
- radial functions/integrals are piecewise-linear with exact splits at z/k boundaries;
- `f_invalid = 1 - valid_mass/total_mass`;
- Layer-A acceptance is inclusive `f_invalid <= 0.05`;
- exact-threshold numerical ambiguity is not rounded to PASS/FAIL;
- positive absolute operator/window envelope is support bookkeeping only; measured Wm remains signed;
- zero normalization remains invalid-for-science, not scientific support FAIL;
- negative radial support remains a representation failure.

The numerical semantics continue to be governed by:

- `docs/ARTICLE3_LAYERA_FACTORIZED_DES_SUPPORT_EVALUATOR_2026-08-30.md`;
- Exp073AC hosted synthetic QA;
- the frozen exact-5% comparison amendment / Exp073AD ambiguity firewall.

## Anti-leakage firewall

The **admission decision** may inspect only manifest provenance/completeness and frozen Layer-A constants. It must not inspect any actual row support score or any downstream quantity.

All must remain false at admission time:

- `physical_support_evaluated`;
- `operator_f_invalid_computed`;
- `retained_coordinates_evaluated`;
- `layer_b_evaluated`;
- `fiducial_P_weighting_used`;
- `covariance_read`;
- `whitening_performed`;
- `nuisance_geometry_read`;
- `nuisance_svd_performed`;
- `relation_null_read`;
- `chi_square_read`;
- `p_value_read`;
- `G8_read`;
- `scientific_pass_claimed`.

The gate must retain readiness exactly 52, readiness increment 0, and G7/G8/G9 OPEN.

## Frozen admission output

Only two valid decisions exist:

- `AUTHORIZE_REAL_LAYER_A_EVALUATION` — the manifest is structurally/provenance complete and may be passed to the unchanged real Layer-A evaluator;
- `BLOCK_REAL_LAYER_A_EVALUATION` — any valid but non-admissible state.

Unknown states/keys or malformed provenance fail validation closed.

Neither decision is a Layer-A scientific result. `AUTHORIZE_REAL_LAYER_A_EVALUATION` only permits the later real computation.

## Hosted synthetic matrix

At minimum:

1. exact valid execution-qualified complete candidate -> authorize;
2. candidate incomplete -> block;
3. support already applied -> block;
4. row-count drift -> block;
5. block-count drift -> block;
6. Exp073U ordered-ID SHA drift -> block;
7. wrong Wm_S0 anchor -> block;
8. historical primary-P Wm_S0 -> block;
9. historical Exp073AE join schema -> block;
10. old `canonical_exp073x2` route -> block;
11. old `exp073aa` route -> block;
12. malformed/zero hosted run/job/artifact identity -> reject;
13. malformed artifact digest -> reject;
14. malformed candidate SHA -> reject;
15. missing upstream Exp073AS hosted provenance -> reject;
16. physical-support flag true -> reject;
17. retained-coordinate flag true -> reject;
18. covariance/G8 leakage -> reject;
19. readiness drift -> reject;
20. G7/G8/G9 drift -> reject;
21. unknown top-level field -> reject;
22. unknown nested field -> reject;
23. frozen z/k/5% boundary drift -> reject;
24. exact valid fixture remains deterministic under dictionary insertion-order changes -> pass.

## Required hosted synthetic token

`PASS_EXP073AT_EXECUTION_QUALIFIED_LAYERA_ADMISSION_SYNTHETIC_V0_1`

This token is provenance/release QA only. It is not a real candidate manifest, not Layer A, not scientific PASS, and gives +0 readiness.
