# Exp073AE — Article 3 strict pre-support authority-join schema v0.1

**Frozen:** 2026-08-30 while both prospectively frozen Exp073X2 hosted chains are still in progress, before any remaining 13-task DES angular production execution, before any complete 14-window authority exists, and before any real Layer-A support score is evaluated.

## Purpose

Exp073AE is a **non-classifying schema/firewall gate** for the future immutable Article-3 pre-support finite-operator candidate manifest.

It does not build or score the real candidate manifest. It freezes and tests the exact structural contract that a later real join must satisfy after the canonical Exp073X2 Wm_S0 authority and the remaining 13 Exp073AA angular authorities exist.

A hosted synthetic PASS of Exp073AE is technical/provenance QA only. It contributes **0 percentage points** to strict Article-3 scientific readiness, which remains 52% until real authorities are bound into the full finite-operator candidate manifest.

## Upstream authority identities frozen into the schema

### Exp073U observation skeleton

- run `33274852199`;
- job `99159670108`;
- artifact `9721184683`;
- artifact digest `sha256:d44e628e9312fb5a919a6681b69d9e06e18418cdd299de641e6465e60dadfd68`;
- row count `1410`;
- block order `Wm[780] -> WW[390] -> BOSS[240]`;
- ordered-ID SHA256 `bfaf582518cdbfd34b1e8392da83dac6b0885948bc31f2c29d4e48247c23af75`.

### Exp073Z2 exact DES radial authority

- run `33279208949`;
- job `99171355322`;
- artifact `9722468056`;
- artifact digest `sha256:3eb8b025711e8df6d5452a3a57002f36c9d7de2b9116734b71d15d6822dd20be`;
- token `PASS_EXP073Z2_DES_RADIAL_KERNEL_STABLE_DIRECT_V0_2`.

### Exp073AB row-to-operator mapping authority

- run `33279639316`;
- job `99172491781`;
- artifact `9722589222`;
- artifact digest `sha256:e7bc461eb2066067ac356a23eb073218401181070350bd3ab37555a0b9d66fd4`.

### Exp073W BOSS compatibility/broad-row authority

- run `33277001376`;
- job `99165356858`;
- artifact `9721800577`;
- artifact digest `sha256:b4d6207bda8f7fd9f446609faecfba9adb8fe1783f0e84ec3814be06f3fcac8b`;
- retained BOSS physical-support mask count recorded upstream remains `54/240`; Exp073AE is forbidden to use that retained mask to select the 1410 candidate rows.

## Frozen DES angular task order

The future real join must contain exactly these 14 task identities exactly once and in this order:

1. `Wm_S0`
2. `Wm_S1`
3. `Wm_S2`
4. `Wm_S3`
5. `WW_S0_S0`
6. `WW_S0_S1`
7. `WW_S0_S2`
8. `WW_S0_S3`
9. `WW_S1_S1`
10. `WW_S1_S2`
11. `WW_S1_S3`
12. `WW_S2_S2`
13. `WW_S2_S3`
14. `WW_S3_S3`

Each angular authority entry must expose only an exact canonical selected-window identity:

- dtype exactly `<f8`;
- shape exactly `[39,12288]`;
- SHA256 exactly 64 lowercase hexadecimal characters;
- `Wm_S0` authority class exactly `canonical_exp073x2`;
- all remaining 13 authority classes exactly `exp073aa`.

Exp073AE does not inspect the numerical angular-window values.

## Canonical X2 succession rule

The future `Wm_S0` entry is valid only under both already-frozen governance documents:

- `docs/ARTICLE3_EXP073X2_PARALLEL_AUTHORITY_SELECTION_2026-08-30.md`;
- `docs/ARTICLE3_DES_ANGULAR_14_TASK_MANIFEST_X2_SUCCESSION_AMENDMENT_2026-08-30.md`.

Chain P, run `33300997298`, is primary. Chain Q, run `33301058260`, is contingency only under the frozen infrastructure-INCOMPLETE fallback rule. A scientific/repeatability mismatch FAIL in Chain P may not be bypassed by Chain Q.

This schema deliberately does not choose an X2 output or hash while those hosted runs are in progress.

## Anti-leakage firewall

The candidate pre-support authority record must remain blind to every downstream classification quantity. The schema therefore requires all of the following to remain false:

- `physical_support_evaluated`;
- `operator_f_invalid_computed`;
- `retained_coordinates_evaluated`;
- `layer_b_evaluated`;
- `fiducial_P_weighting_used`;
- `covariance_read`;
- `whitening_performed`;
- `nuisance_geometry_read`;
- `nuisance_svd_performed`;
- `relation_null_read`;
- `chi_square_read`;
- `p_value_read`;
- `G8_read`;
- `scientific_pass_claimed`.

The schema must reject unknown top-level or nested keys rather than silently accepting aliases, extra fields, typo/confusable fields, alternate task identities, reordered tasks, duplicate tasks, missing tasks, wrong shapes/dtypes, malformed hashes, changed readiness, or any non-OPEN G7/G8/G9 state.

## Synthetic test matrix

Hosted Exp073AE self-test must include at least:

1. one exact valid synthetic record -> accepted;
2. duplicate task -> rejected;
3. reordered task list -> rejected;
4. missing task -> rejected;
5. wrong `Wm_S0` authority class -> rejected;
6. wrong non-Wm_S0 authority class -> rejected;
7. shape drift -> rejected;
8. dtype drift -> rejected;
9. malformed SHA -> rejected;
10. Exp073U row-count drift -> rejected;
11. Exp073U ordered-ID SHA drift -> rejected;
12. upstream artifact/digest drift for U/Z2/AB/W -> rejected;
13. forbidden support field switched true -> rejected;
14. forbidden covariance/G8 field switched true -> rejected;
15. unknown top-level field -> rejected;
16. unknown nested field -> rejected;
17. readiness different from 52 -> rejected;
18. any G7/G8/G9 state different from OPEN -> rejected.

## Required hosted QA token

`PASS_EXP073AE_ARTICLE3_PRESUPPORT_AUTHORITY_JOIN_SCHEMA_SYNTHETIC_V0_1`

This token means only that the schema/firewall behaves as preregistered. It is **not** a scientific PASS, does not authorize Layer A, does not authorize covariance/whitening, does not close G7/G8/G9, and does not increase Article-3 scientific readiness.
