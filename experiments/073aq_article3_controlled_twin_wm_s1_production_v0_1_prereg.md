# Exp073AQ — Article 3 controlled-single-thread twin-replica Wm_S1 production v0.1

**Frozen:** 2026-08-30 after real hosted Exp073AP authorized the `controlled_single_thread_exact_v1` successor route, before any Wm_S1 successor-route output exists, before any other remaining 12-task successor production is launched, before the real 14-window aggregate, and before any Layer-A support value exists.

## Purpose

Exp073AQ is the first **real production-authority gate** under the execution-qualified exact successor route authorized by Exp073AO/AP.

It computes only task `Wm_S1`, twice, on two independent GitHub-hosted runners. It reuses the already-frozen Exp073AA physical/angular executor unchanged and adds only the already-authorized controlled single-thread execution environment, independent artifact persistence, and an exact twin-replica comparator.

This is not a dark-sector model science gate. A PASS creates one non-classifying angular operator authority for future assembly. It adds **0 scientific-readiness points** by itself.

## Frozen upstream succession authority

- Exp073AM controlled Wm_S0 anchor canonical SHA: `8ac59fc060195addcc5cd8b6d75e32fbc6dbfeea8456f4c83e8bf0cf034b9220`.
- Exp073AP real decision: `AUTHORIZE_EXECUTION_QUALIFIED_EXACT_SUCCESSOR_ROUTE`.
- Exp073AP hosted run `33324664267`, job `99292571445`, artifact `9735869454`, digest `sha256:8c60618717777a3c913053bcd5437c0ab548e294e98b72b9c5c869dcc52caacf`.
- Historical Q `SCIENTIFIC_REPEATABILITY_FAIL` remains immutable and is not reclassified.

## Frozen task

Exactly one task:

`Wm_S1`

No other task identity is accepted by this workflow.

## Frozen physical/angular contract

Use the unchanged `ci/exp073aa_article3_des_angular_task_runner_v0_1.py` implementation last modified at commit `45ed8d8d1e90cdaf314e0384b6f3cdfef369925b`.

Therefore preserve exactly:

- genuine DES Y1 R1 source-mask authority;
- source bin S1 count map semantics;
- public DES Y1 redMaGiC lens mask, original weights retained iff `mask>0.5`;
- `NSIDE=4096`, RING/C geometry;
- NaMaster/PyMaster 2.7 lineage;
- true ell `0..12287`;
- frozen 39 bandpowers;
- Wm spin-0 x spin-2 selected component `TE <- TE`;
- canonical selected window `<f8 [39,12288]`;
- no effective ell/z/k;
- no radial kernel, physical-k, support, retained-coordinate, fiducial-P, covariance, nuisance, relation/null, or G8 information.

## Frozen execution controls

Each independent replica must set exactly:

- `OMP_NUM_THREADS=1`
- `OPENBLAS_NUM_THREADS=1`
- `MKL_NUM_THREADS=1`
- `NUMEXPR_NUM_THREADS=1`
- `VECLIB_MAXIMUM_THREADS=1`
- `BLIS_NUM_THREADS=1`
- `OMP_DYNAMIC=FALSE`

Each replica must persist its JSON, NPZ, and environment receipt independently before aggregation.

## Exact admission criterion

After both complete replica artifacts exist, the comparator must validate the frozen metadata/firewall and canonical array identity.

`PASS_EXP073AQ_WM_S1_CONTROLLED_TWIN_EXACT_V0_1` is allowed **only** if all are true:

1. both replica JSON records are valid Exp073AA `Wm_S1` PASS records;
2. all frozen metadata and upstream authorities agree;
3. both environment receipts contain the exact thread controls above;
4. each NPZ contains exactly key `window`;
5. each window is finite canonical `<f8 [39,12288]` and hashes to its own JSON `workspace.selected_window_authority.sha256`;
6. replica canonical SHA256 values are exactly identical;
7. `numpy.array_equal(A,B)` is exactly true.

No tolerance, rounding, ULP allowance, majority vote, preferred replica, or closeness-to-historical-P criterion exists.

If both valid complete replicas reach the comparator but fail exact SHA or `array_equal`, emit:

`SCIENTIFIC_REPEATABILITY_FAIL_EXP073AQ_WM_S1_CONTROLLED_TWIN_EXACT_V0_1`.

This is a task-specific computational-repeatability negative result, not a dark-sector physics falsification.

If either replica or the aggregator fails before a valid comparator classification exists, the run is infrastructure-INCOMPLETE and must not be called repeatability FAIL.

## Anti-leakage firewall

Exp073AQ cannot read or use:

- radial kernels;
- physical support / `f_invalid`;
- retained/rejected observation coordinates;
- fiducial P;
- covariance/whitening;
- nuisance tangent geometry/SVD/rank;
- quotient/relation/null statistics;
- G8;
- downstream readiness optimization.

## Scientific accounting

For either exact PASS or exact repeatability FAIL:

- Article-3 scientific readiness remains `52%`;
- readiness increment is `0`;
- Layer A/B remain OPEN;
- covariance/whitening remain BLOCKED;
- G7/G8/G9 remain OPEN;
- no scientific model PASS may be claimed.

A PASS admits only `Wm_S1` into the future execution-qualified 14-window authority. The next possible Article-3 readiness credit remains the real immutable complete 1410-row pre-support finite-operator candidate manifest.
