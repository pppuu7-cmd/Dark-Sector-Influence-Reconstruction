# Exp073FD — Exp073FA WW_S0_S2 home execution envelope v0.1 preregistration

Date: 2026-09-06. Scope: DSIR only.

## Purpose

Freeze and statically audit the fail-closed self-hosted execution envelope for the already frozen Exp073FA ordered distinct-field `WW_S0_S2` A/B science gate. Exp073FD is implementation/resource governance only (`+0/+0`). It cannot create WW authority.

## Prospective authority bindings

- Exp073FA science prereg blob: `edc044792be8ac7b796c8469943924942ae91932`.
- Frozen source head: `de83e20a68f79ccf25b89b0d33eb4206e294c757`.
- Frozen contract fingerprint: `b7845df5ce4bc2bd730461476b7ff0831512003ceb5b3558436005c9876bd251`.
- Exp073FC run/job: `34018341064 / 101446155067`, required exact token `PASS_EXP073FC_EXP073FA_COMMITTED_DRIVER_BINDING_V0_1` and classification `SUPPORT_PLUS_0_PLUS_0`.
- Committed driver Git blobs: v0.1 `5c5d6aff574b8ed2679e345903d5f4447e5d5c18`; v0.2 `9bde1f5de584e3378a1fe365e7a9771146fe08b5`.
- Exp073FB-generated byte SHA256s already bound by FC: v0.1 `fe354b95e9aeefe0772f4c7eecbba6e1944fb1f4955fceb3e9e72ed1c06b293a`; v0.2 `77f321e22c923d8d5996105487cae9afb6eecc5863174d849b092164a26824ba`.
- Qualified PyMaster-2.7 FITS-read file-backed patch Git blob: `d534b698f9131688d263eedcef27260386c58641` (`patches/namaster-v2.7-dsir-filebacked-mcm-read-v0.2.patch`).
- R1 artifact `9720335366`, digest `sha256:ff87d8fc7d53b16b786a4eb3d6ffeb103676efb8a548223a187b9f59689f8abd`.
- NaMaster source head `24365fa59a38c15732f4f37e8b29265b75c442d5`.

## Frozen science semantics inherited without modification

Ordered `(S0,S2)`, R1 indices `[0,2]`; NSIDE=4096; ell `0..12287`; 39 bands; full public BPW `[4,39,4,12288]`; selected `EE<-EE <f8 [39,12288]`; one regular-file-backed unbinned MCM exactly `19,327,352,832` bytes; public `read_from(...,read_unbinned_MCM=True) -> get_bandpower_windows()` route; exact SHA equality plus `numpy.array_equal`; all finite; no tolerance/rounding/smoothing/averaging/manual reconstruction rescue.

Durable namespaces remain exactly `checkpoints/exp073fa-ww-s0-s2-a-v0-1` and `checkpoints/exp073fa-ww-s0-s2-b-v0-1`, with ordered six-stage chain `fresh_sources_complete -> fresh_workspace_mcm_complete -> mcm_fits_verified -> full_window_complete -> selected_ee_complete -> replica_receipt_complete` and fail-closed restore identity checks. No historical WW numerical import and no other-replica numerical output read are permitted.

Candidate token remains exactly `PASS_EXP073FA_WW_S0_S2_FILEBACKED_AB_EXACT_REPEATABILITY_V0_1`; candidate alone does not create authority.

## Home envelope requirements

1. Exactly one DSIR self-hosted owner, enforced by local `flock` plus live GitHub queued/in-progress self-hosted exclusivity audit.
2. Runner labels `[self-hosted, Linux, X64]`; CPU affinity exactly 8; `OMP_NUM_THREADS=8`; nested OpenBLAS/MKL/NumExpr/BLIS/vecLib threads pinned to 1. This gate is a monolithic NaMaster workspace calculation, so the repository's 8-worker band scheduler is not applicable without changing arithmetic; no such change is permitted.
3. WSL and Windows C: free-space floor at least 50 GiB before science.
4. Reuse qualified PyMaster 2.7 stock/patched environments when exact build identity matches; otherwise rebuild prospectively from the frozen NaMaster head and frozen read-patch.
5. Run the exact local Exp073EM storage qualifier before science.
6. Validate R1 artifact digest and authority before source reconstruction.
7. Execute A then B independently, checkpointing every complete stage. A complete replica may be pruned only after validating its immutable receipt and selected-EE SHA; selected output and manifests remain durable.
8. Produce compact terminal/partial evidence on every outcome. Workflow SUCCESS alone is never scientific PASS.

## Exp073FD hosted audit acceptance

The hosted audit must fail closed on prereg/envelope/driver/patch/blob identities, require Exp073FC terminal SUCCESS and raw PASS token, compile the frozen drivers, assert `(S0,S2)`/`[0,2]`/checkpoint/public-BPW/exact-only semantics, and reject tolerance-rescue constructs. PASS token: `PASS_EXP073FD_EXP073FA_HOME_ENVELOPE_STATIC_AUDIT_V0_1`, classification `SUPPORT_PLUS_0_PLUS_0`, `ww_s0_s2_authority_created=false`.

Only after hosted PASS and live zero competing self-hosted DSIR ownership may the same prospectively frozen workflow dispatch exactly one Exp073FA home science job. Any infrastructure failure is `+0/+0` and must preserve valid checkpoints; any exact A/B mismatch under the frozen contract is scientific FAIL and must not be repaired post hoc.