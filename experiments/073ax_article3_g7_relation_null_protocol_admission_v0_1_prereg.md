# Exp073AX — Article 3 G7 relation/null protocol admission schema v0.1

**Frozen:** 2026-08-30, while Exp073AQ Wm_S1 is still running, before any real successor-route covariance, nuisance singular value, quotient response, G7 relation fit, G7 relation statistic, null-control result, withheld-family selection, or G8 read exists.

## Purpose

Article 3 already has the mathematical quotient geometry and a prospectively frozen numerical nuisance-rank rule. Older Exp063A architecture also established an important anti-selection principle: before fitting a discovery relation, freeze the discovery subset, exact mathematical relation form, scalar statistic/decision rule, and null/permutation control, then freeze the fitted relation before any withheld-family exposure.

The current execution-qualified successor chain (`Exp073AR -> AS -> AT -> AU -> AV -> AW`) did not yet have a fail-closed admission schema enforcing that rule. Exp073AX closes only that protocol/provenance gap.

Exp073AX **does not choose a physical relation**, does not fit any relation, does not define a discovery claim, does not read real quotient vectors, does not choose or read a withheld family, and does not execute G7 or G8. It is synthetic architecture QA and earns zero scientific-readiness credit.

## Frozen upstream lineage required for any future G7 discovery protocol

A future concrete G7 protocol must bind, by immutable hosted receipts/hashes, the same successor authority chain:

1. execution-qualified angular authority under Exp073AR;
2. complete immutable 1410-row pre-support manifest under Exp073AS;
3. real Exp073AT-admitted Layer-A PASS;
4. real Exp073AU-admitted Layer-B PASS on the same parent authority;
5. real Exp073AV-admitted covariance/whitening PASS on the same final retained coordinates;
6. Exp073AW nuisance rank/resolution rule and a future hosted nuisance/projector/quotient authority produced under that rule.

The authority route must remain exactly `controlled_single_thread_exact_v1`.

## Prospective protocol-registration requirements

Before any G7 discovery fit or statistic is evaluated, a separate concrete G7 protocol must already and immutably freeze all of the following:

- `discovery_manifest_sha256`: exact ordered discovery/training model-family/parameter and coordinate membership, with no withheld family included;
- `relation_form_id` and `relation_form_sha256`: exact mathematical relation family/form, including all basis functions or functional restrictions;
- `statistic_id` and `statistic_sha256`: exact scalar diagnostic/statistic evaluated on the discovery quotient representation;
- `decision_rule_id` and `decision_rule_sha256`: exact tolerance/threshold/tail convention, including equality handling;
- `null_control_id` and `null_control_sha256`: exact permutation/synthetic/null-control construction and its decision logic;
- `fit_rule_id` and `fit_rule_sha256`: deterministic fitting/estimation rule, including parameter constraints and tie handling;
- `quotient_authority_requirement_sha256`: immutable statement of the required real successor quotient authority and its coordinate order;
- `relation_freeze_before_g8_required=true`.

All SHA256 fields are lowercase 64-hex content hashes. Human-readable labels alone are not authority.

## Withheld/G8 firewall

At protocol-registration time and through the complete G7 discovery fit:

- `withheld_family_selected=false`;
- `withheld_reads=[]`;
- `G8_reads=[]`;
- no withheld-family data/model response may contribute to relation choice, relation basis, fit rule, statistic, threshold/tolerance, null control, nuisance rank, or discovery subset;
- no candidate protocol may be ranked or chosen by later G8 performance.

A fresh withheld family may be selected/exposed only **after** the fitted G7 relation, fitted parameters, discovery statistic, null-control output, and their immutable hashes are frozen in a hosted relation-freeze artifact.

## Representation/resolvability firewall

A future protocol must preserve the final observational representation and Exp073AW status semantics:

- unresolved nuisance directions remain explicit records;
- `NUMERICALLY_UNRESOLVED_NUISANCE_RANK_BOUNDARY` blocks quotient/G7;
- no target-dependent SVD threshold or nuisance deletion is allowed;
- no relation statistic may be evaluated on an incomplete/intermediate theory representation and then described as final observational evidence;
- exact-threshold ambiguity in any upstream scientific gate remains unresolved rather than rounded.

## Scientific-language boundary

Even a future valid G7 PASS may establish only a relation/null result in the prospectively frozen observational quotient. It does **not** by itself establish causality, microscopic identity, a universal dark-sector law, or a fundamental theory. G8 remains a required fresh withheld-family falsification layer and G9 remains open unless separately closed.

## Synthetic admission object

Exp073AX synthetic QA validates only a protocol-registration receipt. The baseline receipt must assert:

- `schema=exp073ax_article3_g7_relation_null_protocol_admission_v0_1`;
- `authority_route=controlled_single_thread_exact_v1`;
- exact ordered `upstream_chain=[exp073ar,exp073as,exp073at,exp073au,exp073av,exp073aw]`;
- `real_quotient_authority_required=true`;
- all six concrete protocol components above are frozen before fit and have valid SHA256 values;
- `fit_executed=false`;
- `target_quotient_read=false`;
- `withheld_family_selected=false`;
- `withheld_reads=[]` and `G8_reads=[]`;
- `relation_freeze_before_g8_required=true`;
- `causal_claim_authorized=false` and `fundamental_law_claim_authorized=false`;
- `article3_scientific_readiness_percent=52` and `readiness_increment=0`;
- `G7=OPEN`, `G8=OPEN`, `G9=OPEN`.

## Required synthetic controls

The validator must fail closed for at least the following cases:

1. valid baseline receipt -> PASS;
2. wrong authority route;
3. missing/reordered upstream successor chain;
4. missing real-quotient requirement;
5. discovery manifest not frozen before fit;
6. relation form not frozen before fit;
7. statistic not frozen before fit;
8. decision rule not frozen before fit;
9. null control not frozen before fit;
10. fit rule not frozen before fit;
11. malformed/missing component SHA256;
12. discovery manifest includes a withheld family;
13. withheld family already selected;
14. any `withheld_reads` entry;
15. any `G8_reads` entry;
16. fit already executed at protocol-registration stage;
17. target quotient already read at protocol-registration stage;
18. relation-freeze-before-G8 requirement disabled;
19. target-dependent nuisance-rank permission;
20. tolerance/rounding rescue of unresolved nuisance rank;
21. covariance/whitening repair permission (jitter/clipping/pseudowhitening);
22. causal claim authorization;
23. fundamental-law claim authorization;
24. nonzero readiness increment or readiness drift;
25. G7/G8/G9 prematurely closed;
26. protocol chosen using G8 performance;
27. effective-coordinate or fiducial-P shortcut permission;
28. malformed unknown keys in protected firewall fields.

## Positive token and classification

Required token:

`PASS_EXP073AX_G7_RELATION_NULL_PROTOCOL_ADMISSION_SYNTHETIC_V0_1`

Required classification:

`HOSTED_SYNTHETIC_PASS_NON_SCIENTIFIC_PLUS_0_READINESS`

A hosted PASS closes only the successor-route **protocol-admission architecture**. It does not authorize a G7 fit until a separate concrete relation/statistic/null protocol is prospectively frozen and all real upstream authorities exist.
