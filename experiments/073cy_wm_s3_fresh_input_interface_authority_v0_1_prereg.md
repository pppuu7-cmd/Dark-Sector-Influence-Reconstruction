# Exp073CY v0.1 — Wm_S3 fresh-input/interface authority audit

Status: prospectively frozen hosted source/interface support gate; accounting `+0/+0`; cannot create Wm_S3 authority or activate Exp073BU.

Prerequisite: Exp073CX v0.1 is historical `A2_IMPLEMENTATION_CONTRACT_FAIL +0/+0` because the unified production A/B driver is absent. Exp073CW H1 and Exp073CV I1 remain admitted component evidence.

## Purpose
Before implementing the missing unified production A/B driver, machine-discover and bind the exact repository authority for fresh Wm_S3 source input and DES lens input/reconstruction interfaces. Chat-history values, SHA prefixes and historical numerical outputs are forbidden substitutes.

## Frozen search identities
- Wm_S3 pixel-record SHA256 to locate in repository authority: `3996f2bacf29d46278773530058d4f5666c0d590bf6cebc06459659166bc60ec`.
- DES lens filename to locate in repository authority: `DES_Y1A1_3x2pt_redMaGiC_MASK_HPIX4096RING.fits`.
- Required implementation/interface concepts: fresh source reconstruction, fresh lens reconstruction, `NmtField`, same-field PCL/workspace handoff, stock `write_to()`, exact 39-band edges and the six Exp073CD checkpoint boundaries.

## Fail-closed classification
- `Y1_FRESH_INPUT_INTERFACE_AUTHORITY_COMPLETE`: exact repository paths provide both frozen input identities and sufficient callable/CLI production interfaces to assemble the unified driver without guessing.
- `Y2_FRESH_INPUT_AUTHORITY_FOUND_INTERFACE_GAP`: both exact input identities are repository-bound, but one or more required production interfaces remain absent.
- `Y3_FRESH_INPUT_AUTHORITY_INCOMPLETE`: one or both exact input identities cannot be located exactly in tracked repository authority.
- `Y4_INFRASTRUCTURE_OR_SOURCE_SCAN_FAIL`: repository/source scan itself fails.

The receipt must list exact matching tracked paths and interface hits. No numerical science is run and no scientific PASS/FAIL is scored.
