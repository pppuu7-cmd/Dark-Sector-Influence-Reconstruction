# Exp073CF v0.1 — Exp073BU executable-component inventory

Status: PROSPECTIVE / HOSTED-ONLY / SUPPORT +0/+0 / NO Wm_S3 AUTHORITY.

Purpose: after Exp073CE I1 static contract PASS, identify repository-native executable source locations for the already validated component lineages before assembling any self-hosted Exp073BU scientific workflow. This gate performs no Wm_S3 numerical computation.

Frozen inventory predicates:
1. Search only this DSIR repository at the exact activation head.
2. Record paths and Git blob SHAs for evidence of: fresh replica-local PCL construction; full-stock NaMaster ncls=2 operation route; stock workspace persistence (`write_to` or native writer); verified OS-mmap row-stream consumer; durable checkpoint/resume implementation with provenance/SHA binding; 8-worker/nested-thread=1 heavy architecture.
3. Historical Wm_S3 numerical payloads, historical PCL/reference bands and old candidate outputs are evidence exclusions, never reusable executable inputs.
4. No source is silently promoted from diagnostic/resource support to scientific authority. Inventory only establishes implementation availability.
5. `E1_EXECUTABLE_COMPONENT_SET_IDENTIFIED` requires at least one exact repository path for every predicate above. Otherwise `E2_EXECUTABLE_COMPONENT_SET_INCOMPLETE`; tool/source failure is `E3_INFRASTRUCTURE_INCOMPLETE`.
6. All outcomes are +0/+0. Exp073BU remains NOT ACTIVATED and Wm_S3 authority remains absent.
