# Exp073AL — Article 3 AI-vs-primary exact stability classifier v0.1

**Frozen:** 2026-08-30 while Exp073AI run `33310888983` is still in progress, before any Exp073AI replica artifact or final numerical authority exists, and before any future Exp073AI succession amendment is allowed.

## Purpose

Exp073AL is a **non-scientific cross-route stability classifier**. It prospectively freezes how a future valid Exp073AI terminal classification is compared against the already hosted Exp073X2R primary-P canonical Wm_S0 authority.

It answers a narrower question than Exp073AI itself:

> If the single-thread route reaches a valid internal exact PASS, is its exact canonical selected-window authority also identical to the previously hosted primary-P canonical authority?

This prevents a future internal AI PASS from being treated post hoc as evidence of cross-route stability if the exact operator hash has moved.

Exp073AL does **not** alter Exp073AI's own PASS criterion. Exp073AI is allowed to PASS when its own two controlled replicas are exactly identical, regardless of historical P. Exp073AL separately classifies historical exact stability after that valid AI result exists.

Exp073AL does not authorize Exp073AA production, does not create a succession amendment, does not rescue the historical Q repeatability FAIL, and contributes **0 scientific-readiness points**.

## Frozen historical authority

Exp073X2R primary P:

- authority class: hosted exact repeatability PASS / non-classifying;
- canonical selected-window SHA256:
  `6ec29f6dbbcf0c29d7af9d6eb98d271bcd06e00d28cebe658b5e266f7ef18d0f`.

Historical Exp073X2 Q remains immutable `SCIENTIFIC_REPEATABILITY_FAIL` and is never reclassified by Exp073AL.

## Required AI completion input

A future real Exp073AL caller may operate only after Exp073AK2 has classified Exp073AI as one of:

- `VALID_HOSTED_EXP073AI_CLASSIFICATION`, carrying verbatim one of the frozen Exp073AI numerical tokens;
- an infrastructure-INCOMPLETE class;
- `INVALID_CONTROL_PLANE_STATE_NO_SCIENCE_CLASSIFICATION`.

Exp073AL must not infer validity from partial job logs or partial artifacts.

For a valid AI numerical PASS, the future input must also carry the immutable canonical selected-window SHA256 from the valid final Exp073AI authority.

For AI numerical FAIL or any incomplete/invalid completion class, no AI canonical SHA is admissible for cross-route stability classification.

## Frozen classification state machine

### 1. AI not validly classified

If Exp073AK2 completion class is not `VALID_HOSTED_EXP073AI_CLASSIFICATION`:

`NO_CROSS_ROUTE_STABILITY_CLASSIFICATION_AI_NOT_VALID`

No stability claim is permitted.

### 2. Valid AI scientific-repeatability FAIL

If completion class is valid but numerical token is

`SCIENTIFIC_REPEATABILITY_FAIL_EXP073AI_SINGLE_THREAD_EXACT_V0_1`:

`CROSS_ROUTE_STABILITY_BLOCKED_AI_INTERNAL_REPEATABILITY_FAIL`

No canonical AI hash is accepted.

### 3. Valid AI PASS with historical-P exact identity

If numerical token is

`PASS_EXP073AI_SINGLE_THREAD_EXACT_REPRODUCIBILITY_V0_1`

and AI canonical SHA256 exactly equals

`6ec29f6dbbcf0c29d7af9d6eb98d271bcd06e00d28cebe658b5e266f7ef18d0f`,

classify:

`EXACT_CROSS_ROUTE_STABILITY_AI_EQUALS_PRIMARY_P`

This is still a non-scientific reproducibility/stability result and does not authorize production.

### 4. Valid AI PASS with a different exact authority

If AI internally PASSes but its canonical SHA256 differs from primary P, classify:

`DETERMINISTIC_SINGLE_THREAD_ROUTE_BUT_EXACT_AUTHORITY_SHIFT_FROM_PRIMARY_P`

This is a negative cross-route exact-stability result. It blocks any claim that the exact operator authority is invariant across the two execution routes. It does not retroactively make Exp073AI an internal FAIL.

### 5. Invalid input/token/hash combinations

Hard reject. Examples:

- valid AI PASS without a canonical SHA;
- valid AI FAIL carrying a canonical SHA;
- incomplete/invalid completion state carrying a canonical SHA;
- unknown numerical token;
- malformed SHA;
- changed primary-P SHA;
- extra/unknown schema fields.

## Anti-leakage firewall

Exp073AL may depend only on:

- frozen primary-P canonical SHA;
- Exp073AK2 completion class;
- the verbatim valid final Exp073AI numerical token;
- the canonical AI SHA only for a valid AI PASS.

It must not read or use:

- angular array values;
- environment diagnostics/AJ2 label;
- radial kernels;
- support fractions or retained coordinates;
- fiducial P;
- covariance or whitening;
- nuisance geometry/SVD/rank;
- quotient/relation/null results;
- G8;
- readiness optimization.

The independent AJ2 provenance label may later be reported alongside Exp073AL but may not alter its exact hash classification.

## Scientific accounting

Always retain:

- Article-3 strict scientific readiness `52%`;
- readiness increment `0`;
- G7/G8/G9 `OPEN`;
- Layer A/B `OPEN`;
- no model scientific PASS;
- no production release.

## Hosted synthetic test matrix

At minimum:

1. valid AI PASS + same P hash -> exact cross-route stability;
2. valid AI PASS + different valid hash -> exact authority shift;
3. valid AI FAIL + no hash -> internal-fail block;
4. infrastructure incomplete + no token/hash -> no classification;
5. invalid control plane + no token/hash -> no classification;
6. valid PASS missing hash -> reject;
7. valid FAIL carrying hash -> reject;
8. incomplete carrying hash -> reject;
9. unknown token -> reject;
10. malformed hash -> reject;
11. primary-P identity drift -> reject;
12. unknown root field -> reject;
13. readiness/gates/firewall remain unchanged.

## Required hosted synthetic QA token

`PASS_EXP073AL_AI_VS_PRIMARY_EXACT_STABILITY_CLASSIFIER_SYNTHETIC_V0_1`

This token means only that the prospective classifier/state machine behaves as frozen. It is not a real AI-vs-P classification, not a scientific PASS, and not production authority.
