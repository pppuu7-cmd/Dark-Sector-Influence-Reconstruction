# Exp073BT — Article-3 NaMaster-2.7 Wigner linkage BJ-environment successor v0.2 — preregistration

**Project:** DSIR only. **Classification:** NONCLASSIFYING infrastructure/source-linkage diagnostic. **Accounting:** `+0/+0` for every outcome.

Frozen prospectively on 2026-09-03 after Exp073BT v0.1 run `33781686508` failed before job creation. GitHub exposed zero jobs for that run, and the v0.1 workflow contains an invalid YAML `run:` scalar at the inherited-diagnostic execution step: `run: "${NMT_PY}" ci/exp073br_namaster27_wigner_linkage_failure_capturing_v0_1.py`. This v0.2 is a control-plane syntax repair only; the historical v0.1 pre-job failure remains immutable.

## Preserved authority and diagnostic science

- Exp073BJ terminal Track-A exact authority PASS remains unchanged.
- Exp073AQ permanent hosted exact-repeatability scientific FAIL remains unchanged.
- Exp073BS and Exp073BT v0.1 remain historical infrastructure/source-linkage incomplete results.
- The exact inherited diagnostic implementation remains `ci/exp073br_namaster27_wigner_linkage_failure_capturing_v0_1.py` at implementation commit `8a70892c9533206e4011eee041914ca89bae2290`.
- Exp073BT v0.1 preregistration remains `07c17496597306ff410633264d1d050f833728b9`.
- No scientific/readiness credit is available from this repair or any BT result.

## Sole allowed v0.2 repair

Preserve the v0.1 workflow logic and hosted Exp073BJ NaMaster-2.7 environment exactly. Replace only the malformed single-line YAML command scalar with a valid block scalar executing the same command:

```yaml
run: |
  "${NMT_PY}" ci/exp073br_namaster27_wigner_linkage_failure_capturing_v0_1.py
```

No diagnostic implementation, environment package set, evidentiary branch, status translation, timeout, runner image, or interpretation may change.

## Hosted audit / execution order

The v0.2 workflow must first run a hosted `static-audit` job that fail-closes on the immutable v0.1 preregistration and inherited diagnostic lineage and verifies that the repaired workflow contains the exact block-scalar command above while retaining `ubuntu-24.04`, conda-forge Python 3.11 + NaMaster 2.7 lineage, and `+0/+0` receipt firewalls. The diagnostic job may execute only with `needs: static-audit` after that hosted audit passes.

## Frozen terminal labels and interpretation

The inherited BR diagnostic is reused unchanged and its internal `BR_` prefix is translated administratively to `BT_` exactly as v0.1 froze. Allowed labels remain:

- `BT_Q1_EXTENSION_EXPORTS_DRC3JJ`
- `BT_Q2_LINKED_DEPENDENCY_EXPORTS_DRC3JJ`
- `BT_Q3_DYNAMIC_SYMBOL_ABSENT_SOURCE_REFERENCE_FOUND`
- `BT_Q4_DYNAMIC_SYMBOL_AND_INSTALLED_SOURCE_REFERENCE_ABSENT`
- `BT_Q5_PARTIAL_DIAGNOSTIC_INCOMPLETE`

Q1–Q4 are source/linkage evidence only. Q5 is infrastructure/diagnostic incomplete. Every outcome is non-authoritative for Wm_S2, Wm_S3, WW, Layer-A/B, covariance/whitening, G7/G8 or new physics and carries `scientific_readiness_increment=0` and `draft_data_readiness_increment=0`.

## Firewalls

- No modification of the inherited diagnostic implementation.
- No tolerance, ULP, rounding, averaging, majority-vote, preferred-replica or result-driven rescue.
- No use of provisional branches as downstream authority.
- No covariance/whitening/nuisance/quotient/relation/null/G8 reads or claims.
- No home/self-hosted runner usage.
- No effect on the fresh-independent-PCL Wm_S3 A/B frontier.

**Readiness remains:** `Verified 52.0% | Draft/data 54.6%`.
