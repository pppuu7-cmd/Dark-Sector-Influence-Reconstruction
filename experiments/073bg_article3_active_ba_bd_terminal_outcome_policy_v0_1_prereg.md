# Exp073BG — Article-3 active BA/BD terminal-outcome policy v0.1 preregistration

**Preregistered:** 2026-08-31 while Exp073BA `33345968620` and Exp073BD `33342265114` are both still `in_progress`.  
**Scope:** DSIR Article-3 only.  
**Type:** prospective governance/provenance classification prerequisite; no scientific data read beyond run status/step state.  
**Scientific readiness increment:** `+0`.  
**Draft/data readiness increment:** `+0` at preregistration.

## Frozen input

Normative decision table was committed before terminal outcomes were known:

`e4a8391d671c3835c00f729a4743e7c07cb3199e` — `docs/EXP073BA_BD_TERMINAL_OUTCOME_DECISION_TABLE_2026-08-31.md`.

No later result may weaken or reinterpret that table.

## Frozen classification rule

### Exp073BA Track A

- Failure/timeout/OOM/missing valid artifact before a frozen exact comparator has two complete valid inputs => infrastructure/resource/harness failure, not scientific FAIL.
- Exact inequality from a frozen comparator supplied with two complete valid Track-A replicas => scientific exact-repeatability FAIL for that BA stage regardless of mismatch magnitude.
- Scientific PASS requires the full frozen BA chain to complete through exact compact PASS, both finalizers, exact final PASS, and an immutable valid authority artifact. Job success without authority artifact is insufficient.
- BA individual-window authority remains `+0` scientific-readiness increment under the frozen accounting ledger.

### Exp073BD Track P

- Any incomplete/missing branch => `P3 PROVISIONAL_INCOMPLETE_NO_DOWNSTREAM_USE`, no new draft/data credit.
- Two complete finite `[39,12288]` branches plus valid pair diagnostic => provisional branch pair eligible for downstream sensitivity propagation, irrespective of exact A/B equality; both branches must be retained.
- Such completion makes Wm_S2 the third complete angular data object under the already-frozen Track-P ledger, so Draft/data becomes `54.57142857142857%`; Verified remains `52.0%`.
- BD can never become Track-A authority retroactively because it was frozen and run as Track P.
- `downstream_claim_classification=NOT_YET_EVALUATED` is not P1/P2 claim authorization.

## Immutable historical constraint

Exp073AQ remains permanent `SCIENTIFIC_REPEATABILITY_FAIL_EXP073AQ_WM_S1_CONTROLLED_TWIN_EXACT_V0_1`. Exp073BG cannot repair or supersede that historical classification.

## Anti-leakage / G7 ordering

No BA/BD terminal outcome authorizes a shortcut. Required order remains:

`validated physical forward/power-input bridges -> preregistered physical support-validity mask -> Layer A/Layer B prerequisites -> covariance restriction/whitening -> nuisance tangent rank/SVD -> quotient/relation/null control -> actual G7 authorization -> fresh G8 withheld family`.

No covariance/whitening, G8, or withheld-family information may leak into support selection or earlier stages.

## Execution rule

Exp073BG launches no heavy workflow. It exists solely to bind future interpretation of the already-running BA/BD runs before their outcomes are known. Terminal handling must cite the immutable hosted artifacts and this prospective policy.
