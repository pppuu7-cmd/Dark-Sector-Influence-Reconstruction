# Exp073GI — DSIR-4 angular-authority binding static audit v0.1

Classification target: `SUPPORT_PLUS_0_PLUS_0` only.

Frozen object: `docs/dsir4/DSIR4_ANGULAR_AUTHORITY_BINDING_CONTRACT_V0_1.md` as created before this audit.

The audit must verify, without numerical model evaluation:
- predecessor `G_DOMAIN_MAPPING` is a required precondition;
- complete symmetric WW source-pair inventory S0..S3 is enumerated;
- candidate/workflow-success/partial-checkpoint evidence is forbidden as authority;
- exact `EE<-EE`, `<f8 [39,12288]`, NSIDE=4096, ell 0..12287, 39-band semantics are preserved;
- missing authority maps to `NOT_YET_TESTABLE`, never PASS/FAIL;
- infrastructure/resource failure remains distinct from scientific failure;
- no tolerance, effective-coordinate, fiducial-P, interpolation, smoothing, averaging or source-pair substitution rescue is allowed;
- this static audit creates no `G_ANGULAR_AUTHORITY`, no scientific model authority, and starts no self-hosted science.

Exact support token on success:
`PASS_EXP073GI_DSIR4_ANGULAR_AUTHORITY_BINDING_STATIC_AUDIT_V0_1`

Required emitted flags:
`classification=SUPPORT_PLUS_0_PLUS_0`
`g_angular_authority_created=false`
`scientific_model_authority_created=false`
`self_hosted_science_started=false`
