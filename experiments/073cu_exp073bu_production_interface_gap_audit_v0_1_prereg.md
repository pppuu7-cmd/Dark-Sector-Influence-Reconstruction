# Exp073CU v0.1 — Exp073BU production-interface gap audit

Status: PROSPECTIVE / HOSTED-ONLY / SUPPORT +0/+0 / NO Wm_S3 AUTHORITY.

Collision authority: exact recursive tree before reservation contained no `073cu` path.

Motivation: Exp073CT T1 recovered exact interfaces and showed that the validated full-stock/mmap QA components expose synthetic `--emulator/--out` interfaces, whereas the fresh-PCL component exposes a production-data CLI and the CR resource driver exposes checkpoint/resource orchestration. Before assembling a scientific driver, determine exactly which validated primitives are production-capable versus synthetic-only so synthetic QA is never silently promoted into Wm_S3 science.

PASS-like diagnostic tokens:
- `U1_PRODUCTION_ASSEMBLY_ROUTE_COMPLETE` only if repository-native frozen sources expose production-data interfaces covering fresh PCL, stock full MCM/window generation, persistence/mmap consumption, checkpoint/resume and final exact A/B receipt.
- `U2_PRODUCTION_ASSEMBLY_GAP_IDENTIFIED` if one or more required production interfaces are absent while their synthetic QA analogues exist.
- `U3_INFRASTRUCTURE_INCOMPLETE` for audit failure.

U1/U2/U3 are all support `+0/+0`; none activates Exp073BU. If U2, the receipt must list exact missing production stages and exact validated source blobs from which a prospective production implementation may be derived without changing arithmetic.
