# Exp073CT v0.1 — Exp073BU executable component interface contract audit

Status: PROSPECTIVE / HOSTED-ONLY / SUPPORT +0/+0 / NO Wm_S3 AUTHORITY.

Collision authority: exact recursive repository tree at pre-Exp073CS head `56ae01872478f72325b14feea244284ad35e9bbd` contained no `073ct` path. Exp073CT is reserved prospectively.

Purpose: after Exp073CS exact blob binding, statically recover machine-checkable callable/CLI interfaces of the frozen executable components needed to assemble one future Exp073BU A/B driver without editing their scientific arithmetic.

PASS `T1_COMPONENT_INTERFACE_CONTRACT_COMPLETE` requires: (a) every Exp073CS blob remains exact; (b) fresh-PCL source exposes `build_fresh_pcl` and a CLI with `--r1-root`, `--lens-mask`, `--work-root`, `--out-npy`, `--out-json`; (c) each remaining frozen Python component parses as valid Python and exposes at least one callable/main entry point; (d) checkpoint sync shell source exists with exact bound blob; (e) emitted receipt records discovered functions and CLI flags for assembly. Failure is `T2_COMPONENT_INTERFACE_CONTRACT_INCOMPLETE`; infrastructure failure `T3_INFRASTRUCTURE_INCOMPLETE`.

This gate does not run Wm_S3 numerics, does not alter component code, cannot activate Exp073BU and is always +0/+0.
