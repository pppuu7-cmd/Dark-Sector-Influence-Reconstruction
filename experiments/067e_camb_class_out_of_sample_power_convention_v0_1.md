# Exp067E — CAMB↔CLASS out-of-sample physical power-convention certification v0.1

Date: 2026-08-26
Status: PREREGISTERED BEFORE FIRST OUT-OF-SAMPLE SOLVER COMPARISON

## Motivation and immutable lineage

Exp067B remains permanently `FAIL_CAMB_CLASS_PHYSICAL_POWER_CONVENTION_BRIDGE_V0_1`. Its spectral convention comparisons passed, but a preregistered raw-CAMB rank-one coherence bound failed. Exp067C localized that coherence defect to native CAMB powers. Exp067D causally established that the defect is generated by float32-first multiplication of CAMB `MatterTransferData%TransferData` values and disappears when the same stored transfers are promoted before multiplication.

Exp067E is a new certification, not a rescue of Exp067B. It asks whether the corrected physical convention works prospectively on two previously untested LambdaCDM reference cosmologies while the pinned CAMB numerical precision signature remains explicitly controlled.

## Frozen solver pins

- CAMB `cmbant/CAMB@fa3f097343fbbe427cc04b4f5f0041c22c6ec764`
- CLASS `lesgourg/class_public@e85808324f51fc694d12e3ed7439552a3c3f9540`

Linear scalar spectra only. No nonlinear correction.

## Frozen physical convention

Use physical `k [Mpc^-1]` and power in physical `Mpc^3` units.

CAMB variables:

- `P_mm = P(delta_nonu,delta_nonu)`;
- `P_Wm = P(Weyl,delta_nonu)`;
- `P_WW = P(Weyl,Weyl)`;
- operational CAMB `Weyl = k^2(phi+psi)/2`.

CLASS construction:

\[
q_W(k,z)=\frac{k^2(\phi+\psi)}{2d_m},
\quad
P_{Wm}=q_W P_{mm},
\quad
P_{WW}=q_W^2P_{mm}.
\]

No Poisson equation or quasistatic replacement may be inserted.

## Frozen target support

For every reference cosmology:

`z = [0.0, 0.5, 1.0, 2.0]`

`k = [0.005, 0.02, 0.05, 0.10, 0.20] Mpc^-1`.

Interpolation rules are inherited from Exp067B:

- CAMB: direct `PkInterpolator`, `hubble_units=False`, `k_hunit=False`;
- CLASS: log-linear interpolation for positive `P_mm` and linear interpolation in `ln k` for signed `q_W` on CLASS native grids.

## Frozen reference cosmologies

All use `Omega_k=0`, `mnu=0`, `N_massless=3.046`, `T_cmb=2.7255`, `YHe=0.24`, `tau=0`, `w=-1`, scalar adiabatic initial conditions and pivot `0.05 Mpc^-1`.

### R0 — regression anchor

- `h=0.67`
- `omega_b=0.0224`
- `omega_cdm=0.1200`
- `A_s=2.10e-9`
- `n_s=0.965`

R0 reproduces the Exp067B reference and is a regression anchor only.

### R1 — fresh low-matter/high-h reference

- `h=0.72`
- `omega_b=0.0220`
- `omega_cdm=0.1050`
- `A_s=2.00e-9`
- `n_s=0.970`

### R2 — fresh high-matter/low-h reference

- `h=0.62`
- `omega_b=0.0230`
- `omega_cdm=0.1350`
- `A_s=2.20e-9`
- `n_s=0.960`

R1 and R2 are frozen before any CAMB↔CLASS comparison in those cosmologies.

## Frozen spectral criteria

For each reference independently define

\[
D_{mm}=\max|\ln(P_{mm}^{CLASS}/P_{mm}^{CAMB})|,
\]

\[
D_{WW}=\max|\ln(P_{WW}^{CLASS}/P_{WW}^{CAMB})|,
\]

\[
D_{Wm}=\max|\ln(|P_{Wm}^{CLASS}|/|P_{Wm}^{CAMB}|)|.
\]

The Exp067B bound is inherited unchanged:

`D_mm <= 0.03`, `D_WW <= 0.03`, `D_Wm <= 0.03`.

Require the CAMB and CLASS `P_Wm` signs to match on all 20 target cells per reference.

## Frozen CLASS internal control

For each reference, CLASS built-in Weyl power and the explicit `q_W^2 P_mm` construction must share the same native grids and satisfy maximum relative difference `<=1e-10`.

## Frozen CAMB precision-signature control

Raw CAMB rank-one coherence is **not** assigned a relaxed physical threshold. Instead, on every native CAMB k node in `0.005<=k<=0.2 Mpc^-1` for each reference:

1. construct the official native residual `r_off=P_Wm^2/(P_WW P_mm)-1`;
2. reconstruct `r32` using explicit float32-first multiplication of the same stored `delta_nonu` and `Weyl` transfers;
3. promote those same stored transfers to float64 before multiplication and construct `r64`.

Require:

- `max_abs(r32-r_off) <= 5e-12`;
- `max_abs(r64) <= 1e-12`;
- common-factor reconstruction spread among `mm/Wm/WW <= 5e-12`.

These tolerances are inherited from the causal Exp067D mechanism audit, not tuned from R1/R2 outputs.

No criterion requires raw CAMB `max|r_off| <= 5e-8`; that obsolete condition remains attached only to the permanently failed Exp067B.

## Frozen missing-k² negative control

For each reference replace the CLASS Weyl auto power by the deliberately wrong `P_WW/k^4`. The median absolute target-cell log discrepancy against CAMB must satisfy the inherited Exp067B requirement

`median_abs_log_wrong_over_CAMB >= 5.0`.

## Frozen certification rule

`PASS_CAMB_CLASS_OUT_OF_SAMPLE_POWER_CONVENTION_V0_1` only if:

1. both solver pins and source contracts pass;
2. R0, R1 and R2 each pass all three `D<=0.03` spectral bounds and all cross signs;
3. every reference passes CLASS internal `1e-10` Weyl control;
4. every reference passes the Exp067D precision-signature controls;
5. every reference passes the missing-k² negative control;
6. at least the two fresh references R1 and R2 are present and evaluated exactly as preregistered.

Otherwise preserve

`FAIL_CAMB_CLASS_OUT_OF_SAMPLE_POWER_CONVENTION_V0_1`.

No cosmology, support point, unit, variable, interpolation rule, sign convention, k² factor, threshold, or reference may be changed or deleted after the first R1/R2 output.

## Scientific meaning

A PASS certifies only the physical solver convention needed by the solver-neutral ACT × unWISE projector across this preregistered LambdaCDM reference envelope. It is not a dark-sector law, does not close G7/G8/G9, and does not alter the historical Exp067B FAIL.

## Gate consequence

Regardless of outcome: **G7 OPEN, G8 OPEN, G9 OPEN**. No fresh withheld dark-sector family is selected in Exp067E.
