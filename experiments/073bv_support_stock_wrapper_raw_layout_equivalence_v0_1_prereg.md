# Exp073BV v0.1 — stock wrapper/raw-layout exact-equivalence support QA preregistration

Date frozen: 2026-09-04
Scope: DSIR only; hosted synthetic support QA only.
Accounting: `+0/+0` for every outcome. This experiment cannot create Wm_S3 scientific authority and cannot activate Exp073BU.

## Motivation fixed before output

Exp073BU selected compact support QA produced Q2 numeric-only disagreement with stock NaMaster 2.7 at ~1e-15. Exp073BU source audit then closed S1 and identified the stock Python/SWIG/C operation route. Before implementing a memory-stable stock-C/GSL emulator, isolate whether any discrepancy can originate solely from the public Python `get_bandpower_windows()` reshape/transpose layer.

## Frozen lineage

- runtime package: `pymaster` exactly 2.7 / 2.7.x from conda-forge;
- upstream source authority: `LSSTDESC/NaMaster`, tag `v2.7`, commit `24365fa59a38c15732f4f37e8b29265b75c442d5`;
- source-audit authority: run/job `33816987697` / `100851195938`, status `S1_STOCK_OPERATION_ROUTE_IDENTIFIED`, artifact `9916910028`, digest `sha256:edde986db34cd3311ff52936ba20db1bb2732e51f0061d9942da8b60fdc0047e`;
- installed/pinned `pymaster/workspaces.py` SHA256: `442e23eb542087566689271ad1c897d5da45f5b76e39def05b37d93b0098178f`;
- helper path: `ci/exp073bv_stock_wrapper_raw_layout_equivalence_v0_1.py`;
- helper Git blob SHA frozen here: `73d8cb082712f887f35eb522137a0fd6dcc81fb0`;
- helper introduction commit: `f73c7a722fda54bf328741079a9beee771115983`.

Any helper/source-lineage mismatch is fail-closed and cannot be interpreted numerically.

## Frozen synthetic domain

Exactly three deterministic nontrivial weighted mask pairs are generated by the helper at `NSIDE=16`, RING, `lmax=47`, with band edges `[0,4,8,12,16,24,32,40,48]`. No DES, Exp073R1, historical Wm, Exp073CR/CQ/CM numerical payload, or current Exp073BU physical data may be read.

For each case:
1. construct fresh spin-0 and spin-2 `NmtField`s;
2. construct a fresh `NmtWorkspace` and stock coupling matrix;
3. evaluate the public `w.get_bandpower_windows()` result;
4. independently call the same frozen stock SWIG library entry point `pymaster.nmtlib.get_bandpower_windows(w.wsp, size)`;
5. apply only the exact frozen stock wrapper reshape `[NB,NCLS,L,NCLS]` and transpose axes `[1,0,3,2]` to the direct raw buffer;
6. canonicalize both to C-order `<f8` and compare whole arrays by SHA256 and `numpy.array_equal`.

Expected full shape is exactly `[2,8,2,48]` for every case.

## Frozen classifications

- `R1_EXACT_WRAPPER_RAW_LAYOUT_EQUIVALENCE`: every case has identical canonical SHA256 and `numpy.array_equal=true`. This closes Python-wrapper/layout as an exact source of the prior Q2 discrepancy and permits prospectively implementing/testing the C/GSL operation-order emulator. It does **not** authorize a scientific runtime.
- `R2_WRAPPER_RAW_LAYOUT_MISMATCH`: provenance is valid but any case fails exact SHA/array equality. This is a negative support result; investigate wrapper/raw-buffer semantics before emulator work. No tolerance rescue.
- `R3_SOURCE_LINEAGE_MISMATCH`: helper blob, prereg/source authority or PyMaster 2.7 lineage does not match the frozen identities. BLOCKED `+0/+0`; do not interpret numerical output.
- `R4_INFRASTRUCTURE_INCOMPLETE`: dependency/runner/workflow/software failure before a valid complete receipt. Infrastructure `+0/+0`; diagnose first causal failure prospectively.

No numeric closeness threshold exists in this gate. Exact equality only.

## Frozen workflow discipline

The hosted workflow must first perform a static/freeze audit of this preregistration and helper blob, install exact NaMaster 2.7, and verify the runtime wrapper source SHA against the S1 authority. Only then may it execute the three synthetic cases and upload the complete JSON receipt. Workflow success alone is not R1; the raw receipt/artifact must be consumed.

No self-hosted runner, checkpoint namespace, DES data, science authority, readiness increment, or home-run ownership is permitted.

## Next action by classification

R1 -> prospectively preregister an exact stock-C/GSL operation-order emulator/equivalence QA that preserves full component semantics and tests exact stock equality on multiple synthetic masks.

R2 -> inspect exact wrapper/raw layout and runtime source without changing science criteria.

R3/R4 -> diagnose provenance/infrastructure first causal failure; smallest prospective repair only.
