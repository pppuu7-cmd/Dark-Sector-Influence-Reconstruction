# Exp073DM — Article 3 WW_S0_S0 exact-authority preflight v0.1

**Frozen:** 2026-09-05 after authoritative Wm_S3 exact PASS and after terminal Exp073DL payload-evidence recovery, before any new WW_S0_S0 numerical window is computed or inspected.

**Classification scope:** support/readiness only, `+0/+0` for every outcome. Exp073DM cannot create, alter, rescue, or invalidate any Wm authority or Article-3 scientific result.

## 1. Parent authority

Preserve without reinterpretation:

- Wm_S1 Track-A exact PASS;
- admitted Wm_S2 authority;
- Wm_S3 exact PASS from Exp073DJ checkpoint-preserving Exp073BU resume, run/job `33910213781 / 101144660519`, frozen science head `c02c018ede6a1fcf7aef1a848c0118a0669ed67f`, contract fingerprint `b38687bf5aa6cf4cfe01b2f38a7091e96d97196ad38bdf2ea771f7b649ac73da`;
- Wm_S3 terminal token `PASS_EXP073BU_WM_S3_FRESH_AB_EXACT_REPEATABILITY_8CORE_V0_3`;
- canonical Wm_S3 selected-TE SHA256 `d282ebdf98dc04e41a8c85f487e209634a8324ce7677107112b8abfd1660f749`;
- Exp073DL terminal evidence support PASS `+0/+0`, run/job `33934918432 / 101220868663`, artifact `9959830267`, ZIP SHA256 `cc32969cba9802201ac8cf7eae32b430ec47285f3357a58d470850aff8ceb8ab`;
- historical Exp073CM RESOURCE/PERFORMANCE FAIL `+0/+0` and original Exp073BU v0.4 runner-loss-like `INFRASTRUCTURE_INCOMPLETE +0/+0` remain historical and immutable.

The already prospectively frozen successor angular order remains:

`Wm_S1 -> Wm_S2 -> Wm_S3 -> WW_S0_S0 -> WW_S0_S1 -> WW_S0_S2 -> WW_S0_S3 -> WW_S1_S1 -> WW_S1_S2 -> WW_S1_S3 -> WW_S2_S2 -> WW_S2_S3 -> WW_S3_S3`.

Therefore the next angular object is exactly `WW_S0_S0`; no result-dependent task selection is permitted.

## 2. Frozen upstream implementation authority

The preflight may inspect only repository/source metadata and the frozen implementation contract. It must bind:

- `ci/exp073aa_article3_des_angular_task_runner_v0_1.py`;
- genuine DES Y1 R1 source-mask authority used by that executor;
- source bin S0 record/count-map semantics;
- `NSIDE=4096`, RING/C geometry;
- PyMaster/NaMaster 2.7 lineage;
- true ell `0..12287`;
- exact 39 band edges `[0,30,60,90,120,150,180,210,240,272,309,351,398,452,513,582,661,750,852,967,1098,1247,1416,1608,1826,2073,2354,2673,3035,3446,3914,4444,5047,5731,6508,7390,8392,9529,10821,12288]`;
- `WW_S0_S0` parsed as unordered source-bin pair `(0,0)`;
- WW full NaMaster window shape exactly `[4,39,4,12288]`;
- selected canonical component exactly `wins[0,:,0,:] = EE <- EE`;
- canonical selected dtype/shape `<f8 [39,12288]`;
- WW route must not receive or read the redMaGiC lens mask.

No historical WW numerical payload is authorized by this preflight.

## 3. Frozen firewall

Exp073DM must establish statically that the `WW_S0_S0` route does not read or use:

- effective ell/z/k;
- radial kernels or physical-k support;
- `f_invalid`, retained/rejected coordinates, or the common-support result;
- fiducial `P(k)` or nonlinear model weighting;
- covariance/whitening;
- nuisance tangent/SVD/rank;
- quotient/relation/null statistics;
- G8/G9;
- article-selection/readiness optimization;
- any other WW task output.

The audit may not inspect partial numerical output from any live heavy run.

## 4. Preflight PASS criteria

Emit `PASS_EXP073DM_WW_S0_S0_EXACT_AUTHORITY_PREFLIGHT_V0_1` only if all of the following are machine-checked from the committed source/preregistration:

1. parent Wm_S3 authority identifiers and Exp073DL evidence identifiers are frozen in this preregistration;
2. `WW_S0_S0` is present in the executor's frozen `ALL_TASKS` sequence after the four Wm tasks;
3. `parse_task('WW_S0_S0')` resolves to kind `WW` and bins `[0,0]`;
4. the WW branch constructs two spin-2 fields from source count maps and contains the same-object `fa` reuse for S0×S0;
5. the WW branch rejects a supplied lens mask;
6. expected full window shape is `(4,39,4,LMAX_PLUS_ONE)` with `LMAX_PLUS_ONE=12288`;
7. selected component is `wins[0,:,0,:]`, semantics `output=EE`, `input=EE`, component order `EE,EB,BE,BB`;
8. selected output is contiguous canonical `<f8`, shape `(39,12288)`, finite, and positive absolute-response norms are required only as an integrity check, not a scientific tolerance;
9. no tolerance/rounding/smoothing/averaging/ULP or fiducial-P rescue appears in the exact-authority decision path;
10. no numerical WW payload is created by the hosted preflight.

Any missing/mismatched source condition is `BLOCKED_EXP073DM_WW_S0_S0_PREFLIGHT` or infrastructure failure `+0/+0`; it must not be repaired by weakening frozen science.

## 5. Heavy successor boundary

Only an independently consumed raw preflight PASS may authorize preregistration/activation of the real `WW_S0_S0` exact-repeatability computation.

For a future self-hosted/home heavy gate, preserve the repository's canonical 8-core architecture when applicable: exactly 8 outer workers, nested BLAS/OpenMP/MKL/OpenBLAS threading pinned to 1 per worker, independent complete-band/task units dynamically scheduled, durable complete-unit checkpoints, exact restore verification, canonical reassembly, whole-payload SHA256 equality and `numpy.array_equal`, fail closed. No scientific arithmetic may change for performance.

The future heavy gate must use two independent, checkpoint-isolated A/B executions and may create `WW_S0_S0` authority only from an exact comparator. Workflow success alone is never PASS.

## 6. Downstream boundary

Exp073DM itself adds no Article-3 readiness points and opens no covariance, nuisance, G7, G8 or G9 result. After its support PASS, the exact next scientific gate is a separately preregistered `WW_S0_S0` A/B exact-repeatability authority run; after a scientific exact PASS, advance only to `WW_S0_S1`.