# Exp073BI — Article-3 Wm_S1 parallel-execution successor feasibility v0.1 — preregistration

**Frozen prospectively:** 2026-08-31, after terminal Exp073BH class `BH_D2_TIMEOUT_OR_EXTERNAL_CANCELLATION_EVIDENCED` and before any Exp073BI result exists.

## Purpose

Determine whether a fixed two-thread execution policy is a viable execution-only successor candidate for the frozen Exp073BA Wm_S1 Track-A route after the standard GitHub-hosted six-hour boundary was evidenced. Exp073BI is non-classifying synthetic/infrastructure QA. It must not create scientific authority or readiness credit.

## Inherited scientific contract — immutable

Any later classifying successor must preserve unchanged:

- DES `NSIDE=4096`;
- true ell `0..12287` and the frozen 39 bands;
- Wm selected component `TE <- TE`;
- canonical selected window `<f8 [39,12288]`;
- exact Exp073AZ PCL authority/binding;
- exact NaMaster 2.7 lineage;
- exact compact and final replica comparison by `numpy.array_equal` and canonical byte SHA equality;
- no tolerance, ULP, rounding, averaging, majority vote or preferred-replica rescue;
- all frozen support thresholds and anti-leakage boundaries.

Exp073AQ remains permanent scientific exact-repeatability FAIL under its original class.

## Only tested change

Exp073BI changes execution engineering only: fixed `OMP_NUM_THREADS=2` and matching BLAS/thread environment, while keeping the Wm spin-0 x spin-2 general-coupling algebra unchanged.

No classifying full-scale production is authorized by this preregistration.

## Hosted synthetic QA protocol

Use exact NaMaster 2.7 on a small deterministic Wm spin-0 x spin-2 mask problem. Execute two independent Python processes under the same frozen two-thread environment. Each process must produce the reconstructed Wm window using:

`general coupling -> fixed band compression -> K -> solve`.

The hosted result records:

1. canonical SHA-256 of each reconstructed window;
2. `numpy.array_equal` between independent process outputs;
3. maximum absolute difference to a stock `NmtWorkspace` reference, with synthetic QA threshold `< 1e-12` only for implementation-equivalence checking.

The `1e-12` threshold is synthetic QA only and is forbidden from any future scientific comparator.

## Decision classes

- `BI_Q1_PARALLEL_EXACT_QA_PASS`: independent two-thread synthetic outputs are exactly equal and stock-equivalence error is `<1e-12`.
- `BI_Q2_PARALLEL_EXACT_QA_FAIL`: complete outputs exist but exact equality fails and/or stock-equivalence threshold fails.
- `BI_Q3_INFRASTRUCTURE_INCOMPLETE`: the hosted QA does not produce complete valid outputs.

All outcomes are `+0 Verified / +0 Draft-data` and none is a scientific PASS/FAIL.

## Promotion rule

Only `BI_Q1` permits a separately frozen full-scale two-thread Track-A successor to be considered. That later successor must still produce two complete immutable compact replicas, frozen exact compact comparison, both finalizers, frozen exact final comparison and immutable hosted final authority. A full-scale timeout, incomplete output or exact mismatch remains classified under its prospectively frozen successor policy; no rescue is allowed.

## G7 firewall

Required order remains:

`validated physical forward/power-input bridges -> preregistered physical support-validity mask -> Layer A/Layer B prerequisites -> covariance restriction/whitening -> nuisance tangent rank/SVD -> quotient/relation/null control -> actual G7 authorization -> fresh G8 withheld family`.

No Layer A/B, covariance, nuisance, relation/null or G8 input may be read by Exp073BI.

`Verified increment = 0`

`Draft/data increment = 0`
