# Exp073DR — WW_S0_S0 activation/resource preflight v0.1

Frozen 2026-09-05 after raw Exp073DQ durable-driver static PASS and before any self-hosted/full-resolution WW_S0_S0 production run.

Scope: activation/resource/readiness `+0/+0` only. No WW scientific authority may be created.

Parent Exp073DQ: run/job `33938583879 / 101231302981`, artifact `9961000737`, GitHub + independent ZIP SHA256 `93a3db6b27ee9fba9f4d0549b9d6e03c2a50cb7f6ad224c41e773a85b969682c`, raw token `PASS_EXP073DQ_WW_S0_S0_DURABLE_AB_DRIVER_STATIC_ADMISSION_V0_1`, driver SHA256 `0b7a0a2336a89dcea63060d4049d09fabacc9c5e75fad870d2599efd27d0e63b`.

Also bind Exp073DP exact-equivalence PASS: run/job `33938446310 / 101230897808`, artifact `9960969007`, ZIP SHA256 `e34b545b21fc93f8948ad328084afd405885c1045313d7162b553a45583af7a8`, NaMaster v2.7 source commit `24365fa59a38c15732f4f37e8b29265b75c442d5`, exact full/selected equality on all three synthetic controls.

## Hosted audit requirements

The source-only hosted audit must verify:
1. the durable driver imports only Exp073AA R1 validation/S0 reconstruction and Exp073DO WW adapter for numerical semantics;
2. A/B namespaces are dedicated and distinct;
3. checkpoint stages are complete-stage durable boundaries, with source-head/contract binding and payload SHA restore checks;
4. the expensive stock workspace is persisted as `fresh_workspace.fits` immediately after a complete successful `compute_coupling_matrix(f,f,b)`; a verified existing workspace is restored by SHA and never recomputed;
5. the same spin-2 field object is handed to both sides, preserving stock S0×S0 arithmetic;
6. full `[4,39,4,12288]` and selected `<f8 [39,12288] EE<-EE` are created only through the DP-qualified adapter;
7. final A/B comparator is exact SHA + `numpy.array_equal` with no tolerance rescue;
8. driver syntax compiles under Python without importing numerical dependencies;
9. full scientific activation remains impossible until a separate self-hosted environment/exclusivity preflight binds actual PyMaster v2.7, DES R1 inputs, deterministic 8-core downstream runtime proof, checkpoint root writability and zero competing home workload.

## Resource decision

A complete stock workspace is an admissible durable **complete-stage** checkpoint boundary under the repository policy; mid-workspace partial numerical state is neither read nor used to tune science. If interrupted before the workspace manifest is atomically complete, the stage is infrastructure-incomplete and may be restarted from the last prior verified stage; once the workspace manifest and FITS SHA are verified, recomputation is forbidden.

The hosted gate does not claim that the home environment is ready. It may emit only `PASS_EXP073DR_WW_S0_S0_HOSTED_ACTIVATION_RESOURCE_PREFLIGHT_V0_1`, which authorizes a separate tiny self-hosted **readiness/exclusivity preflight only**, not the A/B scientific computation.

The later home preflight must fail closed unless it proves: exactly one DSIR home ownership candidate, no queued/in-progress competing home job, PyMaster 2.7.x, R1 source artifacts with frozen authority, writable dedicated checkpoint root, source-head/contract fingerprint, deterministic downstream compiled for exactly 8 OpenMP workers and runtime `DSIR_OMP_TEAM=8`, nested BLAS/MKL/OpenBLAS/NumExpr=1. It must not compute a full workspace or selected WW payload.

Any mismatch is BLOCKED/infrastructure `+0/+0`; never weaken the frozen science or arithmetic.