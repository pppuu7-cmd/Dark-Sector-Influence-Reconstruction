# Exp073BH — Article-3 BA full-scale execution root-cause successor v0.1 — preregistration

**Frozen prospectively:** 2026-08-31, after Exp073BA terminal cancellation was classified under already-frozen Exp073BG and before any Exp073BH diagnostic result exists.

## Purpose

Diagnose why the full-scale Exp073BA compact Wm_S1 replicas did not produce complete immutable comparator inputs, without changing or weakening any scientific acceptance criterion and without using downstream Layer-A/Layer-B/covariance/nuisance/G7/G8 information.

This is an infrastructure/root-cause successor, not scientific authority by itself.

## Immutable inherited scientific contract

Exp073BH may not alter:

- DES classifying route `NSIDE=4096`;
- true ell `0..12287`, 39 frozen bands;
- Wm selected component `TE <- TE`;
- canonical selected window `<f8 [39,12288]`;
- admitted Exp073AZ PCL provenance/binding requirements;
- exact NaMaster 2.7 lineage requirements;
- exact comparator semantics for any future Track-A authority: `numpy.array_equal` / canonical byte-SHA equality, with no tolerance, ULP, rounding, averaging, majority vote, preferred-replica, or numerical-closeness rescue;
- all frozen physical/support thresholds and the anti-leakage firewall.

Exp073AQ remains permanent exact-repeatability scientific FAIL and is not reclassified by this route.

## Allowed changes

Only execution-observability and infrastructure-diagnostic changes are allowed before a separately frozen scientific successor is attempted, including:

1. deterministic stage timers and peak-RSS/resource telemetry;
2. checkpoint markers before/after major matrix construction/compression/solve phases;
3. explicit runner CPU/model, available RAM/swap, Python/NumPy/SciPy/NaMaster versions and thread-environment capture;
4. fail-fast validation that no downstream science inputs are read;
5. small or non-classifying diagnostic probes that do not produce manuscript science claims;
6. checkpointable/blockwise engineering experiments only if their mathematical equivalence is separately demonstrated before any classifying use.

## Forbidden actions

- no change to frozen scientific thresholds or selected Wm component;
- no post-hoc tolerance rescue;
- no use of incomplete Exp073BA arrays as scientific evidence;
- no preference/salvage of Exp073BD branch B;
- no covariance/whitening/nuisance/relation/null/G8 reads;
- no G8 withheld-family selection;
- no readiness credit for infrastructure diagnostics;
- no claim that Exp073BA cancellation was OOM, timeout, runner loss, or manual cancellation unless directly evidenced.

## Diagnostic decision classes

The first hosted diagnostic successor must end in exactly one infrastructure class:

- `BH_D1_RESOURCE_LIMIT_EVIDENCED`: direct hosted evidence identifies memory/resource exhaustion;
- `BH_D2_TIMEOUT_OR_EXTERNAL_CANCELLATION_EVIDENCED`: hosted logs/metadata directly identify timeout/external cancellation;
- `BH_D3_STAGE_FAILURE_EVIDENCED`: deterministic stage instrumentation identifies a reproducible implementation/dependency failure;
- `BH_D4_FULL_DIAGNOSTIC_COMPLETION_NO_CLASSIFYING_SCIENCE`: diagnostic execution completes but is non-classifying by design;
- `BH_D5_INCONCLUSIVE`: no direct causal evidence.

None is a scientific PASS/FAIL and all receive `+0 Verified / +0 Draft-data`.

## Promotion firewall

A future classifying Track-A successor requires a new preregistration after Exp073BH evidence. That successor must bind the exact inherited scientific contract above. Complete immutable A/B compact replicas must exist before exact comparison. Compact exact PASS is only a prerequisite for frozen finalizers and final exact comparison; scientific authority requires a valid immutable hosted final authority artifact.

## G7 firewall

Required order remains:

`validated physical forward/power-input bridges -> preregistered physical support-validity mask -> Layer A/Layer B prerequisites -> covariance restriction/whitening -> nuisance tangent rank/SVD -> quotient/relation/null control -> actual G7 authorization -> fresh G8 withheld family`.

Exp073BH cannot authorize any downstream step.

## Readiness

`Verified increment = 0`

`Draft/data increment = 0`
