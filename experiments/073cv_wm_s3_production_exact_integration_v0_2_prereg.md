# Exp073CV v0.2 — scoped-verifier repair preregistration

Status: PROSPECTIVE / support-integration only / +0/+0.

## Authority inherited unchanged
Exp073CV v0.1 production arithmetic, validated component identities, NaMaster 2.7/GSL lineage, six Exp073CD durable boundaries, exact full-tensor and TE<-TE semantics, OS-mmap proof, no historical Wm_S3 numerical import, no cross-replica numerical restore, and no tolerance rescue remain unchanged.

Historical v0.1 run 33843210949 / job 100929554219 / head 340baf103192e2cd148daf1b0097a2444c9f3342 is classified I4_INFRASTRUCTURE_OR_SOURCE_BINDING_FAIL +0/+0: the audit raised `forbidden materialization pattern` before numerical execution because its verifier scanned the whole adapter source and self-matched its own forbidden-pattern literal. No receipt was produced and no Wm_S3 scientific authority was created.

## Sole permitted repair
The v0.2 verifier MUST inspect only the production functions `stream_fits_to_canonical_input`, `run_downstream`, and `execute`. Any `get_coupling_matrix` occurrence inside those function bodies is fail-closed. The hosted synthetic stock-reference path remains unchanged. Production arithmetic and all numerical operations are byte-for-byte inherited from v0.1.

## Frozen hosted gate
Three pre-existing deterministic synthetic masks, nside=16, nl=48, edges [0,4,8,12,16,24,32,40,48], ncls=2. For every case the complete `<f8` tensor and selected TE slice must simultaneously satisfy canonical SHA256 equality, `numpy.array_equal == True`, and max absolute difference exactly 0.0 against stock NaMaster 2.7 `get_bandpower_windows()`. The production chain must independently prove canonical mmap backing and FITS `/proc/self/maps` backing through the inherited fail-closed v0.1 execution path.

PASS token: `I1_PRODUCTION_INTERFACE_EXACT_INTEGRATION_PASS`.
Arithmetic mismatch: `I2_ARITHMETIC_EQUIVALENCE_FAIL`.
Memory-contract mismatch: `I3_MMAP_OR_MEMORY_CONTRACT_FAIL`.
Infrastructure/source/verifier failure: `I4_INFRASTRUCTURE_OR_SOURCE_BINDING_FAIL`.
Malformed/missing receipt: `I5_MALFORMED_OR_MISSING_RECEIPT`.

Only I1 is a support/integration PASS, still +0/+0. It does not activate Exp073BU and does not create Wm_S3 authority. No tolerance, rounding, smoothing, averaging, effective-ell/z/k, fiducial-P shortcut, or scientific-boundary change is permitted.
