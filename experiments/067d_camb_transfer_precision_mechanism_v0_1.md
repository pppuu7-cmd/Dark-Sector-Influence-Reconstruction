# Exp067D — CAMB transfer precision mechanism audit v0.1

Date: 2026-08-26
Status: PREREGISTERED BEFORE FIRST PRECISION-MECHANISM CALCULATION

## Motivation and immutable lineage

Exp067B remains permanently `FAIL_CAMB_CLASS_PHYSICAL_POWER_CONVENTION_BRIDGE_V0_1` because its preregistered CAMB rank-one coherence criterion `5e-8` failed. Exp067C then localized that failure to the raw native CAMB power arrays rather than `PkInterpolator`: `E_native=1.616012643701481e-7` on 127 native k nodes in `0.005<=k<=0.2 Mpc^-1`.

Pinned CAMB source gives a concrete precision mechanism:

- `MatterTransferData%TransferData` is declared default Fortran `real` and is exposed to Python as `c_float`;
- `Transfer_GetUnsplinedPower` forms `P_xy` from the product `M%TransferData(s1,...)*M%TransferData(s2,...)` multiplied by a common double-precision factor and stored in `real(dl)` output.

Exp067D tests whether the observed native coherence defect is quantitatively generated by the first multiplication of two float32 transfer values. It is a causal precision-mechanism audit only; it cannot rescue or rerun Exp067B.

## Frozen solver and cosmology

Use exactly the Exp067B/Exp067C setup:

- CAMB `cmbant/CAMB@fa3f097343fbbe427cc04b4f5f0041c22c6ec764`;
- `H0=67`, `ombh2=0.0224`, `omch2=0.1200`, `omk=0`, `mnu=0`, `nnu=3.046`;
- `TCMB=2.7255`, `YHe=0.24`, `tau=0`;
- `As=2.10e-9`, `ns=0.965`, pivot `0.05 Mpc^-1`;
- linear power only;
- requested power redshifts `z=[0,0.5,1,2]`;
- native support retains every CAMB native node satisfying `0.005<=k<=0.2 Mpc^-1`.

## Frozen variables

Use the same CAMB transfer variables as the physical power bridge:

- matter `m = delta_nonu`;
- Weyl `W = Weyl = k^2(phi+psi)/2` in CAMB power convention.

The raw transfer array and the official native powers must come from the same `CAMBdata` result object.

## Frozen redshift alignment

For each output power redshift index `j=0..3` in the ascending `get_linear_matter_power_spectrum` output, the internal transfer redshift index is reconstructed from the exact pinned Fortran mapping

`State%PK_redshifts_index(nz-zix+1)`

with Fortran one-based to Python zero-based conversion. The mapped `results.transfer_redshifts[itf]` must equal the corresponding output power redshift to absolute tolerance `1e-14`. Any mismatch is an infrastructure FAIL; no alternate alignment may be chosen after viewing coherence results.

## Frozen constructions

Let `tm32` and `tw32` be the raw float32 `delta_nonu` and `Weyl` transfer arrays on an aligned native node.

### D1 — official native coherence

Use official CAMB native powers from `get_linear_matter_power_spectrum`:

\[
r_{off}=P_{Wm}^2/(P_{WW}P_{mm})-1.
\]

Reproduce the Exp067C condition `max|r_off| > 5e-8` on the frozen native support. The inherited `5e-8` is not changed.

### D2 — exact float32-first transfer product

Construct with explicit NumPy float32 multiplication:

`mm32 = multiply(tm32,tm32,dtype=float32)`

`wm32 = multiply(tw32,tm32,dtype=float32)`

`ww32 = multiply(tw32,tw32,dtype=float32)`

then cast the three products to float64 and define

\[
r_{32}=wm32^2/(ww32\,mm32)-1.
\]

Because the common `k*2*pi^2*P_R(k)` factor cancels in this quotient, this is the direct precision signature of the pinned Fortran first multiplication.

Hard source-identity criterion:

`max_abs(r32-r_official) <= 5e-12`.

Also require `max|r32| > 5e-8`.

### D3 — promote before multiplication control

Promote the same stored values first:

`tm64 = float64(tm32)`, `tw64 = float64(tw32)`

and only then construct `mm64=tm64*tm64`, `wm64=tw64*tm64`, `ww64=tw64*tw64`.

Require

`max_abs(wm64^2/(ww64*mm64)-1) <= 1e-12`.

This control does not restore information lost when the transfers were stored as float32; it tests only whether the rank-one defect arises from multiplication rounding after storage.

### D4 — common-factor reconstruction

On each retained native cell define

`F_mm=P_mm/mm32`, `F_Wm=P_Wm/wm32`, `F_WW=P_WW/ww32`.

The pinned `Transfer_GetUnsplinedPower` source says all three use the same remaining factor. Require finite nonzero denominators and

`max(abs(F_pair/F_mm-1)) <= 5e-12`

for `pair=Wm,WW`.

### D5 — source declaration contract

Mechanically require in the pinned source:

- `real, dimension(:, :, :), allocatable :: TransferData` in `fortran/classes.f90`;
- Python `_MatterTransferData.TransferData` uses `POINTER(c_float)`;
- `Transfer_GetUnsplinedPower` contains the direct transfer-product expression and `PK` is `real(dl)`.

## Frozen classification

`FLOAT32_TRANSFER_PRODUCT_CAUSALLY_CONFIRMED_V0_1` only if all of the following hold:

1. provenance/source declarations and redshift alignment pass;
2. official native `max|r_off| > 5e-8`;
3. float32-first `max|r32| > 5e-8`;
4. `max_abs(r32-r_official) <= 5e-12`;
5. common-factor reconstruction spread `<=5e-12`;
6. promote-before-product control `max|r64| <=1e-12`.

Otherwise classify as

`FLOAT32_TRANSFER_PRODUCT_NOT_SUFFICIENT_V0_1`.

No alternate tolerance or explanatory mechanism may be substituted inside v0.1 after the first output.

## Interpretation boundary

A causal confirmation would establish a numerical precision floor in the pinned CAMB power-product implementation. It would **not** change Exp067B from FAIL to PASS. A later separately preregistered convention gate may use this diagnosis to define an attainable solver-consistency control without retroactively altering Exp067B.

## Gate consequence

Exp067D is not a G7 law experiment and uses no fresh withheld dark-sector family. Regardless of outcome: **G7 OPEN, G8 OPEN, G9 OPEN**.
