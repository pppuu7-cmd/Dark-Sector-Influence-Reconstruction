# DSIR recovery checkpoint — Exp073AG hosted PASS, X2 still running

**Date:** 2026-08-30  
**Project:** Dark-Sector Influence Reconstruction (DSIR)

## Scientific-accounting headline

- Strict Article-3 scientific repository readiness: **52%**.
- Article-2 repository-for-writing readiness: **100%** for its declared scope; this is not G7/G8/G9 closure.
- G7/G8/G9: **OPEN**.
- Layer A/B: **OPEN**.
- covariance/whitening: **BLOCKED**.
- Exp073AE, Exp073AF and Exp073AG are hosted synthetic/governance QA only and each contributes **0** scientific-readiness points.
- RTK/RQIR remain excluded from DSIR authority.

## Initial authority-state audit for this iteration

Before any new work:

- recent commits ended at the Exp073AF recovery/manual updates;
- `docs/RECOVERY_LATEST.md` reported Article-3 readiness 52%, P/Q X2 in progress, Exp073AA production blocked;
- P run `33300997298` still had both replica jobs in exact workspace computation and **no artifacts**;
- Q run `33301058260` still had both replica jobs in exact workspace computation and **no artifacts**.

Therefore no duplicate X2 or Exp073AA production was launched.

## New non-conflicting prerequisite — Exp073AG

Repository search found no dedicated exact 14-window authority aggregator gate. Exp073AG was therefore prospectively frozen while both X2 chains remained unresolved and before any of the remaining 13 Exp073AA tasks was released.

Purpose: freeze a fail-closed deterministic schema for the future immutable ordered 14-window angular authority:

`canonical X2 Wm_S0 + 13 Exp073AA windows -> exact ordered 14-window authority`.

It does not read real angular artifacts or perform science.

### Prospective chain

- preregistration:
  - `experiments/073ag_article3_exact_14window_authority_aggregator_schema_v0_1_prereg.md`
  - commit `2af44c322e03ce40882891a64c12e39b1e0a3564`;
- validator/self-test:
  - `ci/exp073ag_article3_exact_14window_authority_aggregator_schema_v0_1.py`
  - commit `d49c8f43aa3ae3c6547864278fb9380720e61475`;
- workflow:
  - `.github/workflows/exp073ag-article3-exact-14window-authority-aggregator-schema-v0-1.yml`
  - commit `61dded0fbd0cea8cc5218d6c46d570a873084c4d`;
- workflow freeze:
  - `experiments/073ag_article3_exact_14window_authority_aggregator_schema_v0_1_workflow_freeze.md`
  - commit `80c381ccc2e16091a44649d475966244ca9cfe71`;
- trigger/head:
  - `ci/exp073ag_article3_exact_14window_authority_aggregator_schema_v0_1.trigger`
  - commit `1583760a13072628b307c216857dedc06748b19b`.

All were committed before any real 14-window authority existed.

## Hosted Exp073AG result

- run `33303419856`;
- job `99235598024`;
- conclusion `success`;
- artifact `9729669260`;
- artifact digest `sha256:71929dbc9eb77d59fbe5ad790d6c9cecfb236cefb457ab9a1dbd2e67d4a549c1`;
- token `PASS_EXP073AG_EXACT_14WINDOW_AUTHORITY_AGGREGATOR_SCHEMA_SYNTHETIC_V0_1`.

Classification:

`HOSTED_SYNTHETIC_PASS_NON_SCIENTIFIC_PLUS_0_READINESS`.

Hosted steps passed:

1. prospective freeze enforcement;
2. Python compilation;
3. frozen 18-case synthetic schema matrix;
4. non-scientific accounting enforcement;
5. artifact persistence.

## What Exp073AG now protects

The future real aggregate must contain exactly once and in this exact order:

`Wm_S0, Wm_S1, Wm_S2, Wm_S3, WW_S0_S0, WW_S0_S1, WW_S0_S2, WW_S0_S3, WW_S1_S1, WW_S1_S2, WW_S1_S3, WW_S2_S2, WW_S2_S3, WW_S3_S3`.

Authority classes are frozen:

- `Wm_S0 -> canonical_exp073x2` only;
- remaining 13 -> `exp073aa` only.

Each entry requires positive hosted run/job/artifact identities, `sha256:<64 lowercase hex>` artifact digest and canonical selected-window identity:

- dtype `<f8`;
- shape `[39,12288]`;
- SHA256 64 lowercase hex.

Distinct task identities may not silently alias the same selected-window SHA. The aggregate metadata identity is a deterministic SHA256 over canonical sorted-key compact JSON serialization of the ordered 14 entries. Dictionary insertion order does not affect that manifest hash.

The schema rejects task/order/authority/source/digest/dtype/shape/window-hash drift, duplicate window hashes, unknown fields, firewall activation, readiness drift and G7/G8/G9 drift.

## Anti-leakage status

Exp073AG hosted QA explicitly did **not**:

- read real X2/Exp073AA artifacts;
- build a real 14-window authority;
- read radial kernels;
- compute physical k or support;
- select retained rows;
- use fiducial P;
- read covariance/whitening;
- read nuisance geometry/SVD;
- compute quotient/relation/null statistics;
- read G8;
- claim scientific PASS.

Readiness remains exactly 52%.

## X2 state after Exp073AG hosted completion

At the immediately preceding artifact audit:

- P run `33300997298`: no artifacts; both exact replicas remain in workspace computation;
- Q run `33301058260`: no artifacts; both exact replicas remain in workspace computation.

No third X2 and no remaining Exp073AA production task was launched.

## Authorized order from here

1. inspect real X2 P/Q jobs and artifacts;
2. classify immutable P/Q outcomes under frozen P/Q governance;
3. apply hosted-tested Exp073AF release control;
4. if released, execute exactly 13 remaining Exp073AA tasks;
5. build the real exact ordered 14-window authority under Exp073AG;
6. perform the real strict pre-support join under Exp073AE with Exp073U + Exp073Z2 + Exp073AB + Exp073W;
7. freeze the immutable complete 1410-row finite-operator candidate manifest;
8. only then run real Layer A broad operator-support leakage;
9. freeze S_op in inherited Exp073U order;
10. run real Layer B common-response validity;
11. only after Layer A/B PASS may covariance/whitening begin;
12. then nuisance representation/resolvability, signed SVD/rank, quotient/relation/null, and fresh withheld-family G8 after G7 relation freeze.

No scientific readiness increase is authorized before the real complete pre-support finite-operator candidate manifest exists.
