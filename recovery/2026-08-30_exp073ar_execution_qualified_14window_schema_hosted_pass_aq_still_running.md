# DSIR recovery checkpoint — Exp073AR hosted succession PASS; Exp073AQ still running

**Date:** 2026-08-30  
**Project:** Dark-Sector Influence Reconstruction (DSIR)

## 0. Scientific-accounting headline

- Strict Article-3 scientific repository readiness: **52%**.
- Article-2 repository-for-writing readiness: **100%** for declared scope; not G7/G8/G9 closure.
- G7/G8/G9 = **OPEN**.
- Layer A/B = **OPEN**.
- covariance/whitening = **BLOCKED**.
- Exp073AR is hosted synthetic provenance/reproducibility QA only and adds **0 readiness**.
- No RTK/RQIR authority is admitted.

## 1. Duplicate/heavy-work audit at iteration start

`docs/RECOVERY_LATEST.md` showed that the active real heavy gate is Exp073AQ controlled-twin `Wm_S1`, run `33327372191`.

Latest inspected AQ jobs:

- replica A `99299799192`: `IN PROGRESS`, exact controlled Wm_S1 computation;
- replica B `99299799338`: `IN PROGRESS`, exact controlled Wm_S1 computation.

No AQ final authority artifact existed at the initial audit. Therefore no duplicate Wm_S1, no Wm_S2 production, and no other heavy angular workflow was launched.

## 2. Newly identified non-heavy prerequisite gap

Historical Exp073AG v0.1 froze the future 14-window aggregate before the later empirical execution-route findings. It therefore expected:

- `canonical_exp073x2` for Wm_S0;
- `exp073aa` for the remaining 13 tasks.

Subsequent real hosted evidence changed the **prospective production authority route** without rewriting historical results:

- historical primary P remains historical-route Wm_S0 authority only;
- historical Q remains immutable computational `SCIENTIFIC_REPEATABILITY_FAIL`;
- Exp073AM established internally exact controlled single-thread Wm_S0 repeatability with canonical SHA `8ac59fc060195addcc5cd8b6d75e32fbc6dbfeea8456f4c83e8bf0cf034b9220`;
- Exp073AN classified the exact cross-route authority shift;
- Exp073AO prospectively defined `controlled_single_thread_exact_v1`;
- Exp073AP real hosted decision authorized that execution-qualified exact successor route.

Therefore using historical Exp073AG unchanged for the future real 14-window aggregate would risk mixing superseded and successor authority classes.

## 3. Exp073AR — execution-qualified 14-window aggregate succession

Exp073AR was prospectively frozen while the first real successor task, AQ/Wm_S1, was still in progress and before any AQ output was observed.

### Frozen chain

- preregistration:
  `experiments/073ar_article3_execution_qualified_14window_aggregate_succession_v0_1_prereg.md`
  commit `ec6031118430c0e1db797e26bea4ba66102c9e7b`;
- implementation:
  `ci/exp073ar_article3_execution_qualified_14window_aggregate_succession_v0_1.py`
  commit `db5f4c93c46b9674c04c9a7f42e917d79c50babd`;
- hosted workflow:
  `.github/workflows/exp073ar-article3-execution-qualified-14window-aggregate-succession-v0-1.yml`
  commit `729d331c231391ac31e28eb978a491ab61f2a549`;
- workflow freeze:
  `experiments/073ar_article3_execution_qualified_14window_aggregate_succession_v0_1_workflow_freeze.md`
  commit `c862cb04f7e8e31803a84c6f2296f26acd82239b`;
- trigger/head:
  `ci/exp073ar_article3_execution_qualified_14window_aggregate_succession_v0_1.trigger`
  commit `b4b14950ee0831b902cedd527d3797e991107195`.

### Hosted result

- run `33327870470`;
- job `99301112192`;
- run created/started `2026-08-30T18:22:12Z`;
- run completed success `2026-08-30T18:22:26Z`;
- artifact `9736757853`;
- artifact digest `sha256:b3d2a1195299d9daedd469dee1fb394bcfba4499abfdbd04159330566d8c76e9`;
- required token `PASS_EXP073AR_EXECUTION_QUALIFIED_14WINDOW_AGGREGATE_SUCCESSION_SYNTHETIC_V0_1`.

All 24 frozen synthetic checks and the non-scientific accounting firewall passed.

Classification:

`HOSTED_SYNTHETIC_PASS_NON_SCIENTIFIC_PLUS_0_READINESS`.

## 4. Frozen future aggregate succession contract

The future real 14-window aggregate is now required to use only authority class:

`controlled_single_thread_exact_v1`.

### Wm_S0 anchor

Must be exactly Exp073AM:

- run `33321661835`;
- job `99284585530`;
- artifact `9735051043`;
- digest `sha256:167c82d36266efc3b7bd058f0cc307ec636b6c8efdb6b39b6e88f52d6edb3d66`;
- exact canonical SHA `8ac59fc060195addcc5cd8b6d75e32fbc6dbfeea8456f4c83e8bf0cf034b9220`.

Historical primary-P SHA `6ec29f6dbbcf0c29d7af9d6eb98d271bcd06e00d28cebe658b5e266f7ef18d0f` is explicitly rejected as the successor aggregate anchor while remaining preserved as historical-route authority.

### Remaining 13 tasks

Each future task is admissible only after a real hosted controlled-twin comparator authority certifies:

- exact authority class `controlled_single_thread_exact_v1`;
- two independent hosted replicas;
- exact canonical SHA equality;
- `numpy.array_equal == True`;
- selected canonical `<f8 [39,12288]` SHA identical to both replica SHA values;
- single-thread controls verified;
- positive hosted run/job/artifact provenance and valid artifact digest.

No tolerance, rounding, ULP allowance, majority vote, preferred replica, historical-P closeness criterion, or single-replica admission is allowed.

The 14 selected-window hashes and the 13 successor experiment identities must be unique. Duplicate equality across distinct task identities blocks aggregation pending a new prospective provenance review.

## 5. Exp073AG supersession boundary

Exp073AG is **not deleted or reclassified**. Its hosted synthetic PASS remains valid for the historical aggregation schema it tested.

For the future execution-qualified route authorized by AO/AP, Exp073AR supersedes only the aggregate authority-class/provenance schema. The physical/angular semantics remain inherited unchanged from Exp073AA and the frozen Article-3 boundaries.

## 6. Frozen science boundaries remain unchanged

- `0.295 <= z <= 2.33` inclusive;
- `0 < k <= 0.06664762008318016 Mpc^-1`;
- Layer-A `operator_f_invalid <= 0.05` inclusive;
- Layer-B invalid row fraction `<= 0.05` inclusive;
- final retained dimension `>=15`;
- genuine DES Y1, `NSIDE=4096`, PyMaster/NaMaster 2.7;
- true ell `0..12287`, 39 bandpowers;
- Wm `TE <- TE`, WW `EE <- EE`;
- canonical `<f8 [39,12288]`;
- positive absolute operator/window envelope only for support bookkeeping; measured Wm remains signed;
- no effective ell/z/k shortcut;
- no fiducial-P weighting;
- no support/covariance/whitening/nuisance/quotient/relation/null/G8 leakage during angular authority construction;
- exact-threshold numerical ambiguity remains unresolved.

## 7. Anti-leakage/accounting result

Exp073AR did not read or download the running Exp073AQ output and did not build a real 14-window aggregate. It did not read radial kernels, physical support, retained coordinates, fiducial P, covariance, whitening, nuisance geometry, relation/null statistics, chi-square, p-values or G8.

Therefore:

- readiness remains **52%**;
- readiness increment = `0`;
- G7/G8/G9 remain OPEN;
- no scientific model PASS is claimed.

## 8. Authorized order from here

`resolve real Exp073AQ Wm_S1`

`-> if exact controlled-twin PASS, freeze/run Wm_S2 as its own prospective twin gate`

`-> continue the remaining ordered tasks only through exact controlled-twin admission`

`-> Exp073AM Wm_S0 + 13 admitted controlled-twin authorities`

`-> real 14-window aggregate validated under Exp073AR`

`-> real strict pre-support join under Exp073AE`

`-> immutable complete 1410-row finite-operator candidate manifest`

`-> real Layer A -> Layer B -> covariance/whitening -> nuisance/quotient/relation/null -> fresh G8`.

The next possible scientific readiness credit remains the real complete pre-support finite-operator candidate manifest, not Exp073AQ task PASSes and not Exp073AR synthetic QA.
