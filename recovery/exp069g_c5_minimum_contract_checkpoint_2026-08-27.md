# DSIR recovery checkpoint — Exp069G minimum C5 corrective-provider contract

Date: 2026-08-27

## Recovery state

This checkpoint records the G7 bottleneck while Exp069F general-accuracy convergence run `33023027901` is still in progress.

Current scientific state:

- C3/GDM physical provider: **ELIGIBLE** from prospective Exp070C native-grid provider.
- C5/designer-f(R) physical provider: **NOT ELIGIBLE**.
- Exp069B remains permanent scientific FAIL under the frozen `5e-6` designer-zero versus ordinary-GR limit.
- Exp069C: raw zero-limit residual + matter-power k-grid nonconvergence diagnosis.
- Exp069D: mechanism audit formally incomplete because the skip-RGR branch is unstable.
- Exp069E: completed source-native mechanism result; exact-zero selected EFT-function residue `F0=4.7401579076280133e-17`, while all-block power residual `M0=5.302921926164412e-6`; direct finite-source-residue explanation rejected.
- Source-derived analytic theorem: LCDM designer equations contain exact `A=0` GR boundary; literal requested `B0=0` is nevertheless routed through numerical `B0(A)=0` inversion. This is causal/mechanism evidence, not provider certification.
- Exp069F: prospectively frozen q=[1,2,3,4] general-accuracy mechanism audit; cannot certify C5 by itself.
- Exp069G minimum provider contract: frozen before Exp069F result at commit `731f0824379a977ed9a8a3f9107538854c24de65`.

## Frozen continuation logic

1. Do not duplicate Exp069F while its existing run is active.
2. If Exp069F recovers the unchanged `5e-6` closure at a frozen q>1, use the first passing q only as a candidate setting for a separately preregistered C5 provider certification satisfying Exp069G C1-C8.
3. If Exp069F does not recover closure, continue mechanism work on early-background/thermal/transfer initialization or another independently justified solver-native route. Do not promote any q to provider status.
4. Infrastructure failure is separate from scientific FAIL.
5. Do not alter Exp069B or any frozen acceptance criterion retrospectively.
6. Do not start common support-validity mask until both C3 and C5 physical providers are validated.
7. Thereafter preserve exact ordering: support-validity mask -> covariance restriction/whitening -> nuisance tangent rank/SVD -> quotient/relation/null control -> fresh G8 withheld family.

## Exp069G minimum requirements for any future C5 provider

Any future provider certification must be separately numbered and prospectively freeze its concrete settings. At minimum it must demonstrate:

- literal B0=0 closure against ordinary GR with hard criterion no weaker than `5e-6` over `P_mm`, signed `P_Wm`, `P_WW`;
- prospectively frozen strictly-positive B0 continuity sequence toward zero;
- at least one nontrivial production-scale positive-B0 signal;
- signed cross-power semantics and explicit units/k/redshift/variable provenance;
- repeatability and no unintended solver-state mutation;
- requested/readback settings and pinned-upstream integrity;
- no retrospective floor subtraction, renormalization, smoothing, support cherry-picking or threshold relaxation;
- explicit separation of execution status from scientific classification.

## Gate state at this checkpoint

`C3 provider = ELIGIBLE`

`C5 provider = NOT ELIGIBLE`

`support mask = BLOCKED`

`covariance/whitening = BLOCKED`

`nuisance SVD = BLOCKED`

`quotient/relation/null = BLOCKED`

`G7 = OPEN`

`G8 = OPEN`

`G9 = OPEN`
