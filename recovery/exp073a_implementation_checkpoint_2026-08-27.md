# DSIR recovery checkpoint — Exp073A implementation — 2026-08-27

Exp073A preregistration was merged to `main` before this implementation branch was created.

Implementation scope is limited to the frozen GR-reference perturbativity screen:

- bind exact immutable Exp072C artifact/run/digest/JSON SHA;
- reconstruct the exact pinned ACT×unWISE positive operator geometry and 26-coordinate/64-pair ordering;
- reproduce the unique Exp072C frontier and 15-coordinate geometric route before applying perturbativity;
- reuse the exact pinned CAMB linear `delta_nonu x delta_nonu` physical reference with no nonlinear corrections and no extrapolation;
- evaluate `Delta2_m=k^3 P_mm^lin/(2*pi^2)` only inside the unique Exp072C frontier rectangle;
- apply the frozen primary `Delta2_m<=1` mask and diagnostic-only `<=0.5` / `<=2` masks;
- keep the full-pair denominator and unchanged 5% combined invalid-support threshold;
- preserve route requirements `>=15` plus Blue/Green gg/kg coverage;
- execute the frozen CAMB physical-unit roundtrip at `2e-8`;
- do not read covariance, whitening, nuisance rank/SVD, G7 relation/null, G8 response, article-selection quantities, or extend C3/C5 providers.

Allowed completed classifications remain exactly:

- `ELIGIBLE_GR_REFERENCE_LINEAR_ROUTE_EXP073A`;
- `INELIGIBLE_GR_REFERENCE_LINEAR_ROUTE_EXP073A`;
- `FAIL_EXP073A_REPRODUCTION_OR_PROVENANCE`.

Infrastructure failure before a complete numerical record remains `INCOMPLETE_EXP073A` by interpretation.

Exp072A remains permanent scientific FAIL. Exp072B and Exp072C remain unchanged. G7/G8/G9 remain OPEN.

Repository-sync policy remains active: after Exp073A completes, its immutable result/classification and next admissible preregistration/checkpoint must be written back to the repository before the next research iteration is considered current.
