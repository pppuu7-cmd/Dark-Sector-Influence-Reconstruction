# Recovery checkpoint — Exp073G support execution binding

**Date:** 2026-08-27  
**State:** all remaining support-envelope choices frozen; no support fraction evaluated

## Resume point

Start from the commit containing
`experiments/073g_kids_boss_bnt_support_execution_binding_v0_1.md` and its
machine-readable JSON companion.  The parent main state is
`7888f9a9b405e928eaf07c01238a64fa636aeefd`.

The next and only authorized operation is to implement the bound evaluator,
clone the four pinned upstream repositories in clean CI, verify every recorded
hash, compute all 156 positive support envelopes, repeat the frozen numerical
controls, and apply the unchanged 5%/15-coordinate gate.

## Newly resolved ambiguity

The released BOSS `32x180` object is a radial configuration-space window, not
a finite k window.  The missing k contract is now supplied by the official
`KiDS-WL/kcap_boss_module` source: the exact Fourier--Bessel transform lower
limit and multipole-dependent exponential cutoffs are frozen.  The KiDS
harmonic tails are now bound to the public KCAP bandpower code and its exact
`0.1<=ell<=10000` operator interval.

Do not replace these by effective k/ell values, top-hat bands, a model power
spectrum, signed-cancellation leakage, or post-output grid tuning.

## Gates still forbidden

Until and unless the final classification is
`PASS_KIDS_BOSS_BNT_PHYSICAL_SUPPORT_EXP073G`, do not read or restrict a
covariance, perform nuisance rank/SVD, fit a G7 relation, or inspect G8/G9
performance.

G7 OPEN. G8 OPEN. G9 OPEN.
