# Article 3 / Exp073P support architecture resolution

**Date:** 2026-08-29  
**Scope:** current execution architecture only; no scientific support score evaluated.

## Resolution from repository chronology

The repository contains two distinct historical support definitions:

1. the 2026-08-27 Exp073P Cosmotheka Wm/WW+BOSS operator-weight support preregistration;
2. the later 2026-08-28 Article-3 final-coordinate physical-support contract.

The later Article-3 recovery checkpoint `recovery/2026-08-28_article3_support_synthetic_pass_and_antileakage_lexeme_hardening_checkpoint.md` explicitly freezes the active downstream order after genuine DES-Y1 reproduction as:

`bind exact upstream authority -> execute preregistered real physical-support validity mask -> only on PASS_PHYSICAL_SUPPORT_ARTICLE3 restrict covariance -> whiten -> signed nuisance SVD/rank -> quotient/relation/null -> fresh G8`.

Accordingly, for the current Article-3 route:

- `PASS_PHYSICAL_SUPPORT_ARTICLE3` is the active real support classification required before covariance restriction;
- the later Article-3 coordinate-level support semantics are controlling for that classification;
- the legacy Exp073P operator-weight `f_invalid` definition remains preserved as historical preregistered methodology/evidence and must not be silently rewritten;
- the legacy Exp073P operator-weight result must not be substituted into `article3_coordinate_f_invalid`;
- no legacy Exp073P operator-weight PASS is required as an additional unstated Article-3 scientific threshold unless a new prospective contract explicitly reintroduces that requirement before real output.

## Role of current Exp073P v0.4

The current Exp073P v0.4 route is an **aggregate prerequisite/authority interlock**, not the Article-3 physical-support score itself.

Its genuine real `PASS_EXP073P_PREREQUISITE_BINDING_V0_4` with `synthetic=false` and `support_executor_authorized=true` establishes that the frozen upstream DES-Y1 reproduction and all preregistered prerequisite identities are admissible for the next support executor. It does not compute the Article-3 coordinate-level `f_invalid`, does not retain the final Article-3 coordinate mask, and does not authorize covariance by itself.

The next current-route scientific executor after a real v0.4 prerequisite PASS is therefore the real implementation of `docs/ARTICLE3_PHYSICAL_SUPPORT_GATE_CONTRACT_2026-08-28.md` under the prospective authority amendment `docs/ARTICLE3_PHYSICAL_SUPPORT_GATE_AUTHORITY_AMENDMENT_EXP073P_V0_4_2026-08-29.md`.

## Non-retroactivity

This resolution does not delete or revise:

- `experiments/073p_cosmotheka_desy1_boss_exact_common_physical_support_prereg_v0_1.md`;
- `ci/exp073p_frozen_contract_selftest_v0_1.py`;
- any historical Exp073P classification or recovery note.

Those records remain useful evidence about finite-operator support methodology and the evolution of the G7 route. They simply are not aliased to the later Article-3 coordinate-count support statistic.

## Current frozen current-route order

`genuine Exp073R1 v0.7 attempt-3 PASS -> genuine Exp073P v0.4 prerequisite-binding PASS -> real PASS/FAIL/INVALID Article-3 coordinate-level physical-support execution -> covariance restriction/whitening only on PASS -> signed nuisance tangent rank/SVD -> quotient/relation/null -> fresh G8 withheld family`

At this checkpoint:

- no real Article-3 support score has been evaluated;
- covariance restriction is unauthorized;
- G7/G8/G9 remain OPEN;
- Article-3 scientific readiness remains **44%**.