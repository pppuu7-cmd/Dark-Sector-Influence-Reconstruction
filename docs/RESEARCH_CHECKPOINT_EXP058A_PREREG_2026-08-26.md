# Research checkpoint — Exp058A preregistration

Date: 2026-08-26

## Starting state

`main` at `1b9c7f87f140e26422b0f3103e3cd70fe282c83d`.

Exp056B/F29 remains a HARD PROSPECTIVE FAIL. Exp057A established that the failure is structural: the C8 response changes orientation/shape, so a single scalar localization coordinate is not adequate across the frozen C8 grid.

## Action taken

Exp058A preregisters a two-coordinate source-response candidate before any new withheld-family response is inspected. The response coordinates are a fixed localization operator `ell` plus a signed second-mode shape coordinate `q`. The hard prospective statement is topological/order preserving: the ordered `(ell,q)` response path must have nonzero adjacent motion, no non-adjacent segment self-intersections, and the same outcome under leave-one-redshift recomputations.

## Anti-retuning boundary

- C8 cannot be reused as withheld evidence.
- No threshold/operator rotation/sign flip/source-range trimming after withheld response generation.
- The future family selector must be source-only and frozen before response generation.
- A failure is retained as a negative scientific result.

## Gate state

- G7 OPEN.
- G8 OPEN.
- G9 OPEN.
- F27 remains HARD FAIL.
- F28 remains retrospective evidence only.
- F29 remains HARD PROSPECTIVE FAIL.

## Next executable step

Choose a genuinely fresh family C9, freeze a source-only selector and solver/mask provenance, then generate the response exactly once and evaluate Exp058A without changing the preregistered criteria.
