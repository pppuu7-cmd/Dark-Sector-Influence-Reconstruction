# Article 3 — DES 14-task angular manifest: Exp073X2 succession amendment

**Frozen:** 2026-08-30 before any production execution of the remaining 13 angular tasks and before any real DES Layer-A support score.

This amendment changes **only the authority-succession condition** in `docs/ARTICLE3_DES_ANGULAR_14_TASK_MANIFEST_2026-08-30.md`. It does not alter any angular task identity, mask, resolution, bin edge, NaMaster component, numerical dtype, support rule, physical domain, threshold, or science firewall.

## Why an amendment is required

The original 14-task manifest was frozen while Exp073X was still the intended Wm_S0 repeatability pilot and therefore states that production is authorized only after an Exp073X PASS.

Exp073X later ended as:

`INCOMPLETE_INFRASTRUCTURE_RESOURCE_CANCELLED_NO_AUTHORITY_REUSE`

with hosted run `33277263287`, job `99166064222`. No Exp073X final angular authority may be reused.

Therefore the literal Exp073X-PASS succession clause cannot be satisfied and is prospectively superseded as follows.

## Frozen succession rule

1. The 14 scientific/angular task identities remain exactly the original ordered set:
   - `Wm_S0`, `Wm_S1`, `Wm_S2`, `Wm_S3`;
   - `WW_S0_S0`, `WW_S0_S1`, `WW_S0_S2`, `WW_S0_S3`;
   - `WW_S1_S1`, `WW_S1_S2`, `WW_S1_S3`;
   - `WW_S2_S2`, `WW_S2_S3`, `WW_S3_S3`.
2. `Wm_S0` may be supplied by the canonical Exp073X2 split-repeatability authority **if and only if** the canonical chain selected under `docs/ARTICLE3_EXP073X2_PARALLEL_AUTHORITY_SELECTION_2026-08-30.md` reaches its prospectively frozen aggregator PASS.
3. A canonical Exp073X2 PASS replaces only the failed infrastructure topology of Exp073X. It does not alter the Wm_S0 operator contract: real DES Y1 masks, `NSIDE=4096`, RING/C convention, NaMaster 2.7 lineage, the same 39 band edges, ell `0..12287`, and selected `TE <- TE` response.
4. After canonical Exp073X2 PASS, production shall execute only the **remaining 13** unique tasks. It must not recompute Wm_S0 merely because Exp073X failed; the exact X2 authority occupies task identity `Wm_S0` in the ordered 14-window join.
5. If canonical Exp073X2 does not classify PASS, the remaining 13 production tasks remain unauthorized. No production trigger may use a partial X2 replica.
6. The final 14-window join must contain all 14 task identities exactly once, with Wm_S0 bound to the canonical X2 operator and the remaining 13 bound to their prospectively frozen production outputs.
7. No support fraction, radial multiplication, row retention, covariance, whitening, nuisance geometry, quotient/relation/null statistic, chi-square, p-value or G7/G8/G9 output may be evaluated until the complete 14-window authority has subsequently been joined with Exp073Z2 radial authority, Exp073AB row mapping, Exp073W BOSS authority, the frozen factorized evaluator and all manifest/firewall metadata into the immutable pre-support candidate manifest.

## Readiness

This succession amendment is architecture/provenance governance only. Strict Article-3 scientific readiness remains **52%**. A canonical X2 PASS alone also adds zero readiness points.
