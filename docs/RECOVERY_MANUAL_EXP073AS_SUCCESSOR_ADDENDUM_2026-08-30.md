# DSIR recovery manual addendum — Exp073AS successor route

**Authority date:** 2026-08-30

Read this after `docs/RECOVERY_MANUAL.md` and `docs/RECOVERY_MANUAL_LIVE_2026-08-30.md`. Where the older live manual describes the **future** AO/AP successor aggregate or pre-support join using Exp073AG/Exp073AE old authority classes, this addendum supersedes only those future-route instructions. Historical AG/AE/P/Q records remain immutable and valid for their declared scope.

## Scientific state

- strict Article-3 scientific repository readiness: **52%**;
- Layer A/B: OPEN;
- covariance/whitening: BLOCKED;
- G7/G8/G9: OPEN;
- Exp073AR and Exp073AS are hosted synthetic schema/provenance PASSes only and add **0** readiness;
- no scientific model PASS is claimed;
- DSIR authority excludes RTK/RQIR.

## Frozen science boundaries remain unchanged

- `0.295 <= z <= 2.33` inclusive;
- `0 < k <= 0.06664762008318016 Mpc^-1`;
- Layer-A `operator_f_invalid <= 0.05` inclusive;
- Layer-B invalid-row fraction `<=0.05` inclusive;
- minimum final retained dimension `15`;
- DES classifying route `NSIDE=4096`, PyMaster/NaMaster 2.7, true ell `0..12287`, 39 frozen bandpowers;
- Wm `TE<-TE`, WW `EE<-EE`, canonical `<f8 [39,12288]`;
- positive absolute operator/window envelope only for support bookkeeping; measured Wm remains signed;
- no effective ell/z/k shortcut and no fiducial-P weighting;
- no covariance/whitening, nuisance SVD/rank, relation/null or G8 leakage before authorized downstream stage;
- exact-threshold ambiguity remains unresolved.

## Execution-qualified angular authority

Future successor class is exactly `controlled_single_thread_exact_v1`.

Wm_S0 anchor is exactly Exp073AM canonical SHA:

`8ac59fc060195addcc5cd8b6d75e32fbc6dbfeea8456f4c83e8bf0cf034b9220`.

Historical primary-P SHA `6ec29f6dbbcf0c29d7af9d6eb98d271bcd06e00d28cebe658b5e266f7ef18d0f` remains historical-route authority only and is not a future successor anchor. Historical Q remains immutable computational `SCIENTIFIC_REPEATABILITY_FAIL`.

Every remaining angular task requires two independent hosted replicas with all frozen single-thread controls, exact canonical SHA equality and `numpy.array_equal=true`. No tolerance, ULP/rounding equivalence, majority vote, preferred replica, single-replica admission or closeness-to-P rescue is allowed.

## Exp073AQ active heavy gate

At this addendum freeze, run `33327372191` remained active:

- Wm_S1 replica A job `99299799192`: exact controlled computation in progress;
- Wm_S1 replica B job `99299799338`: exact controlled computation in progress;
- no final comparator authority artifact existed.

Do not launch Wm_S2 while AQ is active. After valid comparator classification:

- exact PASS -> Wm_S1 admitted, +0 readiness, prospectively freeze Wm_S2 twin gate;
- exact twin mismatch -> task-specific computational repeatability FAIL and successor chain stops;
- failure before comparator authority -> infrastructure-INCOMPLETE, not repeatability FAIL.

## Exp073AR — future 14-window aggregate

Hosted run `33327870470`, job `99301112192`, artifact `9736757853`, digest `sha256:b3d2a1195299d9daedd469dee1fb394bcfba4499abfdbd04159330566d8c76e9` passed all 24 frozen synthetic cases.

For the AO/AP successor route, Exp073AR supersedes old Exp073AG future aggregation instructions. A real aggregate must contain exactly Exp073AM Wm_S0 plus 13 separately admitted controlled-twin authorities, all in frozen task order and all under `controlled_single_thread_exact_v1` evidence. Historical `canonical_exp073x2` and old `exp073aa` classes are rejected for this future aggregate.

## Exp073AS — future pre-support join

Historical Exp073AE required old `canonical_exp073x2 + exp073aa` angular authority classes and therefore cannot be used unchanged after AO/AP/AR. Exp073AS prospectively supersedes AE only for the future execution-qualified route.

Hosted authority:

- prereg `9957a097d8fd8532708bac3685c3a70683c4a263`;
- validator `66a34a390342984f4de2b01fc10df263243fb168`;
- workflow `f7137643f0652062d5390d36ea1ee673dfdfc56c`;
- workflow freeze `61612b3f8e35615ce47ef59fe63bf068daf398a6`;
- trigger/head `a79e4151f9413a931a9bcfd9b3ed256483640a99`;
- run `33330144734`, job `99307146685`;
- artifact `9737392901`;
- digest `sha256:1542be1b0982916b921d4c908a2ce9d58e4a9e17c34784dc9d9b5ba4273919ff`;
- token `PASS_EXP073AS_EXECUTION_QUALIFIED_PRESUPPORT_JOIN_SUCCESSION_SYNTHETIC_V0_1`;
- 24/24 frozen synthetic cases passed.

Exp073AS is non-scientific and +0 readiness.

The future real join must bind unchanged authorities:

- Exp073U 1410-row observation skeleton / ordered-ID authority;
- Exp073Z2 exact DES radial authority;
- Exp073AB row-to-operator mapping authority;
- Exp073W BOSS compatibility/broad-row authority;
- exactly one real Exp073AR-validated execution-qualified 14-window aggregate.

It must remain blind to support selection and all downstream classifier information. The candidate manifest may be declared complete only once all 1410 pre-support finite-operator rows are bound from real hosted authorities.

## Current authoritative route

1. resolve Exp073AQ Wm_S1 without duplicate heavy launch;
2. if exact PASS, freeze/run Wm_S2 as its own controlled twin gate;
3. continue the remaining tasks one at a time through exact hosted twin admission;
4. Exp073AM Wm_S0 + 13 admitted tasks -> real ordered 14-window aggregate under Exp073AR;
5. bind U/Z2/AB/W + that real aggregate through the strict real pre-support join under Exp073AS;
6. freeze immutable complete 1410-row finite-operator candidate manifest;
7. only then evaluate real Layer A;
8. after Layer A/B PASS, proceed to covariance/whitening, nuisance/quotient/relation/null and fresh G8 under the existing frozen gates.

Until step 6 exists as real hosted authority, strict Article-3 readiness remains **52%**.