# Article-3 Wm_S2 Track-A acceptance / readiness-ledger amendment — 2026-09-02

## Scope and authority boundary

This record is a prospective downstream/accounting admission of the already-completed NEW Exp073CI v0.2 authority. It does not alter any execution criterion, numerical result, historical classification, support boundary, downstream gate, or scientific threshold.

Permanent historical authority remains unchanged:

- Exp073CF continuation compact comparator job `100260974130` is a valid scoped exact PASS for canonical `<f8 [39,12288]` Wm_S2 compact input, SHA `963dfd79bd49119d2c3124de3507330b3c47637b41dcbd7b9536f617186ef7bd`, authority artifact `9848084775`, digest `sha256:29ac6e91f703734cfffcbffd1504fda9c861aa12dcb88822b83af50842983dd2`.
- Exp073CF frozen finalizer v0.1 remains permanently `SCIENTIFIC_REPEATABILITY_FAIL_EXP073CF_WM_S2_FINALIZER_EXACT_V0_1`, comparator job `100261645358`, authority artifact `9848162380`, digest `sha256:f291447e109b2149958114baa30baf37edb6aa75efe9c2b41498d88fe4e193a1`. No tolerance, ULP, rounding, averaging, smoothing, preferred-replica, or environment rescue is permitted.
- Exp073BD remains historical `P3 PROVISIONAL_INCOMPLETE_NO_DOWNSTREAM_USE` and is not reclassified.

## New-version exact authority admitted

Exp073CI is a prospectively versioned deterministic finalizer contract, not a reclassification of Exp073CF v0.1.

Frozen Exp073CI provenance:

- preregistration commit `1cf4ef96a44f26e7170d1ce6bd87c38dcc85cc7f`;
- helper commit `9f2f7870d912314e03f2f5725b07df12ace7fa92`;
- workflow path-history commit `ca1af9bf17496e0f2bcb388356ea6a954844e2ef`;
- binding path-history commit `835a916d5708d394cacc08c028cae1341e195868`;
- trigger/head `f8396c8e5e6b4a83340acf6ea0aaa262c9c71007`;
- run `33646799130`;
- exact comparator job `100304043991`;
- authority artifact `9853165664`, digest `sha256:fcfccb6768948ffe34d28e9ed32da64d3b1d071704028fe6f312c1ab8b440f57`;
- token `PASS_EXP073CI_WM_S2_DETERMINISTIC_FIXED_NEHALEM_FINALIZER_EXACT_V0_2`.

The exact contract holds across four independent hosted workers, both independent compact artifact lanes, and fresh-process repeats:

- compact SHA `963dfd79bd49119d2c3124de3507330b3c47637b41dcbd7b9536f617186ef7bd`;
- K SHA `c24456b19e7248cc7ad68502fc78d6f75b885665641d662b1d9c789cf473f795`;
- fixed-Nehalem W SHA `96248e7699a5a12945854db2c9af150affcfe13f4f9dc0bfcbb87b99f92ff087`;
- exact-only comparison; no tolerance.

The static semantic/criteria-binding audit `PASS_EXP073CI_V0_2_STATIC_SEMANTIC_CRITERIA_BINDING_AUDIT` establishes that v0.2 retains the original `k_from_a`, exact `np.linalg.solve(K,A)` algebra, canonical `<f8 [39,12288]`, band/ell ordering, and Wm `TE <- TE` semantics; the only prospective numerical-environment change is fixed OpenBLAS dispatch.

## Admission decision

`ADMIT_TRACK_A_EXACT_WM_S2_NUMERICAL_ANGULAR_OBJECT_V0_2`

For Article-3 angular-object accounting and downstream dependency selection, the composite authority

`Exp073CF compact scoped exact PASS -> Exp073CI v0.2 deterministic exact finalizer PASS`

is admitted as the complete repeatable Track-A Wm_S2 numerical angular object.

This admission is new-version authority only. It does not erase, rescue, reinterpret, supersede historically, or convert the Exp073CF v0.1 finalizer FAIL. The historical FAIL remains part of the scientific record and motivates why v0.2 exists.

This admission also does not establish physical support validity, Layer-A or Layer-B validity, covariance/whitening validity, nuisance rank/SVD, quotient/relation/null control, G7 authorization, or G8 validation.

## Frozen readiness accounting

The governing frozen document `docs/ARTICLE3_DUAL_READINESS_ACCOUNTING_2026-08-31.md` states:

- individual angular tasks add `+0` to `SCIENTIFIC_AUTHORITY_READINESS` unless separately authorized by scientific accounting;
- each complete manuscript-usable angular object with admitted Track-A exact authority earns `12/14 = 0.8571428571428571` Draft/data percentage points;
- the same object may not be double-counted.

Before this amendment, eligible angular objects were Wm_S0 and Wm_S1: `2/14`, yielding exact Draft/data `53.714285714285715%`.

Wm_S2 v0.2 is a third distinct angular task and was not previously credited because Exp073BD was P3/incomplete. Therefore:

- eligible complete angular objects = `3/14`;
- angular Draft/data credit = `12 * 3/14 = 2.571428571428571%`;
- `SCIENTIFIC_AUTHORITY_READINESS = 52.0%` unchanged;
- `DRAFT_DATA_READINESS = 52.0 + 2.571428571428571 = 54.57142857142857%`;
- normal display = `Verified 52.0% | Draft/data 54.6%`.

The Exp073CI execution record's `[0,0]` delta is preserved: the +0.857142857... Draft/data change here is a separate ledger admission under the pre-existing angular object-completion rule, not retroactive execution scoring.

## Frozen next-order constraints

The required order remains:

`validated physical forward/power-input bridges -> preregistered physical support-validity mask -> Layer A/Layer B prerequisites -> covariance restriction/whitening -> nuisance tangent rank/SVD -> quotient/relation/null control -> actual G7 authorization -> fresh G8 withheld family`.

No G7 or G8 credit is granted by this amendment. No downstream physical claim may use Wm_S2 merely because its numerical angular object is now repeatable; all later frozen validity gates remain mandatory.

## Next permitted gate

Inspect the frozen 14-window/angular task inventory and physical forward/power-input dependency map. Select the next genuinely missing non-duplicative angular/forward object in the existing gate order. Before any new heavy self-hosted computation, prospectively freeze its exact contract and, for a NEW workflow only, perform the authorized staged `4 -> 6 -> 8` exact-equivalence and RSS/swap safety preflight if wider concurrency is relevant. Do not rerun Exp073CF/CI merely to chase final bits.
