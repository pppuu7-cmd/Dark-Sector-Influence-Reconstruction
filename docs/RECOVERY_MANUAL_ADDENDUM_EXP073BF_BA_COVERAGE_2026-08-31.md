# Recovery manual addendum — Exp073BF->Exp073BA coverage audit

**Date:** 2026-08-31

Use this addendum after `docs/RECOVERY_LATEST.md` and `recovery/2026-08-31_exp073bf_ba_coverage_audit_ba_bd_active.md`.

## Durable facts

- Exp073AQ remains permanent exact-repeatability scientific FAIL; never reinterpret it.
- Exp073BF hosted synthetic Wm stock-equivalence QA is PASS but contributes `+0` scientific and `+0` draft/data readiness.
- Static audit `docs/EXP073BF_TO_EXP073BA_IMPLEMENTATION_COVERAGE_AUDIT_2026-08-31.md` confirms BF independently exercised the same Wm low-memory algebraic sequence as BA, while explicitly excluding full-scale/physical/exact-cross-run claims.
- Exp073BA clean rerun `33345968620` is the next Track-A authority-changing gate. At this checkpoint both compact A/B replicas are still computing after successful exact AZ binding; no BA artifacts exist.
- Exp073BD provisional Wm_S2 `33342265114` is still computing both A/B branches; no artifact exists and neither branch may be preferred.
- Do not launch duplicate heavy runs while either is active.

## Frozen interpretation rule

BF's synthetic `<1e-12` stock-equivalence threshold is never a BA scientific tolerance. BA requires exact equality under its frozen comparator. Infrastructure/resource termination before classifying comparator output is not a scientific FAIL. A completed exact comparator disagreement is classified under the preregistered scientific rule without rescue.

## G7 ordering

Validated physical forward/power-input bridges -> physical support-validity mask -> Layer A/B prerequisites -> covariance restriction/whitening -> nuisance tangent rank/SVD -> quotient/relation/null -> actual G7 authorization -> only then fresh G8 withheld family.

## Accounting

`Verified: 52.0% | Draft/data: 53.7%`

This addendum itself changes neither metric.