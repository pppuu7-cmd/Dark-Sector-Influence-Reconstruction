# Gate update — Exp055A / F28 — 2026-08-26

## Result

Exp055A qualifies the endpoint-normalized half-transition relation

`C50 = Delta ln(k50_geo) / Delta ln(k_source) > 0`

as a **retrospective candidate for future preregistration**.

Evidence on already-seen C3 GDM, C5 designer-f(R), and C7 IDM-DR:

- 105/105 valid unique half-transition crossings;
- every adjacent C50 positive;
- sign remains positive under all single-redshift deletions.

## Gate interpretation

- **G7 remains OPEN.** Exp055A is post-C7 candidate construction, not prospective law validation.
- **G8 remains OPEN.** C7 cannot count as withheld evidence for an operator chosen after C7 was inspected.
- **G9 remains OPEN.** No fundamental dynamics/action follows.
- **Exp054C/F27 remains HARD FAIL** for the old raw `R^2` centroid relation.

The next valid G7/G8 attempt must use a genuinely fresh mechanism/family and commit the complete k50/sign contract before its first response is generated.

## What is and is not common

Supported retrospectively: the **direction** of transition motion (`C50>0`).

Not supported: one universal numerical slope coefficient. C7 slopes (`~0.127..0.392`) are substantially larger than the C3/C5 ranges (`~0.0125..0.066`).

Therefore any future preregistration should test sign/order preservation, not fit a shared magnitude band from these three families.
