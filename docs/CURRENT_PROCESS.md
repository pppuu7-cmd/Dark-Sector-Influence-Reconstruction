# DSIR current-process ledger

Updated: 2026-09-11. Scope: **DSIR only**; RTK/RQIR/KMQGB/KMDSB excluded.

## Scientific frontier — unchanged
Canonical 8193->16385 remains independently verified `COMMON_GRID_NEXT_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`, max relative component difference `0.012484060640679777` versus frozen strict `<1e-3`. Frozen `REL_TOL=1e-3`, `h=1e-4`, native kpd20, centered-cubic interpolation, physical domain/masks/107-row accounting, invalid fraction `<=0.05`, retained `>=15`, unsupported=0 and lookup `<=1e-12` remain unchanged.

No 16385->32769 scientific execution is authorized. Covariance restriction remains unauthorized. Wm_S3 remains unopened.

## Cancellation / numerical-conditioning diagnosis — support-complete
The single permitted unchanged top-64 retry `34614119149`, attempt 2/job `103331912096`, reproduced all 64 ordered atoms exactly at the primary guard; `21/64` are `>=1e-3`. Confounder run `34628562363` and independent consumer `34628673923` established `64 -> 58` unique physical atoms, collapsed Spearman `-0.5163800793626381`, DES-only Spearman `-0.5078643578643579`, and cancellation-scale quartile exceedances `13/16`, `6/16`, `1/16`, `1/16`. Exploratory unique-atom run `34628932658` gave lowest-scale-quartile versus rest OR `20.571428571428573`, Fisher one-sided `p=1.6168593301143046e-05`.

These results strengthen a distributed finite-difference cancellation/ill-conditioning diagnosis but remain support-only `+0/+0`. The branch is closed for the current Article III cycle; do not proliferate same-family correlation variants without a prospectively specified distinct confounder.

## Article III methods integration
`docs/dsir4/methods/LAYERB_LOCAL_VS_GLOBAL_CONVERGENCE_SYNTHESIS_V0_2.md` separates the failed scientific gate from the diagnostic mechanism evidence. The manuscript-safe statement is that canonical 8193->16385 remains NOT_CONVERGED while independent post-gate diagnostics localize much of the residual to a distributed cancellation-sensitive numerical-conditioning regime. This does not rescue the gate.

## 32769 next-rung preflight — static canonicalization closed
The logical sequential successor is 32769, but ladder ordering is not execution permission. The non-triggering preflight specification is `docs/dsir4/methods/LAYERB_32769_NONTRIGGERING_PREFLIGHT_SPEC_V0_1.md`.

V126 closed the response-blind canonical-bytes prerequisite. Source run `34630453409` used the inherited frozen `guarded_lattice(n)` semantics and exact NumPy 1.26.4. Five of eight replicas `[1,4,5,7,8]` exactly reproduced the canonical 16385 anchor and emitted byte-identical 32769 candidates; replicas `[2,3,6]` were retained as host variants under the inherited quorum rule. Independent consumer run `34630559826` revalidated the payload.

Canonical 32769 support:
- file: `docs/dsir4/canonical/LAYERB_CANONICAL_FINE_32769_NODES_U64HEX_V0_1.txt`;
- materialization commit: `68a37d43a3496e9f0d760c66bcb494ea520aa13e`;
- Git blob: `7b67706156d7d2016ecba0628210fb08f6f05616`;
- node-payload SHA256: `82c48c17dba812bd08a3437f7e48376208a8c506f4a63087191608793fba4599`;
- u64hex SHA256: `7422d00aab2b32a060878224e81834277a67d42016595a0e9088db05b14166e2`.

Durable authority: `docs/dsir4/authority/LAYERB_RESPONSE_BLIND_CANONICAL_FINE_32769_V0_1.json`, creation commit `b49b0d0065be2878644c9b81a23b4741e80e4387`.

## Remaining production blockers
`CANONICAL_32769_BYTES_FROZEN=TRUE`, but `32769_EXECUTION_AUTHORIZED=FALSE`.

The validated 16385 CLASS build capacity is only `18432`, so it cannot carry 32769. A new capacity-only CLASS patch with point capacity `>=32769`, exact source/patch identities, parser/build parity, and independent proof of scientific-equivalence invariants is required. Ordinary GitHub-hosted `ubuntu-24.04` is also not an admissible default production topology: the canonical 16385 hosted-memory audit exhausted memory in all four roles at roughly 15.4--15.7 GB Python RSS. No 32769 memory requirement is extrapolated from that evidence.

### Exact next action
Freeze and independently audit the >=32769 CLASS capacity-only build prerequisite, then validate a higher-memory resource/lifecycle topology without reading the 32769 scientific response. Only after those static/resource gates and one-live anti-duplication close may a separate explicit one-run 16385->32769 scientific authorization be considered.

## Automation / ownership
`DSIR Continuous Research` remains enabled hourly and was rechecked during this research cycle. It must resume from V126. No duplicate heavy science lane is authorized. Stale self-hosted run `34550495778 / 103112190909` remains superseded and must not receive home-runner ownership.

## Recovery authority
Newest immutable note: `docs/recovery/RECOVERY_2026-09-11_ARTICLE3_32769_STATIC_CANONICALIZATION_CLOSED_V126.md`, creation commit `4a472a65ffbaa86d9a3c5c7aff5997ddd6b91bc6`.

## Readiness
**ARTICLE3_REPOSITORY_READINESS: 68%.** Funnel-freeze readiness: **67%**.

The percentages remain unchanged because the new work closes static reproducibility/preflight infrastructure rather than the frozen scientific convergence gate.
