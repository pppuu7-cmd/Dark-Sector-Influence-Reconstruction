# DSIR current-process ledger

Updated: 2026-09-14. Scope: **DSIR only**; RTK/RQIR/KMQGB/KMDSB excluded.

## Active authoritative frontier — V0.14 same-run repeated-solver determinism audit

Parent terminal authority: `docs/dsir4/authority/LAYERB_BETA_REPLAY_SEQUENCING_V0_13.json`, creation commit `e62158ab101f3da7ff334d39afbfd75520a82699`.

Parent classification: `HOSTED_RUN_REPLAY_NONDETERMINISM_SUPPORTED`, effect `+0/+0`; authorized successor exactly `PROSPECTIVELY_FROZEN_SAME_RUN_REPEATED_SOLVER_DETERMINISM_AUDIT`.

Prospective V0.14 contract: `docs/dsir4/contracts/LAYERB_BETA_SAME_RUN_SOLVER_DETERMINISM_V0_14.json`, commit `ca0dd7e25307d73e6ce0b689542b88aa9468e93b`.

Executor: `ci/layerb_beta_same_run_solver_determinism_v0_14.py`, commit `351f18f25c2b18330ad0449d1d3cbc9c9460c781`, blob `bcc9e117b7491ae4034a8ed299a80939d795f702`.

Workflow: `.github/workflows/layerb-beta-same-run-solver-determinism-v0-14.yml`, commit `127d19739bad80df822bfd58d45fed8bbfc12a64`.

Launch: `docs/dsir4/launch/LAYERB_BETA_SAME_RUN_SOLVER_DETERMINISM_V0_14_LAUNCH.json`, head/commit `e02c2645ce5c74d23145bb2e8d9735695583c448`.

Authoritative active run: `34787822995`. Exactly one run exists for the launch head. The invariant-audit is terminal `success`; four frozen lanes run in parallel: `GRID768/GRID1024 × PURE_PAIR/INTERLEAVED`, each executing three sequential identical solver sequences in one Python process/hosted runner. Decision waits for invariant plus all four lanes.

No substantive partial lane values may be used before the terminal frozen decision.

## Frozen V0.14 scientific/technical contract

Target hypothesis: V0.13 same-profile hosted-job response spread is generated by repeated identical solver construction within one hosted runner/process, rather than solely by cross-job/runner environmental variation.

Object: within-profile maximum pairwise relative spread of signed centered beta responses across three sequential identical repeats on the exact five V0.13 diagnostic cells.

Frozen thresholds/identities remain: production `h=1e-4`; scientific response threshold `<1e-3`; determinism threshold `<1e-5`; exact binding `<=1e-12`; production sampling `0.00035`; TOL300 `1e-12`; same CLASS commit/baseline/precision/JJ/v12 extraction identities. Cross-profile differences are a negative control and cannot count as repeat nondeterminism.

Frozen classifier:

- controls/anchors valid and any REPLAY_FAILURE within-profile repeat spread `>=1e-5` -> `SAME_RUN_REPEATED_SOLVER_NONDETERMINISM_SUPPORTED`;
- controls/anchors valid and all REPLAY_FAILURE spreads `<1e-5` -> `SAME_RUN_REPEATED_SOLVER_DETERMINISM_SUPPORTED`;
- anchor, binding, finite-value, identity or lane/repeat invariant failure -> `SAME_RUN_SOLVER_DETERMINISM_AUDIT_INCONCLUSIVE` / no scientific promotion.

Interpretation ceiling: reproducibility localization only. V0.14 cannot validate full Layer-B, covariance, whitening, nuisance treatment, relation-null, `Wm_S3`, global traversal, dark-sector inference or a physical signal.

## Parent V0.13 facts

Run `34773514342`, decision artifact `10322573705`, digest `sha256:b04db83d530c36e03f2b80ad33fb4be80747817bd08cb2b001d5c8c6375fd202`. Three frozen replay-failure cells showed same-profile independent-hosted-job spreads `1.5506392790000525e-4`, `2.4934371090895516e-5`, `1.0478978062540487e-5`; both anchors were below threshold.

## Frozen boundaries

No full 107-row Layer-B traversal, covariance/whitening/nuisance/relation-null, `Wm_S3`, global 65537 or science gate is authorized. Frozen readiness remains `ARTICLE3_REPOSITORY_READINESS: 68%` and scientific frontier `67%`; reproducibility diagnosis alone changes neither.

## Anti-duplication

Do not launch another V0.14 while run `34787822995` is non-terminal. Consume only its single frozen decision after invariant and all four lanes are terminal, verify artifacts/hashes, then write a durable V0.14 authority and follow only its encoded `next_stage`.
