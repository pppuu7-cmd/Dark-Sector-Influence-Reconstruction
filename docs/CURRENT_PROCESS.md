# DSIR current-process ledger

Updated: 2026-09-11. Scope: **DSIR only**; RTK/RQIR/KMQGB/KMDSB excluded.

## Scientific frontier — unchanged
Canonical 8193->16385 remains independently verified `COMMON_GRID_NEXT_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`, max relative component difference `0.012484060640679777` versus frozen strict `<1e-3`.

Frozen science remains unchanged: `REL_TOL=1e-3`, `h=1e-4`, native kpd20, centered-cubic interpolation, the same physical domain/masks/107-row accounting, invalid fraction `<=0.05`, retained `>=15`, unsupported=0 and lookup mismatch `<=1e-12`.

No 16385->32769 scientific execution is authorized or has occurred. Covariance restriction remains unauthorized. Wm_S3 remains unopened.

## Closed 32769 prerequisites and dormant wiring
Durably closed and independently checked:
- response-blind canonical 32769 bytes + static consumer;
- CLASS point capacity 32769 and parser/build/static scientific equivalence;
- terminal result schema + independent terminal consumer;
- isolated high-memory routing under `dsir-32769-highmem`;
- one-live guard implementation + independent synthetic audit;
- measured-resource consumer/materializer implementations + fail-closed audits;
- first-run one-run authorizer + independent synthetic audit;
- dormant 16385->32769 scientific engine + static scientific-core equivalence;
- dormant dispatch-only production workflow + static orchestration audit;
- API-backed candidate provenance/packaging chain;
- exact-byte resource-authority promotion reviewer + read-only promotion workflow;
- exact materializer-output -> one-live -> one-run-authorizer interface;
- V135 frozen runner configurator + registration/runtime provenance gate + independent audit.

`TERMINAL_SCHEMA_AND_INDEPENDENT_CONSUMER_FROZEN=TRUE`.

## Resource/lifecycle gate — physical execution is the dominant blocker
The exhausted hosted 16385 topology remains frozen at `MemTotal=16372440 kB`, `SwapTotal=3145724 kB`, terminal `MemAvailable=0`, `SwapFree=0`. No numeric 32769 memory requirement is extrapolated.

The current isolated source workflow remains `.github/workflows/layerb-32769-isolated-highmem-resource-lifecycle-pilot-v0-2.yml` but V135 supersedes its old blob. Current Git blob: `3297a556299daf75e4c7ff15f8e56586e45ebdf0`. It routes only on `[dsir-32769-highmem]`, requires physical `MemTotal > 16372440 kB`, preserves the same scientific/resource core blobs, and now fail-closes unless the executing runner is bound to V135 registration evidence.

`HIGH_MEMORY_RESOURCE_LIFECYCLE_PREFLIGHT_PASS=FALSE` until an actual qualifying runner executes the measured pilot successfully.

The prospective real chain is:
`frozen runner configuration -> registration/runtime evidence -> isolated resource pilot -> hosted independent consumer V0.2 -> API-backed provenance packager -> hardened materializer -> read-only candidate artifact -> exact-byte promotion review -> deliberate durable authority copy -> immediate live guard -> current-run-only one-run authorization -> exactly one scientific run -> independent terminal consumer`.

## V135 runner-registration provenance — prospectively closed
Frozen configurator: `scripts/dsir4/configure_layerb_32769_highmem_runner_v0_1.sh`, creation commit `47b556204054c3730d0272e442bebe7239e1b1d4`, Git blob `7489deb49b86245cf399d8020b9ac4fdfcac9278`, SHA256 `2cd02d1a7ac58ea3945a435272f03ea1abe2d7e3410191b5a17a9c62e0a297ce`.

It configures a previously unconfigured runner using exactly one custom label `dsir-32769-highmem` plus `--no-default-labels`, then writes a sanitized receipt containing AgentId/AgentName, exact label configuration, configurator identity and isolation-contract identity. Registration token/credentials are explicitly not recorded.

The current source workflow binds that receipt to the live Actions job using API `runner_id`, `runner_name` and exact dedicated routing labels, then hashes both the registration receipt and sanitized runtime-job receipt into `result.json`.

Registration-aware independent consumer: `ci/layerb_32769_resource_lifecycle_independent_consumer_v0_2.py`, creation commit `972ecd79b096135d9bdbbac64780c4a133b6f0f4`, Git blob `e08f15394ba3b16222b62d061a621e24f3a857ec`.

Independent event-chain workflow retains its V132-compatible name/path and now pins consumer V0.2 plus source workflow blob `3297a556...`; current workflow Git blob `a68148e6826cda223368f8a624014a873cd1dc44`.

Audit workflow `.github/workflows/layerb-32769-runner-registration-evidence-audit-v0-1.yml`, Git blob `c544881cf93c060bb8e2cbd722505a662214736a`. Run `34645311690` passed synthetic consumer job `103414563351`, static source-workflow job `103414563197`, and independent verifier `103414777148`.

Synthetic artifact `10281202698`, ZIP SHA256 `8206c140aefba57a892ab4adb366bf315b8477c911fc78f4850c2b7b163b4fa0`, PASS receipt SHA256 `b4dd200a7437158800fc8074dcf0bcf80e1478d61b58822463f26482a2988225`, summary SHA256 `6564deb28e9ce66129f5e040ede9095cd2eeb9fc75f4dce2da30608956273349`.

Static artifact `10281867381`, ZIP SHA256 `7e7cf6b1ffc31c8ed23bde5aae891f1572c63ec17d20eab03e55446de2bc2d6b`, static receipt SHA256 `d58b9fcc2273ca66485de02b298a212d28930b5a337fc694bec0b3d24dcd9543`.

The valid bound fixture passed. The audit rejected `no_default_labels=false`, wrong configurator identity, an added generic `self-hosted` label, runner-id mismatch and routing-label substitution. Static audit confirmed all scientific/resource core blobs were unchanged.

Durable authority: `docs/dsir4/authority/LAYERB_32769_RUNNER_REGISTRATION_PROVENANCE_AUDIT_V0_1.json`, creation commit `6c6d9e82db30a5dadc5295b7e615822730df1a49`.

## V132-V134 retained interfaces
V132 candidate packaging remains API-backed/read-only and consumes independent classification/valid/errors plus exact independent-validation SHA binding; V135 V0.2 output preserves this interface.

V133 exact-byte promotion review remains read-only and can only review a real candidate for deliberate exact-byte promotion to `docs/dsir4/authority/LAYERB_32769_HIGH_MEMORY_RESOURCE_LIFECYCLE_V0_1.json`.

V134 established exact byte/schema compatibility from materializer output through one-live guard into the one-run authorizer. No real one-run authorization exists yet.

## Dormant scientific engine and production workflow
Dormant engine `ci/layerb_16385_to_32769_canonical_one_live_refinement_v0_1.py`, Git blob `5ae2068d65dd45e0813c75fb795e55aa1bda1d5e`, remains static-equivalent to the prior validated scientific core after only the frozen support-count change.

Dormant production workflow `.github/workflows/layerb-16385-to-32769-canonical-scientific-v0-1.yml`, Git blob `765abe3f6ec0c47392328678fffd5417f2ae4b43`, remains dispatch-only, isolated-label-only, measured-resource-gated, live-guard/current-run-authorized and terminal-consumed. No production run has been launched.

## Remaining true blockers
1. Configure/attach a real high-memory runner **using the V135 frozen configurator**, with a unique new runner name and physical `MemTotal > 16372440 kB`.
2. Dispatch exactly one measured response-blind V0.2 32769 lifecycle pilot; real V135 registration/runtime evidence must be present.
3. Hosted independent consumer V0.2 and candidate chain must PASS and produce a real candidate.
4. The candidate must pass V133 exact-byte promotion review, then the reviewed bytes must be deliberately copied to the durable resource-authority path.
5. Actual immediate one-live guard and current-run-only one-run authorization must PASS.
6. Only then exactly one canonical scientific 16385->32769 run and independent terminal classification.

The old superseded run `34550495778` / job `103112190909` remains queued on generic `[self-hosted, Linux, X64]`; the current dedicated runner must therefore use `--no-default-labels`. The current source workflow also fails closed if that stale job is observed `in_progress`.

The available GitHub connector cannot cancel that queued run or administer/enumerate self-hosted runners, so no physical runner state is invented.

## Exact next action
Configure the new isolated runner with `scripts/dsir4/configure_layerb_32769_highmem_runner_v0_1.sh`, start it, and dispatch exactly one current V0.2 response-blind lifecycle pilot. Do not dispatch canonical science before measured resource authority and live authorization exist.

## Automation / ownership
`DSIR Continuous Research` is currently **disabled**. V134's statement that it remained enabled hourly was stale. It was not re-enabled in V135. Repository state wins over chat state; no duplicate heavy science lane is authorized.

## Recovery authority
Newest immutable note: `docs/recovery/RECOVERY_2026-09-11_ARTICLE3_32769_RUNNER_REGISTRATION_PROVENANCE_V135.md`, creation commit `e9a1ea249573d527e3d8cc6b757645aec9b981a4`.

## Readiness
Frozen publication rubric remains **ARTICLE3_REPOSITORY_READINESS: 68%**.

Funnel-freeze/scientific frontier remains **67%**.

Operational roadmap tracking: **WORKING_PLAN_COMPLETION: 84%**. V135 closes the runner-registration provenance gap prospectively; real measured resource execution and scientific convergence remain open.
