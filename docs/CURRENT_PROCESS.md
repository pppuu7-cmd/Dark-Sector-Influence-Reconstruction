# DSIR current-process ledger

Updated: 2026-09-11. Scope: **DSIR only**; RTK/RQIR/KMQGB/KMDSB excluded.

## Scientific frontier — unchanged
Canonical 8193->16385 remains independently verified `COMMON_GRID_NEXT_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`, max relative component difference `0.012484060640679777` versus frozen strict `<1e-3`. Frozen `REL_TOL=1e-3`, `h=1e-4`, native kpd20, centered-cubic interpolation, physical domain/masks/107-row accounting, invalid fraction `<=0.05`, retained `>=15`, unsupported=0 and lookup `<=1e-12` remain unchanged.

No 32769 execution is authorized. Covariance restriction remains unauthorized. Wm_S3 remains unopened.

## Closed cancellation diagnosis
Exact-hotspot/top-1 cancellation/decomposition support remains terminal and support-only `+0/+0`. The single permitted unchanged top-64 retry also completed successfully: run `34614119149`, attempt `2`, job `103331912096`. It reproduced all 64 ordered atoms with maximum primary reproduction relative error `0.0`; `21/64` are at or above the strict `1e-3` discrepancy threshold.

Durable top-64 authority:
`docs/dsir4/authority/LAYERB_POST_16385_TOP64_ROLE_CANCELLATION_TERMINAL_AUTHORITY_V0_1.json`, Git blob `5e16e8a793a81bf1692df92d8e24459e03fd4838`.

## Top-64 population/confounder robustness — closed
The first parallel-confounder run `34626195929` was an infrastructure-only failure before scientific computation: default shallow checkout made a history-based `git log` lineage guard invalid. Commit `d1f47cec82b3305a53ebe72c3daf9a015ee06ef3` replaced that guard with exact immutable Git-blob identity checks; no scientific parameter changed.

Corrected run `34628562363` passed three explicit audits, then independent terminal consumer run `34628673923` revalidated their payloads.

Key validated results:
- exact duplicate collapse: 64 source atoms -> 58 unique physical atoms; Spearman changes only from `-0.5341210955390675` to `-0.5163800793626381`; `19/58` unique atoms remain `>=1e-3`;
- DES alone: 55 atoms, 18 `>=1e-3`, Spearman `-0.5078643578643579`, so the association is not generated by repeated BOSS_GL64 entries;
- cancellation-scale quartiles: `13/16`, `6/16`, `1/16`, `1/16` strict-threshold exceedances from lowest to highest scale quartile;
- median cancellation scale of `>=1e-3` atoms divided by that of `<1e-3` atoms is `0.13484119009229173`.

Durable authority:
`docs/dsir4/authority/LAYERB_POST_16385_TOP64_CONFOUNDER_TERMINAL_AUTHORITY_V0_1.json`, creation commit `be81a67ed93f4e640e79532f85fda90d32c33afe`, Git blob `e45e978a63e85dce5ede38bcaedb59b50991c208`.

## Exploratory unique-atom stress test
Run `34628932658` evaluated the 58 unique physical atoms. It is explicitly **post-hoc/exploratory support**, not preregistered confirmatory inference.

Results: unique Spearman `-0.5163800793626381`; lowest-scale quartile vs rest odds ratio `20.571428571428573`, one-sided Fisher `p=1.6168593301143046e-05`; lower-half vs upper-half odds ratio `19.125`, one-sided Fisher `p=2.3321946203480245e-05`; unique median-scale ratio (`>=1e-3` / `<1e-3`) `0.12041078397110301`.

Durable exploratory authority:
`docs/dsir4/authority/LAYERB_POST_16385_TOP64_UNIQUE_ASSOCIATION_EXPLORATORY_AUTHORITY_V0_1.json`, creation commit `629ca6dd4bec25e374467b7d06a2d555354688f8`.

## Interpretation / authority boundary
The finite-difference cancellation/ill-conditioning diagnosis is now distributed across the population rather than supported only by one hotspot. It survives duplicate collapse and DES-only stratification, and strict-threshold discrepancies are strongly enriched at the smallest cancellation scales.

All of this remains diagnostic support-only `+0/+0`: no scientific classification was created, no next-rung authorization was created, covariance restriction remains unauthorized and Wm_S3 remains closed.

## Current process decision
The top-64 cancellation/confounder branch is **support-complete for the present Article III cycle**. Do not spend iterations on further variants of the same correlation/duplicate/block/quartile analysis unless a genuinely distinct alternative explanation is prospectively specified first.

### Exact next action
Integrate the validated distributed cancellation-conditioning failure mode into the Article III methods/limitations/funnel ledger and perform a repository-readiness blocker audit. Separate remaining documentary/reproducibility gaps from any still-missing scientific gate. Any new numerical discriminant must be prospectively frozen before execution and must not alter `h`, tolerance, grid family, interpolation, masks or estimator to rescue the current gate.

## Automation / ownership
`DSIR Continuous Research` remains enabled hourly and was rechecked during V125. It should resume from this ledger/recovery state. No duplicate heavy science lane is required for the now-closed top-64 support branch.

Stale self-hosted run `34550495778 / 103112190909` remains superseded and must not receive home-runner ownership.

## Recovery authority
Newest immutable note: `docs/recovery/RECOVERY_2026-09-11_ARTICLE3_TOP64_CONFOUNDER_ROBUSTNESS_CLOSED_V125.md`, creation commit `2f518aaf0b7e9dc96925bec962dbc3e528f43b4c`.

## Readiness
**ARTICLE3_REPOSITORY_READINESS: 68%.** Funnel-freeze readiness: **67%**.

The percentages are intentionally unchanged because the new work is support-only `+0/+0` and the frozen scientific frontier did not move.
