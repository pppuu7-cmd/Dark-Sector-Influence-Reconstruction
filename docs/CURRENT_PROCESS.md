# DSIR current-process ledger

Updated: 2026-09-11. Scope: **DSIR only**; RTK/RQIR/KMQGB/KMDSB excluded.

## Scientific frontier — unchanged
Canonical 8193->16385 remains independently verified `COMMON_GRID_NEXT_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`, max relative component difference `0.012484060640679777` versus frozen strict `<1e-3`. Frozen `REL_TOL=1e-3`, `h=1e-4`, native kpd20, centered-cubic interpolation, physical domain/masks/107-row accounting, invalid fraction `<=0.05`, retained `>=15`, unsupported=0 and lookup `<=1e-12` remain unchanged.

No 16385->32769 scientific execution is authorized or has occurred. Covariance restriction remains unauthorized. Wm_S3 remains unopened.

## Closed 32769 prerequisites and dormant wiring
Durably closed and independently checked:
- response-blind canonical 32769 bytes + static consumer;
- CLASS point capacity 32769 and parser/build/static scientific equivalence;
- terminal result schema + independent terminal consumer;
- isolated high-memory route under dedicated `dsir-32769-highmem` label;
- one-live guard implementation + independent synthetic audit;
- independent measured-resource consumer implementation + event chain;
- measured-resource materializer + independent synthetic fail-closed audit;
- first-run one-run authorizer + independent synthetic fail-closed audit;
- dormant 16385->32769 scientific engine + independent static core-equivalence audit;
- dormant dispatch-only canonical production workflow + independent static orchestration audit;
- API-backed real-resource authority candidate provenance packager + independent synthetic audit;
- read-only independent-consumer -> authority-candidate event chain + independent static audit;
- exact-byte real-resource authority promotion reviewer + independent synthetic fail-closed audit;
- manual-dispatch/read-only real promotion-review workflow + independent static audit.

`TERMINAL_SCHEMA_AND_INDEPENDENT_CONSUMER_FROZEN=TRUE`.

## Resource/lifecycle gate — physical execution is now the dominant blocker
The exhausted hosted 16385 topology remains frozen at `MemTotal=16372440 kB`, `SwapTotal=3145724 kB`, terminal `MemAvailable=0`, `SwapFree=0`. No 32769 memory requirement is extrapolated.

The isolated dispatch-only resource workflow remains `.github/workflows/layerb-32769-isolated-highmem-resource-lifecycle-pilot-v0-2.yml`, Git blob `8ec0027d6fb45f3f12aa5f410de62093a7144924`, with `runs-on: [dsir-32769-highmem]` only and physical `MemTotal > 16372440 kB` required.

`HIGH_MEMORY_RESOURCE_LIFECYCLE_PREFLIGHT_PASS=FALSE` until an actual qualifying isolated runner executes the measured pilot successfully.

The prospective real-resource chain is frozen as:
`isolated resource pilot -> hosted independent consumer -> API-backed provenance packager -> hardened materializer -> read-only candidate artifact -> exact-byte promotion review -> deliberate durable authority copy`.

### V132 candidate packaging retained
Packaging contract Git blob `ade2fd2d43ce7e556bf473f93ae8491a694e868f`; packager Git blob `c92ffc97c04c27ff6cdbf05b7f84527884848f40`.

Synthetic audit run `34637254849` passed jobs `103388059249` + `103388098677`; artifact `10278453264`, ZIP SHA256 `7c21596f22fd19987aedd0def81cdcf6f8e64fae4359e35569584a4022bde9c3`.

Real read-only packaging workflow Git blob `93dc50385c4b126b91d384e79f6b7079bae283a6`. Static audit run `34637435128` passed jobs `103388656014` + `103388709957`; artifact `10278693444`, ZIP SHA256 `b14abf859ea61bc35dbe04d6fef6b83ab312b3af41e0dbff50b2e3e366c54bbb`.

## V133 exact-byte promotion review — prospectively closed
Promotion-review contract: `docs/dsir4/contracts/LAYERB_32769_RESOURCE_AUTHORITY_PROMOTION_REVIEW_CONTRACT_V0_1.json`, Git blob `7abd3b1d2a04161ef1012726668db0a8d4e02af7`.

Reviewer: `ci/layerb_32769_resource_authority_promotion_reviewer_v0_1.py`, Git blob `5c6ea5d2603d03625e13ad1f0534ecea8a2ba199`.

The reviewer requires a completed successful exact candidate-packaging run, exactly one matching non-expired candidate artifact, downloaded-ZIP/API-digest agreement when available, exactly one provenance/candidate/receipt file, real non-synthetic candidate/provenance/receipt, exact receipt SHA bindings, frozen materializer/packager identities, high-memory resource PASS shape, response-blind/downstream-false bits and exact provenance IDs/hashes.

Synthetic audit run `34639405093` passed source job `103395140406` and independent verifier `103395196951`. Artifact `10279332147`, ZIP SHA256 `b04d00c3a860419c5ff3a683f602edafe2e22719a4953a1e61b23c303111dc54`, summary SHA256 `af062e4eff3c0a157cf5aeb6be61e10f8175d2de646f9a7f33d1fe0e7adb629c`, PASS receipt SHA256 `a3b4e80aa4fb6f6f6d0557c3ccbb75f0a3fcce0a5194cee82439c27d719cbb2e`.

It accepted the exact unmarked shape and rejected synthetic marking, receipt candidate-SHA mismatch, duplicate artifact, ZIP digest mismatch and a downstream scientific-authorization bit.

Durable synthetic authority: `docs/dsir4/authority/LAYERB_32769_RESOURCE_AUTHORITY_PROMOTION_REVIEWER_SYNTHETIC_AUDIT_V0_1.json`, creation commit `6af7d91a347e91954dbc161251716c5b85b153b1`.

Real promotion-review workflow: `.github/workflows/layerb-32769-resource-authority-promotion-review-v0-1.yml`, creation commit `4a6d1bca9c6df21c517e28bc7c7a5cbb62c12c0f`, Git blob `3b1de80ae99aae6925411737f821c04f3ff6cf95`.

It is `workflow_dispatch`-only, takes a `candidate_run_id`, has only `contents: read` + `actions: read`, requires that the durable target authority does not already exist, downloads and manifests the exact candidate artifact, runs the frozen reviewer and uploads a review artifact only. It contains no repository mutation, durable-authority creation, scientific solver invocation or scientific dispatch path.

Static audit run `34639622299` passed source job `103395846351` and independent verifier `103395881837`. Artifact `10280210112`, ZIP SHA256 `8ad67432ae09453be4098cffc49a9e2f27d84706355573840ecc8f37485d9741`, result SHA256 `f0220d5ee425ddc17689c3661c1b485ac5feb6e62bc55df8c7a165f1b01ccb12`.

Durable static authority: `docs/dsir4/authority/LAYERB_32769_RESOURCE_AUTHORITY_PROMOTION_WORKFLOW_STATIC_AUDIT_V0_1.json`, creation commit `a6a9c7c3486c33a23b44285c1cc61e9c3bd15d45`.

A future promotion-review PASS authorizes only an **exact-byte deliberate copy** of the reviewed `resource_authority_candidate.json` to `docs/dsir4/authority/LAYERB_32769_HIGH_MEMORY_RESOURCE_LIFECYCLE_V0_1.json`. The review itself never creates that file and never authorizes science.

## One-live + one-run authorization — implementation ready, real PASS open
`ONE_LIVE_ANTI_DUPLICATION_GUARD_PASS=FALSE` until the guard runs against real live first-production state. The one-run authorizer remains prospectively frozen and independently synthetic-audited, but `actual_one_run_authorization_exists=false` and `scientific_execution_authorized=false`.

## Dormant scientific engine and production workflow
Dormant engine `ci/layerb_16385_to_32769_canonical_one_live_refinement_v0_1.py`, Git blob `5ae2068d65dd45e0813c75fb795e55aa1bda1d5e`, remains independently static-equivalent to the prior validated scientific core after normalizing only coarse `8193->16385` and fine `16385->32769` support counts.

Dormant production workflow `.github/workflows/layerb-16385-to-32769-canonical-scientific-v0-1.yml`, Git blob `765abe3f6ec0c47392328678fffd5417f2ae4b43`, remains workflow-dispatch-only, isolated-label-only, measured-resource-gated, live-guard/current-run-authorized and separately terminal-consumed. No production run has been launched.

## Remaining true blockers
1. Real isolated runner `dsir-32769-highmem`, no default labels, physical `MemTotal > 16372440 kB`.
2. Exactly one measured response-blind V0.2 32769 lifecycle pilot PASS.
3. Automatic independent-consumer/candidate chain must produce a real candidate.
4. Manual V133 exact-byte promotion review must PASS, followed by deliberate exact-byte durable resource-authority copy.
5. Actual immediate one-live guard PASS and current-run-only one-run authorization.
6. Only then exactly one canonical scientific 16385->32769 run and independent terminal classification.

The available GitHub connector exposes no physical self-hosted runner administration/enumeration, so no runner fact is invented.

## Exact next action
Attach/use a qualifying isolated `dsir-32769-highmem` runner and dispatch exactly one V0.2 response-blind lifecycle pilot. Do not dispatch scientific production before the resulting real candidate passes V133 review and the exact reviewed bytes are deliberately stored at the durable resource-authority path.

## Automation / ownership
`DSIR Continuous Research` remains enabled hourly. Repository state wins over chat state. No duplicate heavy science lane is authorized.

## Recovery authority
Newest immutable note: `docs/recovery/RECOVERY_2026-09-11_ARTICLE3_32769_RESOURCE_AUTHORITY_PROMOTION_REVIEW_V133.md`, creation commit `de0796d825bcd159a99bf6ca701a9fa9bd67e3f1`.

## Readiness
Frozen publication rubric remains **ARTICLE3_REPOSITORY_READINESS: 68%** and funnel-freeze readiness **67%**.

Operational roadmap tracking: **WORKING_PLAN_COMPLETION: 82%**. V133 closes exact-byte resource-authority promotion review prospectively; real measured resource execution and scientific convergence remain open.
