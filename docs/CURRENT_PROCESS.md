# DSIR current-process ledger

Updated: 2026-09-17. Scope: **DSIR only**. Repository/Actions state, terminal artifacts and frozen authorities are authoritative; chat is not authority.

## Scientific boundary

V0.25 remains terminal numerical classification `FORCED_BASELINE_EXACT_TARGET_UNION_REMEDY_SUPPORTED`, effect `+0/+0`. V0.26 R1 remains frozen under preregistration blob `545e5be589e0f8029d23db2edb4e2faad116c3a0` and contract blob `b510d8e97baf1c0b7b216c0605d83cdd029254e9`.

Frozen boundary is unchanged: 107 retained rows = DES 53 + BOSS 54; alpha `3e-10`; beta exact-target `1e-12`; scientific strict `<1e-3`; technical strict `<1e-5`; requested-node binding `<=1e-12`; full replay 738 CLASS constructions. Full 107 rows, covariance, whitening, nuisance marginalization, relation-null, `Wm_S3`, global 65537, statistical/model inference and physical dark-sector inference remain closed.

Original run `35033268924` and successor v0.1 run `35174721773` remain permanently consumed and non-rerunnable.

## GRID896 response-blind prerequisite

Diagnostic run `35177449482`, run #1 / attempt #1, completed success with classification `GRID896_NUMPY_DISPATCH_PATH_DEPENDENCE_CONFIRMED`. Artifact `10479491325`, ZIP SHA256 `ae4fa512f4d0559c5266d10fdf803d1d2c863049b1796824fde95200bc90136d`, inner result SHA256 `a6f309198ffd75e807264fddfd96b4115fdf81385e0bafd01e1b328994c8a2a5`. Independent Critic `PASS_SCOPED` supports only the prospective primitive: materialize the exact frozen GRID896 bytes under a native producer dispatch, then consume those exact bytes inside the unchanged forced science runtime. It did not establish cross-host native byte invariance.

## Successor sentinel v0.2 frozen package

Launch authority blob `f5932ce87acc392220ca2fa71abbade8c5899723`; design Critic blob `f89dc41d46cbe5727bc16f216e21432cdc4a4a57` (`PASS_SCOPED`); executor blob `1f727056491a6f4997298bb63dba26589fd3ddec`; target-workflow blob `00bb68196c995079cad50da70be555d737d28ecb`; terminal-history workflow blob `9dc4962288471fbcd0803d22cabe1182bfac8ed3`; decision blob `97f675e48e5b63a3d6dc7a10b0c2f2e6833cf2d9`.

Experiment id `LAYERB_BETA_V0_26_R1_SUCCESSOR_SENTINEL_SCIENCE_V0_2`; nonce `DSIR-V026R1-SUCCESSOR-20260917-B1-4C92E7A1`. Same-nonce second attempt and historical reruns are forbidden. Exact dataset, response-blind plan, CLASS commit/runtime and scientific thresholds are unchanged.

PR #202 merge `1fd68a9814ac987300710c4e67d080c7455efa34` added the one-shot launch marker blob `69d827768bc852e8d5f62d2d27e137826dd82e25`.

## Terminal v0.2 execution and provenance closure

Target run `35181812498`, workflow `360201395`, exact head `1fd68a9814ac987300710c4e67d080c7455efa34`, event `push`, run #1 / attempt #1 is terminal `failure`. Independent exact-head enumeration returns exactly one run; no rerun/selection contamination exists.

Authorization job `105075391316` and plan job `105075530174` completed success. Decision job `105080542988` completed success after the matrix settled. The decision finalizer found only 10 lane artifacts while the frozen decision code requires exactly 32 distinct replicate documents `R01`...`R32`; therefore it classified the attempt `SENTINEL_INVALID` before numerical/scientific evaluation.

Decision artifact `10480279579`, `layerb-beta-v026-r1-successor-decision-v0-2`, ZIP SHA256 `8718c9ece145907aaa4f4d1b812a7245fd226aa22c6415042a3fa293bda068b6`. Independent materialization verifies inner `sentinel_decision.json` SHA256 `4a6f064348f8cf042bc56483101af0674930a890949bea6bf171cdb772bc0fbc`, matching its embedded manifest.

Terminal-history run `35183519508`, workflow `360201396`, run #1 / attempt #1 completed success. Terminal-history artifact `10480539101`, `layerb-beta-v026-r1-successor-terminal-runtime-v0-2`, ZIP SHA256 `0d61e9f90f03d875e8a67d99a7882530f44e5d6bd875d900c33391033c5fb329`. Independent materialization verifies copied decision SHA256 `4a6f064348f8cf042bc56483101af0674930a890949bea6bf171cdb772bc0fbc` and `terminal_receipt.json` SHA256 `e8c9cee498e66ef53224041eee98860cab89ea031351ecdcf91bfd8b47e0ff49`, matching the embedded terminal manifest. The receipt runtime-binds exact target run/head/attempt/nonce and closes terminal history.

Historical terminal classifier is preserved: `SENTINEL_INVALID`; provenance validity `INVALID`; numerical validity `NOT_EVALUATED`; exact-target validity `NOT_EVALUATED`; scientific criterion `NOT_EVALUATED`; model/statistical interpretation `NOT_EVALUATED`; full107 false. This is not a scientific FAIL and not dark-sector evidence.

## Independent implementation qualification — terminal

Funnel Auditor terminal qualification `docs/dsir4/authority/LAYERB_BETA_V0_26_R1_SUCCESSOR_SENTINEL_V0_2_TERMINAL_IMPLEMENTATION_QUALIFICATION_V0_1.json` has verdict `INVALID_IMPLEMENTATION` and preserves the historical `SENTINEL_INVALID` classifier.

Concrete counterexample: failed lane `R01`, job `105075573436`, passed response-free native/forced fingerprint stages and then failed before CLASS inside inherited v0.1 `reconstruct()` with `RuntimeError: GRID896 binary identity mismatch` during v0.2 per-lane native materialization. The exact v0.2 executor recomputes GRID896 independently on each lane host under native NumPy dispatch and requires the locally produced bytes to equal frozen SHA256 `8499eb0f240b85581e0dcb804b841ba911c4b3a58e8fe15702aea134e40bd65d` before the frozen-byte consumer path can run.

Completed materialization controls `R09` and `R15` passed the same exact byte guard on other hosted lanes. No partial response values are used. Thus the nominal producer identity `ubuntu-24.04 + NumPy 1.26.4 + native dispatch` is not sufficient to determine one exact GRID896 payload across the authorized hosted-runner population. The v0.2 implementation overgeneralized the earlier single-host dispatch diagnostic into a cross-host per-lane producer assumption.

This invalidity is infrastructure/provenance implementation scoped. Failed lanes stop before CLASS; numerical reproducibility, exact-target validity and scientific criterion remain `NOT_EVALUATED`.

## Current funnel position

`V0.25 TERMINAL -> V0.26 R1 FROZEN -> ORIGINAL SENTINEL CONSUMED -> V0.5 GOVERNANCE CLOSED -> SUCCESSOR V0.1 CONSUMED -> GRID896 DIAGNOSTIC TERMINAL -> CRITIC PASS_SCOPED -> SUCCESSOR V0.2 FROZEN/EXECUTED -> TARGET 35181812498 TERMINAL FAILURE -> DECISION SENTINEL_INVALID -> TERMINAL-HISTORY 35183519508 SUCCESS/CLOSED -> INNER ARTIFACT PROVENANCE VERIFIED -> V0.2 INVALID_IMPLEMENTATION (PER-LANE NATIVE GRID PRODUCER NOT HOST-INVARIANT) -> RESPONSE-BLIND DIAGNOSIS/DESIGN ONLY -> FULL 107 ROW CLOSED`.

Effect remains `+0/+0`. No administrative readiness/frontier increase is authorized.

## Exact next admissible action

1. Never rerun target `35181812498`, any failed job, historical sentinel runs, or a same-nonce attempt.
2. Preserve historical `SENTINEL_INVALID`; do not reinterpret it as scientific FAIL/PASS.
3. Author only a new prospective response-blind cross-host GRID896 producer-identity diagnostic/design contract. It must test or eliminate dependence on arbitrary per-lane host-native NumPy dispatch without reading CLASS scientific responses.
4. Independently review that frozen diagnostic/design contract before any diagnostic execution. This ledger does not authorize a diagnostic run and does not authorize successor sentinel science.
5. A single immutable independently hashed GRID896 byte artifact distributed byte-for-byte to lanes is one possible future design, but it is not selected or authorized here; any design must be prospective and separately audited.
6. Full 107 rows, covariance, whitening, nuisance marginalization, relation-null, `Wm_S3`, global 65537 and all downstream statistical/model/physical science remain closed.
