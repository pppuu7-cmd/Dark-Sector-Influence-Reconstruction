# DSIR RECOVERY LATEST — authoritative live pointer

**Updated:** 2026-09-03  
**Scope:** DSIR only; RTK/RQIR excluded.  
**Article-3 readiness:** **Verified 52.0% | Draft/data 54.6%** (exact Draft/data `54.57142857142857%`).

Repository state, immutable recovery notes, validated GitHub Actions logs/artifacts and durable checkpoint branches outrank chat wording. Resource/checkpoint/infrastructure/static work gives `+0/+0` unless a frozen scientific ledger explicitly says otherwise. Frozen science boundaries are unchanged.

## Immediate frontier — Exp073CR v0.3 r1 repaired ll3-sharded Wm_S3 resource gate

Wm_S3 angular scientific authority remains absent. Scientific Wm_S3 A/B production remains forbidden until a prospectively frozen resource gate passes. No competing self-hosted task is permitted.

### Exp073CQ v0.2 frozen terminal result

Run/job/head `33752799918` / `100640079011` / `011852feb6d40152f4b33bde732b00520cd28f79`; terminal checkpoint `checkpoints/exp073cq-wm-s3-missing29-38-resource-v0-2` / `32bf0d1bdbcc2480f8b77f936ea6dc1f425812b0`. Final receipt: complete `<f8 [39,12288]`, exact first-8 array/SHA equality PASS, SHA `36ee9fca9fb276a30d8ebb97cb04fddc7e95cff18fb29248c033bb364ea2d8cf`, swap increase `0 KiB` PASS, measured CPU fraction `0.6638297425690942 < 0.90`, status `FAIL_EXP073CQ_V0_2_WM_S3_CPU_TARGET`.

Classification: **RESOURCE/PERFORMANCE FAIL `+0/+0`; not Wm_S3 scientific arithmetic FAIL.** Artifact `9897551836`, digest `sha256:0f10c863ee65f3d7c27177a324cafe2830e6b8b1096da054e35c638b26d6104c`.

### Exp073CR v0.3 frozen architecture and hosted seed authority

Exp073CR v0.3 is a new prospectively frozen resource architecture, not a retrofit/rescue of Exp073CQ. Exact complete-band arithmetic/reference authority is preserved while bands `29..38` are represented by 64 prospectively frozen source-order ll3 shards solely for utilization. Frozen rules: exactly 8 outer workers, max 8 in flight, nested threads=1, durability order `store_shard -> durable_sync -> refill_submit`, exact reconstruction/reference equality, CPU fraction `>=0.90`, swap increase `0 KiB`, no tolerance rescue.

Bound authority: prereg `fb10a589ee5ac03f478160c9cfd28484169e48ca`; driver `365fd7a8527b2dafe4785f95fa104276788c11d1`; original self-hosted workflow `85993d73565c3fc4d1389cc942bc69073b89d89e`; hosted seed-audit workflow `5f5a7a060b17e11b0f53453d6ca6898cda00d2fd`; binding `0e0d13a6f7736eb56689d57c3557410007ec48d2`; static audit `312a526997dde0a43a58fa64b5ce6f051d2df9d8`; candidate SHA `d48e46197b48a6fcdf7d3eb3b0817973a2eadb25bbb617e7b8060c8c17209462`; queue SHA `3ba315d9bc24883ef746d92e785e0a040f9b13e751f59dda9a93e825a6390db4`; namespace `checkpoints/exp073cr-wm-s3-ll3-sharded-resource-v0-3`.

Hosted seed/static/bitwise audit run/job `33768977707` / `100694004982` SUCCESS with raw tokens `PASS_EXP073CR_V0_3_SOURCE_ORDER_STATIC_AUDIT`, `PASS_EXP073CR_HOSTED_AUTHORITATIVE_LL3_BITWISE_REGRESSION_V0_1`, `PASS_EXP073CR_V0_3_HOSTED_SEED_STATIC_BITWISE_AUDIT`. Durable seed head `cb408d4edb2a73413db8d3181e9cb1680dc19276`, fingerprint `3eb54728878e5913fcb39b9e6411480c413d6a5a6a968c67e623f1fa48e6ec29`. Artifact `9898817387`, digest `sha256:6d0435cfb99e4c05c49a5e61f8944d85a786820e3bf2f0913135b263bb0c734d`.

### Exp073CR v0.3 first home attempt — infrastructure failure

Run `33770178685`, authorize job `100698111100` SUCCESS, home job `100698177477` FAILURE before seed restore or compute. Decoded raw log establishes first cause in `Bind v0.3 runtime`: `test "$(nproc)" = "8"` exited 1 before Python/static-audit output. No shard was computed; seed remained immutable. Secondary `CR_ROOT` unbound failure occurred only in always-run artifact handling.

Classification: **INFRASTRUCTURE/CONTROL-PLANE FAILURE `+0/+0`**, not resource/performance FAIL and not science FAIL.

Prospective repair changes only the machine availability guard from `nproc==8` to `nproc>=8`; it does **not** change the actual compute architecture: exactly 8 outer workers and nested threads=1 remain frozen, as do 64 shard geometry/order, exact arithmetic, CPU `>=0.90`, and swap criterion. Repaired workflow commit `9eafc1c431f508d7a34800328b6718f146b346b5`. Immutable note: `recovery/2026-09-03_exp073cr_v0_3_home_bind_failure_r1_repair.md`.

Dedicated hosted repair audit run/job `33770476672` / `100699131834` completed SUCCESS. Raw log token: **`PASS_EXP073CR_V0_3_R1_NPROC_CONTROL_AUDIT`**. The audit re-ran the base source-order static contract, verified exactly 8 ProcessPool workers and CPU_MIN=.90, verified the corrected `nproc>=8` machine guard and absence of `nproc==8`, verified the bound driver/namespace, and verified the exact seed branch still points to `cb408d4e...`.

### LIVE repaired continuation

Immediately before repaired activation, live Actions audit showed `0 in_progress` and `0 queued`. Activation was prospectively rebound at commit `8eded6a41271e77750a0206ba2766fbbb7819dc3` with the hosted repair PASS token.

Current run: **`33770577708`**, head `8eded6a41271e77750a0206ba2766fbbb7819dc3`.
- hosted authorize job **`100699474546`** was **IN_PROGRESS** at the last live reconciliation;
- the self-hosted job had not yet been instantiated at that instant;
- once instantiated, that single job owns DSIR-HOME-PC exclusively while queued/in_progress;
- no competing home run may be launched.

Exact next action: consume run `33770577708` as it advances. If home compute becomes queued/in_progress, do not duplicate. At terminal state inspect exact seed/fingerprint, all durable shard receipts, durability-before-refill, complete-band reconstruction/reference equality, swap and frozen CPU metric. Resource PASS remains `+0/+0` and only then may authorize preregistration of the scientifically permitted fresh-independent-PCL Wm_S3 A/B successor. Resource FAIL is preserved negative resource evidence; infrastructure/software failure requires first-causal diagnosis and checkpoint-safe prospective repair.

## Preserved authority and boundaries

- Wm_S1 Track-A exact PASS preserved.
- admitted Wm_S2 authority preserved.
- Wm_S3 scientific angular authority absent.
- Exp073CM/Exp073CN/Exp073CQ resource outcomes remain historical resource results, not rewritten as science failures.
- frozen boundaries: `0.295<=z<=2.33`; `0<k<=0.06664762008318016 Mpc^-1`; Layer-A invalid `<=0.05`; Layer-B invalid-row fraction `<=0.05`; retained dimension `>=15`; DES NSIDE=4096; ell `0..12287`; 39 bands; Wm `TE<-TE`; WW `EE<-EE`; canonical `<f8 [39,12288]`; no effective ell/z/k or fiducial-P shortcut; exact-threshold ambiguity `numerically_unresolved`; no tolerance/rounding/smoothing/averaging rescue.

Use `docs/CURRENT_PROCESS.md` for the latest process ledger. Newer validated repository/checkpoint/Actions state supersedes this pointer if another DSIR authority advances before the next reconciliation.
