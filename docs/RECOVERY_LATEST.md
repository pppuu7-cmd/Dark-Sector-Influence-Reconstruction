# DSIR authoritative recovery — latest

Updated: 2026-09-11. Scope: **DSIR only**. Never mix RTK, RQIR, KMQGB or KMDSB.

Newest immutable current-front recovery note: `docs/recovery/RECOVERY_2026-09-11_ARTICLE3_TOP1_ARITHMETIC_CLOSED_TOP64_RETRY_ACTIVE_V123.md`, creation commit `c4ae0ca37aafa643f05d69ff1f9ba9717c237edc`. V122 and all earlier recovery notes remain immutable history; V123 supersedes them for current-front recovery.

## Scientific frontier
Canonical 8193->16385 remains independently verified `COMMON_GRID_NEXT_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`, max relative component difference `0.012484060640679777` versus strict `<1e-3`. Frozen `REL_TOL=1e-3`, `h=1e-4`, native kpd20, centered-cubic interpolation, physical domain/masks/107-row accounting, invalid fraction `<=0.05`, retained `>=15`, unsupported=0 and lookup `<=1e-12` remain unchanged. No 32769 execution is authorized; covariance restriction remains unauthorized; Wm_S3 remains unopened.

## Closed exact-hotspot/top-1 authority
Exact-hotspot run/job/artifact `34592951737 / 103242180883 / 10266223990` and prospectively frozen top-1 decomposition `34608169257 / 10268078468` with terminal consumer `34611473046 / 103302765790 / 10268761538` remain independently verified support-only `+0/+0`. The full plateau is localized to the frozen DES beta-response atom call 204 / target 44 / k `0.01698756109424023` / z `0.7475`, where tiny deterministic raw-role changes are amplified by strong beta finite-difference cancellation. Durable top-1 authority completion commit: `c7e4968fb13880fb2d7c7615d10a0821073b2243`.

A newer arithmetic-only terminal authority is also closed support-only `+0/+0`: source run `34624525157`, terminal consumer run/job/artifact `34624598782 / 103346512972 / 10273886027`, durable authority completion commit `4392df8fe76680ee6e4bfed7e073263e4729e39f`. Independent artifact consumption verified ZIP SHA256 `72e2fb96df45ddb05d49bcd215618a366a08df62c5b386ca88c9ab6118593c26` and terminal payload SHA256 `0273ad038f5b5e13ab1f1598f7d84a829983ea474e379808342a5c2fb63449ef`. Beta subtraction condition numbers exceed `2e8` on both grids, while exact Decimal replay of subtraction from the stored binary64 operands has zero replay rounding-relative error and the signed role shifts exactly recombine to the beta-numerator shift. This supports cancellation/ill-conditioning amplification rather than ordinary floating-point subtraction rounding as the local top-1 mechanism; no science gate changes.

## Top-64 census infrastructure failure and resume
Top-64 census attempt 1, run/job `34614119149 / 103311649147`, is terminal FAILURE at atom-0 primary reproduction `2.8125401822500933e-06` versus frozen `<=1e-12`. It remains classified **infrastructure/numerical-reproducibility FAIL +0/+0**, not scientific FAIL. No science setting or threshold changed.

The single exact unchanged retry is run `34614119149`, attempt 2, job `103331912096`, launch head `6b684ac91c7a261e6b588da1a4922a411bd65614`. At the V123 update it is IN_PROGRESS on GitHub-hosted ubuntu-24.04: frozen identity/source/hash checks, numerical-stack install and pinned CLASS-IV build are complete; the frozen top-64 numerical census is active. No duplicate top-64 execution is allowed.

If attempt 2 passes, independently consume/hash all artifacts and enforce the original exact 64/lifecycle/lookup/per-atom `<=1e-12` contract. If it repeats the reproduction failure, do not relax the contract or repeatedly retry; classify the census BLOCKED by hosted numerical reproducibility and freeze a separate support-only cross-VM hotspot reproducibility diagnostic.

Stale superseded self-hosted run `34550495778 / 103112190909` remains queued and must not receive home-runner ownership.

## Readiness
**ARTICLE3_REPOSITORY_READINESS: 68%.** Funnel-freeze readiness: **67%**.

## Exact next action
Track the single unchanged retry of top-64 census run `34614119149`. If terminal PASS, independently consume and hash all artifacts and enforce source/provenance identity, exact 64 ordering, lifecycle `8/max1/final0`, unsupported=0, lookup `<=1e-12`, every primary reproduction error `<=1e-12`, and downstream-closed flags. If retry fails reproduction again, stop retries and move to a prospectively frozen cross-VM hotspot reproducibility diagnostic. Never execute 32769 or alter frozen science to rescue this support gate.
