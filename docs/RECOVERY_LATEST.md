# DSIR authoritative recovery — latest

Updated: 2026-09-10. Scope: **DSIR only**. Never mix RTK or RQIR.

Newest immutable authority note: `docs/recovery/RECOVERY_2026-09-10_ARTICLE3_IR_UNRESOLVED_IV_DIAGNOSTIC_RUNNING_V58.md`, creation commit `3cace1e607d8d1a89c866d53aec33e712b498a94`. Earlier notes remain immutable history.

## Preserved authority
All earlier admitted DSIR scientific authority remains unchanged. Article-3 Exp073IQ Layer-A remains PASS: run/job `34423479633 / 102703685034`, artifact `10131794281`, digest `sha256:ac959926639d3b46ecfe114e396ce03cb20a0763ef2781db454408d8ad0759df`, retained set 107.

Original Exp073IR `34428699659 / 102719344981` remains `INVALID_FOR_SCIENCE_ARTICLE3_SUPPORT`, not scientific FAIL. Exp073IS, IT and IU remain support-only `+0/+0` authorities establishing the exact gauge-invariant public `d_m` route; no further IU run is permitted.

## Newly closed — repaired Exp073IR
Repaired Exp073IR run/job `34432102035 / 102729564736`, head `38604061abe6cbcccbf9614b22e3ca0ca6a9ec0f`, completed workflow SUCCESS, but the raw scientific accounting status is exactly `NUMERICALLY_UNRESOLVED_EXP073IR`, not PASS/FAIL. Artifact `10134905199`, ZIP SHA256 `165a85c8348fc4fc4c52f3926aa1a46989179efc8413a059f9fc2d6aa9dfbb21`.

Raw result: 0 invalid rows, `f_B=0`, retained 107; finite/nonzero status unchanged; row labels unchanged; BOSS dense-z disagreement false. The prospectively frozen production-vs-dense convergence control fails because max relative component difference is `0.9998247463807295 > 1e-3`. Covariance restriction remains unauthorized. The public `d_m` source route and downstream covariance/whitening/nuisance/relation-null firewall are intact.

## Current process — Exp073IV support-only mechanism diagnostic
Exp073IV preregistration commit `1e3398631973bc410a4c36a1bf06dd558a925863`; diagnostic implementation `c41ecdcca7fea456975868aa16bee38ecdda4ce4`; hosted workflow `628daeb44c84a8844f50540ec852222a95a2834d`; trigger/head `f20b31bef9178ab3097ed606d8fcc983952222c8`.

Authoritative active run/job: `34435415273 / 102739350045`, GitHub-hosted `ubuntu-24.04`, checkpoint N/A, home/self-hosted ownership none. At latest inspection it is QUEUED.

Exp073IV re-executes the exact frozen Exp073IR arithmetic and only instruments the already-required production-vs-dense comparison to record per-component maxima with exact z, k, production value and dense value. It is `SUPPORT_PLUS_0_PLUS_0` only: it cannot create Layer-B scientific authority, authorize covariance, or modify `REL_TOL=1e-3`, `h=1e-4`, domains, interpolation or atomization.

Exact next action: terminal-consume Exp073IV; verify raw log and artifact/digest. A valid diagnostic PASS must reproduce `NUMERICALLY_UNRESOLVED_EXP073IR`, reproduce the exact parent max `0.9998247463807295`, identify both component maxima, and preserve the firewall. Only then may a separate prospective numerical-resolution experiment be defined from the diagnosed mechanism.

Global frozen boundaries remain unchanged: `0.295<=z<=2.33`; `0<k<=0.06664762008318016 Mpc^-1`; Layer-A `operator_f_invalid<=0.05`; Layer-B invalid-row fraction `<=0.05`; retained dimension `>=15`; DES NSIDE=4096; ell `0..12287`; 39 bands; Wm `TE<-TE`; WW `EE<-EE`; canonical `<f8 [39,12288]`; exact-threshold ambiguity `numerically_unresolved`; no tolerance/rounding/smoothing/averaging/effective-coordinate/fiducial-P rescue.