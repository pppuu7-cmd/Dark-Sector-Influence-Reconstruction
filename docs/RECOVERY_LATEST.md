# DSIR RECOVERY LATEST — authoritative live pointer

**Updated:** 2026-09-05
**Scope:** DSIR only; RTK/RQIR excluded.

Repository state, immutable recovery notes, validated Actions raw logs/artifacts and durable checkpoints outrank chat wording. Historical outcomes remain immutable. Frozen science boundaries remain unchanged.

## Preserved scientific authority — Wm_S3 exact PASS

Wm_S1 Track-A exact PASS and admitted Wm_S2 authority remain preserved. **Wm_S3 scientific angular authority is exact PASS** from Exp073DJ checkpoint-preserving Exp073BU resume:
- run/job `33910213781 / 101144660519`;
- frozen science head `c02c018ede6a1fcf7aef1a848c0118a0669ed67f`;
- contract fingerprint `b38687bf5aa6cf4cfe01b2f38a7091e96d97196ad38bdf2ea771f7b649ac73da`;
- terminal token `PASS_EXP073BU_WM_S3_FRESH_AB_EXACT_REPEATABILITY_8CORE_V0_3`;
- canonical selected-TE A/B SHA256 `d282ebdf98dc04e41a8c85f487e209634a8324ce7677107112b8abfd1660f749`;
- exact whole SHA equality and `numpy.array_equal=true`, no tolerance rescue;
- artifact `9959064322`, independently matching ZIP SHA256 `4c9cbebdf4be2e901943a738ebb7df9c1040a6d9524bdd359feb3d3331a647c9`.

Historical Exp073CM remains RESOURCE/PERFORMANCE FAIL `+0/+0`; original Exp073BU v0.4 runner-loss remains infrastructure `+0/+0`. Neither is rewritten.

## Closed terminal support — Exp073DL PASS `+0/+0`

The previous live pointer to Exp073DL was stale and is superseded. Validated terminal authority:
- run/job `33934918432 / 101220868663`;
- artifact `9959830267`;
- GitHub + independent ZIP SHA256 `cc32969cba9802201ac8cf7eae32b430ec47285f3357a58d470850aff8ceb8ab`;
- A/B `selected_te.bin`: each 3,833,856 bytes, canonical `<f8 [39,12288]`, finite, SHA256 `d282ebdf98dc04e41a8c85f487e209634a8324ce7677107112b8abfd1660f749`, independently `numpy.array_equal=true`.

Classification: evidence/support PASS `+0/+0`; it confirms artifact reproducibility but creates no separate Wm_S3 authority.

## Successor frontier recovered from prospectively frozen repository order

The existing successor angular order is:

`Wm_S1 -> Wm_S2 -> Wm_S3 -> WW_S0_S0 -> WW_S0_S1 -> WW_S0_S2 -> WW_S0_S3 -> WW_S1_S1 -> WW_S1_S2 -> WW_S1_S3 -> WW_S2_S2 -> WW_S2_S3 -> WW_S3_S3`.

Therefore the next angular object after Wm_S3 is exactly **`WW_S0_S0`**. No `Wm_S4` or result-dependent substitute is authorized.

## Closed prerequisite — Exp073DM WW_S0_S0 exact-authority preflight PASS `+0/+0`

Prereg commit `030310dbf78652972f74afd5b8cf85ab435b6a43` froze the source-only WW target before any new WW numerical output: S0×S0 spin-2 auto, full NaMaster `[4,39,4,12288]`, selected `wins[0,:,0,:]=EE<-EE`, canonical `<f8 [39,12288]`, no lens-mask route.

Initial run/job `33937934428 / 101229402478` failed only in the hosted static harness `+0/+0`; decoded logs identified the first cause as a self-referential anti-rescue check matching its own forbidden-string literals. No numerical WW output existed. Smallest prospective harness repair commit `ab72044ff0393eba58a669ae77521bd4c4321a03`; retrigger head `71e8ad4ea601eb756e0e0b2218620b5347416cbe`.

Repaired run/job `33937980591 / 101229540163` is terminal SUCCESS. Artifact `9960805717`; GitHub digest and independent ZIP SHA256 both `fa19dbf8c6735e25ec1a500a3f8540f2f868f30e92aa589d3194d7f9deb5c8e3`. Raw receipt gives exactly `PASS_EXP073DM_WW_S0_S0_EXACT_AUTHORITY_PREFLIGHT_V0_1`, `SUPPORT_READINESS_PASS_PLUS_0_PLUS_0`, `science_gate_scored=false`, `ww_authority_created=false`, `numerical_ww_payload_created=false`. Frozen executor SHA256 is `3cf95b66e592e0948127455f5e1dd0a6d07a5d7163e5e2c36626a3ca6e924730`.

## Current live process — Exp073DN checkpoint-architecture binding `+0/+0`

A direct source audit found that the proven Exp073BU Wm durable production base is not directly reusable unchanged for WW: it is hard-bound to lens+S3 reconstruction, spin-0×spin-2 fields, required `--lens-mask`, `ncls=2`, full `[2,39,2,12288]`, `selected_te_complete`, `selected_te.bin`, `TE<-TE`, and Wm-specific checkpoint namespaces. Reusing it with runtime flags would violate the frozen WW semantics.

Exp073DN prospectively freezes the correct boundary: reuse only fail-closed architecture invariants, but require a new WW-specific exact adapter/driver before home computation.

- prereg commit `08271223c92a2aabbb81238d9455ac1e49acbce3`;
- source-only audit commit `9d42ed49da74df53205930f6a7cfa5b23f6bd64c`;
- workflow/head `24762df625db3516d6da7f07447b9d2599627152`;
- live run/job `33938100671 / 101229887636`;
- expected support token `PASS_EXP073DN_REQUIRE_WW_SPECIFIC_CHECKPOINT_ADAPTER_V0_1`;
- checkpoint namespace `N/A` (hosted source-only audit);
- current state at pointer write: `IN_PROGRESS`;
- **DSIR-HOME-PC is FREE**; no self-hosted/heavy process is queued or authorized by this gate.

On raw-artifact PASS: implement a separate WW-specific checkpointed adapter/driver preserving S0-only spin-2×spin-2 auto, exact stock `[4,39,4,12288]` before selecting `EE<-EE`, dedicated checkpoint namespaces, source-head/contract fingerprint fail-closed binding, complete-stage atomic persistence, exact restore SHA verification and exact A/B SHA + `numpy.array_equal` comparator. Then a separate exact-equivalence/activation audit must PASS before any home WW science run.

On FAIL/BLOCKED: diagnose first source/static cause prospectively; do not launch home compute and do not alter WW arithmetic.

Detailed immutable recovery: `recovery/2026-09-05_exp073dl_closed_exp073dm_pass_exp073dn_live.md`. Current process ledger: `docs/CURRENT_PROCESS.md`.

## Frozen execution/science boundaries

`0.295<=z<=2.33`; `0<k<=0.06664762008318016 Mpc^-1`; Layer-A operator_f_invalid `<=0.05`; Layer-B invalid-row fraction `<=0.05`; retained dimension `>=15`; DES NSIDE=4096; ell `0..12287`; 39 bands; Wm `TE<-TE`; WW `EE<-EE`; canonical `<f8 [39,12288]`; no effective ell/z/k or fiducial-P shortcut; exact-threshold ambiguity `numerically_unresolved`; no tolerance/rounding/smoothing/averaging rescue.

For future self-hosted heavy work, preserve prospectively frozen durable checkpoint/resume and the canonical 8-core architecture when applicable: exactly 8 outer workers, nested BLAS/MKL/OpenBLAS/NumExpr threads=1, complete-unit checkpoints, exact canonical reassembly and fail-closed restore. Scientific arithmetic may not change for performance.

## Exact next gate

Consume terminal `33938100671 / 101229887636` without duplication. If and only if the raw Exp073DN artifact validates the frozen PASS, proceed to the WW-specific checkpoint implementation/equivalence chain. Do not launch a self-hosted `WW_S0_S0` scientific run before that chain passes.