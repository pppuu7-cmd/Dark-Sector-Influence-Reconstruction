# DSIR RECOVERY LATEST — authoritative live pointer

**Updated:** 2026-09-05
**Scope:** DSIR only; RTK/RQIR excluded.

Repository state, immutable recovery notes, validated Actions raw logs/artifacts and durable checkpoints outrank chat wording. Historical outcomes remain immutable. Frozen science boundaries remain unchanged.

## Preserved scientific authority
Wm_S1 Track-A exact PASS, admitted Wm_S2 and Wm_S3 exact scientific PASS remain preserved. Historical Exp073CM resource/performance FAIL `+0/+0` and original Exp073BU runner-loss infrastructure `+0/+0` remain historical and unchanged.

## Frozen successor frontier
`Wm_S1 -> Wm_S2 -> Wm_S3 -> WW_S0_S0 -> WW_S0_S1 -> WW_S0_S2 -> WW_S0_S3 -> WW_S1_S1 -> WW_S1_S2 -> WW_S1_S3 -> WW_S2_S2 -> WW_S2_S3 -> WW_S3_S3`.

Current scientific target remains exactly `WW_S0_S0`.

## Closed WW support/readiness chain
- Exp073DP exact-equivalence PASS `+0/+0`: run/job `33938446310 / 101230897808`, artifact `9960969007`, ZIP SHA256 `e34b545b21fc93f8948ad328084afd405885c1045313d7162b553a45583af7a8`.
- Exp073DQ durable A/B driver static PASS `+0/+0`: run/job `33938583879 / 101231302981`, artifact `9961000737`, driver SHA256 `0b7a0a2336a89dcea63060d4049d09fabacc9c5e75fad870d2599efd27d0e63b`.
- Exp073DR activation/resource PASS `+0/+0`: run/job `33938637212 / 101231459805`, artifact `9961019381`.
- Exp073DS v0.1 remains governance-invalid `+0/+0`.
- Exp073DS v0.2 readiness PASS `+0/+0`: run `33938789513` attempt 2, jobs `101233076119 / 101233097355`, artifact `9961211035`, ZIP SHA256 `d12693ce2b2ec17abfef7008e82eca2bf9f9a29b99f43b00c83e30b2313df53d`, token `PASS_EXP073DS_WW_S0_S0_HOME_READINESS_EXCLUSIVITY_V0_1`.

## Exp073DT first attempt — infrastructure interruption only
Prospectively frozen preregistration commit `946964121f12c67e053514109bf974050eeb0cc9`; frozen workflow/head `c450aef42d96eb0bfe0b4c78d5a0fdc850d9a2cd`.

Run `33940588308`: hosted preflight job `101237102962` SUCCESS; self-hosted science job `101237118421` terminal FAILURE because the runner received a shutdown signal at `2026-09-05T03:56:42Z`. The frozen science step was cancelled and terminal evidence upload was skipped. Therefore this attempt is `INFRASTRUCTURE_INCOMPLETE +0/+0`, not a scientific FAIL or PASS, and `WW_S0_S0` authority remains absent.

The same attempt had already passed live exclusivity, PyMaster 2.7 validation and runtime OpenMP team=8 before shutdown. No code/science defect is evidenced by the terminal log. The durable science root is `$HOME/.cache/dsir/exp073dt-ww-s0-s0-ab-v0-1` with frozen DQ namespaces `checkpoints/exp073dq-ww-s0-s0-a-v0-1` and `checkpoints/exp073dq-ww-s0-s0-b-v0-1`.

The frozen DQ driver is fail-closed on restore: complete-stage manifests bind replica, checkpoint namespace, source head, contract fingerprint and payload hashes; finished receipts/payloads are revalidated. Therefore a retry of only the failed self-hosted job is authorized to reuse verified complete stages and recompute only interrupted/incomplete stages. No arithmetic, threshold, tolerance, domain, component blob, source head or contract fingerprint changes are allowed.

Immutable note: `recovery/2026-09-05_exp073dt_runner_shutdown_infrastructure_resume.md`.

## Current process
Checkpoint-preserving retry of failed self-hosted job `101237118421` within run `33940588308` is the authoritative next process. Frozen source authority head `de83e20a68f79ccf25b89b0d33eb4206e294c757`; contract fingerprint `b7845df5ce4bc2bd730461476b7ff0831512003ceb5b3558436005c9876bd251`; expected scientific token `PASS_EXP073DT_WW_S0_S0_EXACT_REPEATABILITY_8CORE_V0_1`.

On retry dispatch, **DSIR-HOME-PC is RESERVED BY Exp073DT resume**. Do not launch any competing heavy process.

Scientific PASS still requires the final DQ provisional comparator to report exact SHA equality, exact `numpy.array_equal=true`, no tolerance rescue, plus independent terminal reread of both A/B selected `<f8 [39,12288]` payloads with valid source/contract/component/checkpoint provenance. Exact A/B inequality is scientific repeatability FAIL. Infrastructure/runtime/checkpoint/provenance/artifact failures remain `+0/+0`.

## Frozen science/execution boundaries
`0.295<=z<=2.33`; `0<k<=0.06664762008318016 Mpc^-1`; Layer-A invalid `<=0.05`; Layer-B invalid-row `<=0.05`; retained dimension `>=15`; DES NSIDE=4096; ell `0..12287`; 39 bands; Wm `TE<-TE`; WW `EE<-EE`; canonical `<f8 [39,12288]`; no effective ell/z/k or fiducial-P shortcut; exact-threshold ambiguity `numerically_unresolved`; no tolerance/rounding/smoothing/averaging rescue.

## Exact next gate
Rerun only failed job `101237118421` of run `33940588308`. Consume its terminal result and raw evidence immediately. If exact PASS, create `WW_S0_S0` authority and advance to the prospectively frozen `WW_S0_S1` frontier; if infrastructure fails again, diagnose the first new cause and preserve verified checkpoints.
