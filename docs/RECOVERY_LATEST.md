# DSIR authoritative recovery — latest

Updated: 2026-09-10. Scope: **DSIR only**. Never mix RTK or RQIR.

Newest immutable authority note: `docs/recovery/RECOVERY_2026-09-10_ARTICLE3_JO_V03_FRESH_TAIL_TARGETED_RECOVERY_V86.md`, creation commit `1134dadd467217a198430722ced6fe46ef74af7a`. Earlier recovery notes remain immutable history.

## Preserved scientific authority
Repaired Exp073IR remains `NUMERICALLY_UNRESOLVED_EXP073IR` with native-grid maximum `0.9998247463807295`. Exp073JI preserves all 107 retained rows with zero unsupported targets. Exp073JJ and Exp073JK remain support-only NOT_CONVERGED at `0.037280144773915974` and `0.016330535730270664`. Covariance restriction remains unauthorized; Wm_S3 remains unopened. Frozen `REL_TOL=1e-3`, `h=1e-4`, support/masks/domain/traversal and anti-rescue rules remain unchanged.

Exp073JP separately has independently verified support-only authority `PREDICTION_ARTIFACT_ADMITTED_PLUS_0_PLUS_0`, commit `de45b113ac34dcec30de29d6d583a1caad89fbb2`, run/job `34513792510 / 102994107061`, artifact `10166838614`, ZIP SHA256 `e803eb34b66103d6e48d301b541d5ea4ecab04bf2ec7eb29cadb82f6730c1749`. It admits the already-frozen C2 local-tangent prediction basis as deterministic prediction-ready only; no scientific model authority or observational evaluation is created. Article III readiness remains 68%; validated overall funnel readiness is now 67%.

## JO process state
JO v0.2 run `34506027292` retains independently verified exact PASS receipts for `history_slot10` (artifact `10164136352`, ZIP SHA256 `0a06f375ba59f4da33172d3d9f742af7b29c82263c600c2e7696fd6676af41cc`) and `cache_roundtrip_slot10` (artifact `10164156389`, ZIP SHA256 `5facd22fbf6edca62b0ff37e7457d411d232525aeead20738de8cb68c9bca735`). Its slot20 control attempts remain infrastructure `+0/+0` history.

Prospectively frozen JO v0.3 run `34512184648`, head `f82bb38b5ed4f9de45be9b8ce2bdae10d9040d52`, split the unchanged slot20 history control into exact `slot20_after_history` and `slot20_fresh_tail` receipts. Both original phase jobs passed contract/build guards but were externally shut down during the frozen numerical step before any result/artifact: jobs `102988758987` (exit 137) and `102988759155` (exit 143). Aggregate `102991182740` was skipped. These are `INVALID_INFRA_PLUS_0_PLUS_0`, not history-control or scientific FAILs.

The minimal recovery is serialized: only the missing unchanged `slot20_fresh_tail` job was re-run first. Current job `103005530198` in the same frozen run/head was QUEUED at dispatch. No competing home/self-hosted process exists. Only after a validated fresh-tail receipt is preserved may the still-missing unchanged `slot20_after_history` be recovered; only after both receipts exist may the exact v0.3 aggregate equality gate execute.

Prepared JL recovery must retain the frozen science-source bundle guard and exact terminal topology: slot10=441 response calls / 83666 target evaluations; slot20=569 / 121682; unsupported=0. Heavy checkpointed JL recovery remains forbidden until an independently verified JO aggregate PASS authority exists. Valid recovered JL CONVERGED activates only JN; valid recovered JL NOT_CONVERGED activates only JM; infrastructure failure activates neither.

## Exact next action
Terminal-consume JO v0.3 targeted fresh-tail recovery job `103005530198`. Valid PASS -> independently verify raw log plus artifact ZIP/JSON/response/capacity hashes, preserve receipt, then recover only still-missing unchanged `slot20_after_history`. Infrastructure interruption -> record `+0/+0` and self-heal without changing frozen science. Exact assertion failure -> classify under frozen JO contract and keep checkpoint replay forbidden. No tolerance rescue.
