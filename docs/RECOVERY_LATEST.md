# DSIR RECOVERY LATEST — authoritative live pointer

**Updated:** 2026-09-05
**Scope:** DSIR only; RTK/RQIR excluded.

Repository state, immutable recovery notes, validated Actions raw logs/artifacts and durable checkpoints outrank chat wording. Historical outcomes remain immutable. Frozen science boundaries remain unchanged.

## Preserved scientific authority
Wm_S1 Track-A exact PASS and admitted Wm_S2 authority remain preserved. Wm_S3 exact scientific PASS remains Exp073DJ checkpoint-preserving Exp073BU run/job `33910213781 / 101144660519`, frozen science head `c02c018ede6a1fcf7aef1a848c0118a0669ed67f`, contract fingerprint `b38687bf5aa6cf4cfe01b2f38a7091e96d97196ad38bdf2ea771f7b649ac73da`, raw token `PASS_EXP073BU_WM_S3_FRESH_AB_EXACT_REPEATABILITY_8CORE_V0_3`, canonical A/B selected-TE SHA256 `d282ebdf98dc04e41a8c85f487e209634a8324ce7677107112b8abfd1660f749`, exact SHA equality and `numpy.array_equal=true`, no tolerance rescue. Historical Exp073CM remains resource/performance FAIL `+0/+0`; original Exp073BU runner-loss remains infrastructure `+0/+0`.

## Frozen successor frontier
`Wm_S1 -> Wm_S2 -> Wm_S3 -> WW_S0_S0 -> WW_S0_S1 -> WW_S0_S2 -> WW_S0_S3 -> WW_S1_S1 -> WW_S1_S2 -> WW_S1_S3 -> WW_S2_S2 -> WW_S2_S3 -> WW_S3_S3`.

Thus the next angular object is exactly `WW_S0_S0`.

## Closed support/evidence chain
- Exp073DL evidence PASS `+0/+0`: run/job `33934918432 / 101220868663`, artifact `9959830267`, GitHub + independent ZIP SHA256 `cc32969cba9802201ac8cf7eae32b430ec47285f3357a58d470850aff8ceb8ab`.
- Exp073DM WW_S0_S0 source-only preflight PASS `+0/+0`: repaired run/job `33937980591 / 101229540163`, artifact `9960805717`, GitHub + independent ZIP SHA256 `fa19dbf8c6735e25ec1a500a3f8540f2f868f30e92aa589d3194d7f9deb5c8e3`, raw token `PASS_EXP073DM_WW_S0_S0_EXACT_AUTHORITY_PREFLIGHT_V0_1`. Frozen target: S0×S0 spin-2 auto, full `[4,39,4,12288]`, selected `EE<-EE`, canonical `<f8 [39,12288]`, no lens mask.
- Exp073DN checkpoint-architecture binding PASS `+0/+0`: run/job `33938100671 / 101229887636`, artifact `9960842558`, GitHub + independent ZIP SHA256 `955cbe2f58b1809fec34815d33b105edac8f02777f99e6d4e36f57b29f64a259`, token `PASS_EXP073DN_REQUIRE_WW_SPECIFIC_CHECKPOINT_ADAPTER_V0_1`. Direct Wm driver reuse is forbidden; a WW-specific implementation is required.
- Exp073DO WW exact-adapter static PASS `+0/+0`: run/job `33938228418 / 101230263277`, artifact `9960883461`, GitHub + independent ZIP SHA256 `53e66714727fac20c3d69cda893e75aecb3e1357b6cd868f467418a8d1646c5a`, token `PASS_EXP073DO_WW_EXACT_ADAPTER_STATIC_ADMISSION_V0_1`. New adapter is `ncls=4`, full `[4,nb,4,nl]`, exact selected `wins[0,:,0,:]=EE<-EE` into `selected_ee.bin`, and has no Wm/lens/S3/TE production semantics. It does not authorize home science.

## Exp073DP attempt 1 — infrastructure/dependency FAIL `+0/+0`
Original hosted exact-equivalence run/job `33938315128 / 101230515264` failed before downstream compilation and before any stock-vs-adapter numerical comparison. Decoded logs identify the first cause: workflow requested `pymaster==2.7.0` from PyPI, but no such distribution exists there. Therefore no comparator/artifact exists and this is infrastructure/dependency `+0/+0`, not a WW implementation/scientific FAIL.

## Current live process — repaired Exp073DP hosted exact equivalence `+0/+0`
The frozen Exp073DP preregistration and comparator code are unchanged. Only dependency provenance was prospectively repaired at commit/head `b2dc8a395991963885d47a964d44c50c3ef2927e`:
- clone official LSSTDESC/NaMaster source at exact tag `v2.7`;
- require exact tag identity;
- record source commit and fail closed unless commit prefix is `24365fa`;
- install from that source tree;
- fail closed unless installed PyMaster metadata is `2.7` or `2.7.*`;
- no fallback to 2.6 or 3.0.

Current run/job `33938446310 / 101230897808`, GitHub-hosted only, `IN_PROGRESS` at latest reconciliation. No checkpoint namespace is used; no full-resolution DES numerical data are read. **DSIR-HOME-PC is FREE.** Expected token remains `PASS_EXP073DP_WW_EXACT_ADAPTER_SMALLNSIDE_EQUIVALENCE_V0_1`.

PASS requires each of three deterministic synthetic S0-like spin-2 auto controls to reproduce stock PyMaster WW full bandpower windows exactly through the new adapter and an 8-worker deterministic downstream: whole-array SHA equality, `numpy.array_equal=true`, max absolute difference exactly `0.0`, plus the same exact checks for selected `EE<-EE`. No tolerance/rounding/ULP/smoothing/majority rescue.

On raw-artifact PASS: implement and separately audit a full-resolution durable A/B `WW_S0_S0` driver with dedicated checkpoint namespaces and fail-closed resume before any home scientific launch. On FAIL/BLOCKED: diagnose the first new dependency/build/implementation cause and preserve exact arithmetic and lineage.

Immutable detailed repair note: `recovery/2026-09-05_exp073dp_pymaster_v2_7_source_binding_repair.md`. Current process ledger: `docs/CURRENT_PROCESS.md`.

## Frozen science/execution boundaries
`0.295<=z<=2.33`; `0<k<=0.06664762008318016 Mpc^-1`; Layer-A invalid `<=0.05`; Layer-B invalid-row `<=0.05`; retained dimension `>=15`; DES NSIDE=4096; ell `0..12287`; 39 bands; Wm `TE<-TE`; WW `EE<-EE`; canonical `<f8 [39,12288]`; no effective ell/z/k or fiducial-P shortcut; exact-threshold ambiguity `numerically_unresolved`; no tolerance/rounding/smoothing/averaging rescue.

Any future self-hosted WW work must be prospectively checkpoint/resume safe. When applicable, use exactly 8 outer workers with nested BLAS/MKL/OpenBLAS/NumExpr threads pinned to 1, complete-unit checkpoints, exact canonical reassembly, exact restore checks and fail-closed semantics. No scientific arithmetic may change for performance.

## Exact next gate
Consume terminal `33938446310 / 101230897808` without duplication. No self-hosted `WW_S0_S0` process is authorized yet.