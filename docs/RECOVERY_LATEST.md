# DSIR RECOVERY LATEST — authoritative live pointer

**Updated:** 2026-09-05
**Scope:** DSIR only; RTK/RQIR excluded.

Repository state, immutable recovery notes, validated Actions raw logs/artifacts and durable checkpoints outrank chat wording. Historical outcomes remain immutable. Frozen science boundaries remain unchanged.

## Preserved scientific authority
Wm_S1 Track-A exact PASS, admitted Wm_S2 and Wm_S3 exact scientific PASS remain preserved. Historical Exp073CM resource/performance FAIL `+0/+0`, original Exp073BU runner-loss infrastructure `+0/+0`, and Exp073DT attempts 1–3 external runner shutdowns remain historical and unchanged.

Frozen frontier:
`Wm_S1 -> Wm_S2 -> Wm_S3 -> WW_S0_S0 -> WW_S0_S1 -> WW_S0_S2 -> WW_S0_S3 -> WW_S1_S1 -> WW_S1_S2 -> WW_S1_S3 -> WW_S2_S2 -> WW_S2_S3 -> WW_S3_S3`.
Current scientific target is exactly `WW_S0_S0`.

## Current authoritative heavy process — Exp073DT attempt 4
Frozen preregistration commit `946964121f12c67e053514109bf974050eeb0cc9`; workflow/head `c450aef42d96eb0bfe0b4c78d5a0fdc850d9a2cd`; source authority `de83e20a68f79ccf25b89b0d33eb4206e294c757`; contract fingerprint `b7845df5ce4bc2bd730461476b7ff0831512003ceb5b3558436005c9876bd251`.

- run `33940588308`, attempt `4`;
- hosted preflight `101288015425`: SUCCESS;
- self-hosted science `101288014666`: **QUEUED** at latest live reconciliation;
- durable root `$HOME/.cache/dsir/exp073dt-ww-s0-s0-ab-v0-1`;
- namespaces `checkpoints/exp073dq-ww-s0-s0-a-v0-1` and `checkpoints/exp073dq-ww-s0-s0-b-v0-1`;
- expected token `PASS_EXP073DT_WW_S0_S0_EXACT_REPEATABILITY_8CORE_V0_1`;
- **DSIR-HOME-PC RESERVED BY Exp073DT attempt 4** while queued/in_progress.

Attempts 1–3 are `INFRASTRUCTURE_INCOMPLETE +0/+0` external shutdowns, not scientific FAIL. Only complete hash/identity-verified stages may restore; interrupted/incomplete stages recompute; malformed/mismatched state fails closed.

## Required terminal provenance closure — Exp073EB
Exp073EB remains prospectively armed support-only `+0/+0`. The frozen Exp073DT terminal artifact does not export the full six-stage checkpoint chain, so workflow SUCCESS/token alone is insufficient. On DT SUCCESS, only frozen token + independently verified exact A/B equality + Exp073EB full checkpoint-provenance PASS may create `WW_S0_S0` authority.

## Exp073DU v0.1 consumed — historical qualifier FAIL +0/+0
The previously activated hosted distinct-field qualifier was found and consumed: run/job `33955300558 / 101277450615`, artifact `9966167115`. GitHub artifact digest and independently computed ZIP SHA256 both equal `34a90ebe024c53c1bb833465346bd0ef6ca3196184bb49a8ba18e543eca8bba1`.

Raw terminal receipt: `classification=QUALIFIER_FAIL`, token `FAIL_EXP073DU_WW_S0_S1_CROSSFIELD_SMALLNSIDE_EXACT_ADAPTER_V0_1`, `science_gate_scored=false`, `ww_s0_s1_authority_created=false`, accounting `+0/+0`.

The receipt shows all distinct-source/object, ordered-cross-vs-auto, shape, finiteness and no-tolerance checks passed. Only adapter-full/selected exact equality and selected SHA against the direct pre-serialization in-memory W01 failed. The production adapter consumes serialized `w01.fits`. Preserved Exp073EA evidence already established exact saved-LU equivalence to official serialized→reloaded PyMaster state while pre-serialization state may differ at last bits. Therefore the first causal defect is the DU v0.1 qualifier reference-state contract, not scientific WW arithmetic. DU v0.1 remains immutable historical FAIL `+0/+0`.

## Prospective minimal repair — Exp073DW
Exp073DW is frozen support-only before any repair output. It repeats the same deterministic S0→S1 small-NSIDE construction and same production adapter, but its exact authority reference is official `NmtWorkspace.read_from(w01.fits)` state. Pre-serialization-vs-reloaded equality is recorded only as a diagnostic negative control. No tolerance/allclose/rounding/smoothing/averaging rescue is permitted.

Provenance: prereg commit `1128f33ccb5c0fd9cc70393812821ed88b7f9856`; qualifier commit `619f138ba2405fd15527ad045244738955077ec5`; component freeze `32385f11f2b9461aab77beb9b7f5e22bf55c0e81`; workflow final commit `1d9c7342f930d1674adb919d794fe4ef1ae955a7`; activation/research-log head `f2cda20023503b189977a05b2e482816c3e03f1d`.

Hosted run/job `33967669396 / 101310531746` is **IN_PROGRESS** at latest reconciliation. It is hosted-only and does not consume DSIR-HOME-PC. Allowed support PASS token only: `PASS_EXP073DW_WW_S0_S1_SERIALIZED_RELOAD_EXACT_ADAPTER_V0_1`; it can never create WW authority.

## Prospective next-front design — Exp073DV
`experiments/073dv_ww_s0_s1_full_resolution_activation_design_v0_1.md` remains `PREPARED_NOT_ACTIVATED`. Future full-resolution WW_S0_S1 requires valid WW_S0_S0 authority, required Exp073EB provenance closure on DT PASS, consumed repaired distinct-field qualifier Exp073DW, and zero competing self-hosted heavy processes.

## Frozen science/execution boundaries
`0.295<=z<=2.33`; `0<k<=0.06664762008318016 Mpc^-1`; Layer-A invalid `<=0.05`; Layer-B invalid-row `<=0.05`; retained dimension `>=15`; DES NSIDE=4096; ell `0..12287`; 39 bands; Wm `TE<-TE`; WW `EE<-EE`; canonical `<f8 [39,12288]`; no effective ell/z/k or fiducial-P shortcut; exact-threshold ambiguity `numerically_unresolved`; no tolerance/rounding/smoothing/averaging rescue.

## Exact next gates
1. Do not launch another self-hosted task while Exp073DT attempt 4 is queued/in_progress.
2. Consume Exp073DW immediately when terminal; PASS/FAIL remains support-only `+0/+0`.
3. When Exp073DT `33940588308 / 101288014666` becomes terminal, consume raw artifact/digest, A/B payloads/comparator and checkpoint provenance. On SUCCESS also consume Exp073EB before admitting any WW_S0_S0 authority.
