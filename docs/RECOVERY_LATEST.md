# DSIR RECOVERY LATEST — authoritative live pointer

**Updated:** 2026-09-02  
**Scope:** DSIR only; RTK/RQIR excluded.  
**Article-3 readiness:** **Verified 52.0% | Draft/data 54.6%** (exact Draft/data `54.57142857142857%`).

Repository state and immutable GitHub Actions artifacts outrank chat wording. Synthetic/infrastructure/provenance/numerical/performance/static/diagnostic QA gives `+0/+0` unless a frozen ledger explicitly states otherwise.

## Immediate live frontier — Wm_S3 resource qualification

Historical Wm_S3 authority remains absent: Exp073AF blocked the Exp073AA remaining-13 production route. A new prospective successor is required.

Exp073CK v0.1 direct-8 resource run `33680749033` reached the self-hosted job `100416492795` but failed before any scientific or numerical comparison. The exact failure site was `Bind exact DES Y1 lens mask`: the DES public server delivered the ~100 MiB mask at only tens of KiB/s and repeatedly terminated responses (`curl (18)`). The user then stopped the manually-started runner; the job ended with exit code 130 and runner shutdown. All Wm_S3 PCL and coupling-comparison steps were skipped. Classification: **INFRASTRUCTURE_INCOMPLETE_EXP073CK_V0_1**, `+0/+0`; this is not a scientific FAIL.

Exp073CK also exposed a prospective performance-design issue before execution: its frozen benchmark range contained only one band, while the frozen OpenMP helper parallelizes across bands. Therefore one-band `threads=8` cannot measure or qualify the requested near-full 8-CPU utilization. Exp073CK remains immutable historical infrastructure evidence and is not edited/retried as scientific authority.

**Next permitted action:** prospectively preregister a new Wm_S3 resource successor that (1) stages the exact DES mask on GitHub-hosted infrastructure and passes it to the home runner as a verified artifact, avoiding direct DES-server transfer on the home runner, and (2) compares an exact 1-thread reference against an 8-thread target over exactly eight concurrently schedulable frozen bands. The target must preserve per-band arithmetic/order, use exact equality and identical canonical SHA, require no swap increase, and record process CPU-seconds/wall-seconds so 8-core utilization can be tested. Resource qualification remains `+0/+0`.

## Wm_S2 accepted authority

Wm_S2 v0.2 remains admitted Track-A numerical angular object under the composite authority `Exp073CF compact scoped exact PASS -> Exp073CI deterministic fixed-dispatch exact finalizer PASS`. Historical Exp073CF finalizer v0.1 remains permanently scientific FAIL and is not rewritten.

## Frozen boundaries and order

Preserve: Exp073BJ Wm_S1 Track-A exact PASS; Exp073AQ historical exact-repeatability FAIL; Exp073BD P3 `PROVISIONAL_INCOMPLETE_NO_DOWNSTREAM_USE`; Exp073BV source-lineage PASS; Exp073BW streaming-equivalence PASS; Exp073BZ checkpoint/failover PASS; Exp073CC/CD/CE nonclassifying +0/+0; Exp073CF attempts1/2 infrastructure incomplete +0/+0; Exp073CF compact scoped PASS + permanent finalizer v0.1 FAIL; Exp073CG/CH diagnostics +0/+0; Exp073CI new-version exact PASS; Exp073CJ governance +0/+0; Exp073CK infrastructure incomplete +0/+0.

Frozen science: `0.295 <= z <= 2.33`; `0 < k <= 0.06664762008318016 Mpc^-1`; Layer-A `operator_f_invalid <= 0.05`; Layer-B invalid-row fraction `<=0.05`; retained dimension `>=15`; DES `NSIDE=4096`; ell `0..12287`; 39 bands; Wm `TE <- TE`; WW `EE <- EE`; canonical `<f8 [39,12288]`; no effective ell/z/k or fiducial-P shortcut; exact-threshold ambiguity `numerically_unresolved`.

Required order remains `validated physical forward/power-input bridges -> preregistered physical support-validity mask -> Layer A/Layer B prerequisites -> covariance restriction/whitening -> nuisance tangent rank/SVD -> quotient/relation/null control -> actual G7 authorization -> fresh G8 withheld family`. No G8 jump.

**Home runner:** listener was manually stopped after Exp073CK network stall; a new self-hosted run will require `cd ~/actions-runner-dsir && ./run.sh` once its hosted staging/authorization is ready. At most one heavy DSIR self-hosted job at a time.
