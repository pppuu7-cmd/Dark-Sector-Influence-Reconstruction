# DSIR authoritative recovery — latest

Updated: 2026-09-11. Scope: **DSIR only**. Never mix RTK, RQIR, KMQGB or KMDSB.

Newest immutable current-front recovery note: `docs/recovery/RECOVERY_2026-09-11_ARTICLE3_TOP64_CENSUS_INFRA_RETRY_V122.md`, creation commit `460ff572adeb18c86fde880430af17bfbb34af95`. V121 and all earlier recovery notes remain immutable history; V122 supersedes them for current-front recovery.

## Scientific frontier
Canonical 8193->16385 remains independently verified `COMMON_GRID_NEXT_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`, max relative component difference `0.012484060640679777` versus strict `<1e-3`. Frozen `REL_TOL=1e-3`, `h=1e-4`, native kpd20, centered-cubic interpolation, physical domain/masks/107-row accounting, invalid fraction `<=0.05`, retained `>=15`, unsupported=0 and lookup `<=1e-12` remain unchanged. No 32769 execution is authorized; covariance restriction remains unauthorized; Wm_S3 remains unopened.

## Closed exact-hotspot/top-1 authority
Exact-hotspot run/job/artifact `34592951737 / 103242180883 / 10266223990` and prospectively frozen top-1 decomposition `34608169257 / 10268078468` with terminal consumer `34611473046 / 103302765790 / 10268761538` remain independently verified support-only `+0/+0`. The full plateau is localized to the frozen DES beta-response atom call 204 / target 44 / k `0.01698756109424023` / z `0.7475`, where tiny deterministic raw-role changes are amplified by strong beta finite-difference cancellation. Durable top-1 authority is `docs/dsir4/authority/LAYERB_POST_16385_TOP1_ROLE_CANCELLATION_TERMINAL_AUTHORITY_V0_1.json`, completion commit `c7e4968fb13880fb2d7c7615d10a0821073b2243`.

## Top-64 census infrastructure failure and resume
Top-64 census attempt 1, run/job `34614119149 / 103311649147`, is terminal FAILURE. Identity/source/hash checks, numerical-stack install and pinned CLASS-IV build passed. The first fail-closed causal gate was `RuntimeError: atom 0 primary reproduction 2.8125401822500933e-06`, versus the prospectively frozen per-atom requirement `<=1e-12`.

This is classified **infrastructure/numerical-reproducibility FAIL +0/+0**, not scientific FAIL. The top-64 and validated top-1 helpers use the same frozen response formulas and numerical choices; top-1 previously reproduced rank 0 exactly on another hosted VM. No science setting or acceptance threshold is changed. The failed attempt remains durable history.

The smallest self-heal is one exact unchanged retry of the failed job. If that unchanged retry fails the reproduction gate again, do not relax the contract or repeatedly retry; classify the census BLOCKED by hosted numerical reproducibility and freeze a separate support-only reproducibility diagnostic.

Stale superseded self-hosted run `34550495778 / 103112190909` remains queued and must not receive home-runner ownership.

## Readiness
**ARTICLE3_REPOSITORY_READINESS: 68%.** Funnel-freeze readiness: **67%**.

## Exact next action
Track the single unchanged retry of top-64 census run `34614119149`. If terminal PASS, independently consume and hash all artifacts and enforce the original exact 64/lifecycle/lookup/per-atom `<=1e-12` contract. If it repeats the reproduction failure, stop retries and move to a prospectively frozen hosted-reproducibility diagnostic. Never execute 32769 or alter frozen science to rescue this support gate.
