# DSIR RECOVERY LATEST — authoritative live pointer

**Updated:** 2026-09-05
**Scope:** DSIR only; RTK/RQIR excluded.

Repository state, immutable recovery notes, validated Actions raw logs/artifacts and durable checkpoints outrank chat wording. Historical outcomes remain immutable. Frozen science boundaries remain unchanged.

## Scientific authority preserved
Wm_S1 Track-A exact PASS and admitted Wm_S2 authority remain preserved. Wm_S3 exact scientific PASS remains Exp073DJ checkpoint-preserving Exp073BU run/job `33910213781 / 101144660519`, frozen science head `c02c018ede6a1fcf7aef1a848c0118a0669ed67f`, contract fingerprint `b38687bf5aa6cf4cfe01b2f38a7091e96d97196ad38bdf2ea771f7b649ac73da`, raw token `PASS_EXP073BU_WM_S3_FRESH_AB_EXACT_REPEATABILITY_8CORE_V0_3`, canonical A/B selected-TE SHA256 `d282ebdf98dc04e41a8c85f487e209634a8324ce7677107112b8abfd1660f749`, exact SHA equality and `numpy.array_equal=true`, no tolerance rescue.

Historical Exp073CM remains RESOURCE/PERFORMANCE FAIL `+0/+0`; original Exp073BU v0.4 runner-loss remains infrastructure `+0/+0`.

## Closed support/evidence chain after Wm_S3
### Exp073DL terminal evidence PASS `+0/+0`
Run/job `33934918432 / 101220868663`, artifact `9959830267`; GitHub and independent ZIP SHA256 `cc32969cba9802201ac8cf7eae32b430ec47285f3357a58d470850aff8ceb8ab`. Exported A/B payloads are canonical `<f8 [39,12288]`, finite, identical SHA `d282ebdf...f749`, exact `numpy.array_equal=true`. This confirms evidence only and creates no Wm_S3 authority.

### Frozen successor order
`Wm_S1 -> Wm_S2 -> Wm_S3 -> WW_S0_S0 -> WW_S0_S1 -> WW_S0_S2 -> WW_S0_S3 -> WW_S1_S1 -> WW_S1_S2 -> WW_S1_S3 -> WW_S2_S2 -> WW_S2_S3 -> WW_S3_S3`.

Therefore the next angular object is exactly `WW_S0_S0`; no `Wm_S4` or result-dependent substitute is authorized.

### Exp073DM WW_S0_S0 exact-authority preflight PASS `+0/+0`
Prereg commit `030310dbf78652972f74afd5b8cf85ab435b6a43`. Initial hosted run/job `33937934428 / 101229402478` was infrastructure/static-harness FAIL `+0/+0`; first causal log failure was a self-referential anti-rescue test matching its own forbidden string literals. Smallest prospective repair commit `ab72044ff0393eba58a669ae77521bd4c4321a03`, activation head `71e8ad4ea601eb756e0e0b2218620b5347416cbe`.

Repaired run/job `33937980591 / 101229540163` terminal SUCCESS; artifact `9960805717`; GitHub + independent ZIP SHA256 `fa19dbf8c6735e25ec1a500a3f8540f2f868f30e92aa589d3194d7f9deb5c8e3`. Raw token `PASS_EXP073DM_WW_S0_S0_EXACT_AUTHORITY_PREFLIGHT_V0_1`; support/readiness `+0/+0`; no numerical WW payload. Target frozen to full `[4,39,4,12288]`, selected `EE<-EE`, canonical `<f8 [39,12288]`, no lens-mask route.

### Exp073DN checkpoint-architecture binding PASS `+0/+0`
Prereg commit `08271223c92a2aabbb81238d9455ac1e49acbce3`, audit commit `9d42ed49da74df53205930f6a7cfa5b23f6bd64c`, workflow/head `24762df625db3516d6da7f07447b9d2599627152`. Run/job `33938100671 / 101229887636` terminal SUCCESS; artifact `9960842558`; GitHub + independent ZIP SHA256 `955cbe2f58b1809fec34815d33b105edac8f02777f99e6d4e36f57b29f64a259`. Raw token `PASS_EXP073DN_REQUIRE_WW_SPECIFIC_CHECKPOINT_ADAPTER_V0_1`; `direct_wm_driver_reuse_authorized=false`, `ww_specific_adapter_required=true`, `science_gate_scored=false`, `ww_authority_created=false`.

Scientific implication is governance-only: the proven Wm base is hard-bound to lens+S3, spin-0×spin-2, `ncls=2`, `[2,39,2,12288]`, `TE<-TE` and Wm namespaces. Those semantics cannot be reused unchanged for WW. Reusable pieces are only fail-closed checkpoint identity, complete-stage atomic persistence, restore SHA validation, independent A/B namespaces, no other-replica reads and exact comparator principles.

## Current live process — Exp073DO WW exact-adapter static admission `+0/+0`
A WW-specific adapter has been prospectively implemented before any WW numerical production:
- prereg commit `eb8312f25a8fe059e8baf7a09bfc875ec9b91aa6`;
- adapter implementation commit `33c2de30427136f0c1e95ab0d0eda9a5b1377dca`;
- static audit commit `99c188f8d1298524d3266510b4f983240cdf10ba`;
- workflow/head `f9355339f9148d5152f67f91e5b0b451f2d1cf80`;
- live run/job `33938228418 / 101230263277`;
- state at pointer write `IN_PROGRESS`;
- expected token `PASS_EXP073DO_WW_EXACT_ADAPTER_STATIC_ADMISSION_V0_1`;
- checkpoint namespace `N/A`, hosted static only;
- **DSIR-HOME-PC FREE**.

Frozen adapter requirements: `ncls=4`; full `[4,nb,4,nl]`; selected `wins[0,:,0,:]=EE<-EE` to `selected_ee.bin`; selected semantic checkpoint `selected_ee_complete`; no Wm/lens/S3/TE production semantics; mmap FITS->canonical input, exact byte-size checks, source-head/contract/checkpoint/component-blob binding, no tolerance rescue. The deterministic downstream is generic in `ncls` and a later 8-worker build must runtime-prove exactly `DSIR_OMP_TEAM=8` while preserving per-scalar accumulation order.

Exp073DO cannot authorize home execution. On independently consumed raw PASS, the exact next allowed gate is a separately preregistered hosted small-NSIDE stock PyMaster WW-vs-adapter exact-equivalence test. Only after exact equivalence and a separate WW durable A/B driver/activation audit may home `WW_S0_S0` science run.

On FAIL/BLOCKED, diagnose first implementation/static cause prospectively and change only the smallest defect; never change WW arithmetic or frozen criteria.

Detailed recovery note: `recovery/2026-09-05_exp073dl_closed_exp073dm_pass_exp073dn_live.md`. Detailed current process: `docs/CURRENT_PROCESS.md`.

## Frozen boundaries
`0.295<=z<=2.33`; `0<k<=0.06664762008318016 Mpc^-1`; Layer-A operator_f_invalid `<=0.05`; Layer-B invalid-row fraction `<=0.05`; retained dimension `>=15`; DES NSIDE=4096; ell `0..12287`; 39 bands; Wm `TE<-TE`; WW `EE<-EE`; canonical `<f8 [39,12288]`; no effective ell/z/k or fiducial-P shortcut; exact-threshold ambiguity `numerically_unresolved`; no tolerance/rounding/smoothing/averaging rescue.

Future home heavy work must have prospectively frozen durable checkpoint/resume. When applicable, use exactly 8 outer compute workers with nested BLAS/MKL/OpenBLAS/NumExpr threads pinned to 1, complete-unit checkpoints, exact canonical reassembly and fail-closed restore; scientific arithmetic may not change for performance.

## Exact next gate
Consume terminal `33938228418 / 101230263277`. If raw Exp073DO PASS validates, preregister and launch the hosted small-NSIDE exact-equivalence gate. Do not launch self-hosted `WW_S0_S0` before that equivalence plus durable-driver activation chain passes.