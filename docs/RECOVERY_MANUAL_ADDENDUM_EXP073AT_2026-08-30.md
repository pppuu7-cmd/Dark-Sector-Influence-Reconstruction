# DSIR recovery manual addendum — Exp073AT Layer-A admission firewall

**Date:** 2026-08-30

This addendum extends `docs/RECOVERY_MANUAL_LIVE_2026-08-30.md` for the execution-qualified Article-3 successor route.

## Current scientific state

- Article-3 strict scientific repository readiness: **52%**.
- Layer A/B: OPEN.
- covariance/whitening: BLOCKED.
- G7/G8/G9: OPEN.
- individual angular authorities and synthetic/provenance/release QA remain +0 readiness.

## Active heavy work

Exp073AQ Wm_S1 run `33327372191` remains active with replicas A `99299799192` and B `99299799338` still in exact controlled computation at the latest inspection. No AQ authority artifact exists yet. Do not duplicate AQ and do not launch Wm_S2 until AQ resolves under its frozen exact comparator.

## Successor-route chain now frozen through Layer-A admission

The durable forward chain is:

`Exp073AO/AP execution-qualified succession`

`-> Exp073AM Wm_S0 anchor + 13 exact controlled-twin task authorities`

`-> real ordered 14-window aggregate under Exp073AR`

`-> real complete 1410-row pre-support candidate manifest under Exp073AS`

`-> Layer-A admission under Exp073AT`

`-> unchanged real Layer-A factorized evaluator semantics`.

## Exp073AT hosted authority

Frozen commits:

- prereg `507c210419223e20f77dd0c5a9ffc6f8d150b41b`;
- validator `931bb2334e901044fbbaaa6a7ddf8a6b326daf62`;
- workflow `1bf8f72a4690c0b45686a52b6e54630f2fbf7849`;
- workflow freeze `acff4891f05bb5a56acd1d8f7ef1a80cc093e77d`;
- trigger/head `d67a6ebe8cf48c416081747e2966811b46282062`.

Hosted result:

- run `33331600899`;
- job `99310991453`;
- artifact `9737797541`;
- digest `sha256:4eb8bb758bdf01aae6808a358220762f5b2e6d5da15b617c5d95b727b597c1f3`;
- token `PASS_EXP073AT_EXECUTION_QUALIFIED_LAYERA_ADMISSION_SYNTHETIC_V0_1`;
- 24/24 synthetic cases PASS;
- classification `HOSTED_SYNTHETIC_PASS_PROVENANCE_RELEASE_QA_NON_SCIENTIFIC_PLUS_0_READINESS`.

## Binding Layer-A admission rule

A future real Layer-A evaluator may receive only a complete immutable hosted Exp073AS candidate manifest with:

- route `controlled_single_thread_exact_v1`;
- join schema `exp073as_execution_qualified_presupport_join_v0_1`;
- `candidate_manifest_complete=true`;
- `support_selection_applied=false`;
- 1410 rows ordered as Exp073U with `Wm=780`, `WW=390`, `BOSS=240`;
- Exp073U ordered-ID SHA `bfaf582518cdbfd34b1e8392da83dac6b0885948bc31f2c29d4e48247c23af75`;
- controlled Wm_S0 anchor SHA `8ac59fc060195addcc5cd8b6d75e32fbc6dbfeea8456f4c83e8bf0cf034b9220`;
- real hosted Exp073AS provenance and deterministic manifest SHA.

Historical primary-P Wm_S0, `canonical_exp073x2`, old `exp073aa`, historical Exp073AE join schema, incomplete/already-selected candidates or malformed hosted provenance are not admissible to the successor Layer-A route.

## Layer-A mathematics remain unchanged

Exp073AT is only an admission firewall. It does not alter the frozen numerical support contract:

- `0.295 <= z <= 2.33` inclusive;
- `0 < k <= 0.06664762008318016 Mpc^-1`;
- `f_invalid = 1-valid/total`;
- inclusive `f_invalid <= 0.05`;
- exact boundary splits;
- integer-ell sum of `abs(W_ell)` with no extra ell measure;
- piecewise-linear radial integration/inverse-chi;
- exact-threshold ambiguity remains unresolved rather than rounded;
- no covariance/nuisance/G8 leakage.

## Resume instruction

First inspect Exp073AQ run `33327372191`. If it produces a valid exact PASS authority, the next heavy task is Wm_S2 under its own prospectively frozen controlled-twin gate. If AQ is still active, perform only independent non-classifying prerequisites. If AQ reaches exact repeatability FAIL, preserve that negative result and do not continue the 13-task successor chain without a new prospectively admissible scientific/governance decision.
