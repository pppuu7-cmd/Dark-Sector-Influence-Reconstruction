# Exp073GZ — C2 runtime-admission receipt contract static audit v0.1

Date: 2026-09-07
Scope: DSIR only; C2_IDE_LOCAL_TANGENT_CONE support boundary.
Classification on PASS: SUPPORT_PLUS_0_PLUS_0 only.

## Frozen object

Audit exactly `docs/dsir4/mappings/C2_IDE_RUNTIME_ADMISSION_RECEIPT_CONTRACT_V0_1.md` with Git blob identity `1c2e30e6563efe3976ee0b1825dfb250a902a529`.

## Frozen PASS requirements

The hosted audit MUST fail closed unless all of the following are simultaneously true:

1. exact blob identity above;
2. hypothesis id `C2_IDE_LOCAL_TANGENT_CONE` and solver lineage `kaeonikc/class_iv@ac627d54e9ce196a08878d1ba33999819925d19c` are present;
3. exact grid declaration is 7 redshift x 4 physical-k = 28 packets, ordering `z-major/k-minor`;
4. packet width is exactly 64 bytes and aggregate width exactly 1792 bytes;
5. receipt binds packet_count=28, aggregate_bytes=1792, raw aggregate SHA-256, producer run/job terminal identity, and artifact identity/digest;
6. receipt boundary explicitly keeps `decoded=false`, `mapped=false`, `prediction_ready=false` and does not create scientific model authority;
7. fail-closed rejection covers nonterminal/incomplete producer, coordinate/order/count/byte/identity mismatch, aggregate SHA mismatch, pre-admission decoding/transformation/interpolation/filtering/reordering/duplication/deletion/averaging/smoothing/tolerance rescue, missing provenance, synthetic-as-runtime substitution, and downstream-selection influence;
8. branch violations are classified `OUTSIDE_DOMAIN`, while missing immutable tangent/matched-setting provenance remains `NOT_YET_TESTABLE`;
9. next transition is real runtime producer only when heavy-run exclusivity is clear; decoding/mapping/scientific comparison stay separate later gates.

No CLASS execution, home runner, decoded scientific values, observable mapping, model PASS/FAIL, tolerance adjustment, or scientific authority is permitted in Exp073GZ.

Exact PASS token: `PASS_EXP073GZ_C2_RUNTIME_ADMISSION_RECEIPT_CONTRACT_STATIC_AUDIT_V0_1`.

Any audit failure is implementation/static-audit `+0/+0`, not scientific FAIL. A passing audit only authorizes the already-frozen next transition; it does not launch it while another home-heavy process owns the runner.
