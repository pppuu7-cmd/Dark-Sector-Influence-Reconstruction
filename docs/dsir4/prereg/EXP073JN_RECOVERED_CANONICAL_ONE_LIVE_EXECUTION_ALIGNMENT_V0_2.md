# Exp073JN — recovered canonical one-live execution alignment v0.2

Date frozen: 2026-09-11 while recovered Exp073JL retry `34544293038` is still in the frozen numerical step and before its terminal verdict is known. Scope: implementation/provenance alignment only. The scientific contract remains `EXP073JN_ARTICLE3_LAYERB_SHARED_GRID_SCIENTIFIC_CLOSURE_RERUN_V0_1.md` unchanged.

## Activation — unchanged

This implementation may execute only if a terminal recovered Exp073JL authority has already been independently verified and its exact classification is `COMMON_GRID_THIRD_REFINEMENT_CONVERGED_PLUS_0_PLUS_0`. Classification plus independent artifact verification is the frozen branch activation condition. Numerical observation fields may be cross-checked after activation but may not become extra activation predicates.

If JL is NOT_CONVERGED, INVALID_INFRA, malformed, unverified or absent, this implementation must not run.

## Why v0.2 is required

The v0.1 JN helper predates canonical-lattice and recovered-lifecycle authorities. It regenerates the JL lattices with host `guarded_lattice()` and reintroduces the historical multi-live `ResolutionSuite`. Response-blind host-replica work has now directly demonstrated binary64 `geomspace` host variation, while Exp073JT supplies exact canonical JL lattice bytes and Exp073JW/recovered-JL static work supplies the eight-build/max-one-live execution architecture.

Therefore JN v0.2 changes execution/provenance only:

- consume exact committed Exp073JT canonical coarse 2049 and fine 4097 u64hex byte streams;
- reuse the frozen recovered Exp073JL v0.2 engine rather than reimplementing CLASS lookup/interpolation/finite-difference arithmetic;
- use exact response-blind request plan: 441 coarse and 569 fine transfer requests per model role, 4040 total;
- exactly four coarse + four fine CLASS constructions, role order `reference, alpha_minus, beta_plus, beta_minus`, `max_live_instances=1`;
- raw finite-difference operands stay within the same Python process;
- replay through unchanged Exp073IR 107-row accounting;
- fine GL128 remains dense-z status control only.

No scientific quantity from the activating JL artifact may be reused as the fresh JN response. JN executes a fresh numerical traversal.

## Scientific classification — unchanged from JN v0.1

After the fresh recovered engine run, independently apply the frozen JN decision categories:

- structural/provenance failure -> `INVALID_INFRA_PLUS_0_PLUS_0`;
- structural pass but strict numerical convergence failure -> `NUMERICALLY_UNRESOLVED_EXP073JN`;
- numerical convergence pass but Layer-B physical thresholds fail -> `FAIL_PHYSICAL_SUPPORT_ARTICLE3_SHARED_GRID_V0_1`;
- numerical convergence pass plus Layer-B invalid-row fraction `<=0.05`, retained dimension `>=15`, all forbidden downstream reads false -> `PASS_PHYSICAL_SUPPORT_ARTICLE3_SHARED_GRID_V0_1`.

Strict numerical convergence remains `max_relative_component_difference < 1e-3`, with no equality rescue. `h=1e-4`, native kpd20, centered-cubic interpolation, lookup ceiling `1e-12`, domain, masks and estimator remain unchanged.

Only `PASS_PHYSICAL_SUPPORT_ARTICLE3_SHARED_GRID_V0_1` creates JN scientific Layer-B support authority and permits a separately frozen covariance/whitening stage to be prepared. It still does not itself pass covariance restriction or open Wm_S3.

## Static gate before any launch

The v0.2 implementation must pass a no-CLASS/no-science static audit proving activation is no stricter than the frozen branch token + independent verification; canonical files/hashes are fixed; no runtime `guarded_lattice`/`geomspace` appears in JN; the recovered engine identity is pinned; fresh engine execution is mandatory; the four JN result categories and thresholds remain exact; no raw operand or response is imported from the activating JL artifact.

Creating this alignment does not activate JN and does not change Article III readiness.
