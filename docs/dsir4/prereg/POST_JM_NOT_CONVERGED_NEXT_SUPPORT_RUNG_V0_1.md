# Post-Exp073JM NOT_CONVERGED — deterministic next Layer-B support rung v0.1

Date prospectively frozen: 2026-09-11 while recovered Exp073JM run `34548136827` is active and before any JM numerical response or verdict is inspected. Scope: DSIR Article III convergence support only; effect `+0/+0`.

## Activation
This branch may proceed only if Exp073JM terminates as a structurally valid, independently artifact-verified `COMMON_GRID_FOURTH_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`. JM CONVERGED stops the support ladder and selects the separately frozen post-JM science-closure branch. INVALID_INFRA, malformed, incomplete or unverified JM activates neither branch.

## Deterministic density
The already-frozen sequential-ladder standard fixes the next comparison before JM result to base 8192 -> 16384. Therefore the canonical requested lattices are fixed to:
- coarse: exact existing canonical 8193, decoded SHA256 `6d35a9d79aa8826554e8e39315ab12a8be1675d32d25a86ec04c978c7723a515`, text SHA256 `90b9aee9e01784a4af89ab0aa1bd5cbf1e95061f2d412d7a0e1b8ee5332ba7d6`;
- fine: exact dormant response-blind canonical 16385, decoded SHA256 `3e10cea6e46a6d0c07eac825a4e508829727914c8690d42b187ef894eaa2c975`, text SHA256 `7e8f13abe29617bc0d331f07143e6680f50512e2f3811e017bbc74ea847dff69`;
- guard counts `(0,1)` for each.

The 16385 authority was materialized before JM production and remains dormant unless this activation condition occurs.

## Frozen scientific architecture
Preserve unchanged:
- pinned CLASS-IV `ac627d54e9ce196a08878d1ba33999819925d19c` and the already-audited public `d_m` exposure/build compatibility;
- four finite-difference roles per lattice: `reference`, `alpha_minus`, `beta_plus`, `beta_minus`;
- role-major sequential lifetimes, exactly eight constructions total and max one live;
- same-process raw finite-difference operands;
- native `k_per_decade_for_pk=20`;
- `h=1e-4`;
- centered-cubic interpolation in `ln(k)` with the inherited stencil;
- requested-node lookup mismatch `<=1e-12`;
- exact inherited response-blind 107-row observational request plan and 4040 transfer calls;
- physical support, masks, row identities, BOSS operators, dense-z stability control, parent identity and Layer-B accounting;
- strict convergence rule `max_atomic_coarse_vs_fine_relative_component_difference < 1e-3`.

No runtime `geomspace`, result-dependent density, tolerance or estimator change is allowed.

## Prospective infrastructure envelope
Before any scientific/support execution, a separate no-science static audit and resource-only pilot are mandatory. To eliminate post-result capacity selection, the first and only prospectively allowed capacity envelope for this rung is the deterministic doubling of JM's infrastructure envelope:
- `_MAX_NUMBER_OF_K_FILES_ = 18432`;
- `_ARGUMENT_LENGTH_MAX_ = 524288`.

Both require exact one-replacement checks and pre/post SHA256 receipts. These are infrastructure-only capacities. If this envelope is not reproducibly feasible, the result is a resource/infrastructure diagnosis; it does not permit a smaller grid, alternative density or scientific rescue. A later minimal process-only repair may alter execution mechanics only if prospectively documented without changing the scientific architecture.

## Frozen support decision
After activation and successful static/resource gates, a complete valid next-rung support run is `COMMON_GRID_NEXT_REFINEMENT_CONVERGED_PLUS_0_PLUS_0` only if all structural gates pass and the maximum relative component difference is strictly `<1e-3` with Layer-B invalid-row fraction `<=0.05` and retained dimension `>=15`.

A structurally valid result that does not satisfy strict convergence is `COMMON_GRID_NEXT_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`. Provenance/build/parser/capacity/canonical-grid/lifecycle failure is `INVALID_INFRA_PLUS_0_PLUS_0`.

Every support outcome remains `+0/+0` and cannot itself authorize covariance restriction or open Wm_S3. If this rung converges, a fresh separately frozen science-authorizing closure is required; if it validly does not converge, the already-frozen ladder doubling rule governs the next density, again subject to response-blind canonicalization and resource feasibility.

## Anti-adaptation
No denominator floor, averaging, smoothing, clipping, rounding, extrapolation, atom/row dropping, altered masks/support, effective coordinate, fiducial-P shortcut, changed `h`, changed native kpd or changed tolerance is permitted. This branch was frozen before JM verdict.
