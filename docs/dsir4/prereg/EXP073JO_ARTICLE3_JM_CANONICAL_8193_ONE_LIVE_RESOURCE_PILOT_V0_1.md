# Exp073JO — Article III JM canonical-8193 one-live resource pilot v0.1

Date prospectively frozen: 2026-09-11, after independently verified recovered Exp073JL NOT_CONVERGED and before any Exp073JM 4097->8193 production response is computed. Scope: DSIR Article III process/resource feasibility only. Effect `+0/+0`.

## Activation

Exp073JO may run only because all of the following already exist independently of any future JM response:

1. recovered Exp073JL terminal authority is independently verified `COMMON_GRID_THIRD_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`, activating only the pre-frozen JM branch;
2. recovered JM v0.2 implementation/static audit is independently verified PASS while branch-disabled, with the original JM science contract preserved;
3. exact response-blind canonical 8193 bytes were materialized before the JL verdict and independently anchored to the authoritative 4097 lineage;
4. recovered JM alignment explicitly requires an 8193 resource-only pilot before full JM production.

Exp073JO does not activate JN, create JM convergence authority, authorize covariance restriction or open Wm_S3.

## Purpose

Exp073JW established that the canonical recovered role-major architecture is feasible through 4097 requested nodes. JM's next deterministic rung requires 8193 requested nodes and larger infrastructure-only capacities. Exp073JO tests only whether **one canonical 8193 lattice** can complete all four finite-difference model-role CLASS lifetimes sequentially in one process under the already-frozen JM capacities.

It deliberately does not execute the full 107-row DES+BOSS traversal and does not compare JM coarse 4097 against fine 8193. Therefore it cannot classify JM as CONVERGED or NOT_CONVERGED.

## Exact fixed inputs

- canonical 8193 file `docs/dsir4/canonical/EXP073JM_CANONICAL_FINE_8193_NODES_U64HEX_V0_1.txt`;
- decoded `<f8` SHA256 `6d35a9d79aa8826554e8e39315ab12a8be1675d32d25a86ec04c978c7723a515`;
- text/u64hex SHA256 `90b9aee9e01784a4af89ab0aa1bd5cbf1e95061f2d412d7a0e1b8ee5332ba7d6`;
- exact anchor-selected canonical authority `EXP073JM_RESPONSE_BLIND_CANONICAL_FINE_8193_V0_1.json`;
- pinned CLASS-IV commit `ac627d54e9ce196a08878d1ba33999819925d19c`;
- public `d_m` exposure/build-compat patches already used by the recovered lineage;
- infrastructure-only `_MAX_NUMBER_OF_K_FILES_=9216` and `_ARGUMENT_LENGTH_MAX_=262144`;
- OMP/OpenBLAS/MKL/NUMEXPR threads each exactly 1;
- native CLASS `k_per_decade_for_pk=20.0`;
- `h=1e-4` only to form the same decision-neutral pilot response estimator;
- requested-node lookup mismatch ceiling `1e-12`;
- exact inherited JJ requested-node recovery and centered-cubic interpolation.

The canonical 8193 grid MUST be decoded from the committed u64hex stream and MUST NOT be regenerated with `np.geomspace`, `guarded_lattice` or any execution-host floating construction.

## Fixed pilot requests — inherited unchanged from JW

The resource target set is not chosen using the JL or JM numerical result. It is exactly the already-frozen JW A/B control set:

- A: `z = float.fromhex('0x1.3851eb851eb85p-1')`; k/Mpc^-1 = `[0.0013, 0.0047, 0.013, 0.041]`;
- B: `z = float.fromhex('0x1.1c28f5c28f5c3p+0')`; k/Mpc^-1 = `[0.0019, 0.0073, 0.021, 0.057]`.

Request order is A then B for every model role.

## Exact one-process lifecycle

One hosted Python process performs exactly four solver constructions on the same canonical 8193 lattice, at most one CLASS instance live at any instant:

1. `reference` `(alpha,beta)=(0,0)` -> A,B -> cleanup;
2. `alpha_minus` `(-1e-4,0)` -> A,B -> cleanup;
3. `beta_plus` `(0,+1e-4)` -> A,B -> cleanup;
4. `beta_minus` `(0,-1e-4)` -> A,B -> cleanup.

Raw A/B operands stay in the same Python process. After all four roles complete, the same finite-difference pilot responses may be formed:

- `abs((alpha_minus-reference)/(-H))`;
- `abs((beta_plus-beta_minus)/(2*H))`;

in the inherited column order. Their numerical magnitudes are decision-neutral for Exp073JO. No coarse/fine convergence statistic is defined or allowed.

## Resource PASS

`CANONICAL_8193_ONE_LIVE_FOUR_BUILD_RESOURCE_PILOT_PASS_PLUS_0_PLUS_0` requires all of:

- valid independently verified JL NOT_CONVERGED activation authority;
- valid independently verified branch-disabled JM static PASS authority;
- exact canonical-8193 authority and exact committed text/decoded node SHAs;
- exact CLASS source commit and exact infrastructure capacities 9216/262144;
- exactly four completed solver constructions in the frozen role order;
- machine-counted `max_live_instances=1` and final live count zero;
- all A/B requested targets supported; unsupported count zero;
- maximum requested-node coordinate mismatch `<=1e-12`;
- finite transfer-derived operands and finite `[4,2]` A/B response arrays;
- raw operands combined only inside the same process;
- terminal artifact emitted cleanly.

Any build/runtime/runner/provenance/capacity/canonical-grid/lookup/support failure is `CANONICAL_8193_RESOURCE_OR_INFRA_NOT_FEASIBLE_PLUS_0_PLUS_0`. It is not JM scientific non-convergence and activates no denser rung.

## Anti-adaptation boundary

The pilot may not change node count, choose a host variant, reduce the 8193 grid, alter `h`, native kpd, interpolation, target set or lookup ceiling after seeing a failure. A resource failure requires diagnosis and the smallest prospective process-only repair. Scientific JM inputs remain frozen.

## After PASS

Only an independently verified Exp073JO PASS plus a fresh anti-duplication check may authorize exactly one recovered Exp073JM 4097->8193 production convergence run under the already-frozen JM science classifier.

Exp073JO remains process/resource `+0/+0` and cannot change `ARTICLE3_REPOSITORY_READINESS=68%` or funnel-freeze readiness `=67%` by itself.
