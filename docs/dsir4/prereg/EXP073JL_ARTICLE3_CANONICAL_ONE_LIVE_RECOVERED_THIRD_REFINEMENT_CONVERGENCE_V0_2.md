# Exp073JL — Article III canonical one-live recovered third-refinement convergence v0.2

Date prospectively frozen: 2026-09-11. Scope: **DSIR Article III Layer-B support convergence only**. Effect `+0/+0` regardless of CONVERGED/NOT_CONVERGED. This v0.2 is an execution recovery of the original prospectively frozen Exp073JL science, not a new scientific hypothesis or threshold.

## Activation authority

This run may be implemented/launched only because all of the following were independently verified before this preregistration was written:

1. Exp073JV authority `SEQUENTIAL_CANONICAL_JL_EXECUTION_AUTHORIZED_PLUS_0_PLUS_0` remains valid.
2. Exp073JW repaired-repeat run/job `34540689350 / 103082427124` is independently verified `CANONICAL_ONE_LIVE_EIGHT_BUILD_RESOURCE_PILOT_PASS_PLUS_0_PLUS_0`, authority file `docs/dsir4/authority/EXP073JW_ARTICLE3_CANONICAL_ONE_LIVE_EIGHT_BUILD_RESOURCE_PILOT_V0_1.json`, authority commit `1e1e3efd7de38b0d43db12e8f1dc8e1822689a70`.
3. The response-blind request-plan audit run/job `34541423516 / 103084699643` is independently verified `RESPONSE_BLIND_REQUEST_PLAN_AUDITED_PLUS_0_PLUS_0`, authority file `docs/dsir4/authority/RECOVERED_EXP073JL_RESPONSE_BLIND_REQUEST_PLAN_V0_1.json`, authority commit `b4f20c0b2e3249a449e763b3c69618abec8ac83a`.
4. Exp073JT canonical committed coarse/fine grids remain the only permitted lattice byte streams.
5. Exp073JK remains independently verified common-grid second-refinement NOT_CONVERGED and Exp073JI remains independently verified guard-node full-support feasible, preserving the original JL scientific trigger.

Historical Exp073JL v0.1 attempts produced no scientific artifact and remain infrastructure-only failures. They are not inputs to the numerical classifier except as execution-history motivation for the one-live recovery.

## Frozen scientific question — unchanged from original JL

Does the support-only Layer-B common-grid finite-difference response become numerically converged under the third prospectively frozen refinement, comparing canonical coarse 2049 nodes against canonical fine 4097 nodes, with the original Exp073IR physical atom traversal and a strict relative tolerance of `1e-3`?

No result from Exp073JW, including its decision-neutral A/B diagnostics, is scientific evidence for this question.

## Exact fixed scientific inputs

- parent Layer-A authority run/job/artifact `34423479633 / 102703685034 / 10131794281`;
- exactly 107 parent-retained coordinates, retained-ID SHA256 `44b57c6c910bc3612310ce415d773c8c180497528bfc5fc2927ce61da6ad40d7`;
- full 1410-order SHA256 `bfaf582518cdbfd34b1e8392da83dac6b0885948bc31f2c29d4e48247c23af75`;
- 53 retained DES coordinates and 54 retained BOSS coordinates;
- physical domain exactly `0.295 <= z <= 2.33` and judged `0 < k <= 0.06664762008318016 Mpc^-1`;
- DES NSIDE/ell authority unchanged: ell `0..12287`, 39 bands, Wm `TE<-TE`, WW `EE<-EE`, canonical angular arrays and Exp073IM radial authorities unchanged;
- BOSS z3 operators and masks unchanged;
- pinned CLASS-IV source commit `ac627d54e9ce196a08878d1ba33999819925d19c` with the already-authorized public `d_m` exposure/build compatibility patches only;
- input capacities `4608` and parser `131072` are infrastructure capacities only;
- nested OMP/OpenBLAS/MKL/NUMEXPR threads each exactly 1;
- native `k_per_decade_for_pk=20.0` on **both** coarse and fine architectures;
- finite-difference `H=1e-4`;
- requested-node lookup relative ceiling `1e-12`;
- centered-cubic interpolation in `ln(k)` with inherited stencil and arithmetic;
- exact inherited response components/order:
  1. `abs((alpha_minus - reference) / (-H))`,
  2. `abs((beta_plus - beta_minus) / (2*H))`,
  assembled by the inherited `column_stack` ordering;
- direct coarse/fine component comparison exactly `abs(coarse-fine) / maximum(abs(coarse),abs(fine))` after inherited finite/positive status checks;
- convergence is strict: `max_atomic_coarse_vs_fine_relative_component_difference < 1e-3`. Equality at `1e-3` is not CONVERGED.

No tolerance/rounding/smoothing/averaging/clipping/effective-coordinate/fiducial-P/support-domain/interpolation/estimator rescue is allowed.

## Exact canonical grids — no host regeneration

The solver input grids MUST be decoded from committed u64hex authority files and MUST NOT be regenerated by `np.geomspace`, `guarded_lattice`, or an equivalent host-local floating construction:

- coarse file `docs/dsir4/canonical/EXP073JL_CANONICAL_COARSE_2049_NODES_U64HEX_V0_1.txt`, 2049 nodes, text SHA256 `72c732a02b9f6f4adeab70c7792f16e4490f038544c221aed9b72a14e5b1ec47`, decoded `<f8` SHA256 `6305c95025e52296b6cb623903f82dc45bb66ac692708b242fc354862ac54c46`;
- fine file `docs/dsir4/canonical/EXP073JL_CANONICAL_FINE_4097_NODES_U64HEX_V0_1.txt`, 4097 nodes, text SHA256 `290075f777c88964aa6b670694063e9b4ad0d5d4dcb16ee12adad5103ca1b6c7`, decoded `<f8` SHA256 `f30556be2547732ed0b87b56368e01e46504a975f94fcbbc5e7bc8cfc525e5eb`.

Any mismatch is infrastructure/provenance invalidity and must stop before a scientific classification.

## Response-blind request-plan gate

Before constructing the first CLASS instance, the helper must reconstruct the exact request-plan identity and match the independently verified authority:

- coarse common plan SHA256 `505716116cc385d5700e455017d8e541c5bba7774fa6647298392495c83ecfe0`;
- fine plan including fine-only GL128 SHA256 `0f7ce2e5fa459e15954268dfd46045eaa0ab6356dfdab5a2e282514e7a34ae4e`;
- DES z nodes 2001;
- DES nonempty solver requests per role 377, empty-z count 1624;
- DES target scalars per role 64658;
- DES active retained-row bindings 13644;
- BOSS target scalar vector length 297;
- GL64 z count 64;
- fine-only GL128 z count 128;
- 441 coarse transfer requests per role;
- 569 fine transfer requests per role;
- 4040 transfer requests over all eight model-role lifetimes.

This gate is response-blind: failure invalidates infrastructure and may not be repaired by looking at or adapting to response values.

## Frozen recovered one-live execution order

Exactly eight CLASS constructions occur in one Python process. At most one CLASS instance may be live at any time. Model order is fixed.

### Coarse canonical lattice
1. `reference`: traverse exact DES request plan then BOSS GL64; store raw interpolated operands locally; cleanup.
2. `alpha_minus`: identical request plan/order; cleanup.
3. `beta_plus`: identical request plan/order; cleanup.
4. `beta_minus`: identical request plan/order; cleanup.

After all four coarse raw-role operands exist in the same process, form the unchanged response estimator. Retain only process-local coarse response information necessary for later direct comparison and row accounting.

### Fine canonical lattice
5. `reference`: traverse exact DES request plan, BOSS GL64, then BOSS GL128; cleanup.
6. `alpha_minus`: identical fine request plan/order; cleanup.
7. `beta_plus`: identical fine request plan/order; cleanup.
8. `beta_minus`: identical fine request plan/order; cleanup.

After all four fine raw-role operands exist in the same process, form the unchanged response estimator. Fine GL64 participates in the direct coarse/fine relative-component comparison. Fine GL128 is used **only** for the inherited BOSS dense-z finite/nonzero/row-label stability control and must not enter the direct relative-component maximum.

Exactly 8 solver constructions and `max_live_instances=1` must be machine-counted. Any deviation is infrastructure-invalid.

## Same-process raw-operand boundary

All raw finite-difference role operands must remain within this single Python process. Local NumPy arrays or local scratch memmaps are permitted only as backing storage inside the same running process. Raw operands may not be uploaded, checkpointed for cross-process continuation, or combined scientifically in a later run/job/process.

Only terminal classified output and non-scientific aggregate/provenance receipts may be uploaded.

## Unchanged 107-row Layer-B accounting

The recovered implementation must reproduce the Exp073IR traversal/accounting semantics exactly:

- same DES radial-positive and angular-active masks;
- same row atom counts;
- same finite-and-positive response requirement;
- same retained coordinate identities/order;
- same BOSS masks;
- same GL64 direct comparison;
- same fine-only GL128 dense-z control and `boss_dense_z_disagreement` semantics;
- same finite/nonzero-status and row-label-change checks;
- Layer-B invalid-row fraction threshold `<=0.05` and minimum retained dimension `>=15` remain inherited bookkeeping constraints.

Because Exp073JL is support-only, even a valid convergence result does **not** itself authorize covariance restriction and does not open Wm_S3. Downstream conditional branches remain separately gated.

## Frozen valid classifications

A scientifically valid terminal result may be exactly one of:

- `COMMON_GRID_THIRD_REFINEMENT_CONVERGED_PLUS_0_PLUS_0` if every inherited validity/accounting check is satisfied and the strict maximum relative-component difference is `<1e-3`;
- `COMMON_GRID_THIRD_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0` if the execution is valid but the inherited convergence/status criteria do not converge under the frozen rule.

Infrastructure/provenance/request-plan/canonical-grid/lifecycle/build/runner/timeout failures MUST use an infrastructure-invalid status distinct from scientific NOT_CONVERGED and create no scientific result.

No scientific authority is created until the terminal artifact and raw execution provenance are independently verified after the run.

## Downstream branch rule — unchanged

Only after independent verification of the recovered JL terminal result:

- valid NOT_CONVERGED may activate only the already-frozen non-convergence branch lineage (historically Exp073JM), subject to a fresh authority audit binding it to this recovered execution;
- valid CONVERGED may activate only the already-frozen convergence branch lineage (historically Exp073JN), subject to a fresh authority audit binding it to this recovered execution;
- infrastructure-invalid activates neither.

No branch is selected in advance.

## Readiness boundary

At preregistration, `ARTICLE3_REPOSITORY_READINESS=68%` and funnel-freeze readiness `=67%`. Writing/implementing this recovered execution does not increase either score. Only a terminal, independently verified JL scientific-support classification may justify reassessing the Article III frontier.
