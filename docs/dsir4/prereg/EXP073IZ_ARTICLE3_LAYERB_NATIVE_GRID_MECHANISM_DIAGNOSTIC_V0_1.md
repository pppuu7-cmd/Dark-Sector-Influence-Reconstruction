# Exp073IZ — Article 3 Layer-B native-grid convergence-mechanism diagnostic v0.1

Date frozen: 2026-09-10. Scope: DSIR Article 3 / C2 only.

Status: PROSPECTIVELY FROZEN AFTER EXP073IY `CROSS_BUILD_EXACT_PLUS_0_PLUS_0` AND BEFORE ANY EXP073IZ NUMERICAL OUTPUT.

## Bound authority

Exp073IR remains `NUMERICALLY_UNRESOLVED_EXP073IR`: 107 retained coordinates, zero invalid rows, `f_B=0`, covariance restriction unauthorized, and exact production-vs-dense maximum `0.9998247463807295 > 1e-3` under the frozen `k_per_decade_for_pk=10` versus `20` comparison.

Exp073IY run `34447756148` establishes exact cross-build reproducibility: independent hosted rebuilds A and B and the authoritative Exp073IR parent are byte-identical with output SHA256 `6dea43dcc0eadaf91887f181732d20dceef04866d5687aa231b747312fec0cb1`, despite A/B runner-image differences. Therefore the next admissible question is the numerical mechanism of the frozen convergence failure, not environment drift.

Historical Exp073IV support instrumentation localized representative extrema at:
- alpha-left component: `z=0.7000000000000001`, `k=0.002502504647141259 Mpc^-1`;
- beta-symmetric component: `z=0.9351`, `k=0.01860440444314368 Mpc^-1`.

These coordinates are used only as frozen diagnostic probes. Exp073IV remains historical `SUPPORT_INVALID_PLUS_0_PLUS_0`; these probes cannot create scientific authority or alter the Exp073IR acceptance rule.

## Purpose

Determine whether the large production/dense response discrepancy is generated by native CLASS transfer-grid interpolation/cancellation behavior as `k_per_decade_for_pk` is refined, while preserving the exact Exp073IR response definitions and finite-difference amplitude.

## Frozen execution

Use the exact pinned and audited CLASS-IV source `ac627d54e9ce196a08878d1ba33999819925d19c`, exact public gauge-invariant `d_m` exposure patch, exact baseline/precision files, and the exact Exp073IR response arithmetic.

For each of the two frozen `(z,k)` probes and each prospectively frozen native-grid density

`k_per_decade_for_pk ∈ {10, 20, 40, 80}`

compute the same four CLASS transfer models used by Exp073IR:

`(alpha,beta) = (0,0), (-1e-4,0), (0,+1e-4), (0,-1e-4)`.

For every model/grid/probe record:
1. exact native k-array length, minimum and maximum k;
2. the two native bracketing k values around the target;
3. the corresponding native `d_m` endpoint values;
4. the exact log-k linear interpolation fraction used by `np.interp(log(k_target), log(k_native), d_m_native)`;
5. the resulting interpolated `d_m` value.

Then compute exactly the Exp073IR components:
- alpha-left: `abs((d_m(-h,0)-d_m(0,0))/(-h))`;
- beta-symmetric: `abs((d_m(0,+h)-d_m(0,-h))/(2h))`;
with `h=1e-4` exactly.

Also record adjacent-grid relative differences for 10→20, 20→40 and 40→80 using the unchanged Exp073IR relative metric `abs(a-b)/max(abs(a),abs(b))` when both values are finite and positive.

## Science firewall

This is support-only `+0/+0`. It MUST NOT:
- alter `REL_TOL=1e-3`, `h=1e-4`, model parameters, redshift/k domain, source definitions, interpolation rule, atomization, row membership or acceptance criteria;
- average, smooth, round or tolerance-rescue any result;
- read covariance, whitening, nuisance, relation-null or manuscript-selection information;
- authorize covariance restriction, Layer-B PASS, model authority or manuscript claims.

The extra grid densities 40 and 80 are diagnostic samples only; they do not retrospectively redefine the frozen Exp073IR production/dense gate.

## Frozen classifications

- `NATIVE_GRID_MECHANISM_OBSERVED_PLUS_0_PLUS_0`: all requested raw native-grid/bracket/interpolation/response records are valid and complete.
- `INVALID_INFRA_PLUS_0_PLUS_0`: source/hash/build/interface/input or diagnostic execution failure prevents valid observation.

No numerical pattern is pre-labelled as scientific PASS/FAIL. Scientific interpretation occurs only after the complete diagnostic artifact is consumed. The exact next action after a valid diagnostic is to determine prospectively whether a follow-up resolution test can distinguish interpolation-grid sampling, finite-difference cancellation, or another solver-resolution mechanism without changing the original `1e-3` scientific criterion.
