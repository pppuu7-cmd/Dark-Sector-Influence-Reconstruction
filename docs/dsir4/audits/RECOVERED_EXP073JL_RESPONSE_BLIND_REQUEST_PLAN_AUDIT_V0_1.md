# Recovered Exp073JL — response-blind request-plan audit v0.1

Date frozen: 2026-09-11. Scope: DSIR Article III implementation preparation only. Effect `+0/+0`.

This audit is prospectively frozen while authoritative Exp073JW repeat `34540689350` is still running. It is **not** recovered-Exp073JL preregistration, does not authorize any CLASS execution for JL, reads no JL/JW scientific response values, creates no scientific authority, does not authorize covariance restriction and does not open Wm_S3.

## Frozen audit helper

Helper: `ci/recovered_exp073jl_response_blind_request_plan_audit_v0_1.py`.

Creation commit: `a4a4f159ea0689aeca119fc9812cf5ab152d2a14`.
Git blob identity: `7180b74641acfa602c34db07ad6cc916c17ebd4f`.

The audit imports only frozen Exp073IR/Exp073IQ geometry/accounting helpers plus the frozen CAMB radial reconstruction route. It never imports `classy`, never constructs CLASS, and never evaluates a matter-transfer response.

## Purpose

Freeze the exact response-blind request sequence that a future recovered one-live Exp073JL would have to traverse, before any recovered JL response exists. This removes ambiguity about loop inversion and prevents a later implementation from accidentally changing which physical atoms are evaluated while reducing solver lifetimes.

## Frozen inputs and geometry

The plan must reconstruct the existing authorities exactly:

- Layer-A parent run/job/artifact `34423479633 / 102703685034 / 10131794281`;
- retained count 107, retained-ID SHA256 `44b57c6c910bc3612310ce415d773c8c180497528bfc5fc2927ce61da6ad40d7`;
- full-order SHA256 `bfaf582518cdbfd34b1e8392da83dac6b0885948bc31f2c29d4e48247c23af75`;
- 53 retained DES rows and 54 retained BOSS rows;
- exact Exp073IM manifest and angular payload identities;
- fresh reconstruction of the exact fine DES radial representation already bound by Exp073IM;
- exact BOSS z3 operators already hash-guarded by Exp073IR;
- physical domain `0.295 <= z <= 2.33`, `0 < k <= 0.06664762008318016 Mpc^-1`;
- DES ell coordinates `(ell+0.5)/chi(z)` for ell `0..12287`;
- BOSS GL64 direct-comparison z nodes and fine-only GL128 dense-z-control z nodes generated by the unchanged Exp073IR helper.

## Canonical request-plan serialization

Two SHA256 streams are frozen:

1. `coarse_common_plan_sha256`: parent/domain identities, DES z/chi arrays, every DES nonempty/empty z state, exact union ell indices and target-k values, retained-row active masks on each union, exact BOSS retained IDs/needed mask/target-k vector/row masks, and GL64 z nodes.
2. `fine_plan_with_gl128_sha256`: the exact same common stream plus the fine-only GL128 z-node vector.

Each tagged payload is serialized as little-endian tag length, UTF-8 tag, little-endian payload length, then exact payload bytes. Floating arrays are canonical `<f8`; DES union indices are `<u4`; logical masks are `uint8`.

The audit result may record only geometry/request counts, byte/resource implications and these hashes. It may not inspect any CLASS response, convergence diagnostic or resource-pilot response.

## Execution-shape implication

The audit computes from the frozen plan, without invoking a solver:

- number of nonempty DES solver requests per model role;
- total DES target scalars per role;
- BOSS target count;
- exact coarse request calls per role = DES nonempty requests + 64;
- exact fine request calls per role = DES nonempty requests + 64 + 128;
- implied total get-transfer calls for four coarse and four fine roles;
- flattened `<f8` raw-operand storage sizes.

It must report the intended recovered lifecycle as exactly four coarse + four fine solver constructions, total 8, `max_live_instances=1`. This is a design implication only; feasibility remains exclusively the authority of terminal independently verified Exp073JW.

## Frozen classification

A successful plan-only audit is exactly:

`RESPONSE_BLIND_REQUEST_PLAN_AUDITED_PLUS_0_PLUS_0`.

Any parent/manifest/radial/angular/BOSS identity failure or helper/runtime failure is infrastructure-invalid and creates no request-plan authority. There is no scientific PASS/FAIL/CONVERGED/NOT_CONVERGED outcome in this audit.

## Readiness boundary

This audit cannot change `ARTICLE3_REPOSITORY_READINESS=68%` or funnel-freeze readiness `=67%`. It only precomputes an implementation invariant so that, if and only if JW later passes, recovered JL can be prospectively frozen around an already response-blind traversal identity rather than designed after seeing response values.
