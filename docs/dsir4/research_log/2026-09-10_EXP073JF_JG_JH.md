# DSIR research log — 2026-09-10 — Exp073JF/JG/JH

Scope: DSIR Article 3 / C2 only. This note is a research-log supplement; scientific authority remains controlled by prospective contracts, validated artifacts, immutable recovery notes and `docs/RECOVERY_LATEST.md`.

## Exp073JF
Repaired run/job `34480349238 / 102881291672`, artifact `10153458465`, independently verified ZIP SHA256 `f71c1603f46e622a6c71ebab6733d30e9633cc92e9dc50a98cecdf364b28fffb`. Valid support-only classification `SHARED_FIXED_K_GRID_EXTENDED_DENSITY_OBSERVED_PLUS_0_PLUS_0`.

Common fixed physical-k grid removes kpd10-vs20 dependence exactly at the two historical worst probes. However simple linear ln(k) interpolation remains above the unchanged exact-reference REL_TOL=1e-3 for beta at N=384 and N=512: errors `0.031479742832626514` and `0.026575285704422986`; alpha is already below threshold at both. No scaling candidate exists through current exact N=512 capacity.

## Exp073JG
Run/job `34485992147 / 102900288767`, artifact `10155745915`, independently verified ZIP SHA256 `e3282ea5f145a273aff63cd70ff74e23cbf6f19960bb83f8390c2c855ae70dbb`, result JSON SHA256 `3e9cd6c4395e4fc67fafd4805b8e1a0ade7e6f4239a1234be50910ff021869cf`. Valid support-only classification `LOCAL_INTERPOLATION_CURVATURE_OBSERVED_PLUS_0_PLUS_0`.

At fixed N=512 all prospectively defined higher-order local ln(k) rules pass the unchanged 1e-3 exact-k criterion at both historical probes and remain exactly invariant between kpd10 and kpd20. Relative errors `(alpha,beta)` are: quadratic-left `(1.6655427759553507e-06,6.801424456717797e-05)`, quadratic-right `(2.7311728168928298e-06,0.0002303585618472267)`, centered cubic `(1.2105247820888992e-07,0.00016088505716613062)`. Linear parent remains `(0.00010245604306152275,0.026575285704422986)`.

Interpretation: the residual shared-grid N=512 error at the historical worst probes is dominated by local interpolation curvature rather than kpd/native-density drift. This is mechanism evidence only, not Layer-B authority.

## Exp073JH
Prospectively frozen after JG validation. Centered cubic is selected structurally because its four-node stencil is balanced two-sided; it is not selected by minimizing the observed probe error. The audit applies the candidate architecture to the complete unchanged Exp073IR real atomic support for all 107 retained Layer-A rows. Targets without a complete stencil fail closed; no clipping or extrapolation is permitted. Frozen h=1e-4, REL_TOL=1e-3, z/k domain, atomization, operators and parent authorities remain unchanged. All outcomes are +0/+0.

Current authoritative run/job at time of this log: `34487047656 / 102903883547`, head `0834a45f5e767d411ce694d081069403ea6fd908`, GitHub-hosted ubuntu-24.04, home/self-hosted ownership none. Exact next action is terminal consumption and independent artifact verification. A feasible result can only permit a separately preregistered scientific Layer-B rerun; it cannot authorize covariance itself.
