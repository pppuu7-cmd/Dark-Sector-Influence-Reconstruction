# Research checkpoint — Exp068B PASS / Exp069A prereg — 2026-08-26

## Canonical state

Exp068B has completed as `PASS_ACT_UNWISE_LITERAL_PCA_PHYSICAL_FORWARD_V0_1` under the separately preregistered literal pinned-upstream tracer semantics. The physical linear/no-CLEFT raw ACT×unWISE forward bridge is therefore validated for supplied `P_WW/P_Wm/P_mm` inputs under the frozen R0 interface domain and `5e-13` raw-equivalence criterion.

Exp068A remains permanently preserved as `FAIL_ACT_UNWISE_PHYSICAL_FORWARD_REPRODUCTION_V0_1`; Exp068B is a corrective prospective experiment, not a rewrite of Exp068A.

Provenance for the successful Exp068B execution:

- workflow run: `33007727478`;
- workflow job: `98305721279`;
- artifact id: `9621695396`;
- artifact digest: `sha256:6f6cd856d6ea9f2f728fb56908f589c9e25006f3e98243776c796fcd05431d40`;
- upstream likelihood commit: `6302c30d9e70f8e4ff2d4a84a9977b4471705179`;
- CAMB commit: `fa3f097343fbbe427cc04b4f5f0041c22c6ec764`;
- official archive SHA256: `1b2d1563c5eb548ca6488ed8d60c5260d9e110b743a2e3a84620cfe46fbb6570`;
- Exp068B merge commit: `ca11c3fa67b220923d9262ebf87ffacebb189a7b`.

The first successful archive fetch also populated the immutable GitHub Actions cache keyed by the same official archive SHA256. The cache is infrastructure-only; every use remains subject to the hard SHA256 verification before science.

## Next scientific prerequisite

The G7 ordering is now:

1. physical forward interface — Exp068B PASS;
2. training-family physical power-input bridges;
3. preregistered common physical support/leakage mask;
4. covariance restriction and direct no-repair whitening;
5. nuisance tangent rank/SVD;
6. quotient, one training-only relation and frozen null/permutation control;
7. only then select a fresh G8 withheld family.

Exp069A is now preregistered as the C5 designer-f(R) physical power-input bridge. Its exact nonzero production control is frozen at `B0=1e-6`, the minimum point in the existing canonical high-precision C5 production region; `B0=1e-7` remains excluded as the known solver-threshold transition control.

Exp069A requires direct modified-solver `P(delta_nonu,delta_nonu)`, `P(Weyl,delta_nonu)` and `P(Weyl,Weyl)` with physical units, linear theory, signed cross power, a GR-limit comparison, a coherence/Cauchy control, and a deliberately wrong missing-k^2 negative control. No GR Poisson reconstruction of Weyl is allowed.

No Exp069A physical output had been evaluated when the acceptance contract and exact `B0=1e-6` value were frozen.

After Exp069A, the next power-input bridge is C3/GDM via a read-only exposure of the solver's already-computed comoving matter source `D_m`, followed by the preregistered transfer-product bridge.

## Gate state

- G7: OPEN
- G8: OPEN
- G9: OPEN

No universality/discovery claim follows from Exp068B alone.
