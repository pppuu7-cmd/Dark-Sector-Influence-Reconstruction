# DSIR research log supplement — 2026-09-06 Exp073FN / FO / FP

Scope: DSIR only.

Exp073FG `WW_S0_S3` run/job `34034377795 / 101489679508` completed as a raw-verified exact candidate PASS. Artifact `9993520467` has GitHub digest and independently re-downloaded ZIP SHA256 `8ddd1e1b81e5fa9c3a4de16c6d72b35353cb42bba04bb77c736aa4998340bde0`. Exact selected A/B SHA is `db58af980e2997ebbe327ce91dfafb682c38fda1ba841c3d5acba78e429007d3`; canonical `<f8 [39,12288]` `EE<-EE`, all finite, byte-identical and exact-array-equal. Both six-stage chains, public file-backed BPW route and `19,327,352,832`-byte mmap proof were independently validated.

A governance collision was detected: `Exp073FL` had already been assigned to the earlier S1S1 driver-generation static audit, but a later S0S3 admission implementation reused that label. Historical collided admission run/job `34047839320 / 101525992295` also failed before admission because `gh api .../logs` rejected terminal escape sequences. It remains `INFRASTRUCTURE_LOG_TRANSPORT_FAIL_PLUS_0_PLUS_0` and creates no authority.

A new unused label Exp073FN prospectively corrected only the admission implementation. Exp073FN run/job `34050154578 / 101532191756` passed the same frozen candidate evidence after the transport-only `--allow-escape-sequences` repair and emitted `PASS_EXP073FN_WW_S0_S3_FILEBACKED_PROVENANCE_ADMISSION_V0_1`; `WW_S0_S3` is therefore admitted scientific authority.

The next manifest cell is `WW_S1_S1`, preregistered as Exp073FM. Exp073FO run/job `34050224161 / 101532385479` passed hosted transformation-readiness support with token `PASS_EXP073FO_WW_S1_S1_PRODUCTION_TRANSFORMATION_READINESS_V0_1`, classification `SUPPORT_PLUS_0_PLUS_0`, no authority and no self-hosted science.

Exp073FP was then prospectively preregistered to audit exact committed S1S1 production code before any home launch. Exact committed implementation files are `ci/exp073fm_ww_s1_s1_durable_ab_production_v0_1.py` and `_v0_2.py`; the former enforces one S1 reconstruction, one spin-2 field, same-object coupling, complete six-stage restore validation and exact A/B semantics, while v0.2 restores the exact file-backed MCM byte/proc-maps proof. Exp073FP is hosted-only and may not create `WW_S1_S1` authority.
