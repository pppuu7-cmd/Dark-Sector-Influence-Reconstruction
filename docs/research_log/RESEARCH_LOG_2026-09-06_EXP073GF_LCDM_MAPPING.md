# Research log — Exp073GF LambdaCDM/GR mapping

Date: 2026-09-06. DSIR only.

Exp073FM `WW_S1_S1` remained in progress throughout this iteration and no partial numerical result was inspected.

Independent DSIR-4 work closed the first concrete existing-model mapping artifact under the frozen common residual convention. The LambdaCDM/GR symbolic baseline uses `T_known = baryons + photons + standard neutrinos`, leaving pressureless CDM and exact Lambda in `X_munu`. All six required residual components are explicit; Lambda perturbations, scalar isotropic pressure perturbation, and scalar anisotropic stress are represented as structural zeros where appropriate. The mapping covers the frozen DSIR z/k domain in linear scalar theory without quasi-static or sub-horizon assumptions.

Exp073GF run/job `34059473109 / 101557289555` raw-log token: `PASS_EXP073GF_DSIR4_LCDM_GR_MAPPING_STATIC_AUDIT_V0_1`. Classification: `SUPPORT_PLUS_0_PLUS_0`; mapping ready only; prediction not ready; no numerical evaluation; no scientific model authority.

Next: consume Exp073FM immediately when terminal. If still running, proceed only with independent prospectively frozen DSIR-4 mapping/prediction-interface work.
