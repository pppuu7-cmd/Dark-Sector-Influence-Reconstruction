# DSIR RECOVERY MANUAL — live 2026-08-30 overlay

This is the active overlay to `docs/RECOVERY_MANUAL.md`.

Read it together with:

1. `docs/RECOVERY_LATEST.md`;
2. `docs/DSIR_CROSS_CHAT_AUTHORITY_CONSOLIDATION_2026-08-30.md`;
3. `docs/DSIR_ALL_CHAT_REPOSITORY_RECONCILIATION_2026-08-30.md`;
4. the newest recovery checkpoint;
5. the Exp073AT/AU/AV/AW/AX addenda where exact gate details are needed.

Repository/hosted authority outranks chat wording. RTK and RQIR are excluded from DSIR authority.

## 1. Active scientific state

- Article-2 repository-for-writing readiness: **100%** for its declared manuscript scope only.
- Strict Article-3 scientific repository readiness: **52%**.
- Layer A: OPEN.
- Layer B: OPEN.
- covariance/whitening: BLOCKED.
- G7: OPEN.
- G8: OPEN.
- G9: OPEN.
- synthetic, infrastructure, governance, provenance and numerical-QA PASSes add `+0` scientific-readiness points.

The next possible Article-3 scientific-readiness increase still requires the **real complete pre-support finite-operator candidate manifest** and its authorized accounting route.

## 2. Historical scientific negatives that must not be erased

The all-chat reconciliation re-confirmed several older results that are easy to lose in top-level summaries:

- F27 / Exp054C: prospective C7 IDM–DR falsification of the frozen quantitative `R^2`-centroid common-slope law; all four adjacent slopes had the wrong sign. Immutable status `FAIL_IDM_DR_COMMON_SOURCE_RESPONSE_SLOPE_V0_1`.
- F28 / Exp055A: positive **retrospective** endpoint-half-transition candidate on already-seen C3/C5/C7; not withheld evidence and not a negative control.
- F29 / Exp056B: prospective C8 IDM–photon hard scientific FAIL of the F28 half-transition sign relation. All 35 crossings existed, but two of four adjacent `C50` slopes were negative; failure survived leave-one-redshift checks.
- F30 / Exp061A: genuine withheld C9 IDM–baryon hard prospective PASS for a pre-frozen two-coordinate localization+shape/orientation path representation. It does not erase F27/F29 and does not close G7/G8/G9.
- F31 / Exp064A: DESI ShapeFit AP/growth/shape common-plane candidate failed the frozen covariance-null nontriviality criterion (`p_lambda≈0.26534`, `p_LOO≈0.36123`); result `NO_NONTRIVIAL_COMMON_PLANE_RELATION_V0_1`.
- Exp068A: permanent frozen scientific FAIL due to incorrect PCA-column semantics, despite exact upstream raw-forward equivalence. Never overwrite it with Exp068B.
- Exp068B: separate corrected literal-PCA PASS for the declared raw physical forward bridge only.
- Exp071C: known-sector K2 baryon/CDM redistribution at fixed total matter passed the matter-only F30 path, yielding `F30_DARK_SPECIFICITY_WEAKENED_BY_KNOWN_SECTOR_CONTROL`. Do not retrain F30 to reject K2.
- Exp071L/M/N: later representation/resolvability and ray/line/subspace corrections supersede any early “unique fingerprint” interpretation.

For exact provenance and numbers, read `docs/DSIR_ALL_CHAT_REPOSITORY_RECONCILIATION_2026-08-30.md`.

## 3. Frozen Article-3 scientific/support boundaries

Never alter post hoc:

- `0.295 <= z <= 2.33` inclusive;
- `0 < k <= 0.06664762008318016 Mpc^-1`;
- Layer-A `operator_f_invalid <= 0.05` inclusive;
- Layer-B invalid observation-row fraction `<= 0.05` inclusive;
- final retained observation dimension `>= 15`;
- DES classifying route `NSIDE=4096`;
- true ell `0..12287`, 39 frozen bandpowers, PyMaster/NaMaster 2.7 lineage;
- Wm selected component `TE <- TE`; WW selected component `EE <- EE`;
- canonical selected window `<f8 [39,12288]`;
- positive absolute operator/window envelope only for support bookkeeping; measured Wm remains signed;
- no effective ell/z/k shortcut;
- no fiducial-P weighting;
- no covariance, whitening, nuisance SVD/rank, quotient, relation, null or G8 information during support selection;
- exact-threshold numerical ambiguity remains `numerically_unresolved` and blocks classification rather than being rounded.

## 4. Historical Article-3 authority/reproducibility records

Preserve separately:

- Exp073X: `INCOMPLETE_INFRASTRUCTURE_RESOURCE_CANCELLED_NO_AUTHORITY_REUSE`; no partial output reuse.
- historical primary P Wm_S0 exact SHA `6ec29f6dbbcf0c29d7af9d6eb98d271bcd06e00d28cebe658b5e266f7ef18d0f`; historical-route authority only.
- historical Q: immutable `SCIENTIFIC_REPEATABILITY_FAIL`; computational repeatability, not dark-sector model physics.
- Exp073AH2: `WORKSPACE_OUTPUT_ONLY_NUMERICAL_DIVERGENCE`, with no frozen input/contract drift detected.
- historical Exp073AF remains blocked by `P PASS + Q SCIENTIFIC_REPEATABILITY_FAIL`.
- Exp073AG historical synthetic aggregate-schema QA remains valid history but is superseded for future execution-qualified aggregation by Exp073AR.
- Exp073AI original aggregator environment-path failure occurred before repeatability classification and remains infrastructure-INCOMPLETE.

Historical failures are never erased by later route succession.

## 5. Controlled single-thread exact successor authority

Exp073AM repaired aggregation prospectively while reusing immutable controlled replicas:

- run `33321661835`;
- job `99284585530`;
- artifact `9735051043`;
- digest `sha256:167c82d36266efc3b7bd058f0cc307ec636b6c8efdb6b39b6e88f52d6edb3d66`;
- token `PASS_EXP073AI_SINGLE_THREAD_EXACT_REPRODUCIBILITY_V0_1`;
- canonical Wm_S0 SHA `8ac59fc060195addcc5cd8b6d75e32fbc6dbfeea8456f4c83e8bf0cf034b9220`;
- exact A/B equality, `numpy.array_equal=true`, differing entries `0/479232`.

Exp073AN classified the observed route dependence as

`DETERMINISTIC_SINGLE_THREAD_ROUTE_BUT_EXACT_AUTHORITY_SHIFT_FROM_PRIMARY_P`.

Exp073AO/AP authorize only prospective authority class

`controlled_single_thread_exact_v1`.

No tolerance, ULP, rounding, majority-vote, preferred-replica, or closeness-to-historical-P rescue exists.

Every future angular replica must use exactly:

- `OMP_NUM_THREADS=1`
- `OPENBLAS_NUM_THREADS=1`
- `MKL_NUM_THREADS=1`
- `NUMEXPR_NUM_THREADS=1`
- `VECLIB_MAXIMUM_THREADS=1`
- `BLIS_NUM_THREADS=1`
- `OMP_DYNAMIC=FALSE`

and each task requires two independent hosted replicas, exact canonical SHA equality and `numpy.array_equal == True`.

## 6. Exp073AQ — active real Wm_S1 gate

Frozen run:

`33327372191`.

Latest reconciliation inspection:

- replica A job `99299799192`: **IN_PROGRESS**, exact Wm_S1 compute;
- replica B job `99299799338`: **IN_PROGRESS**, exact Wm_S1 compute;
- no valid AQ comparator authority artifact yet.

Do **not** launch Wm_S2 and do not duplicate Wm_S1 while AQ is active.

Only valid terminal comparator classes are:

- `PASS_EXP073AQ_WM_S1_CONTROLLED_TWIN_EXACT_V0_1`;
- `SCIENTIFIC_REPEATABILITY_FAIL_EXP073AQ_WM_S1_CONTROLLED_TWIN_EXACT_V0_1`.

A failure before valid comparator classification is infrastructure-INCOMPLETE. Exact PASS admits Wm_S1 only and adds `+0` readiness.

Remaining task order after Wm_S1:

`Wm_S2, Wm_S3, WW_S0_S0, WW_S0_S1, WW_S0_S2, WW_S0_S3, WW_S1_S1, WW_S1_S2, WW_S1_S3, WW_S2_S2, WW_S2_S3, WW_S3_S3`.

## 7. Successor downstream firewalls AR–AX

All of the following are **hosted synthetic/non-scientific +0 readiness** prerequisites unless explicitly stated otherwise.

### Exp073AR — execution-qualified 14-window aggregate schema

- run `33327870470`;
- artifact `9736757853`;
- 24/24 synthetic checks passed.

Future real aggregate must use Exp073AM Wm_S0 plus 13 separately admitted `controlled_single_thread_exact_v1` task authorities. Historical P is not the successor aggregate anchor.

### Exp073AS — complete 1410-row pre-support join schema

- run `33330144734`;
- artifact `9737392901`;
- 24/24 synthetic checks passed.

This is the successor schema for joining the complete exact 14-window authority to the immutable upstream 1410-row skeleton and other pre-support authorities. It does not itself create the real candidate manifest.

### Exp073AT — candidate -> Layer-A admission

- run `33331600899`;
- artifact `9737797541`;
- 24/24 synthetic checks passed.

Real Layer A remains blocked until the exact complete candidate manifest exists under the successor authority chain.

### Exp073AU — Layer-A PASS -> Layer-B admission

- run `33332508516`;
- artifact `9738046768`;
- 26/26 synthetic checks passed.

Layer B may only follow a valid real Layer-A PASS on the same immutable candidate authority.

### Exp073AV — covariance/whitening read admission

- run `33332732811`;
- job `99314123379`;
- artifact `9738105208`;
- digest `sha256:96d708a11f2b631aa4e75b121ab2fc3b8aab4fd724b6df3b0d834ce637ff3933`;
- 26/26 synthetic checks passed.

Real covariance numerical contents remain blocked until real same-authority Layer-A PASS and Layer-B PASS exist, final retained dimension is at least 15, and there are zero threshold ambiguities. Covariance contract remains raw-symmetry gate + unrescued Cholesky, no jitter/clipping/nearest-SPD/pseudowhitening/mode rescue.

### Exp073AW — target-independent nuisance SVD rank/resolution rule

- run `33332915782`;
- job `99314619888`;
- artifact `9738157877`;
- digest `sha256:ad9c924f582d07bcb5ac72791a70f762f2a7e9e36acddff117eeda770c0b6de2`;
- 20/20 synthetic checks passed.

For future whitened nuisance matrix `N_w` of shape `(d,m)`:

`tau_rank = eps64 * max(d,m) * sigma_max`.

Retain a singular mode only if `sigma_i > tau_rank`; exact equality is `NUMERICALLY_UNRESOLVED_NUISANCE_RANK_BOUNDARY` and blocks quotient/G7. SVD is required; inversion of `N_w^T N_w` for rank is forbidden.

A non-authoritative duplicate AW branch was removed from the live tree; its history remains preserved as infrastructure/synthetic history. Disposition commit `d3c7211a9b05c9eecbdef1a2776dc0270d62d1d3`.

### Exp073AX — G7 relation/statistic/null/fit protocol admission

Frozen chain culminated in:

- run `33333749461`;
- job `99316853350`;
- artifact `9738393252`;
- digest `sha256:4693fe35cf7666530167d164b3b194be07948083fbadded3e10e96ee1e514af0`;
- token `PASS_EXP073AX_G7_RELATION_NULL_PROTOCOL_ADMISSION_SYNTHETIC_V0_1`;
- 29/29 synthetic checks passed.

Exp073AX does **not** define or fit a physical G7 relation. A future separate concrete protocol must be content-hash frozen before fit and specify:

- exact discovery/training manifest with no withheld family;
- exact mathematical relation form;
- exact scalar statistic;
- exact decision/tolerance/equality rule;
- exact null/permutation/control construction;
- deterministic fit rule;
- exact real successor-quotient authority requirement.

Before and during G7 fitting:

`withheld_family_selected=false`, `withheld_reads=[]`, `G8_reads=[]`.

After fitting, relation parameters, discovery statistic and null-control output must be frozen in a hosted immutable relation-freeze artifact **before** selecting/exposing a fresh withheld G8 family.

No G7 outcome authorizes causal, universal-law, microscopic-identity or fundamental-theory claims.

## 8. Current authorized route

1. resolve Exp073AQ Wm_S1;
2. only after exact AQ PASS, prospectively freeze/run Wm_S2 controlled twins;
3. continue the remaining angular tasks one-by-one in frozen order;
4. combine Exp073AM Wm_S0 + 13 admitted tasks into the real Exp073AR execution-qualified 14-window authority;
5. execute real Exp073AS complete immutable 1410-row pre-support candidate manifest;
6. Exp073AT -> real Layer A;
7. Exp073AU -> real Layer B;
8. Exp073AV -> real covariance restriction + unrescued Cholesky whitening;
9. Exp073AW -> target-independent nuisance rank/resolution, signed nuisance SVD/projector and real quotient;
10. prospectively freeze a concrete Exp073AX-compliant G7 relation/statistic/decision/null/fit protocol;
11. execute G7 and freeze the fitted relation immutably;
12. only then select/expose a fresh withheld G8 family.

No downstream information may choose an upstream support set, threshold, nuisance rank, relation form or withheld family.

## 9. All-chat reconciliation result

The 2026-08-30 audit across DSIR chat eras found no material scientific result that is both scientifically consequential and still lacks repository backing.

It did find:

- underrepresented F27–F31 and Exp071C history, now indexed in `docs/DSIR_ALL_CHAT_REPOSITORY_RECONCILIATION_2026-08-30.md`;
- a chat-memory misdescription of Exp055A/F28, corrected in favor of repository authority;
- this live manual lagging behind `RECOVERY_LATEST` through AS–AX, now repaired.

Any future chat-only recollection without repository/hosted backing is `CHAT_DERIVED_NON_AUTHORITATIVE` and cannot change gates/readiness.

## 10. Recovery read order

1. `docs/RECOVERY_MANUAL.md`
2. `docs/RECOVERY_MANUAL_LIVE_2026-08-30.md`
3. `docs/RECOVERY_LATEST.md`
4. `docs/DSIR_ALL_CHAT_REPOSITORY_RECONCILIATION_2026-08-30.md`
5. `docs/DSIR_CROSS_CHAT_AUTHORITY_CONSOLIDATION_2026-08-30.md`
6. newest file under `recovery/` referenced by `RECOVERY_LATEST`
7. `docs/RECOVERY_MANUAL_ADDENDUM_EXP073AT_2026-08-30.md`
8. `docs/RECOVERY_MANUAL_ADDENDUM_EXP073AU_2026-08-30.md`
9. `docs/RECOVERY_MANUAL_ADDENDUM_EXP073AV_2026-08-30.md`
10. `docs/RECOVERY_MANUAL_ADDENDUM_EXP073AW_2026-08-30.md`
11. `docs/RECOVERY_MANUAL_ADDENDUM_EXP073AX_2026-08-30.md`
12. the frozen experiment preregistration for the next authorized gate.
