# DSIR RECOVERY MANUAL — live overlay, synchronized 2026-08-31

This file is the compact active overlay to `docs/RECOVERY_MANUAL.md`. Detailed historical records remain in Git history and the referenced reconciliation/addendum files; this overlay does not reclassify or erase any earlier PASS/FAIL/INCOMPLETE state.

## Read order

1. `docs/RECOVERY_LATEST.md`
2. `recovery/2026-08-31_exp073ay_runtime_budget_prereg_aq_active_forecast.md`
3. `experiments/073ay_article3_controlled_twin_runtime_budget_policy_v0_1_prereg.md`
4. `docs/ARTICLE3_52_PERCENT_BARRIER_FORECAST_2026-08-31.md`
5. `docs/DSIR_ALL_CHAT_REPOSITORY_RECONCILIATION_2026-08-30.md`
6. `docs/RECOVERY_MANUAL_ADDENDUM_EXP065B_EXP067E_2026-08-30.md`
7. `docs/DSIR_CROSS_CHAT_AUTHORITY_CONSOLIDATION_2026-08-30.md`
8. Exp073AT/AU/AV/AW/AX recovery addenda where exact downstream rules are needed.

Repository/hosted authority outranks chat wording. RTK and RQIR are excluded from DSIR authority/readiness.

## Active scientific state

- Article-2 repository-for-writing readiness: **100%** for declared manuscript scope only.
- Strict Article-3 scientific repository readiness: **52%**.
- Layer A: OPEN.
- Layer B: OPEN.
- covariance/whitening: BLOCKED.
- G7: OPEN.
- G8: OPEN.
- G9: OPEN.
- synthetic/infrastructure/governance/provenance/numerical-QA/forecast work: `+0` readiness.

The next possible readiness increase requires the **real complete pre-support finite-operator candidate manifest** under the authorized execution-qualified angular chain.

## Historical negative/supersession states that must never be erased

- F27 / Exp054C: prospective C7 falsification of the frozen quantitative `R^2`-centroid common-slope law.
- F28 / Exp055A: positive retrospective half-transition candidate only; not withheld evidence.
- F29 / Exp056B: prospective C8 IDM-photon hard FAIL of that relation.
- F30 / Exp061A: genuine withheld C9 multicoordinate path PASS, but not universal-law/G7 closure.
- F31 / Exp064A: ShapeFit common-plane candidate rejected by frozen covariance null.
- Exp066B: permanent selected-bandpower closure FAIL.
- Exp066C: separate exact shot-noise-template corrective PASS; does not reclassify Exp066B.
- Exp067B: permanent CAMB<->CLASS convention HARD FAIL under its frozen coherence threshold.
- Exp067C/D: native-CAMB numerical-floor localization and causal float32-first multiplication diagnosis.
- Exp067E: separate preregistered out-of-sample physical convention PASS; Exp067B remains HARD FAIL.
- Exp068A: permanent PCA-semantics FAIL.
- Exp068B: separate corrected literal-PCA PASS.
- Exp071C: known-sector K2 control weakens dark-specific interpretation of F30.
- Exp071L/M/N: representation resolvability and ray/line/subspace geometry supersede early unique-fingerprint rhetoric.
- historical Exp073X: infrastructure-INCOMPLETE; no partial reuse.
- historical Exp073X2 Q: immutable computational `SCIENTIFIC_REPEATABILITY_FAIL`.
- later controlled-route success never erases historical P/Q route records.

## Frozen Article-3 support boundaries

Never alter post hoc:

- `0.295 <= z <= 2.33` inclusive;
- `0 < k <= 0.06664762008318016 Mpc^-1`;
- Layer-A `operator_f_invalid <= 0.05` inclusive;
- Layer-B invalid observation-row fraction `<= 0.05` inclusive;
- final retained observation dimension `>= 15`;
- DES classifying route `NSIDE=4096`;
- true ell `0..12287`, 39 frozen bandpowers;
- Wm selected component `TE <- TE`;
- WW selected component `EE <- EE`;
- canonical selected window `<f8 [39,12288]`;
- positive absolute operator/window envelope only for support bookkeeping; measured Wm remains signed;
- no effective ell/z/k shortcut;
- no fiducial-P weighting;
- no covariance/whitening/nuisance SVD/rank/quotient/relation/null/G8 information during support selection;
- exact-threshold ambiguity is `numerically_unresolved`, never rounded to PASS/FAIL.

## Controlled single-thread exact authority

Exp073AM controlled Wm_S0 anchor:

`8ac59fc060195addcc5cd8b6d75e32fbc6dbfeea8456f4c83e8bf0cf034b9220`.

Authority class:

`controlled_single_thread_exact_v1`.

Every successor task requires two independent hosted replicas and exact:

- `OMP_NUM_THREADS=1`
- `OPENBLAS_NUM_THREADS=1`
- `MKL_NUM_THREADS=1`
- `NUMEXPR_NUM_THREADS=1`
- `VECLIB_MAXIMUM_THREADS=1`
- `BLIS_NUM_THREADS=1`
- `OMP_DYNAMIC=FALSE`
- canonical SHA equality
- `numpy.array_equal == True`.

No tolerance, ULP, rounding, majority vote, preferred replica or closeness-to-historical-P rescue exists.

## Exp073AQ — active Wm_S1 production

Frozen run:

`33327372191`.

Latest inspection:

- replica A `99299799192`: `IN_PROGRESS` in exact Wm_S1 compute;
- replica B `99299799338`: `IN_PROGRESS` in exact Wm_S1 compute;
- no valid comparator authority existed.

Run start: `2026-08-30T18:12:00Z` / `2026-08-30 21:12 Europe/Helsinki`.

Current AQ replica timeout is 240 minutes, so its current workflow budget ends around `2026-08-31 01:12 Europe/Helsinki` unless compute completes first.

Do not launch Wm_S2 while AQ is active.

Valid comparator states only:

- `PASS_EXP073AQ_WM_S1_CONTROLLED_TWIN_EXACT_V0_1`;
- `SCIENTIFIC_REPEATABILITY_FAIL_EXP073AQ_WM_S1_CONTROLLED_TWIN_EXACT_V0_1`.

Failure/timeout before comparator remains infrastructure-INCOMPLETE.

## Exp073AY — runtime-budget policy

Prospectively frozen before any AQ output at commit:

`3aeffe02afd44c5474cc15cc53007f9beec2b160`.

For new separately preregistered successor angular tasks:

`timeout-minutes: 360`

on unchanged `ubuntu-24.04` standard GitHub-hosted route.

Nothing scientific/numerical changes: same PyMaster 2.7 lineage, same physical runner, same thread controls, same exact comparator, fresh twin replicas, no partial reuse, +0 readiness.

If AQ reaches valid comparator PASS/FAIL, no Wm_S1 recovery is needed. If AQ ends before comparator as infrastructure-INCOMPLETE, only a separately frozen fresh Wm_S1 recovery may use the 360-minute budget. If 360 minutes is insufficient, no silent runner/hardware/algorithm change is allowed; a separately numbered prospective authority succession is required.

## 52% barrier forecast

Operational planning only, not authority:

- earliest plausible: **1-2 September 2026**;
- central estimate: **2-4 September 2026**;
- infrastructure-risk case: **4-8 September 2026 or later**.

The required minimal chain is:

`Wm_S1 -> Wm_S2 -> Wm_S3 -> 10 WW tasks -> real Exp073AR 14-window aggregate -> real Exp073AS complete immutable 1410-row pre-support candidate manifest`.

Individual angular PASSes are +0 readiness. The candidate manifest is the first stage at which moving above 52% becomes eligible under current frozen accounting.

## Downstream successor firewalls already frozen

- Exp073AR: execution-qualified 14-window aggregate schema.
- Exp073AS: complete 1410-row pre-support join schema.
- Exp073AT: candidate -> Layer-A admission.
- Exp073AU: Layer-A PASS -> Layer-B admission.
- Exp073AV: same-authority A+B PASS -> covariance-read admission; raw symmetry + unrescued Cholesky only.
- Exp073AW: target-independent nuisance numerical-rank/resolvability rule.
- Exp073AX: G7 relation/statistic/null/fit protocol-admission firewall.

Exp073AW rank threshold:

`tau_rank = eps64 * max(d,m) * sigma_max`.

Retain singular mode only if `sigma_i > tau_rank`; equality is numerically unresolved. SVD required; normal-equation inversion for rank forbidden.

Exp073AX requires a future concrete content-hash-frozen G7 protocol before fitting. During discovery/fit:

`withheld_family_selected=false`, `withheld_reads=[]`, `G8_reads=[]`.

A fitted relation/null result must be frozen immutably before a fresh G8 family is selected/exposed.

## Current authorized order

1. resolve Exp073AQ Wm_S1;
2. if exact PASS: admit Wm_S1 +0 and separately preregister Wm_S2 with Exp073AY 360-minute infrastructure budget;
3. if exact repeatability FAIL: preserve FAIL and block successors;
4. if infrastructure-INCOMPLETE before comparator: fresh separately frozen Wm_S1 recovery only, no partial reuse;
5. continue Wm_S2, Wm_S3 and ten WW tasks in frozen order, each by exact controlled twins;
6. build real Exp073AR 14-window authority;
7. build real Exp073AS 1410-row candidate manifest;
8. only then score the first legitimate readiness move above 52%;
9. proceed real Layer A -> Layer B -> covariance/whitening -> nuisance quotient -> G7 -> frozen relation -> fresh G8.
