# DSIR RECOVERY MANUAL — live 2026-08-30 overlay

This file is the current-state overlay to the stable historical `docs/RECOVERY_MANUAL.md`. Read the historical manual for derivations and long-lived methodology, then this overlay, `docs/RECOVERY_LATEST.md`, and the cross-chat consolidation for the active Article-3 frontier.

## Active scientific/publication state

- Strict Article-3 scientific repository readiness: **52%**.
- Article-2 repository-for-writing readiness: **100%** for its declared manuscript scope under the 2026-08-28 closure; this is not global G7/G8/G9 closure.
- G7 = OPEN. G8 = OPEN. G9 = OPEN.
- covariance/whitening remains blocked until both real Layer A and real Layer B have passed under the frozen anti-leakage ordering.
- no synthetic/infrastructure QA result may increase scientific readiness.

## Cross-chat recovery/supersession rule

The durable reconciliation of useful DSIR material recovered from the related chats is:

`docs/DSIR_CROSS_CHAT_AUTHORITY_CONSOLIDATION_2026-08-30.md`.

Use repository authority, not remembered chat language, to resolve conflicts:

1. immutable hosted evidence and prospective freezes outrank chat wording;
2. later explicit supersession documents outrank earlier interpretations;
3. chat-only results remain contextual until repository evidence is located;
4. scientific PASS/FAIL, `INVALID_FOR_SCIENCE`, numerical implementation failure, infrastructure-INCOMPLETE and synthetic/infrastructure QA are distinct;
5. negative/infrastructure records are preserved rather than erased by later successful routes;
6. RTK and RQIR are separate projects and contribute no DSIR authority/readiness.

For DSIR-2 specifically, do not restore the early unique-fingerprint interpretation. The current hierarchy is:

`representation -> resolvability -> ray/line/subspace -> channel-conditioned equivalence -> physical support -> observational quotient`.

Exp071M representation-kernel and Exp071N two-sided nuisance-overlap constraints remain part of the canonical interpretation.

## Frozen support boundaries

- redshift support: `0.295 <= z <= 2.33`, inclusive;
- wavenumber support: `0 < k <= 0.06664762008318016 Mpc^-1`;
- Layer-A operator invalid fraction: `<= 0.05`, inclusive;
- Layer-B invalid observation-row fraction: `<= 0.05`, inclusive;
- minimum retained observation dimension: `15`;
- classifying DES angular route: `NSIDE=4096`;
- use positive absolute operator/window envelopes only for support bookkeeping; measured Wm is signed;
- no effective ell/z/k shortcut;
- no fiducial-P weighting;
- no covariance/whitening, nuisance SVD/rank, quotient/relation/null, or G8 information during support selection;
- exact-threshold numerical ambiguity must remain `numerically_unresolved`, never rounded into PASS/FAIL.

## Current established non-classifying authority chain

- Exp073R1 hosted real DES source-mask reproduction;
- Exp073P hosted prerequisite receipt;
- Exp073U immutable 1410-row observation skeleton;
- Exp073V broad-row support schema;
- Exp073W BOSS k-compatibility authority;
- Exp073Y exact DES released n(z) inventory;
- Exp073Z2 stable-direct DES radial authority;
- Exp073AB row-to-operator mapping authority.

Exp073AD is hosted synthetic boundary-classifier QA only. It verifies that ambiguous exact-5% cases become `numerically_unresolved`; it is not a science PASS and adds zero readiness.

Exp073Z v0.1 remains a numerical implementation failure, not science. Its replacement by Z2 does not erase that historical classification.

## Angular authority frontier

### Exp073X — retained incomplete infrastructure record

Exp073X attempted Wm_S0 exact real-DES repeatability by computing two full workspaces sequentially in one hosted job and persisting only after both. Hosted run `33277263287`, job `99166064222`, did not yield a reusable final authority. Preserve it exactly as:

`INCOMPLETE_INFRASTRUCTURE_RESOURCE_CANCELLED_NO_AUTHORITY_REUSE`.

Do not reinterpret it as scientific FAIL and do not reuse a partial Wm_S0 calculation.

### Exp073X2 Chain P — primary

This chain preserves the exact angular contract and changes only execution/persistence topology:

- prereg `efe8a4e17638dfd9568fa710e24f56cd10526c6a`;
- replica code `df2eecd73ed0d8de080348ba155a2f1a3e84d7e1`;
- aggregator code `8ec6f94ea9ddf3cc0a4c98e5af696d28d995b2b3`;
- workflow `a14047090d46e024965d1bd76b60830ef21616e9`;
- workflow freeze `5bd0ba084b00d963c670db6d04b1db6ea53e8f36`;
- execution trigger/head `2403d9680e1d08a3853084034eb2878faa52b4e0`;
- hosted run `33300997298`, started `2026-08-30T08:12:01Z`.

Replica A and B independently reconstruct masks and compute one exact `NSIDE=4096`, NaMaster-2.7, 39-band, `TE <- TE` Wm_S0 window each, persisting their own outputs before aggregation.

### Exp073X2 Chain Q — prospectively governed contingency/redundant chain

A second X2 chain was already launched before duplicate detection. Preserve it rather than silently dropping it:

- prereg `29740bea67bb02e7e8f4ae80d8e6ebc633754cf5`;
- implementation `09e9cdb5b9e50531ca3e6ecb8bdda48a520161d8`;
- final pre-freeze workflow `c8deb4f4489f13416a613aa96711ee35207fa84f`;
- workflow freeze `599d7ca509a716a1f7ad29c07bdc5d8bf4da74ae`;
- execution trigger/head `730ae4951ab8cd8e1dd2c392e991c3120345678a`;
- hosted run `33301058260`, started `2026-08-30T08:13:31Z`.

### Binding X2 authority rule

1. Chain P is primary.
2. P aggregator PASS -> P canonical; Q cannot displace it.
3. P scientific/repeatability mismatch FAIL -> Q PASS cannot bypass the FAIL.
4. P infrastructure-INCOMPLETE before classification -> Q may serve only as prospectively authorized fallback if Q reaches its own frozen aggregator PASS.
5. both PASS -> P remains canonical and the two canonical hashes must agree; nominal PASS/hash disagreement blocks production.
6. both infrastructure-INCOMPLETE -> new prospective infrastructure repair required.

At this manual update, both P (`33300997298`) and Q (`33301058260`) remain in progress. Do not launch a third X2.

X2 PASS alone adds zero scientific-readiness points and leaves G7/G8/G9 open.

## Exp073AE — strict pre-support authority-join firewall

While both heavy X2 chains were active, an independent lightweight prerequisite was prospectively frozen and executed: Exp073AE.

Purpose: validate the exact structural schema that a future real pre-support authority join must satisfy **without** observing X2 output or evaluating support/covariance/G8.

Frozen chain:

- prereg `6d554f96e5fb331c2fcc7920e172af7e35289886`;
- implementation `122d80712d631349a3bfd60590bfbf42edf8321a`;
- workflow `5559707db4c2d918f3a5e95f58af63ad6ff38e62`;
- workflow freeze `839d2f001e5a43ef52349cccafbbb604525ffe97`;
- trigger/head `374ebd6692d37cdc63a51dfb51249eef34dfb50b`;
- hosted run `33301598268`, job `99230706936`;
- run started `2026-08-30T08:26:46Z`, completed success `2026-08-30T08:26:57Z`;
- artifact `9729115927`;
- digest `sha256:57a16aa9d95b13278f7abb2497edb9f1c8d5a6714612b35c43c5cc214e632117`;
- token `PASS_EXP073AE_ARTICLE3_PRESUPPORT_AUTHORITY_JOIN_SCHEMA_SYNTHETIC_V0_1`.

Classification:

`HOSTED_SYNTHETIC_PASS_NON_SCIENTIFIC_PLUS_0_READINESS`.

The schema freezes exact U/Z2/AB/W upstream identities, the exact 14-task angular ordering, canonical selected-window dtype/shape/hash format, `Wm_S0 -> canonical_exp073x2`, the other 13 -> `exp073aa`, readiness 52%, G7/G8/G9 OPEN and all support/covariance/nuisance/G8/science flags false. Unknown fields and preregistered drift cases fail closed.

Do not misread Exp073AE as a real operator join or a support result.

## Frozen 14-task angular order

1. `Wm_S0`
2. `Wm_S1`
3. `Wm_S2`
4. `Wm_S3`
5. `WW_S0_S0`
6. `WW_S0_S1`
7. `WW_S0_S2`
8. `WW_S0_S3`
9. `WW_S1_S1`
10. `WW_S1_S2`
11. `WW_S1_S3`
12. `WW_S2_S2`
13. `WW_S2_S3`
14. `WW_S3_S3`

Only a valid canonical X2 result may occupy `Wm_S0`; Exp073AA is authorized afterward only for the remaining 13 tasks. Each selected exact angular authority is `<f8 [39,12288]` over true ell `0..12287`; no effective-ell substitute is allowed.

## Successor ordering

Do not duplicate X2 while either heavy chain is active.

After resolving P/Q under the frozen selection rule:

1. bind the exact canonical X2 Wm_S0 replacement authority;
2. execute the remaining 13 exact DES angular tasks under the already preregistered Exp073AA semantics;
3. join all 14 ordered angular authorities;
4. perform the strict real pre-support join under Exp073AE with Exp073Z2 radial authority, Exp073AB mapping, Exp073W BOSS authority and the Exp073U 1410-row skeleton;
5. freeze the complete 1410-row pre-support finite-operator candidate manifest;
6. only then run real Layer A broad support;
7. freeze `S_op` in inherited Exp073U order;
8. run real Layer B common-response validity;
9. only after both support layers PASS may covariance restriction and unrescued Cholesky whitening begin;
10. nuisance tangent representation/resolvability, signed SVD/rank, quotient/relation/null and fresh G8 remain later stages.

No support/covariance/nuisance/G8 quantity may choose an earlier candidate set, authority, threshold or representation post hoc.

## Exact upstream identities for the eventual real join

### Exp073U

- run `33274852199`, job `99159670108`, artifact `9721184683`;
- digest `sha256:d44e628e9312fb5a919a6681b69d9e06e18418cdd299de641e6465e60dadfd68`;
- row count `1410`;
- order `Wm[780] -> WW[390] -> BOSS[240]`;
- ordered-ID SHA256 `bfaf582518cdbfd34b1e8392da83dac6b0885948bc31f2c29d4e48247c23af75`.

### Exp073Z2

- run `33279208949`, job `99171355322`, artifact `9722468056`;
- digest `sha256:3eb8b025711e8df6d5452a3a57002f36c9d7de2b9116734b71d15d6822dd20be`.

### Exp073AB

- run `33279639316`, job `99172491781`, artifact `9722589222`;
- digest `sha256:e7bc461eb2066067ac356a23eb073218401181070350bd3ab37555a0b9d66fd4`.

### Exp073W

- run `33277001376`, job `99165356858`, artifact `9721800577`;
- digest `sha256:b4d6207bda8f7fd9f446609faecfba9adb8fe1783f0e84ec3814be06f3fcac8b`.

The retained BOSS `54/240` support mask must not be used to select the 1410 pre-support rows.

## Separation rule

DSIR is independent of RTK and RQIR. Do not import their theory assumptions, gates, priors, code authority, thresholds or readiness accounting into DSIR.

## Recovery read order for the active frontier

1. `docs/RECOVERY_MANUAL.md` — stable derivations and long-term rules;
2. `docs/RECOVERY_MANUAL_LIVE_2026-08-30.md` — this live overlay;
3. `docs/RECOVERY_LATEST.md` — shortest current pointer;
4. `docs/DSIR_CROSS_CHAT_AUTHORITY_CONSOLIDATION_2026-08-30.md` — recovered-chat supersession map;
5. `recovery/2026-08-30_cross_chat_consolidation_exp073ae_hosted_pass_x2_dual_chains_running.md` — exact current chronology;
6. `docs/ARTICLE3_EXP073X2_PARALLEL_AUTHORITY_SELECTION_2026-08-30.md`;
7. `docs/ARTICLE3_DES_ANGULAR_14_TASK_MANIFEST_X2_SUCCESSION_AMENDMENT_2026-08-30.md`;
8. `experiments/073ae_article3_presupport_authority_join_schema_v0_1_prereg.md`;
9. `ci/exp073ae_article3_presupport_authority_join_schema_v0_1.py`;
10. the current Article-2 claim matrix when discussing DSIR-2.
