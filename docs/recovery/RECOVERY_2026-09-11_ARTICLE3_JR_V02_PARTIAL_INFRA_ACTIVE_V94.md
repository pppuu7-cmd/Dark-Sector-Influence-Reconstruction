# DSIR recovery V94 — JR v0.2 beta_minus infra shutdown; three model jobs still active

Date: 2026-09-11. Scope: **DSIR only**. Never mix RTK or RQIR.

## Preserved scientific authority
Exp073IR remains `NUMERICALLY_UNRESOLVED_EXP073IR`; Exp073JI support remains feasible; Exp073JJ/JK remain support-only NOT_CONVERGED at `0.037280144773915974` / `0.016330535730270664`. Frozen Exp073JL science remains unchanged: guarded 2049->4097 comparison, `REL_TOL=1e-3`, `h=1e-4`, native kpd20, centered-cubic interpolation, same 107-row traversal/accounting and anti-rescue rules. Covariance restriction remains unauthorized; Wm_S3 unopened.

## JR v0.2 corrected run live state
Authoritative run `34534521584`, head `a4b60431977fc2ee87dfd87e1c8e4f74cd04322d`.

`beta_minus` job `103062907332` passed frozen identity, dependency stack and exact pinned capacity-patched CLASS-IV build, entered `Execute fine-4097 model-operand exact control` at `2026-09-10T21:53:57Z`, then the GitHub-hosted runner reported `The runner has received a shutdown signal` at `21:56:27Z`; the operation was cancelled before result upload. No artifact exists. Classification: `INVALID_INFRA_PLUS_0_PLUS_0`, not scientific/model inequality.

Three sibling model jobs remain in progress inside the same frozen numerical control and must not be duplicated:
- beta_plus `103062907633`
- reference `103062907728`
- alpha_minus `103062907742`

No run artifact is currently available. The durable coarse v0.1 PASS receipt remains preserved and must not be recomputed.

## Exact next transition
Continue watching the three active sibling jobs. Consume any terminal result immediately. Do not rerun beta_minus while sibling computation remains active. Once the authoritative run is terminal, preserve any independently verified PASS receipts and recover only missing/infra-invalid model roles. A valid four-role fine exact PASS plus the durable coarse PASS permits only prospective registration of a full fixed-chunk same-run execution of unchanged Exp073JL. Any valid exact model inequality forbids sequential architecture. No tolerance/grid/science rescue.

Stable readiness remains Article III 68%, overall funnel 67%; infrastructure/process events do not raise scores.
