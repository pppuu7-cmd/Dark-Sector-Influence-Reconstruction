# DSIR recovery — 2026-09-08 Exp073HB prospective build-audit contract V30

Scope: **DSIR only**. RTK/RQIR excluded.

## Preserved authority

All scientific and governance authority from V29 is preserved unchanged. Wm_S1 Track-A exact PASS and admitted Wm_S2/Wm_S3 remain preserved. WW authority through `WW_S2_S3` remains admitted; `WW_S3_S3` remains NOT_YET_ADMITTED.

The single authoritative heavy process remains Exp073GA recovery run `34197207582`, head `f6d8b429ac64643e7cb7766e0bfa2ab51abe751d`; hosted job `101967492875 SUCCESS`; home job `101967543808 IN_PROGRESS`; checkpoint root `$HOME/.cache/dsir/exp073ga-ww-s3-s3-filebacked-ab-v0-1`. No competing home-heavy work was launched and no partial GA numerical output was inspected.

## Exp073HB prospective freeze

After raw-log validated Exp073HA PASS, the next independent C2 implementation gate is now prospectively frozen as `docs/dsir4/prereg/EXP073HB_C2_DIAGNOSTIC_EXACT_ENDPOINT_PRODUCER_BUILD_AUDIT_V0_1.md`.

- creation commit: `4ca51c172c89c52a3f5a2613e30133a59d03d38e`;
- prereg blob: `fc5f08889f84e628cb789070abd9179a74ef7e04`;
- expected token: `PASS_EXP073HB_C2_DIAGNOSTIC_EXACT_ENDPOINT_PRODUCER_BUILD_AUDIT_V0_1`;
- classification ceiling: `SUPPORT_PLUS_0_PLUS_0`;
- required no-runtime markers: `cosmological_run_started=false`, `record_payload_created=false`, `runtime_record_count=0`, `prediction_ready=false`, `scientific_model_authority_created=false`, `G_DOMAIN_MAPPING=NOT_YET_TESTABLE`.

HB binds any future producer implementation to the pinned CLASS-IV commit and source blobs already audited by HA, exact native `k_output_values` insertion/indexing, literal-z `background_tau_of_z`, terminal accepted NDF15 `ynew` at `tnew=tfinal=tau_target`, pre-transform `ppw->delta_m/ppw->theta_m`, no perturbation-state interpolation, no nearest/tolerance/effective coordinate rescue, and single-thread semantics if global diagnostic state is used.

HB permits only deterministic patch implementation plus GitHub-hosted build/static audit. It forbids cosmological execution and forbids creation of the 28-packet/1792-byte runtime payload during the audit.

## Current next actions

Heavy: terminal-consume Exp073GA run `34197207582` when it finishes. Validate raw log/artifact, provenance, complete A+B chains, same-field `S3->S3/[3,3]`, file-backed MCM evidence, finiteness and exact equality. Candidate PASS alone does not create authority; separate Exp073GB admission remains required.

C2 independent: implement the deterministic diagnostic producer patch and hosted Exp073HB build/static audit against the frozen HB contract. Real C2 28-packet runtime remains `BLOCKED_BY_HEAVY_RUN_EXCLUSIVITY` while GA owns the home runner.
