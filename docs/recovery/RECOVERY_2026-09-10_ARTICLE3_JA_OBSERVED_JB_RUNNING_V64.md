# DSIR authoritative recovery V64 — Exp073JA observed; Exp073JB running

Date: 2026-09-10. Scope: **DSIR only**. Never mix RTK or RQIR.

## Preserved authority
All earlier admitted DSIR scientific authority remains unchanged. Repaired Exp073IR run/job `34432102035 / 102729564736`, artifact `10134905199`, remains `NUMERICALLY_UNRESOLVED_EXP073IR`: 0 invalid rows, `f_B=0`, retained 107, exact convergence maximum `0.9998247463807295 > 1e-3`; covariance restriction unauthorized. No tolerance rescue.

## Newly closed — Exp073JA
Run/job `34457952958 / 102808675330`, head `b510d33339530f7d0643e158ff0bf3200f869ee1`, raw token `PASS_EXP073JA_HIGH_DENSITY_CANCELLATION_OBSERVED_V0_1`, is validated support-only `HIGH_DENSITY_CANCELLATION_OBSERVED_PLUS_0_PLUS_0`. Artifact `10144467663`; GitHub digest and independently downloaded ZIP SHA256 both equal `5cec1bc0a6d3c05b37fab53c4ad5f76d7ea079bf81066eed7145a31bfa8a71ed`. Durable JA authority file was committed at `d838cfe908d4069845c0ec4cac2aa80326b04dca`.

JA high-density sequence keeps exact `h=1e-4` and kpd `{80,160,320,640}`. Alpha adjacent relative changes decrease `0.13578796082083944 -> 0.0628310090978744 -> 0.030272882346383527`, while beta remains strongly resolution-sensitive `0.6867141285588996 -> 0.5809273304446889 -> 0.42559958697775674`. In both probes a large model-dependent native-grid term nearly cancels a comparatively stable endpoint-value term. This is mechanism evidence only, never scientific Layer-B authority.

## Current process — Exp073JB common-physical-bracket transport diagnostic
Prospective preregistration commit `181626cffef1a7216ac3a1f46ab869f63ae77715`; implementation commit `3f040796b2da1e1d1532cd7b6b080abd875ab5ba`; workflow/launch head `1be9887d1d6cebc3d21879ec573ce77ad5ba12b2`.

Authoritative workflow/run/job: `exp073jb-article3-layerb-common-physical-bracket-transport-v0-1 / 34463544046 / 102826719366`. GitHub-hosted `ubuntu-24.04`; checkpoint N/A; home/self-hosted ownership none. Last observed state IN_PROGRESS; prospective contract/lineage SUCCESS and frozen stack installation active.

Exp073JB is support-only `+0/+0`. It uses the same two historical probes and kpd `{80,160,320,640}`, but constructs an explicitly counterfactual common physical reference bracket: each perturbed transfer table is interpolated onto the reference model's exact physical `k_lo/k_hi`, then to the frozen target using the reference log-k fraction. It compares this common-bracket response to the ordinary model-native response to isolate model-dependent native-grid movement. It cannot replace Exp073IR, alter `REL_TOL=1e-3`, authorize covariance restriction, or create model/manuscript authority.

Exact next action: terminal-consume run `34463544046`; inspect raw job log and artifact, verify digest/provenance and complete transport records, then classify only under the frozen JB support contract. If the common-bracket sequence removes the dominant resolution sensitivity, prospectively investigate whether a grid-invariant evaluation architecture can be justified without changing the physical estimator; otherwise continue mechanism isolation. No acceptance criterion may be weakened.

Global frozen boundaries remain unchanged: `0.295<=z<=2.33`; `0<k<=0.06664762008318016 Mpc^-1`; Layer-A `operator_f_invalid<=0.05`; Layer-B invalid-row fraction `<=0.05`; retained dimension `>=15`; DES NSIDE=4096; ell `0..12287`; 39 bands; Wm `TE<-TE`; WW `EE<-EE`; canonical `<f8 [39,12288]`; `REL_TOL=1e-3`; `h=1e-4`; exact-threshold ambiguity `numerically_unresolved`; no tolerance/rounding/smoothing/averaging/effective-coordinate/fiducial-P rescue.
