# DSIR recovery V66 — Exp073JC observed; Exp073JD exact-k diagnostic running

Date: 2026-09-10. Scope: DSIR only. Never mix RTK or RQIR.

## Preserved scientific authority
All earlier admitted DSIR scientific authority remains unchanged. Repaired Exp073IR run/job `34432102035 / 102729564736` remains `NUMERICALLY_UNRESOLVED_EXP073IR`: 0 invalid rows, `f_B=0`, retained 107, exact native production-vs-dense convergence maximum `0.9998247463807295 > REL_TOL=1e-3`; covariance restriction unauthorized. No tolerance rescue.

Historical resource/performance Exp073CM remains `+0/+0` and must not be reinterpreted as Wm_S3 scientific arithmetic authority. Wm_S3 remains unopened pending its separately prospectively versioned resource gate and later lawful science path.

## Newly closed — Exp073JC
Validated support-only classification: `ORIGINAL_PAIR_REFERENCE_BRACKET_OBSERVED_PLUS_0_PLUS_0`. Run/job `34468721378 / 102843338789`, head `b2a3bf38f66435ca0ef7b94d757f7afbd451aa3e`, raw token `PASS_EXP073JC_ORIGINAL_PAIR_REFERENCE_BRACKET_OBSERVED_V0_1`. Artifact `10148654192`; GitHub-uploaded digest and independently downloaded ZIP SHA256 both `d6c89ed81e20feb952b4c2d6290e37f66e2a931929b4aa2d5b11fd32a128132d`; result JSON SHA256 `a15070c8cb8c835b52d7b05b424984f30cf4e301e53c1f8b66f819b92567481f`. Durable authority commit `ec39d96c9faebf8a9b2b09114af92c133292a0f3`.

At the historical alpha-left probe, native 10-vs-20 relative discrepancy is `0.9998247463807295`; common-reference-bracket discrepancy is still `0.9870472335808126`. At the beta-symmetric probe, native discrepancy is `0.9996789502402558`; common-reference-bracket discrepancy falls to `0.7099380201271409` but remains catastrophically above `1e-3`.

Interpretation: model-dependent native-grid motion is a genuine contributor, particularly for beta, but common-bracket transport does not cure the original coarse-grid failure. The remaining instability is consistent with a second component associated with sparse k sampling/output interpolation or another same-k numerical layer. Exp073IR remains numerically unresolved; covariance restriction remains unauthorized.

## Source-level mechanism opportunity
Pinned CLASS-IV `ac627d54e9ce196a08878d1ba33999819925d19c` accepts `k_output_values` and explicitly adds requested values into every internal perturbation k list. Its transfer output evaluates public transfer/source columns on `ppt->k`. Therefore the same patched public gauge-invariant `d_m` can be read at an actually integrated requested physical k, without post-hoc interpolation between neighboring native k rows.

This is an implementation fact only, not scientific authority. It motivates the next support diagnostic while preserving the same physical observable.

## Current process — Exp073JD
Purpose: on the original frozen kpd `10` versus `20` pair and the same two historical worst probes, inject the exact target physical k values through `k_output_values`, read the same public patched `d_m` directly at those inserted rows, and recompute the frozen alpha one-sided / beta symmetric finite-difference responses with `h=1e-4` and no k interpolation.

Prospective preregistration commit `be4f9c3421e2cde60349e9fbd2abd5dc575526d1`; implementation commit `e27152388759ccf3c1da4b2b392738d68f11e1df`; workflow/launch head `00994fba5f77100a3ffff11f205abb3be5e1a1dc`.

Authoritative workflow/run/job: `exp073jd-article3-layerb-exact-k-injection-original-pair-v0-1 / 34472918931 / 102856790715`. GitHub-hosted `ubuntu-24.04`; checkpoint N/A; home/self-hosted ownership none. Last observed state IN_PROGRESS.

Exp073JD is support-only `+0/+0` regardless of numerical outcome. It cannot retroactively PASS Exp073IR, change `REL_TOL=1e-3`, change `h=1e-4`, authorize covariance restriction, create Wm_S3 authority, or create model/manuscript authority.

Exact next action: terminal-consume run `34472918931`; verify raw token, exact-k coordinate provenance, artifact digest and independent ZIP hash. If both exact-k original-pair discrepancies fall below `1e-3`, prospectively design a full-row exact-k evaluation architecture audit with the frozen physical operator and thresholds before any scientific rerun. If either remains above `1e-3`, continue mechanism isolation without tolerance rescue.

Global frozen boundaries remain unchanged: `0.295<=z<=2.33`; `0<k<=0.06664762008318016 Mpc^-1`; Layer-A `operator_f_invalid<=0.05`; Layer-B invalid-row fraction `<=0.05`; retained dimension `>=15`; DES NSIDE=4096; ell `0..12287`; 39 bands; Wm `TE<-TE`; WW `EE<-EE`; canonical `<f8 [39,12288]`; `REL_TOL=1e-3`; `h=1e-4`; exact-threshold ambiguity `numerically_unresolved`; no tolerance/rounding/smoothing/averaging/effective-coordinate/fiducial-P rescue.
