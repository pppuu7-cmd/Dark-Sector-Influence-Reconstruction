# DSIR recovery V122 — top-64 census infrastructure retry

Updated: 2026-09-11. Scope: **DSIR only**. Never mix RTK, RQIR, KMQGB or KMDSB.

## Scientific frontier preserved
Canonical 8193->16385 remains independently verified `COMMON_GRID_NEXT_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`, max relative component difference `0.012484060640679777` versus frozen strict `<1e-3`. Frozen `REL_TOL=1e-3`, `h=1e-4`, native kpd20, centered-cubic interpolation, physical domain/masks/107-row accounting, invalid fraction `<=0.05`, retained dimension `>=15`, unsupported=0 and lookup `<=1e-12` remain unchanged. No 32769 execution is authorized; covariance restriction remains unauthorized; Wm_S3 remains closed.

## Newly terminal top-64 attempt 1
Prospectively frozen top-64 cancellation census run/job `34614119149 / 103311649147`, checkout/static-authority head `717a15a615061cea441910d03fdda1f9a4a2b2c2`, terminated FAILURE after completing frozen identity checks, exact source-artifact verification, numerical-stack install, pinned CLASS-IV build and the 8-construction numerical census.

The first fail-closed causal gate was exactly:
`RuntimeError: atom 0 primary reproduction 2.8125401822500933e-06`.
The frozen contract requires every atom's primary source-discrepancy reproduction error `<=1e-12`, so attempt 1 is classified **infrastructure/numerical-reproducibility FAIL +0/+0**, not scientific FAIL and not a valid census result. No scientific authority was created.

## Diagnosis
The rank-0 selected atom is the already frozen exact hotspot. The top-64 helper and the independently validated top-1 decomposition use the same four roles, the same `h=1e-4`, the same alpha/beta numerator and response formulas, the same canonical grids, solver stack and interpolation. The independently validated top-1 artifact `10268078468` reproduced the source rank-0 metric exactly as `0.012484060640679777`, while attempt 1 ran on a different GitHub-hosted VM and missed the prospective reproduction gate by relative `2.8125401822500933e-06`.

No sign, normalization, component-selection, tolerance, grid, h, interpolation or estimator defect was found. Because the atom is strongly near-cancelled, this is treated as a runner-level numerical reproducibility/infrastructure failure unless an unchanged retry proves otherwise. The `1e-12` gate is **not weakened** and the failed attempt remains durable history.

## Self-heal action
The smallest admissible resume is one exact unchanged retry of the failed census job, preserving the already frozen helper blob `92f35f0f563fe5c5ba54b3984ae9a82e2abaf8e7`, prereg blob `940a09d194fe9e68e6fc14271e7ab99b6aded3e4`, source identities, static authority, `h`, grids, solver/interpolation and strict per-atom reproduction gate. This is an infrastructure retry, not a duplicate scientific computation and cannot create scientific authority.

If the unchanged retry fails the same gate again, do **not** relax the contract or keep retrying. Record the top-64 census as BLOCKED by hosted numerical reproducibility and prospectively design a separate support-only reproducibility diagnostic; 32769 remains unauthorized.

## Runner ownership
Stale superseded self-hosted run `34550495778 / 103112190909` remains queued and must not receive home-runner ownership. No home runner is assigned to this support census.

## Readiness
**ARTICLE3_REPOSITORY_READINESS: 68%.** Funnel-freeze readiness: **67%**.
