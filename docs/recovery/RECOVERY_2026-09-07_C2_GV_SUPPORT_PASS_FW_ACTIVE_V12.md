# DSIR immutable recovery — C2 Exp073GV support PASS while Exp073FW remains active V12

Date: 2026-09-07. Scope: DSIR only; RTK/RQIR excluded.

All previously admitted Wm/WW authority is preserved. `WW_S2_S2` remains NOT ADMITTED. Exp073FW run `34125785882`, home job `101754061309`, remained the single authoritative self-hosted process at reconciliation; no competing heavy run was launched and no partial numerical output was inspected.

## Exp073GV — future exact-record packet admission support gate

Prospective prereg commit `6bf70f7ebdc164a63c2896dc5afeebd6b2972866`; fixture commit `23217fa330c550f9bd543e03989c90ac5836d6b2`; workflow commit `54e844ce0db3ebddc5f27b4dda60c98c57bc4421`.

Hosted run `34135739339`, job `101786143296`, completed SUCCESS and was independently raw-log inspected. The validator accepted only the unmodified clearly synthetic 64-byte test vector and failed closed on one-byte mutation, truncation, extension, ordinal mutation, coordinate mutation, solver provenance mutation and record-digest mutation. Exact raw classifications were `synthetic_test_vector_only=true`, `scientific_record_admitted=false`, `cosmological_run_started=false`, `self_hosted_science_started=false`, `prediction_ready=false`, `scientific_model_authority_created=false`, `G_DOMAIN_MAPPING=NOT_YET_TESTABLE`, `classification=SUPPORT_PLUS_0_PLUS_0`, with token `PASS_EXP073GV_C2_IDE_RECORD_PACKET_ADMISSION_STATIC_AUDIT_V0_1`.

GV therefore freezes only a fail-closed future packet admission boundary binding one exact request ordinal/coordinate to exact inherited GT/GR/solver/recorder/GU provenance and an exact 64-byte opaque record SHA256. It performs no field interpretation, gauge transformation, interpolation, smoothing, averaging, tolerance, solver mutation or prediction. No real cosmological payload was produced or admitted.

## Heavy frontier

Authoritative heavy workflow remains Exp073FW run `34125785882`, head `1c635f5192d26e76e0ec82a308363b666e5a248b`; hosted audit `101754018941` SUCCESS; home job `101754061309` remains the tracked self-hosted owner on `DSIR-HOME-PC-2`; checkpoint namespace `~/.cache/dsir/exp073fw-ww-s2-s2-filebacked-ab-v0-1`.

Exact heavy next action: terminal-consume FW; inspect raw final logs/artifact, verify GitHub digest/ZIP, complete checkpoint/provenance chain, frozen S2->S2 same-field semantics, exact `19,327,352,832`-byte file-backed proof, canonical finite `<f8 [39,12288] EE<-EE`, and exact A/B equality. Workflow success alone is not scientific PASS. Only separately frozen Exp073FX may create `WW_S2_S2` authority.

Independent C2 work may continue only as hosted metadata/support work while FW owns the runner. Real C2 extraction remains forbidden until a later prospective gate explicitly authorizes it and there is no conflicting self-hosted owner.