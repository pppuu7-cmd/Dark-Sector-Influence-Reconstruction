# DSIR recovery V123 — top-1 arithmetic closed; top-64 retry active

Updated: 2026-09-11. Scope: **DSIR only**. Never mix RTK, RQIR, KMQGB or KMDSB.

## Scientific frontier preserved
Canonical 8193->16385 remains independently verified `COMMON_GRID_NEXT_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`, max relative component difference `0.012484060640679777` versus frozen strict `<1e-3`. Frozen `REL_TOL=1e-3`, `h=1e-4`, native kpd20, centered-cubic interpolation, physical domain/masks/107-row accounting, invalid fraction `<=0.05`, retained dimension `>=15`, unsupported=0 and lookup `<=1e-12` remain unchanged. No 32769 execution is authorized; covariance restriction remains unauthorized; Wm_S3 remains closed.

## Newly closed support-only top-1 arithmetic authority
A response-blind/static arithmetic follow-up to the already independently validated top-1 decomposition is now terminal and independently consumed. Parallel arithmetic source run `34624525157`, head `107b453d485a14f8a812539d04abbbf9c63d8885`, produced three support artifacts for subtraction conditioning, signed role attribution, and Decimal subtraction replay. Terminal consumer run/job/artifact `34624598782 / 103346512972 / 10273886027`, head `664c68b0e306e4e28518e73383a67d4dfb5afa46`, classified `POST_16385_TOP1_DECOMPOSITION_ARITHMETIC_TERMINAL_VALIDATION_PASS_PLUS_0_PLUS_0`.

Independent artifact consumption verified terminal ZIP SHA256 `72e2fb96df45ddb05d49bcd215618a366a08df62c5b386ca88c9ab6118593c26` and `terminal_validation.json` SHA256 `0273ad038f5b5e13ab1f1598f7d84a829983ea474e379808342a5c2fb63449ef`.

The immutable validated arithmetic result shows beta subtraction condition numbers above `2.0e8` on both canonical grids (`207066036.01189664` on 8193 and `204481011.0632948` on 16385), while exact Decimal replay of subtraction from the stored binary64 role operands gives zero subtraction-rounding relative error in the replayed numerator. The signed role shifts recombine to the cross-grid beta-numerator shift with relative residual `0.0`; the plus/minus role shifts reinforce in the difference. This supports severe cancellation/ill-conditioning amplification rather than ordinary floating-point subtraction rounding as the local top-1 mechanism. It is strictly support-only `+0/+0`: CLASS was not invoked by these audits, no scientific classification or authority was created, and no downstream gate was opened.

Durable authority: `docs/dsir4/authority/LAYERB_POST_16385_TOP1_DECOMPOSITION_ARITHMETIC_TERMINAL_AUTHORITY_V0_1.json`, completion commit `4392df8fe76680ee6e4bfed7e073263e4729e39f`.

## Top-64 census attempt 2 remains the only active scientific-support execution
Top-64 census attempt 1, run/job `34614119149 / 103311649147`, remains terminal infrastructure/numerical-reproducibility FAIL `+0/+0` because atom-0 primary reproduction was `2.8125401822500933e-06` against the frozen `<=1e-12` contract. No science setting or threshold was changed.

The single permitted exact unchanged retry is run `34614119149`, attempt 2, job `103331912096`, launch head `6b684ac91c7a261e6b588da1a4922a411bd65614`. At this recovery update it is `IN_PROGRESS` on GitHub-hosted ubuntu-24.04. Frozen identity/source/hash checks, numerical-stack install and pinned CLASS-IV build are complete; the frozen top-64 numerical census step is active. No duplicate top-64 execution is permitted.

If attempt 2 passes, independently consume/hash all artifacts and enforce exact source/provenance identity, exact 64 ordered atoms, lifecycle `8/max1/final0`, unsupported=0, lookup `<=1e-12`, every primary reproduction error `<=1e-12`, and downstream-closed flags. If attempt 2 repeats the reproduction failure, do not relax the contract and do not retry again; classify the top-64 census BLOCKED by hosted numerical reproducibility and prospectively freeze a separate support-only cross-VM hotspot reproducibility diagnostic.

## Runner ownership
Exactly one useful hosted lane is owned by top-64 retry attempt 2: `34614119149 / 103331912096`. Stale superseded self-hosted run `34550495778 / 103112190909` remains queued and must not receive home-runner ownership.

## Readiness
**ARTICLE3_REPOSITORY_READINESS: 68%.** Funnel-freeze readiness: **67%**.
