# DSIR immutable recovery — Exp073FW guard self-match repair, home active V08

Date: 2026-09-07. Scope: DSIR only; never mix RTK/RQIR.

## Preserved scientific authority

All previously admitted DSIR authority is preserved unchanged. In particular, `WW_S1_S3` was admitted only by Exp073FV run `34120000242`, hosted admission job `101735763144`, token `PASS_EXP073FV_WW_S1_S3_FILEBACKED_PROVENANCE_ADMISSION_V0_1`. `WW_S2_S2` is not yet admitted.

## Terminal Exp073FW implementation failures — +0/+0 only

Run `34121012410`, home job `101738820469`, reached the home wrapper and failed immediately with `fail-closed tolerance/rescue path` before numerical science/checkpoint creation. Artifact collection found no files. This is an implementation failure `+0/+0`, not a scientific arithmetic FAIL.

First causal defect: the FW outer lexical audit scanned the inherited FM wrapper source containing the inherited fail-closed scanner’s own forbidden-token literals, causing a self-match. Frozen driver arithmetic, source/domain, exact acceptance criteria, tolerance policy, field semantics, and checkpoint architecture were not implicated.

Prospective repair chain reconciled from the repository:
- `09cf5b398d22286afab7715fbee450b114d4d065` — guard self-match repair;
- `e7129a3299f1e2f49ec3a1b4546a578a64fc9cf8` — bind repair; run `34125359067` then failed only in hosted audit because its required explanatory marker was absent; home skipped, static/implementation `+0/+0`;
- `f1f29bf2fef56d52b3ce3f13645de22d8fb682d2` — restore hosted-audit marker;
- binding head `2fa1a9e50700ef921b3735c80324fbf412740e60` pins current home-wrapper blob `ffddbe96946e3f143be9f9334c24b23d75e00498`.

The repaired wrapper excludes exactly the inherited scanner’s two guard lines from its outer wrapper-source lexical audit while retaining the inherited fail-closed scanner over the transformed scientific shell. No science/domain/arithmetic/tolerance/acceptance change is authorized.

## Authoritative live process

Exp073FW run `34125530921`, head `2fa1a9e50700ef921b3735c80324fbf412740e60`.
- hosted launch audit job `101753205410`: SUCCESS;
- home-science job `101753245014`: IN_PROGRESS;
- runner owner: `DSIR-HOME-PC-2` / machine `win-ws338`;
- checkpoint namespace: `~/.cache/dsir/exp073fw-ww-s2-s2-filebacked-ab-v0-1`;
- expected gate: frozen `WW_S2_S2` exact file-backed A/B gate;
- competing DSIR heavy run: none observed when this note was created.

Do not inspect partial numerical output to tune frozen criteria. Do not launch a competing home job.

## Exact next action

When run `34125530921` becomes terminal, consume raw logs and artifact in the same iteration. Verify GitHub artifact digest/ZIP SHA, checkpoint/provenance identity, same-field `S2->S2` semantics, exact `19,327,352,832`-byte file-backed MCM proof, canonical finite `<f8 [39,12288] EE<-EE`, and exact A/B equality. Workflow success alone is not scientific PASS. Only prospectively frozen Exp073FX provenance admission may create `WW_S2_S2` authority. On infrastructure/software failure, preserve any valid complete checkpoints and repair only the first causal defect; on genuine frozen numerical failure, record a scientific negative result without weakening the gate.
