# DSIR Article III recovery V144 — chunk production authorized and launched

Updated: 2026-09-12. Scope: **DSIR only**.

## Scientific frontier at note creation
Canonical 8193->16385 remains independently verified `COMMON_GRID_NEXT_REFINEMENT_NOT_CONVERGED_PLUS_0_PLUS_0`, max relative component difference `0.012484060640679777` versus frozen strict `<1e-3`.

No terminal 16385->32769 scientific classification existed when this immutable note was created. Covariance restriction remains unauthorized and Wm_S3 remains unopened.

## V141–V143 prerequisites retained
V141 exact 8193 monolithic-vs-chunk method equivalence is PASS for all four frozen roles.

V142 canonical 32769 eight-chunk response-blind resource route is PASS on standard hosted runners, peak RSS `11865712 kB`, no observed swap consumption.

V143 canonical 32769 blinded partition invariance is PASS: two prospectively distinct partitions agree exactly for all four frozen roles and all six z probes, max normalized/absolute response difference `0.0`, minimum exact binary64 fraction `1.0`.

## New V144 static production authority
Production static audit run `34695256186` at head `55283ec18d9ca4b12c09097eada4ea43a37ff749` is terminal PASS.

Jobs: source identities `103557525777`, compile/synthetic `103557525909`, workflow topology `103557525929`, acquisition-only scientific delta `103557525943`, independent finalizer `103557551483`.

Final artifact `10298507794`, ZIP SHA256 `24b3a5d38198b5e403535e4db8306c2272d6fe1908a0cec8242e5981650631f5`; independently consumed raw `static_audit.json` SHA256 `b9547d407023b5e51a4d91a7764c594a5a61df0fe0bdf7ef16eb229d99353b39`. Classification `LAYERB_16385_TO_32769_CHUNK_PRODUCTION_STATIC_AUDIT_PASS_PLUS_0_PLUS_0`, missing receipts `[]`, scientific response read=false, scientific execution authorized=false.

Durable static authority: `docs/dsir4/authority/LAYERB_16385_TO_32769_CHUNK_PRODUCTION_STATIC_AUDIT_V0_1.json`, creation commit `f194ae32e03fe321ea787633a74b61782605fe0a`.

## Exactly one production launch
One-shot launch sentinel `docs/dsir4/launch/LAYERB_16385_TO_32769_CHUNK_PRODUCTION_LAUNCH_V0_1.json` was created once at commit `646d93a7cb0e38dc8b3d358f737e47655567055b` and triggered production workflow run `34695347893`.

Production workflow: `.github/workflows/layerb-16385-to-32769-canonical-chunk-scientific-v0-1.yml`.

Current-run `authorize` job `103557768980` is PASS. Independently consumed authorization artifact `10298499340` confirms:
- current run id `34695347893`;
- `authorized_canonical_run_count=1`;
- V141/V142/V143, parent refinement and request-plan exact authority bindings all independently verified;
- one-live guard PASS with `matching_current_runs=1`, `matching_other_live_authoritative_runs=0`, `matching_other_all_status_runs=0`;
- `relative_tolerance=0.001`, strict `<`, `h=0.0001`, native kpd20 unchanged;
- covariance restriction=false and Wm_S3=false;
- errors `[]`.

At note creation, `materialize-plan` job `103557768867` is in progress. Only after it passes may the workflow fan out to the prospectively frozen 8-way role/grid acquisition matrix (4 roles x coarse/fine). No duplicate production run is authorized.

## Frozen production rules
Science semantics remain unchanged: strict max relative component difference `<1e-3`, `h=1e-4`, native kpd20, centered-cubic interpolation, canonical 16385/32769 lattices, parent 107-row accounting, invalid fraction `<=0.05`, retained `>=15`, unsupported=0, lookup mismatch `<=1e-12`.

Only 32769 fine response acquisition changes to the validated low-capacity eight-chunk method. Final classifier must use the frozen parent execute/replay semantics. Silent science retry and post-result threshold changes are forbidden.

## Readiness at note creation
`ARTICLE3_REPOSITORY_READINESS: 68%`.

Funnel-freeze/scientific frontier: `67%`.

Operational roadmap: `WORKING_PLAN_COMPLETION: 94%`. This increase is operational only: the complete prospective control plane is independently audited, authorized and the one canonical production run is active. Scientific convergence has not yet advanced.
