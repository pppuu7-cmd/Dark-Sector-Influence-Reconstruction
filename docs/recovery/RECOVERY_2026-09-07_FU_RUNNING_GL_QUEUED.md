# DSIR immutable recovery — FU running / GL queued

Date: 2026-09-07. Scope: DSIR only.

Preserve all authority in `docs/RECOVERY_LATEST.md`. Exp073FU run `34089383137`, home job `101639652148`, remains the unique self-hosted owner and is still inside the frozen `WW_S1_S3` A/B compute step; no partial numerical output was inspected.

Independent C2 work did not create a competing interface. Existing `C2_IDE_DELTAM_EXTRACTION_CONTRACT_V0_1.md` remains frozen. Exp073GL prospectively freezes only a source-eligibility audit for the pinned `kaeonikc/class_iv@ac627d54e9ce196a08878d1ba33999819925d19c` pre-transform boundary. Prereg commit `319457d0079ade637c012c945ac81545859a2f10`; auditor commit `ffb485f83fcfd013b3c70850138ca1dd847df9e7`; workflow activation/head `846368c14550f67ec3c4f0aed25ab637e89c0c70`; hosted run `34093447421`, job `101651685666`, queued at last reconciliation. Classification ceiling is support `+0/+0`; it cannot set `prediction_ready` or scientific authority.

On FU terminal: consume artifact/log and apply the frozen FU contract before any FV admission. On GL terminal: inspect raw job log; only exact token `PASS_EXP073GL_C2_IDE_PRETRANSFORM_SOURCE_ELIGIBILITY_STATIC_AUDIT_V0_1` with pinned source identity counts as hosted support PASS. Any GL failure is static/infrastructure `+0/+0` unless the frozen contract explicitly says otherwise.
