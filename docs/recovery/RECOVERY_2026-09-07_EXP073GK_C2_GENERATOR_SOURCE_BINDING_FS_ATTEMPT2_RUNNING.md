# DSIR recovery — Exp073GK C2 generator/source binding; Exp073FS attempt 2 running

Date: 2026-09-07
Scope: **DSIR only**. Never mix RTK or RQIR.

## Preserved authority

All previously admitted DSIR scientific authority remains unchanged. In particular, WW admitted authorities through `WW_S1_S1` / Exp073FR remain preserved. No result in this note alters frozen scientific thresholds or reinterprets historical failures.

## Authoritative heavy process — Exp073FS attempt 2 / WW_S1_S2

Live GitHub Actions reconciliation during this iteration found exactly the same authoritative heavy process:

- workflow/run: Exp073FS `34067352681`, attempt `2`;
- branch/head: `main` / `f3e49041a5b869ddf22be8ca7a612901ec9f9458`;
- home job: `101592579318`;
- runner ownership: `DSIR-HOME-PC-2` / runner id `22`, exclusively owned by job `101592579318`;
- state: `IN_PROGRESS` in `Run frozen WW_S1_S2 A/B gate with durable checkpoints`;
- hosted launch audit `101592593435`: SUCCESS, support only;
- evidence collection/upload remain pending;
- checkpoint roots: `~/.cache/dsir/exp073fs-ww-s1-s2-filebacked-ab-v0-1/checkpoints/A` and `/B`;
- last durable checkpoint: `UNKNOWN_NOT_INSPECTED_WHILE_RUNNING` by policy;
- no partial numerical output was inspected and no competing heavy run was launched.

Frozen candidate token remains `PASS_EXP073FS_WW_S1_S2_FILEBACKED_AB_EXACT_REPEATABILITY_V0_1`. On terminal success the artifact must be independently consumed before any scientific classification; only prospectively frozen Exp073FT may create `WW_S1_S2` authority. Exact mismatch remains a scientific FAIL; infrastructure/resource problems remain repair/resume conditions with valid checkpoints preserved.

## Exp073GK — deterministic C2 IDE Delta_m generator implementation/static audit

While Exp073FS remained active, an independent non-biasing C2 IDE support task was performed.

Prospective prereg:

- path: `experiments/073gk_c2_ide_delta_m_generator_static_audit_v0_1_prereg.md`;
- creation commit: `139fd5a949c7fbfca13b92a6550245440f84d490`.

Generator implementation:

- path: `analysis/exp073gk_c2_ide_delta_m_generator_v0_1.py`;
- creation commit: `b763b11a349422758414ffad3524305e0c84c965`.

The generator is support-only: it consumes explicitly source-bound arrays and enforces the already-frozen Exp073GJ coordinates, parameter points, branch masks, pinned solver identity, deterministic JSON serialization/SHA and exact arithmetic. It does not run CLASS and cannot create model authority by itself.

### Exp073GK v0.1 first hosted audit — implementation/static FAIL +0/+0

- workflow creation commit: `64e49b0a7b6aeaf7512bde9b65845376b8699aaf`;
- run/job: `34084884016 / 101626888946`;
- first causal failure from raw log: `AssertionError: 0.06664762008318016`.

Cause: the audit harness required the inherited frozen k-domain maximum literal to appear in the local GK prereg/script, although that boundary is explicitly inherited from the parent Exp073GJ prereg. This was an audit-implementation defect, not a numerical/scientific C2 failure. No self-hosted science ran and no model authority was created.

### Minimal repair and repaired PASS

Minimal workflow-only repair commit: `c80cb15cdb131dac785f8119d11155e2eb70d832`. The repair changed only the static harness so that the inherited k-domain maximum is verified against the parent Exp073GJ prereg. No formula, coordinate, parameter, threshold, solver lineage or science criterion changed.

Repaired hosted run/job:

- `34084914369 / 101626977858`;
- exact raw token: `PASS_EXP073GK_C2_IDE_DELTA_M_GENERATOR_STATIC_AUDIT_V0_1`;
- `classification=SUPPORT_PLUS_0_PLUS_0`;
- `self_hosted_science_started=false`;
- `scientific_model_authority_created=false`.

The audit verifies syntax, deterministic byte-identical synthetic generation/SHA, an independently recomputed exact arithmetic cell, and fail-closed mutations for solver commit, k grid, source identity, physical branch and primordial-normalization/phase provenance. Synthetic `prediction_ready=true` is fixture-only and creates no real prediction authority.

## New pinned-source finding — standard CLASS d_m cannot be fed into the frozen bridge as current-gauge delta_m

Pinned source `kaeonikc/class_iv@ac627d54e9ce196a08878d1ba33999819925d19c`, `source/perturbations.c`, was audited without reading any Exp073FS partial output.

The pinned code first constructs current-gauge matter quantities, including the IDE matter contribution, with state equivalent to `delta_rho_m/rho_m` and `rho_plus_p_theta_m/rho_plus_p_m`. Before exporting the standard total-matter source, however, it explicitly transforms the density using

`ppw->delta_m += 3 * a * H * ppw->theta_m / k^2`

and identifies the exported `index_tp_delta_m` as gauge-invariant total-matter overdensity. The pinned source diagnostic also states that CLASS `delta_m` is by default the gauge-invariant density fluctuation in comoving gauge. In synchronous gauge the standard matter velocity source is likewise shifted before export.

Therefore the standard CLASS `d_m` / `index_tp_delta_m` transfer output must **not** be supplied as the current-gauge `delta_m` input to the frozen Exp073GJ formula and corrected again; doing so would double-apply the gauge transformation.

This finding is recorded in `docs/dsir4/mappings/C2_IDE_NATIVE_SOURCE_BINDING_AUDIT_V0_1.md`, creation commit `66bbf83bd87d6937cde3ada50adc0e5e7116fec8`.

Classification is fail-closed interface/provenance `+0/+0`, not a scientific failure of C2. Exp073GK remains a valid arithmetic/serialization support layer only when its input manifest proves that `delta_m` and `theta_m` are the required pre-transform native quantities.

## C2 current state and exact independent next step

C2 remains:

- `mapping_ready=true`;
- deterministic generator/static audit: support PASS only;
- real native extraction binding: `BLOCKED / NOT_YET_ADMITTED`;
- `prediction_ready=false`;
- `G_DOMAIN_MAPPING=NOT_YET_TESTABLE`;
- scientific contribution `+0/+0`.

Exact next independent C2 step, if Exp073FS is still running: prospectively freeze and statically audit a minimal pinned-lineage **pre-transform extraction hook/patch** that emits current-gauge `delta_rho_m/rho_m`, current-gauge `theta_m`, and exact `aH` at the frozen coordinates, with source-file/blob identities and proof that solver evolution/physics are unchanged. Only after that provenance route is admitted may real C2 prediction generation be considered.

## Exact heavy next action

Heavy priority remains terminal consumption of Exp073FS attempt 2. Verify artifact/ZIP digest, complete A/B checkpoint chains and restored-vs-new provenance, ordered `S1->S2` distinct-field semantics, frozen source/contract/implementation identities, exact file-backed mmap proof, finiteness and exact canonical A/B equality. Candidate PASS alone creates no authority; only Exp073FT may admit it and dispatch Exp073FU.