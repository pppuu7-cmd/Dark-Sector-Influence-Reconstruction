# DSIR immutable recovery — Exp073FF preregistered while Exp073FA runs

Date: 2026-09-06. Scope: DSIR only; RTK/RQIR excluded.

Live reconciliation found exactly one in-progress Actions run: Exp073FD/Exp073FA run `34020756634`, home job `101452805620`, head `894885b2c2b811954d1724c2733d2a810a486d70`; queued runs = 0. Hosted audit job `101452788638` remains SUCCESS `+0/+0`. `DSIR-HOME-PC` remains exclusively owned by home job `101452805620`. Partial numerical output and durable checkpoint stage were not inspected.

While that frozen computation remains in progress, the next result-independent authority-writing gate was preregistered as `experiments/073ff_ww_s0_s2_filebacked_checkpoint_provenance_admission_v0_1_prereg.md`, blob `c6f1fd11c4a0dc68bb17669a58854979fe84869e`, creation commit `2e3425cae2d564bf368417123af48b2662730557`.

Exp073FF is provenance/admission only and is `PREREGISTERED_NOT_ACTIVATED`. It preserves Exp073FA's exact `(S0,S2)` science, both six-stage checkpoint chains, source/contract identities, file-backed MCM geometry, canonical `<f8 [39,12288]` `EE<-EE`, exact SHA plus `numpy.array_equal`, finiteness, and Exp073FE restore-hardening requirements. Future Exp073FA terminal artifact ID/digest are deliberately unknown until terminal state and independent ZIP SHA256 verification.

Only `PASS_EXP073FF_WW_S0_S2_FILEBACKED_PROVENANCE_ADMISSION_V0_1` may create `WW_S0_S2` authority. A candidate Exp073FA PASS alone is non-authoritative. Exact A/B mismatch remains scientific FAIL; missing/incomplete provenance remains BLOCKED or infrastructure/provenance `+0/+0` and must not be rescued scientifically.
