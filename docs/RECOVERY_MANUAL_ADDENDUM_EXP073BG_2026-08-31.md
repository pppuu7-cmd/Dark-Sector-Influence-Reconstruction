# DSIR recovery manual addendum — Exp073BG active-run outcome firewall

**Date:** 2026-08-31  
**Applies to:** Article-3 active Exp073BA/Exp073BD only.  
**Readiness effect:** `+0` scientific, `+0` draft/data at creation.

This addendum supplements the live recovery manual without modifying historical frozen results or thresholds.

## Recovery rule

Before interpreting either active heavy run, read:

1. `docs/EXP073BA_BD_TERMINAL_OUTCOME_DECISION_TABLE_2026-08-31.md`;
2. `experiments/073bg_article3_active_ba_bd_terminal_outcome_policy_v0_1_prereg.md`;
3. immutable hosted job/artifact metadata for the terminal run.

Do not infer scientific PASS from GitHub job `success` alone. Track-A PASS requires the exact frozen authority artifact. Do not infer scientific FAIL from timeout/OOM/runner/harness failure before a comparator has two complete valid scientific inputs.

For Exp073BA, exact inequality at the frozen compact or final comparator is scientific repeatability FAIL for the corresponding BA stage, with no tolerance rescue. Missing/incomplete inputs before comparator classification are infrastructure/resource/harness failures.

For Exp073BD, preserve both complete branches symmetrically. One incomplete branch means P3 and no downstream use. Two complete branches plus the frozen pair diagnostic make a provisional data object only; exact equality cannot retroactively convert Track P into Track A.

## Readiness accounting

Current state remains:

`Verified: 52.0% | Draft/data: 53.7%`

If and only if Exp073BD produces two complete finite final branch windows and the valid frozen pair diagnostic, the already-frozen draft/data ledger admits Wm_S2 as the third complete angular data object and Draft/data becomes `54.57142857142857%`. Verified remains `52.0%`.

No BA individual-window PASS automatically increases Verified; the frozen scientific accounting keeps individual angular authority at `+0` until the later aggregate/support barrier is genuinely satisfied.

## Historical negative result

Exp073AQ remains permanent hosted exact-repeatability scientific FAIL. Never rewrite or delete it when recording BA succession results.

## Anti-leakage and order

Maintain:

`physical forward/power bridges -> preregistered support-validity mask -> Layer A/B prerequisites -> covariance/whitening -> nuisance rank/SVD -> quotient/relation/null -> actual G7 authorization -> fresh G8 withheld family`.

No G8 or withheld-family read is allowed before actual G7 authorization.
