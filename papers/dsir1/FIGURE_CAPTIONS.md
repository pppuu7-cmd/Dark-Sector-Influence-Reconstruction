# DSIR-I main-figure captions — v0.2

These captions are part of the paper claim boundary. They are intentionally explicit about theory-response versus observation-space statements, withheld status, and permanent negative results.

## Figure 1 — Observational equivalence as an induced quotient

**Figure 1. DSIR operator architecture and channel-conditional equivalence.** A theory response `r(theta)` is mapped through a physical/window operator `K_B`, covariance whitening `W_B`, and nuisance quotient `Q_B` to the retained signature `s_B=A_B r`, with `A_B=Q_B W_B K_B`. Exact channel-conditional equivalence is `r_1 ~_B r_2` iff `A_B(r_1-r_2)=0`. For independently quotiented compatible channel blocks, stacking implies `ker A_(B+C)=ker A_B ∩ ker A_C`, so an added retained channel can split but not merge an existing exact equivalence class. The last statement requires the compatibility assumption: a joint fit with newly shared nuisance directions need not preserve the blockwise quotient. This diagram is a formal identifiability architecture, not a new physical law and not evidence that the final survey-level DSIR quotient is already complete.

## Figure 2 — Failure of the additive scale+time core

**Figure 2. Irreducible scale-time structure in the frozen low-k response atlas.** Panels A–C show the normalized designer-`f(R)` response `R(z,k)`, its best two-way additive projection `mu+T(k)+tau(z)`, and the residual interaction `I(z,k)`, recomputed directly from the frozen response vectors. Panel D shows `chi_I=||I||^2/||R||^2` for the six frozen low-k directions. The compact additive core is a scientific FAIL for the tested atlas: designer-`f(R)` retains `chi_I≈0.300`, GDM retains a material `~0.04–0.05`, smooth dark energy is weakly nonseparable, and the tested IDE directions are nearly separable. Thermal WDM is absent rather than assigned zero because its informative response lies on a distinct high-k block. `chi_I` is a representation diagnostic on the stated domain, not a fundamental dark-sector degree of freedom.

## Figure 3 — Finite-amplitude hierarchy and node robustness

**Figure 3. Finite-amplitude scale-time nonseparability hierarchy and deterministic grid robustness.** Panel A shows the sampled `chi_I` envelopes for IDE, smooth dark energy, GDM, and designer-`f(R)` from Exp047A. The envelopes remain non-overlapping in the descriptive order `IDE < smooth DE < GDM < designer-f(R)` over the frozen sampled amplitudes. Panel B shows full-grid values and leave-one-node ranges from Exp047B; the class ordering is preserved in all 12 deterministic single-node deletions. No post-hoc scientific stability threshold was introduced. The result is a robust descriptive ordering on the stated sampled domains, not a universal mechanism law; in particular, the absolute smooth-dark-energy value is sensitive to the lowest-k node.

## Figure 4 — Degeneracy is channel conditional

**Figure 4. Frozen examples of channel-conditional degeneracy breaking.** Left: GDM pressure and viscosity are nearly collinear in low-k matter response (`0.3226 deg`) and Weyl-amplitude response (`0.3007 deg`) but are strongly separated by metric slip (`137.94 deg`), with an equalized Weyl+slip angle of `56.96 deg`. Right: GDM pressure/viscosity and designer-`f(R)` are nearly aligned in their leading scale mode (`0.0781/0.1017 deg`) yet separate in the time mode (`25.18/25.49 deg`) and in the oriented full response (`154.82/154.51 deg`). These are certified theory-response angles under frozen constructions; they are not survey detection significances. The observational equivalence relation additionally depends on physical projection, covariance whitening, and nuisance quotienting.

## Figure 5 — Curvature and distinct localization mechanisms

**Figure 5. Curved one-parameter response trajectories and distinct mechanism-localization coordinates.** Panel A shows the maximum sampled turn of normalized response directions: smooth dark energy and GDM pressure remain nearly straight over their tested rays, whereas GDM viscosity and designer-`f(R)` turn by `7.18 deg` and `12.14 deg`, respectively. Thus a one-parameter microscopic family can require multiple linear representation modes without acquiring additional microscopic degrees of freedom. Panel B shows the preregistered withheld thermal-WDM cutoff coordinate `k_0.1` for masses 2.5–4.5 keV across all frozen redshifts; the cutoff moves monotonically to higher k with mass while showing very weak redshift drift. Panel C shows the genuinely withheld DCDM-to-dark-radiation temporal centroid `z_R`, which increases by more than `10^-3` at every consecutive frozen `Gamma/H0` step. These diagnostics intentionally are not collapsed into a single universal scalar law: WDM is scale-localized, DCDM is time-localized, and GDM/`f(R)` can carry substantial scale-time interaction.

## Figure 6 — Negative results remain evidence

**Figure 6. Failure-resistant scientific provenance.** Panel A preserves the Exp070A C3 bridge FAIL (`~4.75%` reconstruction defect against its frozen threshold) beside the later, separately frozen Exp070C native-grid provider PASS (`~2.81e-14` closure under its own contract); the later provider does not reclassify the original experiment. Panel B shows the prospectively frozen C5 numerical-accuracy ladder: the q=1 configuration misses the unchanged `5e-6` GR-limit criterion, while increasing accuracy produces monotone convergence and motivates a separately frozen q=3 provider certification. Panel C shows the prospective F27 universality test: a positive common-centroid-slope interval calibrated from immutable C3/C5 products was frozen before the withheld IDM–DR response existed, but all four measured withheld slopes are negative and therefore fail the band. The band is not widened and the sign is not redefined. This falsifies the specified raw full-response-centroid relation, not all mechanism-native localization ideas; G7, G8, and G9 remain open.

## Figure 7 — Physical support must close before the observational quotient

**Figure 7. Observation-space support closure and perturbativity eligibility for the frozen ACTxunWISE C3/C5 route.** Panel A shows the Exp072A out-of-support kernel fraction for all 26 candidate observational coordinates against the preregistered 5% threshold; none is retained on the current certified physical domain. Exp072B subsequently shows that an upper-`k` extension alone cannot produce a finite recovery target for any of the 26 coordinates. Panel B shows the unique Exp072C joint planning frontier that would geometrically retain 15 coordinates, requiring `z_min=0.0087346` and `k_max=4.81826 Mpc^-1`, corresponding descriptively to about `33.77x` greater low-redshift reach and `72.29x` greater upper-`k` reach relative to the then-current common domain. This is planning geometry, not a certified provider extension. Panel C shows the independent Exp073A perturbativity test of that enlarged linear/no-CLEFT route: only 7 of 64 source pairs satisfy the primary `Delta^2<=1` condition, while the retained observational dimension remains zero under diagnostic thresholds 0.5, 1, and 2. Therefore no covariance-whitened or nuisance-quotiented C3/C5 ACTxunWISE survey distance is quoted from this route. The completed corrected-source Exp073B audit further shows that the projection architecture already accepts independent nonlinear `P_mm`, signed `P_Wm`, and `P_WW` blocks, while the required physical nonlinear three-block C3/C5 provider layer is absent; its first checkout-failed run remains infrastructure-only provenance. Exp073C finds no complete public/composable provider satisfying the frozen cross-family contract. Exp073D then separates a provider limitation from a model-definition limitation: C5 nonlinear dynamics are defined in principle by the covariant `f(R)` theory, whereas the frozen phenomenological C3/GDM vector does not uniquely determine nonlinear dynamics. Exp073E finds no currently defensible finite completion ensemble that preserves the full frozen C3 semantics under E1–E8. These later audits explain why the support failure cannot be repaired by an unlabelled nonlinear extrapolation. The result establishes a support/physics/model-definition eligibility boundary for applying `A_B=Q_B W_B K_B`; it does not falsify DSIR response geometry, does not prove nonlinear modelling impossible, and leaves G7, G8, and G9 open.
