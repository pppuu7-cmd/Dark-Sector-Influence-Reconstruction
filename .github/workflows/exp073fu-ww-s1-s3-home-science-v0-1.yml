name: Exp073FU WW_S1_S3 autonomous audited home science v0.1

on:
  workflow_dispatch:
    inputs:
      predecessor_admission_run:
        description: Exp073FT S1S2 admission run id
        required: true
        type: string

permissions:
  contents: read
  actions: write

concurrency:
  group: dsir-exp073fu-ww-s1-s3-home-science-v0-1
  cancel-in-progress: false

env:
  FU_PREREG_BLOB: '9a65174bb6e4f2ce4def3a83bad2f992ddd38d6a'
  FV_PREREG_BLOB: '6f6e995bdeeea02b64e192f91fd2f5078d5eeedd'
  DRIVER_V01_BLOB: '0b05e8dbf5cf87c6b35e6729bc3c4e5443a1421d'
  DRIVER_V02_BLOB: '26b88d930eef2a04c21752c55b0bf7240e1540e9'
  PRUNER_BLOB: '9ab0838ea27fcb5dddbf2ecc761ed29f8894da63'
  COMPARE_BLOB: '8266a04a579d68b51503daa928a1fad7b533fd1c'
  HOME_BLOB: 'ec42afbd6b976ae0e2f4162836ce1fe77bd8154e'
  ADMIT_VERIFY_BLOB: '03cf7109def0fa1b422bd46e080d5a65121c700f'
  FS_DRIVER_BLOB: '030f64f4d1b5bb233ad977b6a6971ebb197d45bf'
  FS_HOME_BLOB: 'a9f1777c68db522c02068828bc9e47a78b065681'
  TASKRUNNER_BLOB: '050ed7dd3387c4fb031f877825e6b3f4d4ce3ef2'
  PATCH_BLOB: 'd534b698f9131688d263eedcef27260386c58641'
  FROZEN_SOURCE_HEAD: 'de83e20a68f79ccf25b89b0d33eb4206e294c757'
  CONTRACT_FP: 'b7845df5ce4bc2bd730461476b7ff0831512003ceb5b3558436005c9876bd251'
  R1_ARTIFACT_ID: '9720335366'
  R1_DIGEST: 'sha256:ff87d8fc7d53b16b786a4eb3d6ffeb103676efb8a548223a187b9f59689f8abd'
  NAMASTER_HEAD: '24365fa59a38c15732f4f37e8b29265b75c442d5'
  ARTIFACT_NAME: 'exp073fu-ww-s1-s3-filebacked-ab-v0-1'
  NEXT_WORKFLOW: 'exp073fw-ww-s2-s2-home-science-v0-1.yml'

jobs:
  hosted-launch-audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Freeze S1S3 code and require admitted S1S2 predecessor
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PREV_RUN: ${{ inputs.predecessor_admission_run }}
        run: |
          set -euo pipefail
          [[ "$PREV_RUN" =~ ^[0-9]+$ ]]
          test "$(git rev-parse HEAD:experiments/073fu_ww_s1_s3_filebacked_full_resolution_ab_science_v0_1_prereg.md)" = "$FU_PREREG_BLOB"
          test "$(git rev-parse HEAD:experiments/073fv_ww_s1_s3_provenance_admission_v0_1_prereg.md)" = "$FV_PREREG_BLOB"
          test "$(git rev-parse HEAD:ci/exp073fu_ww_s1_s3_durable_ab_production_v0_1.py)" = "$DRIVER_V01_BLOB"
          test "$(git rev-parse HEAD:ci/exp073fu_ww_s1_s3_durable_ab_production_v0_2.py)" = "$DRIVER_V02_BLOB"
          test "$(git rev-parse HEAD:ci/exp073fu_verify_and_prune_replica_v0_1.py)" = "$PRUNER_BLOB"
          test "$(git rev-parse HEAD:ci/exp073fu_compare_terminal_receipts_v0_1.py)" = "$COMPARE_BLOB"
          test "$(git rev-parse HEAD:ci/exp073fu_home_filebacked_fullres_v0_1.sh)" = "$HOME_BLOB"
          test "$(git rev-parse HEAD:ci/exp073fv_verify_fu_candidate_v0_1.py)" = "$ADMIT_VERIFY_BLOB"
          test "$(git rev-parse HEAD:ci/exp073fs_ww_s1_s2_durable_ab_production_v0_1.py)" = "$FS_DRIVER_BLOB"
          test "$(git rev-parse HEAD:ci/exp073fs_home_filebacked_fullres_v0_1.sh)" = "$FS_HOME_BLOB"
          test "$(git rev-parse HEAD:ci/exp073aa_article3_des_angular_task_runner_v0_1.py)" = "$TASKRUNNER_BLOB"
          test "$(git rev-parse HEAD:patches/namaster-v2.7-dsir-filebacked-mcm-read-v0.2.patch)" = "$PATCH_BLOB"
          gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${PREV_RUN}" > prev-run.json
          python3 - <<'PY'
          import json
          r=json.load(open('prev-run.json')); assert r['status']=='completed' and r['conclusion']=='success' and r['name']=='Exp073FS WW_S1_S2 autonomous audited home science v0.1'
          PY
          gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${PREV_RUN}/jobs?per_page=100" > prev-jobs.json
          python3 - <<'PY' > prev-ids.txt
          import json
          for j in json.load(open('prev-jobs.json'))['jobs']:
              if j['status']=='completed' and j['conclusion']=='success': print(j['id'])
          PY
          : > prev.log; while read -r j; do gh api --allow-escape-sequences "repos/${GITHUB_REPOSITORY}/actions/jobs/${j}/logs" >> prev.log; done < prev-ids.txt
          grep -aF 'PASS_EXP073FT_WW_S1_S2_FILEBACKED_PROVENANCE_ADMISSION_V0_1' prev.log >/dev/null
          grep -aF 'ww_s1_s2_authority_created=true' prev.log >/dev/null
          python3 -m py_compile ci/exp073fu_ww_s1_s3_durable_ab_production_v0_1.py ci/exp073fu_ww_s1_s3_durable_ab_production_v0_2.py ci/exp073fu_verify_and_prune_replica_v0_1.py ci/exp073fu_compare_terminal_receipts_v0_1.py ci/exp073fv_verify_fu_candidate_v0_1.py
          bash -n ci/exp073fu_home_filebacked_fullres_v0_1.sh
          grep -F 'source_count_map(r1_root,3)' ci/exp073fu_ww_s1_s3_durable_ab_production_v0_1.py >/dev/null
          grep -F "'ordered_source_indices':[1,3]" ci/exp073fu_ww_s1_s3_durable_ab_production_v0_1.py >/dev/null
          grep -F 'compute_coupling_matrix(f1,f3,b)' ci/exp073fu_ww_s1_s3_durable_ab_production_v0_1.py >/dev/null
          ! grep -Eqi 'np\.(allclose|isclose)|rounding_rescue|smoothing_rescue|averaging_rescue' ci/exp073fu_*.py ci/exp073fv_verify_fu_candidate_v0_1.py experiments/073f[uv]_*.md
          echo PASS_EXP073FU_HOSTED_LAUNCH_AUDIT_V0_1
          echo classification=SUPPORT_PLUS_0_PLUS_0

  home-science:
    needs: [hosted-launch-audit]
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 720
    env:
      OMP_NUM_THREADS: '8'
      OMP_DYNAMIC: 'FALSE'
      OPENBLAS_NUM_THREADS: '1'
      MKL_NUM_THREADS: '1'
      NUMEXPR_NUM_THREADS: '1'
      BLIS_NUM_THREADS: '1'
      VECLIB_MAXIMUM_THREADS: '1'
    steps:
      - uses: actions/checkout@v4
      - name: Run frozen WW_S1_S3 A/B gate with durable checkpoints
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: bash ci/exp073fu_home_filebacked_fullres_v0_1.sh
      - name: Collect compact terminal or partial evidence
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          ROOT="$HOME/.cache/dsir/exp073fu-ww-s1-s3-filebacked-ab-v0-1"; OUT="$RUNNER_TEMP/exp073fu-artifact"; rm -rf "$OUT"; mkdir -p "$OUT"
          ROOT="$ROOT" OUT="$OUT" python3 - <<'PY'
          import os,shutil
          from pathlib import Path
          root=Path(os.environ['ROOT']); out=Path(os.environ['OUT'])
          if root.exists():
              for p in root.rglob('*.json'):
                  try: q=out/p.relative_to(root); q.parent.mkdir(parents=True,exist_ok=True); shutil.copy2(p,q)
                  except OSError: pass
              for rep in ('A','B'):
                  p=root/'checkpoints'/rep/'exact_route'/'selected_ee.bin'
                  if p.exists(): q=out/'checkpoints'/rep/'exact_route'/'selected_ee.bin'; q.parent.mkdir(parents=True,exist_ok=True); shutil.copy2(p,q)
              for n in ('A_driver.log','B_driver.log','A_prune_verify.log','B_prune_verify.log','ab_compare_stdout.txt','resource_telemetry.log','terminal_receipt.json'):
                  p=root/n
                  if p.exists(): shutil.copy2(p,out/n)
          PY
      - name: Upload Exp073FU evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: exp073fu-ww-s1-s3-filebacked-ab-v0-1
          path: ${{ runner.temp }}/exp073fu-artifact
          if-no-files-found: warn
          retention-days: 30

  hosted-provenance-admission:
    needs: [home-science]
    if: ${{ needs.home-science.result == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Download exact FU artifact, verify digest, and admit
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD:experiments/073fv_ww_s1_s3_provenance_admission_v0_1_prereg.md)" = "$FV_PREREG_BLOB"
          gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}/jobs?per_page=100" > jobs.json
          gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}/artifacts?per_page=100" > artifacts.json
          python3 - <<'PY' > evidence.env
          import json,os,shlex
          jobs=json.load(open('jobs.json'))['jobs']; h=[j for j in jobs if j['name']=='home-science']; assert len(h)==1 and h[0]['status']=='completed' and h[0]['conclusion']=='success'
          arts=json.load(open('artifacts.json'))['artifacts']; a=[x for x in arts if x['name']==os.environ['ARTIFACT_NAME']]; assert len(a)==1 and a[0]['expired'] is False and a[0].get('digest','').startswith('sha256:')
          a=a[0]
          for k,v in [('HOME_JOB',h[0]['id']),('ARTIFACT_ID',a['id']),('ARTIFACT_DIGEST',a['digest'])]: print(k+'='+shlex.quote(str(v)))
          PY
          source evidence.env
          gh api --allow-escape-sequences "repos/${GITHUB_REPOSITORY}/actions/jobs/${HOME_JOB}/logs" > candidate.log
          curl --fail --silent --show-error --location --retry 8 --retry-all-errors --retry-delay 2 -H "Authorization: Bearer $GH_TOKEN" -H 'Accept: application/vnd.github+json' "https://api.github.com/repos/$GITHUB_REPOSITORY/actions/artifacts/$ARTIFACT_ID/zip" -o candidate.zip
          test "sha256:$(sha256sum candidate.zip | awk '{print $1}')" = "$ARTIFACT_DIGEST"
          mkdir candidate; unzip -q candidate.zip -d candidate
          python3 ci/exp073fv_verify_fu_candidate_v0_1.py --root candidate --log candidate.log | tee admission.log
          grep -Fx 'PASS_EXP073FV_WW_S1_S3_FILEBACKED_PROVENANCE_ADMISSION_V0_1' admission.log >/dev/null
          grep -Fx 'classification=SCIENTIFIC_AUTHORITY_ADMITTED' admission.log >/dev/null
          grep -Fx 'ww_s1_s3_authority_created=true' admission.log >/dev/null
      - name: Dispatch next frozen heavy target when installed
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          if gh api "repos/${GITHUB_REPOSITORY}/actions/workflows/${NEXT_WORKFLOW}" >/dev/null 2>&1; then
            gh api --method POST "repos/${GITHUB_REPOSITORY}/actions/workflows/${NEXT_WORKFLOW}/dispatches" -f ref=main -f 'inputs[predecessor_admission_run]'="${GITHUB_RUN_ID}"
            echo "DISPATCHED_SUCCESSOR=${NEXT_WORKFLOW}"
          else
            echo "NO_FROZEN_SUCCESSOR_INSTALLED=${NEXT_WORKFLOW}"
          fi
