name: DEPRECATED Exp073R1 v0.6 duplicate route — fail closed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated-route:
    runs-on: ubuntu-24.04
    steps:
      - name: Refuse non-canonical duplicate Exp073R1 v0.6 execution
        run: |
          echo 'This workflow is a deprecated duplicate and MUST NOT execute Exp073R1.' >&2
          echo 'Canonical frozen Stage-B workflow: .github/workflows/exp073r1-desy1-selfhosted-longrun-stageb-v0-6.yml' >&2
          echo 'Preregistration commit: 7e801ce0352faf3a5b8ac232a0cd6e965d22762a' >&2
          echo 'Canonical implementation commit: ed6e55b4938d6f9447112c90cf159f485ec3dbc4' >&2
          echo 'Frozen evaluator blob: 46fe1271d97ddd9e2164d24e7d79cf27bfda805d' >&2
          exit 1
